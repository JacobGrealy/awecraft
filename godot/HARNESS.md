# AweCraft — headless test harness reference (godot/HARNESS.md)

Single reference for every `AWECRAFT_LOGIC` mode + render/shot env hooks, the
`AWECRAFT_BATTERY` runner, known-stable gate values, and sandboxed run recipes.
Written 2026-08-24 (AC-0103) from `godot/scenes/main.gd` as it exists post-AC-0061/0079/0082/0085.
Line refs drift — mode names, RESULT fields, and envs are the stable contract.
Future subagent prompts: point here instead of grepping main.gd.

Engine: `~/tools/godot/godot` (4.7.1), always `--path godot`, run from repo root.
All values below were re-measured on this box on **2026-08-24** unless marked otherwise.

**Two tables here are GENERATED (AC-0301):** §1 (mode table) and §3 (known-stable
gate values), each between `BEGIN/END GENERATED` markers. Their machine source is
`tasks/harness_data.yaml` — a doc must not double as a script's data file, so edit
the data and render:

```bash
python3 tasks/scripts/harness_doc.py --render   # rewrite the blocks
python3 tasks/scripts/harness_doc.py --check    # exit 1 if doc and data disagree
```

Everything outside the markers is hand-written prose, including the run recipes,
the env knobs, the "not in table" notes and the render-proxy warning.

## 1. MODE TABLE

Every `logic == "..."` branch in `godot/scenes/main.gd` (grep count: **37 mode branches**
+ 1 incidental comparison at the headless-idle check). All modes: headless, print
`RESULT {…}` (JSON, also written to `user://debug_result.json`) then quit.
Default world seed 44. All modes run with `fluid_sim_enabled=false` at dispatch
unless noted (boundary sets it true). `AWECRAFT_RADIUS` forces `world.render_radius`
before any mode; without it, harness envs force radius 4.

<!-- BEGIN GENERATED: modes -->
| mode | harness entry | what it tests (1 line) | key RESULT fields | envs | typical wall (2026-08-24) | notes |
|---|---|---|---|---|---|---|
| `player` | `_player_logic_test` (`_player_logic_test_body` in battery) | WASD move, floor, jump peak, fly toggle, time ticks | `start`, `after_fwd`, `horizontal_moved`, `is_on_floor`, `jump_peak_y`, `after_fly_y`, `time_before`, `time_after` | — | 3 s (standalone) / ~11 s battery (AC-0324) | battery smoke mode; AC-0324: waits the SIM BAND (`_await_sim_band`) so the spawn search is ready — the player spawns at the searched column (the pad centre while the pad exists; AC-0314 pending) |
| `spawnsearch` | `_spawnsearch_test` (AC-0324) | deterministic spawn search: per fresh world + sim-band wait, the chosen column's tier conditions re-derived independently (dry / slope over the 4 neighbour cells / un-veg feet+head), determinism (same seed twice → same column), real-player footing (is_on_floor + solid under the feet + no water), fallback tier consistency on the real band + synthetic T1/T2 tables through the pure core (`ChunkWorld._spawn_pick`) | `ok`, `per_seed` (seed, col, cell, top, pos, tier, tier_ok, det_same), `synthetic` (cases t0/t1/t2 + ok), `ms` | `AWECRAFT_LOGIC=spawnsearch` (+ `AWECRAFT_SPAWN_SEEDS` override) | ~25 s (3 seeds × 2 + synthetic) | AC-0324 gate (spec.html PROBE), seeds 44,1,7; BATTSKIP (a world per seed — out of battery scope); the SPAWNSEARCH line (col + surface H) also lands in the game log. AC-0314 (pad removal) re-run: ok true — all three seeds pick the NATURAL anchor column [8,8] as tier 0 (tops 141 / 148 / 200 — the searched position is the natural surface +1, no plateau anywhere). AC-0288 (cave P0 tune) re-run: ok true, same anchor column + tier 0 for all seeds; seed 1's top 148 → **147** (its heightmap H is 146 — the reported top is the EFFECTIVE surface, and the tuned cave field wobbles it H+1 instead of H+2, inside the documented H±R surface band; the heightmap itself is byte-identical — see the genhash row). Tops now 141 / 147 / 200 |
| `look` | `_look_test` | mouse-drag look deltas, pointer-lock behavior, no drag-mine | `yaw_after_drag`, `drag_yaw_delta`, `drag_pitch_delta`, `locked_yaw_delta`, `dragging`, `drag_mined`, `drag_yaw_ok`, `drag_pitch_ok`, `locked_yaw_ok`, `locked_pitch_ok`, `ok` | — | ~4 s (1 s, 2026-08-24, ok:true) | pointer-lock input via synthetic events |
| `interact` | `_interact_test` (`_interact_test_body` in battery) | DDA select, long-press mine → drop + pickup, place block | `target_cell`, `breakable_id`, `highlight_visible`, `after_mine_cell`, `drop_spawned`, `inv_grew`, `place_cell`, `after_place_cell`, `place_ok` | — | 3 s (standalone) / 1.3 s battery | known-stable: `drop_spawned`/`place_ok` true |
| `craft` | `_craft_test` | shapeless 2×log→4 planks, table shaped pickaxe, inv bookkeeping | `ok`, `data` (r: `logs_start`, `out_a`, `planks_a`, …), `inv` (Debug.inv_dump), `armor` (Debug.armor_dump) | — | ~15–30 s (not re-run 08-24; long scripted UI sequence) | slow-ish UI mode |
| `guiclick` | `_guiclick_test` | inventory click/drag routing, held grab/drop, real recipe click (E planks, table pickaxe) | `ok`, `data` (r: `held0`, `held_down`, `hot0_up`, `two_click`, `drop_hot0`, `drop_storage`, + real-recipe keys) | — | ~15–30 s (not re-run 08-24) | AC-0059 gate |
| `combat` | `_combat_test` | bare-hand punch 1 dmg, attack cooldown, sword 4 dmg, mob death drops, no-target safe | `bare`, `bare_hp`, `post_double_hp`, `cooldown_ok`, `weapon_dmg`, `death_drops`, `death_ok`, `no_target`, `ok` | — | ~10–20 s (not re-run 08-24) | spawns mobs |
| `swing` | `_swing_test` | swing-by-rotation (AC-0085): single-axis pitch arc, punch, 0.9 s loop cycles, tool voxel render, tier tint, walk sway/bob | `ok`, `data` (r: `saw_mid_swing`, `mid_frac`, `swing_max_rot` (>0.4, ≈0.85), `settled`, `punch_max_rot`, `loop_cycles_0.9s` (3–5), `tool_111..123`, `tier_differs_111_vs_113`, `fist_after_empty`, `walk_bobs` (4–10), `walk_y_crossings`, `walk_idle_max_off`) | — | ~15 s (not re-run 08-24) | post-AC-0085 asserts rotation not position |
| `survival` | `_survival_test` | tool vs bare mine speed, gated drops, armor DR, hunger drain/regen/starve, eat, respawn | `tool_ratio` (frame-based, bare/pick frames-to-break), `bare_frames`, `pick_frames`, `bare_ms`, `pick_ms`, `drop_bare`, `drop_pick`, `dmg_none`, `dmg_armor`, `hunger_drain`, `hunger_mine`, `hunger_regen`, `starve`, `eat`, `respawn`, `dump`, `ok` | — | ~60–120 s (re-run 09-18 resume, green 44/1/7) | long survival sequence; AC-0314 resume - builds its own 5x5 flat stone fixture via set_block (spawn pad gone; natural terrain has no guaranteed flat 3x3, the old inline search bailed); stander ON the slab (feet=ctop+1) + target at eye level (y=ctop+2) so the horizontal aim clears the slab (the old pad-era aim embedded the capsule in the slab top block and settled the player below it); tool_ratio now frame-based (deterministic; the wall-ms ratio wandered 2.15-2.27 and flipped the tol assertion on borderline seeds); full assertion set re-run green on seeds 44/1/7 |
| `hunger` | `_hunger_toggle_test` | hunger_enabled off pins 20 + regen intact; on → starve damage | `pin_to_full`, `off` {`hunger`,`min_hp`,`regen_gain`,`regen_ms`,`attack_cost_zero`}, `on` {`starve_dmg`,`starve_ms`}, `ok` | — | ~20–40 s (not re-run 08-24) | AC-0063 gate; AC-0314 resume - builds its own 5x5 flat stone fixture via set_block (spawn pad gone; the old inline search bailed); full assertion set re-run on seeds 44/1/7 |
| `light` | `_light_test` | sky light bake: surface 15, cave 0, torch level 14, BFS reach | `surface_eff` (15), `cave_eff` (0), `torch_level` (14), `torch_far_before` (0), `torch_far_after` (9) | — | 4 s (standalone) / 2.9 s battery | known-stable |
| `daynight` | `_daynight_test` | sun energy/sky color at t=0.25 vs 0.75, full cycle period | `time_025`, `time_075` (DayNight readouts), `cycle_elapsed_ms`, `cycle_expected`, `cycle_actual`, `cycle_delta_ok`, `colcheck`, `ok` | `AWECRAFT_TIME` | ~30–60 s (not re-run 08-24) | runs a full day cycle |
| `tint` | `_tint_test` | per-block vertex tint (tinted vs untinted surface samples) | `ok`, `samples` (out) | — | ~10 s (not re-run 08-24) | |
| `settings` | `_settings_test` | settings defaults 50/4 (sim), ranges 4–96, sim→render clamp, load clamp, the AC-0313 sim floor (SIM_MIN 4: set 3 → 4, stored 2 → 4 on load), apply, volume, hunger toggle (AC-0072), the AC-0332 far-tier floor pair (defaults on/0, the 0..24 clamp, toggle round-trip, the set_value apply step via the stub's note_yfloor, the Developer-tab row's presence from the real menu scene + the _sync_controls refresh) | `defaults`, `range`, `sim_clamp`, `load_clamp`, `apply` (dist rebase [7,64] at AC-0313), `sim_floor` {saved 2, after_load 4}, `volume_ok`, `hunger` {saved_off, reloaded_off, back_on, default_true}, `yfloor` {default_on_0, clamp_hi_24, clamp_lo_0, toggle_off_on, apply_step, apply_calls, dev_row_present, sync_set5, toggle_dim}, `ok` | `AWECRAFT_IGNORE_SETTINGS` (self-managed) | ~5 s (not re-run 08-24; fast class) | dispatched at main.gd:576 BEFORE game nodes (works standalone + in battery). AC-0313: SIM_MIN 1→4 (the tier-0 set went, the real band taxi ≤ sim is the footing guarantee) — the apply dist expectation rebased [7,48]→[7,64] (sim 3 → floor 4) and the new `sim_floor` field round-trips the floor through the load path. AC-0332 (2026-09-19): the far-tier mesh floor pair (yfloor_enabled + yfloor_chunks_below_sea) is covered — the apply step rides set_value (Settings.apply_yfloor -> the world's note_yfloor, stub-counted) and the dev-row presence instantiates scenes/menu.tscn (synchronous _ready; the arm stays await-free by contract) |
| `fluids` | `_fluids_test` | source stability, level-7 sideways spread, water+lava reactions, sea zero-write invariant | `shore_after` ([5,7]), `source_after` ([5,8]), `water_delta` (1), `water_on_lava_result` (25), `sideways_lava_result` (9), `sea_stable` (true), `sea_surface_before`/`_final` (2756), `sea_backed_before`/`_final` (914); prints `FLUIDSTAT` line | — | 5 s (standalone) / 3.8 s battery | known-stable; AC-0068 invariant; sea counters are the count over `world.chunks.keys()` — AC-0152/0160 changed the r4 resident set (121→69 chunks) → new baseline 2756/914 (old 2730/888 VOID) |
| `fluidprobe` | `_fluidprobe_test` | probes water column positions (debug scout) | `done` (true) | — | ~5 s (not re-run 08-24) | |
| `fluidfall` | `_fluidfall_test` | AC-0068: source over hole stationary (0 writes), descending column lands + 7-wide spread, flat pool shore | `sy`, `caseA_source`, `caseB_full`, `caseC_flat`, `shore`, `far`, `beyond`, `center`, `caseA_ok`, `caseB_ok`, `caseC_ok`, `ok` | `AWECRAFT_SETTLE_TICKS` (forces hard settle) | ~10 s (not re-run 08-24) | r=2 forced |
| `webfall` | `_webfall_test` | web-fall parity: settled + landed + floor spread ≥100 cells | `settled`, `landed`, `floor_cells`, `total_water`, `sy`, `ok` | — | ~10 s (not re-run 08-24) | r=2 forced |
| `fluidsettle` | `_fluidsettle_test` | world-wide fluid settle: ticks to quiescence, final water count | `ticks_to_settle`, `quiet`, `water_final`, `chunk_count` | `AWECRAFT_SETTLE_TICKS` | ~10–20 s (not re-run 08-24) | |
| `buckets` | `_buckets_test` (`_buckets_test_body` in battery) | bucket scoop water → item 140, place back | `scoop_before`, `after_scoop`, `inv_scoop`, `place_after`, `inv_place` | — | ~5 s (standalone, not re-run 08-24) / ~3 s battery | battery full mode (not in 08-24 smoke) |
| `water` | `_water_test` | player water-column physics: rise to shore top, land on shore, water cell intact | `B`, `shore_top`, `timeline`, `max_y`, `first_above_frame`, `above_top`, `landed_frame`, `standing_on_shore`, `final`, `final_on_floor`, `water_cell_intact`, `ok` | — | ~20 s (not re-run 08-24) | |
| `fpv` | `_fpv_test` | first-person view atlas UVs in range, grass top/side distinct | `has_uv`, `uv_min`, `uv_max`, `uv_in_range`, `grass_top`, `grass_side`, `grass_distinct` | — | ~5 s (not re-run 08-24) | |
| `held` | `_held_test` | held block box vs cross sprite visibility (AC-0066 fidelity) | `ok`, `block_held_ok`, `block_visible`, `block_type_ok`, `sprite_hidden_during_block`, `region_actual`, `region_want`, `tool_held_ok`, `tool_visible`, `tool_type_ok`, `empty_hidden_ok` | — | ~10 s (not re-run 08-24) | |
| `toolres` | `_toolres_test` | tool voxel model resolution: 5 tools present + type + tier color + box/fist fallback | `ok`, `tool_111`/`115`/`119`/`123`/`113` {`present`,`type`,`fist`,`sprite`,`box`,`ok`}, `headcolor_111`/`headcolor_113`, `tier_differs_111_vs_113`, `box_after_tool`, `fist_after_empty` | — | ~10 s (not re-run 08-24) | AC-0070 gate |
| `toolpose` | `_toolpose_test` | tool orientation + scale ×2 + depth-disable (AC-0073/0067/0085) | `ok`, `held_box_scale_x` (≈0.70), `held_sprite_scale_x`, `scale_ok`, `tool_…` {`position_ok`,`arc_ok`,`vertical_ok`,`handle_below_cam`,`bbox_diag`,`diag_2x_ok`,`depth_disabled`,`ok`}, `depth` {tool,box,fist,sprite}, `depth_ok` | — | ~15 s (not re-run 08-24) | handle y < −0.55 |
| `viewmodel` | `_viewmodel_shot` | AC-0085 render hook: idle/apex head+handle centroids for a held tool | `vmshot`, `frac`, `idle_head`, `idle_handle`, `apex_head`, `apex_handle` | `AWECRAFT_VMITEM`, `AWECRAFT_VMSHOT`, `AWECRAFT_VMFRACTION` | ~10 s headless (render hook; xvfb for the shot) | centroid-only in headless |
| `wallshot` | `_wallshot_test` | AC-0067 on-top: held box/sprite vs stone wall pixel regions (before/after) | `before`, `shots`, `wall_face_z`, `block` {`block_frame_stone`,`block_vm_nonstone`,`block_box_vis`,`block_depth_on`}, `item` {`item_vm_cyan`,`item_sprite_vis`} | `AWECRAFT_WALL_BEFORE` (1), `AWECRAFT_WALL_SHOTS`, `AWECRAFT_SNAPSHOT` (paths) | render mode (~300 s timeout) | xvfb; writes shots into `tasks/AC-0067/` |
| `editperf` | `_editperf_test` | single block edit: flush frames, build cost, no hitch | `cell`, `edited_id`, `cell_after`, `flush_done`, `flush_frames`, `max_frame_build_ms`, `single_build_ms`, `total_ms` | — | ~5–10 s (not re-run 08-24) | |
| `perf` | `_perf_test` | full-radius build/drain/first-draw/frame percentiles + memory | `chunks`, `render_radius`, `fog_near`/`fog_far`/`fog_edge`/`fog_ok`, `total_chunks`, `all_meshed`, `collision_shapes`, `collision_ms_total`, `collision_n`, `collision_max_ms`, `collision_slabs`, `collision_slab_ms`, `collision_slab_max_ms`, `collision_slab_hist` (ms bins lt1/1_2/2_5/5_10/10_25/gte25), `reband_exit`, `reband_entry`, `reband_rearm_slabs`, `staged_drained`, `staged_dropped`, `total_ms`, `frames`, `recenter_ms`, `max_frame_ms`, `build_units`, `drain_frames`, `max_drain_ms`, `gen_ms`, `build_ms`, `first_draw_ms`, `p50_ms`, `p95_ms`, `frame_max_ms`, `mem_before_bytes`, `mem_after_bytes`, `drain_s` | `AWECRAFT_RADIUS`, `AWECRAFT_THREADGEN_N`/`TGDEBUG`, `AWECRAFT_RECPROBE`, `AWECRAFT_DRAIN_MS`, `AWECRAFT_GEN_BUDGET`, `AWECRAFT_MESH_INFO` | r4 ≈12.3 s total_ms (measured `.scratch/ac0082_g4_perf_r4_t1.log` 2026-08-24); **r50 ≈850 s total_ms (from 2026-08-24 03:25 log `ac0082_g4_perf_r50_t1.log`)** | SLOW at r50 — use 3000 s timeout |
| `boundary` | `_boundary_test` (via `_await_boundary_core`) | chunk streaming: walk r chunks, crossings, bursts, forward/trailing wall, mem, marker round-trip | `ok`, `radius`, `walk_chunks`, `walk_speed`, `walk_s`, `crossings`, `p50_ms`, `p95_ms`, `max_ms`, `loads`, `unloads`, `flap`, `burst_ms_per_crossing`, `burst_p50_ms`, `burst_p95_ms`, `burst_max_ms`, `forward_wall_ms_per_crossing`, `forward_p95_ms`, `forward_max_ms`, `trailing_*`, `fluid_tick_ms_p95`, `mem_delta_mb`, `marker`, `marker_ok`, `remesh_ok`, `resident_final`, `built_final`, `in_radius_built_final`/`_max`/`_min`, `in_radius_present_final`, `target_in_radius`, `collision_ms_total`, `collision_n`, `collision_max_ms`, `collision_slabs`, `collision_slab_ms`, `collision_slab_max_ms`, `collision_slab_hist` (ms bins lt1/1_2/2_5/5_10/10_25/gte25), `reband_exit`, `reband_entry`, `reband_rearm_slabs`, `staged_drained`, `staged_dropped`, `unbodied_built_final` | `AWECRAFT_RADIUS` (r), `AWECRAFT_WALK`, `AWECRAFT_WALK_SPEED`, `AWECRAFT_TICKTIME`, `AWECRAFT_THREADGEN_N`/`TGDEBUG`, `AWECRAFT_RECPROBE` | r3 45 s, r4 46 s (measured 2026-08-24); r50 ≈1400 s wall (from 2026-08-24 10:34 log `.scratch/ac0079r3_battery/b_r50.log`) | SLOW at r≥8 — 3000 s timeout; fluid sim enabled. **Off-default walk speeds read RED for a budget reason, not a world bug** — `ok = crossings == walk_lines and marker_ok` and the walk line is a fixed length, so at `AWECRAFT_WALK_SPEED=8` the arm needs ~2× the wall and finishes fewer crossings than `walk_lines` (measured 2026-09-17 at AC-0313: 7 crossings, `ok:false`, while the same run's world values were clean — p50 14, p95 21, `marker_ok`/`remesh_ok` true). Judge off-default runs on the p50/p95/`*_final` fields, not on `ok`. |
| `atlas` | `_atlas_test` | spawn chunk mesh UV sanity | `ok` (or `error`) | — | ~5 s (not re-run 08-24) | |
| `r16` | `_r16_test` (AC-0212, rebased at AC-0313, 3-tier rebase at AC-0312) | 16-radius build + moving inside a chunk: per-frame CPU in static vs moving phases (frame ms → effective fps), camera on a 12 m circle inside the spawn chunk (frustum changes every frame). AC-0312 pins: sim 4 / medium_start 5 / low_start 10 / render 16 → REAL [0,4], band A taxi 5 (materialized high), band B 6-10, band C 11-15; the band-A materialization runs in the drain high lane in parallel (NOT part of the real-band gate); the per-slab grid-vs-mesh check reads the LIVE grid at the stored DRAW tier (G=8 tier 2 / G=4 tier 3) | `ok` (measurement arm — hardcoded true; the sub-flags are the contract), `radius` (16), `ho_cap`, `built`/`built_all`/`total_chunks`, `resident`, `build_ms`, `cull_mode`, `perf_cull_passes`/`perf_cull_flips`, `static`/`moving` {`n`,`p50_ms`,`p95_ms`,`max_ms`,`fps_*`}, `drop_fps_static_vs_moving`, `lod` {`inr_ok`/`inr4` (inner-ring holes/pending 0), `wave_ok`, `height_ok`, `no_giant_ok`, `no_downgrade_ok`, `low_thread_ok`, `immediate_ok`, `low_sync_zero_ok`, `air_ok`, `far_low_n` (the payload lows — grid checks skipped, byte-gated by farab), `uv_repeat_ok`}, `gen_timing` {`cols_skip` (the band-A materialization census — NOT dead), `cols_far`, `cols_full`}, `section_first_ok` | `AWECRAFT_FRUSTUM` (engine [default] / manual / 0), `AWECRAFT_RADIUS` (forced ≥16 by the arm) | LONG — run `--fixed-fps 600` so ticks are uncapped and the frame-ms awaits measure true per-frame CPU (same convention as breakspike); ~3–8 min wall (r16 build, 25-min in-arm cap, proceeds on partial build) | AC-0212 #1 gate: A/B `AWECRAFT_FRUSTUM=manual` vs default engine — no 5 fps drop in the moving phase. AC-0313: sim pin 2 → 4, medium_start 4 → 5 (real band [0,2] → [0,4]). AC-0312 3-tier rebase: the low band is now the FAR payload (band B/C) — the per-slab grid-vs-mesh check skips far slabs (`far_low_n`; their emit identity is farab's byte gate, 1080/1080 @ G=4/G=8 + water) and `uv_repeat_ok` counts them; `air_ok` skips the far columns' null slabs (the payload low is the designed state). Measured (2026-09-18, final run): ALL sub-flags green — `gate_ok`/`inr_ok`/`wave_ok`/`height_ok`/`no_giant_ok`/`no_downgrade_ok`/`low_thread_ok`/`immediate_ok`/`low_sync_zero_ok`/`air_ok`/`uv_repeat_ok` true, `inr4` {holes 0, pending 0}, `uv_mismatch` 0/0 (the 4292/456 low slabs are all far payload — `far_low_n`), `cols_skip` 130 / `cols_far` 1647 / `cols_full` 642, `built_all` true, `section_first_ok` true. **Rebased at AC-0329 (2026-09-19, post-AC-0314/AC-0324 tree, seed 44, two consecutive `--fixed-fps 600` runs, both ALL-GREEN):** `air_ok`/`uv_repeat_ok`/`catch_up_ok` true — `air_with_lod` 0 (`air_capsule_n` 3, report-only), `uv_mismatch` 0/271 (the fresh-slab population now exists: forward-frustum tier-4 FULL columns that reached band B/C — the AC-0322 data-feed fix makes the collar generate early/in-frustum at skip=0), `demoted_cols_n` 437, `far_visible` 531, `far_holes` 0 (`far_capsule_n` 22, report-only), `cols_skip` ~474 / `cols_far` 2575 / `cols_full` 580 (the AC-0322 feed shift vs the 130/1647/642 above), `built_all` true, `section_first_ok` true. Three STALE ARM expectations were corrected (data-level proof in `tasks/AC-0329/AC-0329-results.html`; no game change): (1) the color check's flat `vc.a == 1.0` gate predates the AC-0283 P3 sky-alpha design (the vertex alpha = the origin cell's heightmap sky/15, `mesh.cpp` `avg_grid_emit`) — it had never fired on a non-empty population before (pre-AC-0322 runs color-checked 0 fresh slabs), so the alpha is now gated against the emit's own sky (re-derived via `world._halo_sky_for`); (2) `air_ok` treats a STALE low (time capsule) on an all-air slab as report-only — the old LOD shows/stays stored until the lane re-owns the slab (the per-slab high landing, the wave's air bookkeeping, or the demote's payload re-lower); only a FOG or a FRESH low (the lane built it ON air — the bookkeeping failure the handoff guards prevent) violates; (3) `_r16_lod_far` measures h-only far columns at the MESH level (resident stored-high/active-low — the AC-0263 "every far slab visible" contract the pre-3-tier data-based count went blind to once demoted columns became all-null data); resident-but-hidden = a time capsule (report-only `far_capsule_n`), a double draw = the hole. The arm's top-level `ok` is HARDCODED true (below), so read the sub-flags, never `ok`, and always with `--fixed-fps 600`. A mid-series pre-fix run showed a 379-quad `uv_mismatch` on non-far capsule slabs (a keep-all-LOD co-stored payload-era low compared against the new full-data grid — a timing transient; it did not reproduce in the final run). KNOWN CRASH CLASS (bounded by AC-0323 - and it is NOT a flake of this arm): a rare intermittent SIGSEGV (~1 in 10 under heavy streaming load) whose Godot crash handler prints an EMPTY backtrace; the identical banner plus the single frame `libc.so.6(+0x45f60)` (the `rt_sigreturn` stub, i.e. the fault lands ON the signal frame) appears in logs from 2026-09-06 onward (.scratch/ac0231-r16-inrpass.log, ac0236-bat-smoke.log, batt192.log), so it predates the far-band work by ~90 tickets; 43 runs on the 2026-09-18 tree were clean. AC-0323 holds the eliminations (small stacks, OOM, the throwaway-camera cull path, steady-state lane concurrency - all cleared) and the surviving suspects (the uncapped 25-element burst group task, the EditPool raw thread, engine pool internals incl. the statically linked Rust runtime); AC-0328 is the long reproduction campaign. The `!is_inside_tree` warning (272/run) is the pre-existing AC-0284b throwaway-camera noise. AC-0293 (2026-09-19): the 5x5 spawn burst — one of the surviving suspects (the uncapped 25-element burst group task) — is RETIRED; r16 all-sub-flag green on the burst-free tree, so the suspect list for AC-0323/AC-0328 narrows to the EditPool raw thread + engine pool internals. **AC-0331 (2026-09-19, the far-tier floor, `--fixed-fps 600`): ALL 12 sub-flags green (air/catch_up/gate/height/immediate/inr/low_sync_zero/low_thread/no_downgrade/no_giant/uv_repeat/wave). The floor prunes below-floor slabs from the far lanes — `far1` `far_visible` 556 → 391 (low 310 → 174, high 246 → 217, `far_holes` 0, `far_pending` 0 — still well above the `catch_up_ok` >100 threshold, no re-baseline owed), `low_enqueue_n` 47847 → 36057 (**−24.6%** — the dispatch census of the pruned low slabs, the ticket's named cost instrument), `inr4` holes 0 (air 37 → 334 / low 424 → 127 — pruned slabs settle as terminal air, the probes' floor-skip keeps them from re-dispatching), `demoted_cols_n` 421. The materialized-branch exemption in `_r16_lod_far` (a far column's slabs 0..6 are pruned, NOT holes) is what keeps `far_holes` 0**. **AC-0336 (the dead-lane deletion, `--fixed-fps 600`): the (off) fog wave, the in-r lane, and the band-drained order gate are GONE (dead since the AC-0335 one-scheduler unification) — RESULT fields dropped: `gate_ok` + its 4 counters (`gate_holds_n`/`high_before_low_n`/`high_before_low_firstbuild_n`/`gate_reclose_n`), `low_fog_boxes_n`, `far_fog`, `inr_fog`, `fog_created` (air_chunk_test), the pool `mm` key (the fog-box MultiMesh pool retired with the wave; `_pool_targets_for` is now `[mi, col]`). Sub-flags 12 → 11 (the `gate` flag is gone; all others keep their names and thresholds). The r16 idle-wait (`world._drain_units_last`) and the `--fixed-fps 600` convention are unchanged** |
| `spin` | `_spin_test` (AC-0109 G1; row added at AC-0212) | 360° spin: no on-screen flicker (`transitions` 0), verts drop looking sideways, all-chunks-restore, queue flat. | **AC-0212: cull-lane aware** — `cull_mode`=`engine` (default) replicates the render server's exact per-instance AABB-vs-frustum test (no margin) instead of reading `.visible`; `manual` keeps the legacy `.visible` read. New RESULT: `cull_mode`, `manual_pass`, `perf_cull_passes`, `perf_cull_flips`, `instances_total`, `instances_hidden` | `AWECRAFT_FRUSTUM` | ~30–60 s (r4 build + 121 steps) | `ok` = transitions==0 + verts_drop + vis_end==vis_start + queue_flat |
| `genhash` | `_genhash_print` | 5×5 (25) chunk data-gen MD5 parity — world/* change gate | prints `GENHASH cx cz <32-hex>` ×25 + `GENMS` (no RESULT dict) | `AWECRAFT_SEED` (default 44) | 2 s | known-stable: 25/25 identical across verified tasks; seed 99 → all 25 differ |
| `trees` | `_trees_test` | tree generation + cross-quad mesh per chunk | `ok`, `data` (out), `per_chunk` | — | ~10 s (not re-run 08-24) | |
| `save` | `_save_test` | 3-slot save/continue: edits persist, player/inv/slot isolation, clear | `ok`, `saved_ok`, `clear_ok`, `clear_pre_ok`, `blocks_match`, `blocks_total`, `unedited_match`, `player_match`, `pos_ok`, `sel_ok`, `hp_ok`, `hunger_ok`, `time_ok`, `inv_match`, `slot0_ok`, `iso_ok`, `iso_base` | — | ~10–20 s (not re-run 08-24) | uses `user://` saves |
| `dropshot` | `_dropshot_test` | textured block-drop render (AC-0083) | `dropshot` (true), `spawned`, `drop_count`, `cam` | `AWECRAFT_SNAPSHOT` (output path) | render mode (~300 s) | xvfb |
| `crossshot` | `_crossshot_test` | flower cross-sprite render (AC-0084) | `crossshot` (true), `placed`, `tx`, `ty`, `tz` | `AWECRAFT_SNAPSHOT` | render mode (~300 s) | xvfb |
| `quitmenu` | `_quitmenu_test` | pause→quit-to-menu: save written, world freed, back to active menu (AC-0081) | `ok`, `paused_ok`, `menu_scene_active`, `world_cleared`, `save_written`, `seed_ok`, `edits_ok`, `slot`, `script_errors_seen` | — | ~10–20 s (not re-run 08-24) | clears all 3 slots first |
| `mainmenuexit` | `_mainmenuexit_test` | main-menu Exit button: menu-first boot, finds Exit button, press → app quit (AC-0099) | `menu`, `menu_state`, `menu_visible`, `exit_found`, `ready_to_quit` | — | ~5–10 s | self-quit harness (Exit press → `get_tree().quit()` on desktop); intercepted in `_ready()` BEFORE the game dispatch; BATTSKIP in battery (self-quits the process) |
| `occlude` | `_occlude_test` | interior-skip audit + occluder node graph (AC-0110): leaking 6-solid-neighbor faces, per-slab full-solid occluders, enclosed-cave flood, underground verts | `ok`, `chunks_built`, `total_verts`, `underground_verts`, `underground_faces`, `full_solid_slabs`, `occluders`, `box_sample`, `leaking_cells`, `leaking_faces`, `interior_voxels`, `no_emitter`, `cave` (`cells`,`aabb_min`,`aabb_max`,`seed`,`open`), `cull_3d`, `use_occl`, `radius` | — | ~60–90 s (r4 build) | headless: `occluders` 0 (gated off, DisplayServer check); under xvfb: `occluders` == `full_solid_slabs` (25 @ r4 seed 44, box 15³) |
| `bandmap` | `_bandmap_test` | AC-0152/0160 banded streaming evidence @ R=50 (no full drain): tick diamond / render circle / band counts / spawn-3×3 time / trickle queue trend / band-2 full-mesh sample | `render_radius` (50), `band0_r`/`band1_r`, `stream_set_home` (8253), `circle50_chunks` (7845), `tick_set_band0` (41), `band_counts` {b0 41, b1 104, b2 7700, b3 408=ring}, `spawn3x3_ms` (~3.2–4.0 s machine floor), `trickle` [{f, q, built}…1500 frames], `queue_final`, `built` {b0,b1,b2}, `b0_unbuilt`, `b1_unbuilt`, `band2_sample` [{key, band, mesh_built, quad_count}], `elapsed_ms`; `B0LOST` watchdog lines | `AWECRAFT_BAND0`/`AWECRAFT_BAND1` (world.gd band overrides) | ~60–120 s (no full r50 drain) | counts are from the stubbed streaming set; `b1_unbuilt` non-empty is expected (trickle hasn't reached it); band-2 `quad_count` ≫ 1 = full mesh (impostor era value was exactly 1); full r50 drain is intrinsically ~15–25 min wall (H=384 build CPU) — the gate is the TREND (monotone queue, no strand), not completion in-arm. The `spawn3x3_ms` here is the arm-side INNERMOST-9 (3x3) probe only — the LOAD GATE is the sim taxi diamond (the `load` arm's `simband_ms`; AC-0313 clause-4 correction) |
| `chunkio` | `_chunkio_test` | AC-0155 full-column save round-trip under the AC-0287 filter, one process: fresh r=4 41-set → evict (NONE saved — unedited + two rings past the render edge = the no-bloat contract) → recenter back + edit all 41 (one planks cell each) → evict (all 41 saved — the edits clause) → r4 revisit (41 read from disk, byte-identical) → r50 revisit (disk-served) → region fan-out (filter-bypassing machinery test) | `ok`, `wall_ms` (≤180000), `diamond` (41), `unedited_saved` (0 — the AC-0287 no-bloat gate), `files_exist` (41 — all edited), `still_in` (0), `edits_size` (41), `edits_missing` (0), `skip_n` (filter declines), `far_n` (far evicts), `saveall` (0), `byte_identical` (41), `origin_disk` (41), `origin_gen` (0), `r50` {disk 41, gen 0}, `region_files_b` (4), `region_files_r50` (12 = `expected_regions`), `disk_reads_total`, `disk_read_ms`, `gen_count_total` | `AWECRAFT_SEED` | ~15-20 s (AC-0287 rework — the Phase B2 settle wait: all 41 must be FULL (non-far) before the edit+evict; the AHEAD-offset band-3 stubs land far first and the promotion clears the flag) | AC-0287: the old Phase B expectation (the 41 unedited spawn-diamond columns saved on the far evict) is the contract this ticket deletes — the arm now asserts the inverse (`unedited_saved == 0`) and round-trips the EDITED 41 (the only save path left in natural flow). Uses slot 0's `user://chunkdir_0` (cleared at arm start); world counters `disk_reads`/`gen_count`/`chunk_origin` (world.gd) are the provenance evidence; AC-0164: I/O is threaded (encode/decode/FileAccess on the threadgen pool) — RESULT carries an `io` counter block (enq/dedup/fails/main ms) |
| `flysave` | `_flysave_test` (AC-0287) | the save-filter no-bloat proof (the ticket's core evidence, bytes not prose): fly AWECRAFT_FLY_DIST blocks (default 2048) at AWECRAFT_FLY_SPEED (default 32 b/s), trace the slot's region-file bytes at 4 intervals — FLAT under the filter (in fact EMPTY: the unedited evicts are skipped outside the sim diamond and the 3 edited columns are FAR — never encoded, edited or not); the edits sit on the first three far-stamped taxi-5 halo-ring columns (a tier-4 column is far-generated only OUTSIDE the camera frustum — the onscreen collar is full-generated, _gen_skip_flag) at the far heightmap's bit-exact surface H (far_at_edit), then fly back to each and verify the edits read back after REGENERATION (the in-session edits dict + the promotion regen re-apply them — the far column is never disk-resident, disk_origin 0) | `ok`, `wall_ms`, `dist`, `speed`, `radius`, `sim`, `saveall`, `trace` (5× {d, bytes} — the save size per flown distance), `bytes_start`, `bytes_end`, `total_delta`, `tail_delta` (back-half growth — 0 = flat), `peak`, `per_block`, `edits` (3), `edited_saved` (0 — far is never encoded), `disk_origin` (0 — the edits come back regenerated, not from disk), `far_at_edit`, `edit_ok`, `writes` (0), `skips` (unedited out-of-band evicts), `far_writes` (far evicts — informational, grows with distance) | `AWECRAFT_LOGIC=flysave` (+ `AWECRAFT_FLY_DIST` 256–65536, `AWECRAFT_FLY_SPEED` 8–256, `AWECRAFT_SAVEALL=1` = pre-AC-0287 baseline A/B) | ~16 s (dist 256) / ~40 s (dist 1024, measured AC-0287) / ~7 min (10240 heavy) | BATTSKIP (a flight per run). `ok` = far_at_edit + edit_ok + edited_saved 0 + disk_origin 0 + tail_delta ≤ 64 KB + total_delta < 1 KB/block. The 10k HEAVY invocation: `AWECRAFT_FLY_DIST=10240` (+ the `AWECRAFT_SAVEALL=1` twin for the baseline trace). Judge the baseline run on the metric fields, not `ok` (its linear growth trips the flatness gate by design — measured at D=1024: 428 KB written, ~418 B/block, edited_saved 3) |
| `nofallback` | `_nofallback_test` (AC-0208) | no-GDScript-fallback gate: boots a real world and proves EVERY C++ lane ran (counters advanced) while every GDScript reference kernel stayed dead (sentinels 0); torch glow source proves the C++ light lane end-to-end (C++-landed `last_eff` at the torch > 5); direct encode→decode_column C++ slab-IO roundtrip on a real column | `ok`, `mesh_cpp_builds`, `gen_cpp_works`, `strips_cpp_calls`, `light_cpp_pull_calls` (reported; 0 on normal boot — workers self-light in C++), `chunkio_cpp_slab_decodes`, `gd_strips_calls`, `gd_light_pull_calls`, `mesh_chunks`, `torch_placed`, `torch_light`, `roundtrip_ok`, `wall_ms` | — | ~1–2 s | AC-0208: `ok` = all five C++ counters > 0 AND both GDScript sentinels == 0 AND ≥16 mesh chunks AND torch placed + lit AND roundtrip ok. Companion: the fail-fast gate — rename `godot/bin/libchunkio.so` → the game must print the `AWECRAFT CANNOT START` banner + `push_error` and quit (restore the .so after) |
| `tick` | `_tick_test` | AC-0158 20 Hz Bedrock game-tick probe: settles the quiet r=4 41-diamond, then measures 120 ticks — clock rate, scope, random-tick distribution, determinism | `ok`, `hz2` (steady-state ≈ 20, last-half window), `cols` (41), `subchunks` (984), `min_count`/`max_count` (= window), `scope_bad` (0), `band123_ticks` (0), `recompute_mismatch` (0/118080), `window_md5` (byte-identical across fresh runs — `d81250cd…` at AC-0158/0164), `tick_ms_p95` (≈ 4–7), `region_ok` (spawn-predicate self-check), `queue_settled` | `AWECRAFT_SEED` | ~50 s (~22 s settle) | band-0 diamond ONLY ticks (far 13–50 never); the random-tick cell is a pure function of (world seed, tick index, column, subchunk) via splitmix64; full-window `hz` is dragged by catch-up drops on >200 ms frames — gate on `hz2` |
| `load` | `_load_test` | AC-0178 loading-screen + bypass-throttle first-load probe: fresh world at R=50 (7845 cols), measures the full first-load wall with the loading-bypass ON vs the spread (bypass OFF) baseline | `ok` (bypass ON completes), `bypass` (true/false), `cols` (7845), `disk`/`gen`/`meshed` (counters from provenance), `gen_per_s`/`mesh_per_s`, `wall_ms` (45-min in-arm cap), `loading_active_final`, `screen_up`, `simband_ms` (the load-GATE wall: the SIM TAXI DIAMOND taxi <= band0_r fully meshed — 41 cols at sim 4; renamed from `spawn3x3_ms` at AC-0313 clause-4 correction), `tg_inflight`/`tm_inflight`, `queue_final` (band-3 data-only collar) | `AWECRAFT_SEED`, `AWECRAFT_LOADBYPASS` (1=on [default], 0=spread baseline), `AWECRAFT_RADIUS` | ~28 min ON / ≥45 min OFF (OFF does not finish) | **A/B gate: ON < OFF decisively** (ON completes 7845/7845 in ~27.6 min [gen/s 5.0]; OFF hits the 45-min cap at 7061/7845 [gen/s 3.0]). Loading paths are `loading_active`-gated — steady state keeps the legacy throttles (battery/chunkio/save/continue arms run with `loading_active=false`, values unchanged). Exit must be CLEAN: no `Pages in use … WorkerThreadPool GroupE`, no SCRIPT ERROR, no shutdown segfault (the `_exit_tree` poll-drain fix). AC-0313 clause-4 correction: the sub-metric `spawn3x3_ms` (measured off `world._startup_pending()`) is retargeted to the SIM DIAMOND load gate and renamed `simband_ms` — `world._startup_pending()` is a steady-state pacing flag (the forward-3x3 tm_cap bumps), never the gate. AC-0293: the 5x5 spawn burst that used to feed the load window is RETIRED (spawn/recenter ride normal streaming, the gate meshes from it) and the burst was the only `add_group_task` caller — the "Pages in use … WorkerThreadPool GroupE" line class is gone with it, so a clean exit now owes only the poll-drain |
| `starlighttest` | `_starlight_test` (AC-0283 P1) | AweStarlight engine (gdext/src/starlight.cpp) vs the legacy AweLighting pull kernel, cell-by-cell: a hand-built single column (stone, torch-lit cave, lava pool, glowstone ceiling, sky shaft) + 3 edits (wall across the shaft, torch removed, lava→water) each vs the engine re-flood, plus the settled live region | `ok`, `mismatches` (0), `cells` (5,308,416), `synthetic_ok`/`edit_ok` (the synthetic column), `ready`/`base_ok`/`edit_ok` (the live region), `cur_us`/`sl_us` (kernel timings) | `AWECRAFT_LOGIC=starlighttest` | ~3–6 min | The P1 "near light exact" gate — the engine-equivalence proof; must stay 0 after any starlight.cpp or lane change. The legacy kernel (lighting.cpp) is the TEST-ONLY reference this arm compares against (AC-0283 P4) |
| `brightslab` | `_brightslab_test` (AC-0283 P2, P5 star-native) | Bright-slab-on-edit regression in a fully settled 7×7: C1 surface break, C2 cave-wall break letting sky in sideways, C3 torch place, C4 the 93→73 bake storm (the sanctioned FEED delta), C5 halo-boundary break, C6 probe — each edits, drives engine + remesh lane to a quiet world, then asserts three ways | per case: `name`, `engine_vs_ref` (0), `ref_vs_pull` (0), `mesh_vs_eng_bake` (0), `mesh_vs_pull_bake` (0), `max_bright_delta` (0.0); C4: `bakes` (73), `over_bright_max` (0), `lver_drops` (0); `ok` (all cases exact) | `AWECRAFT_LOGIC=brightslab` | ~2–3 min | The P2 edit-lane integration gate: settled star payload ≡ legacy pull reference on the POST-EDIT data, and the applied mesh ≡ the worker re-bake of BOTH payloads (geometry exact; colors within the ArrayMesh 8-bit step). C2/C5/C6 carry one documented under-lit ref quad each (the scoped-bake margin; the classic sky-carry v-channel is CORRECT there — do not "fix" it) |
| `halo` | `_halo_test` (AC-0283 P3, AC-0286 promo block, rebased at AC-0312) | Settled-world gate for the 3-tier far band + promotion/demotion lifecycle, render 9 (real taxi ≤ 4, band A taxi 5, band B 6-7, band C 8; ≥ 9 data-only): (a) every real column seeded+settled ≡ the legacy reference; (b) every B/C column UNseeded and every applied low mesh ≡ a fresh h_avg_emit/low_emit_avg at the LIVE grid (G=8 tier 2 / G=4 tier 3, CB=16/G, exp G³) on the arm-recomputed heightmap sky; (b1) every band-A column far + materialized (the fill-identity on the stored data) + the cached sky eff obeys the rule; (c) promotion of a known band-A column — settled ≡ reference, single flood (seed-at-landing == 1), one enqueue per residency; (d) demote→band A: evicted + far + h-only (nn=0) + the old high-LOD mesh stays resident + visible (no re-materialization owed) + unlit 0; (d2) demote→band B: the re-lower REACHES the far column (low re-emitted at the current stamp, visible), high kept resident | `a` {`settled` (41), `inner_mismatches` (0), `boundary_mismatches` (0 — AC-0331: the old documented boundary-ring class (1) no longer manifests; the floor zeroes exactly the sub-floor margin cells it baked as zero)}, `b` {`slabs`/`slabs_ok` (233/233 byte-identical — AC-0331: slabs entirely below the far floor are pruned, was 821), `unseeded` (84), `payload_bad` (0), `rule_bad` (0)}, `b1` {`cols`/`ok` (20/20)}, `c` {`seed` (1), `enq` (1), `was_far` (true), `unlit` (0), `mismatches` (5 — PRE-EXISTING red, col [5,0], first cell (15,11,0) current=1 starlight=0 — identical pre/post AC-0331, NOT floor-caused; follow-up named in AC-0331 results)}, `d` {`evicted`, `far`, `h_only`, `mesh_resident`, `high_visible`, `unlit` (0), `evicts` (>0)}, `d2` {`far`, `low_visible`, `low_fresh`, `high_resident`, `unlit` (0)}; `ok` | `AWECRAFT_LOGIC=halo` | ~1–2 min | AC-0312 rebase: (b) is G-parameterized (the 8×8 tier is back), (b1) is new (band A), (d) is the band-A demotion contract (h-only form + the old high-LOD mesh stays — the keep-all-LOD data retention is retired; d_diag reports the post-demote form) and (d2) is new (the re-lower reaching far columns). The (c) promo block remains the AC-0286 evidence (single flood + one promotion per residency + no hide). **AC-0331 rebase (2026-09-19): the far-tier floor (Data.SEA, 126) — (b) now re-emits with p_yfloor=Data.SEA on both fresh-emit sides (the byte-identity gate holds ON the floor path) and the slab census drops 821 → 233 (slabs entirely below the floor are pruned from the far emits — the arm's `slabs` counts what the floored world actually draws, self-consistently: `slabs_ok == slabs`); (a)'s documented boundary-ring mismatch (1) reads 0 (the floor zeroes exactly the sub-floor margin cells the ring baked as zero — the class no longer manifests, NOT a fix to the ring bake); (c)'s 5-mismatch red (col [5,0]) is byte-identical pre/post — pre-existing, follow-up named in `tasks/AC-0331/AC-0331-results.html`; (d) `evicts` 33 / (d2) ok unchanged in contract** |
| `lightstate` | `_lightstate_test` (AC-0273, rebased at AC-0312) | Per-slab light-state canaries on an R16 moving camera (`AWECRAFT_LS_RADIUS=16` — the default 4 is drain-only; 16 activates the 3-tier draw band): the slab's opaque-surface vertex colors keyed by quantized position (baked-light A/B — first landing vs a 1-block edit rebuild on chunk 0,1), the double-LOD canary (`low_on_high_n` + the live census) and the sky-wash census over the demote churn (the player jumps 8.5 chunks — the original band drops into band A/B/C and the demotion re-opens the lows) | per-chunk `leaks` (0) / `air_n` / `mean_eff`, `worst` (null — no leaking cell), `wash_detected` (false), `ab` {`arr_diff_cells` (1 — the removed block's own light cell), `vc_diff` (0 — no existing face re-colored by the edit), `vc_common`/`vc_n0`/`vc_n1`}, `demoted` (41), `stragglers` (0), `move` {`low_on_high_n` delta (the FLIP count — see notes), `double_lod_slabs_now`/`double_lod_chunks_now` (0/0), `hole_traj` (the AC-0263 demote-ring hole-fill trajectory, e.g. 1214 → filled), `settle_frames` (band_drained)} | `AWECRAFT_LOGIC=lightstate` `AWECRAFT_LS_RADIUS=16` | ~1 min (the 3-tier far payload made the R16 build cheap — the old "~10–20 min" estimate is stale) | AC-0312 rebase: `low_on_high_n` is now the keep-all-LOD FLIP count (AC-0263 — `_low_place_slab` increments it when the low lands on a slab whose high stays resident+hidden; the demote churn flips 7 in the measured run) — the pre-AC-0312 "delta 0" contract is SUPERSEDED. The invariant that stays: the live double-LOD CENSUS 0/0 (the flip is atomic — one visible tier at a time) + `wash_detected` false + 0 leaks. Measured (2026-09-18): all 20 grid chunks leaks 0, ab vc_diff 0 / arr_diff_cells 1, demoted 41, stragglers 0, census 0/0, wash false |
| `ladder` | `_ladder_test` (AC-0261, rebuilt at AC-0283 P3, rebased at AC-0313, 3-tier rebuild at AC-0312) | The 3-tier far-band draw ladder: small world (render 9; sim 4 / medium_start 5 / low_start 7 pins) — REAL [0, band0_r] = per-slab full meshes; BAND A (taxi 5) = the far column MATERIALIZED to the 16×16×16 no-cave fill (far + far_mat + the materialization identity on the stored data: per-cell top block == payload top, farab's 4a gate; + the cached sky eff obeys the heightmap-sky rule on 16 sample cells × all H + the E strip); AC-0321 band-A texturing gate: every landed non-air slab's opaque surface equals a FRESH real-band high emit of the SAME slabs under the mat dispatch's own inputs (engine precision, _tex_mesh_diff — the UV array byte-exact, non-degenerate) + the visible MeshInstance wears the textured high material (chunk_lit_opaque) sampling EXACTLY the emitted canvas height (the merged-atlas canvas when _tm_ms_full.rects is non-empty, else the plain atlas — NOT the _lod_avg_mat() noise placeholder) + no LOW placeholder visible; BAND B (taxi 6-7) = 8×8 avg slabs, BAND C (taxi 8) = 4×4 (mesh byte-check at the live grid + sky alpha + per-slab tier stamp vs live tier); (c) the LIVE B<->C boundary flip (raise low_start 7→8: the taxi-8 slabs re-emit 4×4→8×8, boundary_flip_n > 0); + the air checks + the Y-layer-rank encoding | `ok`, `band_a_cols`/`band_a_ok` (20/20), `band_a_fail` (per-column reasons, [] when ok — tex_* reasons are the AC-0321 gate: tex_h = material canvas ≠ emitted UV space, tex_emit_slab = UV/vertex mismatch vs the fresh real-band emit, tex_avgmat = the noise placeholder material, tex_low_visible = a placeholder showing), `band_a_tex_q` (opaque quads compared, u-exact), `band_a_tex_maxv` (max landed UV v — merged-canvas space when the merge atlas is on), `band_a_fluid_only_cols` (1 — AC-0331: the deep-ocean band-A column whose floored OPAQUE mesh is empty (col (0,5), H=124 — its only kept row is the waterline); the arm's has_mesh counts the FLUID instance (the water surface), else the floor's new column class false-negatives "the column shows nothing"), `band_a_ms_h` (the emitted canvas height: merged when rects non-empty, else plain atlas — 1888 px, a CANVAS dimension, NOT a wall time), `band_b_slabs`/`band_b_mesh_ok` (155/155 — AC-0331 floor: slabs 0..6 pruned, was 519), `band_c_slabs`/`band_c_mesh_ok` (94/94 — AC-0331 floor, was 318), `boundary_flip_n` (94 > 0 — AC-0331: was 102; the 126-flip "no flips" pre-AC-0312 baseline is gone with the tiers), `tier_mismatch` (0), `sky_mixed_slabs` (72 — AC-0331: was 73), `sky_bad_alpha` (0), `water_surfaces` (0 — no water in the spawn area, census only), `air_synth_ok`, `air_inworld_ok`, `clutter_ok`, `layerrank_ok`, `emit_cpp_b_ms`/`emit_cpp_c_ms` (the real worker emit cost sample on the far payload) | `AWECRAFT_LOGIC=ladder` | ~2–4 min | AC-0312 restored the 8×8 tier (band B) + the band-A materialization — the arm tests all three draw tiers on the SAME h-only far payload. The materialization identity is the resident-form gate: a materialized column holds the fill slabs in c.data (the transient draw form) + c.far + the payload (the save form). AC-0313's standing counts (695/44) are superseded by the 3-tier measured values above (the byte-identity + identity gates are the invariants, not the numbers). AC-0321 added the band-A texturing gate (the mat landing is a FULL build → merged-canvas UVs → the attach must wear the merged-canvas material; the pre-fix evidence was tex_h(1024!=1888) on all 20 columns with the UVs u-exact — the material, not the emit, was wrong). The editmat arm (AC-0187) is the parity arm for the OTHER half of the contract (scoped builds → plain-atlas material): a change to apply_accs/apply_edit_accs that breaks either half turns one of the two arms red. **AC-0331 (2026-09-19, the far-tier floor): the floor prunes slabs 0..6 from every far column — `band_b_slabs` 519 → 155, `band_c_slabs` 318 → 94, `sky_mixed_slabs` 73 → 72, `boundary_flip_n` 102 → 94 (the censuses count what the floored world draws; the self-consistency gate `mesh_ok == slabs` holds). band A is unchanged at 20/20 — but the deep-ocean column (0,5) (H=124: slab-7 terrain zeroed by the floor's per-voxel row gate, water 125-126 kept) now has NO OPAQUE mesh at all — its visible geometry is the water surface as a FLUID instance (build_accs routes id 5 to the fluid acc, never opaque) — `has_mesh` counts fluid instances too (new `band_a_fluid_only_cols` 1); the pre-floor pass rode on the column's pruned deep terrain. The band-A texturing reference build also takes the floor (si0 = 7, the floor slab) so the compared meshes match the applied ones. `band_a_ms_h` is the merged-atlas canvas HEIGHT in px (1888) — a dimension, not a wall time (an earlier read mislabeled it).** |
| `meshprobe` | `_meshprobe_test` (AC-0211) | Mesh byte-identity probe: GDScript-side emit vs the C++ emit on the same inputs — full LOD + LOD quad-color checks (per-quad color from the normal/lighting model), vertex counts, atlas-rect coverage (the high mesh writes (0,0) for un-atlased LOD quads — reported, not gated) | `ok`, `match_rate` (1.0), `verts_gd`/`verts_cpp` (equal, >0), `n_samples` (≥8), `ae_pairs`/`ae_match` (atlas-edge pairs exact), `uv_repeat_ok`, `low_max_h_built` | `AWECRAFT_LOGIC=meshprobe` | ~1–2 min | 1.0 invariant across the AC-0283..0286 series (mesh.cpp gained only `h_avg_emit`, which shares the emit core and is byte-identical to the slab emitter). **AC-0331 (2026-09-19, the far-tier floor): the arm gained a FLOOR pair per chunk that has slab-7 data — the GDScript `world._avg_emit_slab` (the floor mask on grids 6/7/8, p_yfloor=Data.SEA) vs C++ `low_emit_avg(…, yfloor=Data.SEA)`, full v/i/n/c/mh compare; the pre-existing no-floor pair (yfloor=-1, the DEFVAL no-op proof) is kept. `ae_pairs` 68 → 136, `ae_match` 136/136, `match_rate` 1.0 — the floor mask is byte-exact on both sides** |
| `farab` | `_farab_test` (AC-0284b, extended at AC-0312) | FAR (h-only) column A/B battery at BOTH draw grids: (a) skip-filled slabs (`generate_resl` skip=1 — now the band-A materialization fill) through low_emit_avg vs (b) the far payload through `AweMesh.h_avg_emit` — identical grid inputs + fcc/sky must give byte-identical output at G=4 (band C) AND G=8 (band B), including the WATER EXCEPTION (water-topped cells emit the top face with the translucent water material — the wv/wi/wn/wc/wu arrays compared); (4) the MATERIALIZATION IDENTITY — (4a) HARD: the fill's surface row == the payload TOP row, every (x,z) (the band-A fill contract); (4b) REPORTED (never gated): the full column's post-cave top vs the no-cave H (pierce = the band shows solid where the real column has a surface hole); plus the v6 codec round-trip | `ok`, `cols` (45 incl. open-water + shoreline), `h_mismatch` (0), `slab_cmp` (1080, 4×4) + `slab_cmp8` (1080, 8×8), 0 mismatch each, `water_slabs` (4) / `water_slabs8` (5) / `water_verts` (20 — non-vacuous), `mat_surf_diff` (0 — 4a holds), `mat_pierce_cells` (1218) / `mat_pierce_cols` (33 — the 4b honest deviation, RECORDED), `mat_uplift_cells` (8517 — informational), `codec` {far 198 B, bit0 ok, v4 ok} | `AWECRAFT_LOGIC=farab` | ~2–4 min | The AC-0284b invariant: the far H-only payload (H u16 + biome + top, v6 flag bit 1) emits byte-identical meshes — H is the heightmap and stays bit-exact with the full path so promotion never shifts terrain. AC-0312 added the 8×8 grid + the water exception + the materialization identity (the band-A fill contract, 4a HARD / 4b recorded). **AC-0331 (2026-09-19, the far-tier floor): BOTH emit calls now run with `yfloor=Data.SEA` — the byte-identity gate holds ON the floor path (the C++ post-fill mask and the GDScript reference are the same gate), and the water census stays non-vacuous (`water_slabs` 4 / `water_slabs8` 5 / `water_verts` 20 — the water surface SURVIVES the floor mask, the O3 cell-kept-whole semantics in action). All other counts unchanged: `cols` 45, `h_mismatch` 0, `slab_cmp`/`slab_cmp8` 1080/1080, `mat_surf_diff` 0** |
| `texrefresh` | `_texrefresh_test` (AC-0297) | Star-native tex-refresh: swap ONE atlas tile (grass top) through the real pack path (Data.apply_atlas + the tint re-bake) + world.refresh_textures(); then (1) the face-color cache changed, (2) the real column's HIGH slabs are byte-identical to the settled STAR payload re-build AND to their pre-swap mesh (a pixel-only atlas change leaves vertex data untouched), (3) the halo column's re-lowered LOW slabs are byte-identical to a FRESH h_avg_emit and the surface slab is provably different | `ok`, `slabs` (>0), `mism` (0), `surface_changed` (true), per-slab pre/post diffs | `AWECRAFT_LOGIC=texrefresh` | ~1–2 min | The AC-0297 gate: the tex-refresh dispatch is star-native (real = settled payload / halo = h_avg_emit). 7 slabs report "decomp_changed" (lossless per-face → merged quad decomposition — documented, not a failure) |
<!-- END GENERATED: modes -->

`settings` is also a valid battery mode (`AWECRAFT_BATTERY=settings;…`) — it runs the
same `_settings_test` inside the battery (see its table row).

### Not in table (deliberately excluded)
- `logic == ""` (main.gd:116) — incidental comparison in the headless-idle check, not a
  mode branch. (AC-0231 removed the `lodband` + `lodswap` mode branches — band-2
  coarse-LOD is gone with the band; the far-LOD gates now live in the `r16` arm's
  `lod` RESULT block. The `logic ==` grep count includes the battery re-dispatch
  branches, so it is not a flat mode count.) Table coverage of the remaining mode
  rows = 100%.
- `AWECRAFT_PROBE`/`AWECRAFT_BCELL` (main.gd:586–758) — not `logic` branches; debug
  print hooks (`PROBE ocean_cells=…`, `MAP`/`BIO` rows, `SEED` scan, `BCELL`) that run
  under any logic dispatch before the mode body and quit early.
- `AWECRAFT_MESH_INFO` (main.gd:965) — `MINFO {…}` prints in the unknown-logic fallback
  path (falls through to the legacy `_logic_check()` RESULT, now unused).
- `AWECRAFT_IMPORT_PACK` (main.gd:30) — atlas pack import probe in `_ready()`, exits
  before any mode; not a logic branch.

## 2. Render / shot env hooks

All render hooks need the software-GL recipe (§4); typical timeout **300 000 ms**
(llvmpipe is slow; keep `AWECRAFT_RADIUS` 1–2). All are in the `HARNESS_ENVS` list
(main.gd:273) which also forces radius 4 + default settings when set.

**Renders here are a PROXY, not the product renderer (verified 2026-09-16, AC-0298).**
The recipe forces `--rendering-method gl_compatibility` (software GL through llvmpipe, the
only path proven on this box) while the game ships `forward_plus` (AC-0241). gl_compatibility
provably **cannot** show Forward+-only features — the engine logs
`Depth of field blur is only available when using the Forward+ or Mobile renderer` (DOF is a
shipped developer setting, AC-0281) — and it differs in tonemap/clustered-lighting behaviour.
Use a render as evidence about **geometry, layout and UI**, never about final colour, DOF or
lighting feel; the authoritative look is the user's Windows build of the shipped renderer.
The `forward_plus` A/B on this box is unproven (a Vulkan device via lavapipe is present but
untested) — AC-0300.

| env | what it does | required flags / notes |
|---|---|---|
| `AWECRAFT_SNAPSHOT=path.png` | boot world (menu-first unless `AWECRAFT_MENU_BOOT=1`), wait for build, snap viewport PNG | xvfb-run -a + `--rendering-method gl_compatibility` (proxy renderer — see the note above §2); sets `RESULT {"m4":"ok",w,h,cam}` |
| `AWECRAFT_SNAPSHOT2=path.png` | second snap in `AWECRAFT_CAM=shaft` (after fluid settle) | only with cam=shaft |
| `AWECRAFT_CAM=top\|iso\|iso2\|sky\|eyeup\|sandpad\|shaft\|cave` | camera preset for snapshot runs (main.gd:1003–1083) | `sky`/`eyeup` look at sun; `shaft` drops a water column + double-snap; `cave` teleports into the first enclosed cave pocket + 3D torch array + 300-frame settle (AC-0110); default (empty) = first-person player spawn. **AC-0152/0160 finding:** the on-demand player spawn (main.gd:1310, `snapshot_path != "" and player == null`) makes the player camera current AFTER the named-camera block — `cam=top` snapshots silently come out first-person. For a true top-down band/LOD shot use `AWECRAFT_AIM="x,y,z,yaw,pitch"` (e.g. `8,240,8,0,-1.57` = 100 m above spawn, straight down) with the default cam |
| `AWECRAFT_SIZE=W,H` | force window size (e.g. `1280,720`) before boot | no-size → `Settings.apply_window` |
| `AWECRAFT_MENU_SHOT=path.png` | snap the main menu (skips world boot); `AWECRAFT_MENU_VIEW=options` opens options panel | `RESULT {"menu":true,"mode",…,"build","values"}` |
| `AWECRAFT_FLUID_SHOT=1` | with snapshot: teleport player to shore aim, place water bucket, snap before+after | needs player spawn (default cam) |
| `AWECRAFT_ANIM_SHOT=1` + `AWECRAFT_ANIM_PHASE=0..1` | camera above a water cell to capture fluid anim; phase sets shader `phase` | without a water cell near spawn it prints and skips |
| `AWECRAFT_HELD=id` | give + select item id for the held viewmodel in snapshot runs | with `AWECRAFT_SNAPSHOT` |
| `AWECRAFT_INV=1\|table` | open inventory (E) or crafting table with seeded contents for UI shots | `1` also autofills + hovers item 111 |
| `AWECRAFT_FPV_ITEM=id` | held item for first-person snapshot (also used by `AWECRAFT_WALK_SHOT`) | |
| `AWECRAFT_WALK_SHOT=1` | build flat pad, walk forward, snap ×2 (`_w2` suffix) | |
| `AWECRAFT_EMPTYHAND=1` | empty inventory for bare-hand shot | |
| `AWECRAFT_SWING=fraction` | hold swing at fraction for the shot | float 0..1 |
| `AWECRAFT_HP=n` / `AWECRAFT_HUNGER=n` | set player hp / hunger for HUD shots | 0..20 |
| `AWECRAFT_TIME=f` | set `Game.time_of_day` (0..1, wrapped) before boot | also used by daynight mode |
| `AWECRAFT_SEED=n` | world seed (default 44) — all modes + genhash | |
| `AWECRAFT_ONLY=cx,cz;…` | hide mesh/fluid instances of all chunks NOT in the list (visibility filter) | post-recenter; **not** the battery |
| `AWECRAFT_MENU_BOOT=1` / `AWECRAFT_MENU=0` | menu-first boot → click Play / explicit game-first skip | menu-boot is the default (all platforms) |
| `AWECRAFT_PAUSE_SHOT=1` | with menu_boot: snap the pause menu (P key) | `RESULT {"pause_shot":true,…}` |
| `AWECRAFT_NO_FOG=1` / `AWECRAFT_NO_COLLISION=1` | disable env fog / chunk collision at boot | |
| `AWECRAFT_WALL_BEFORE=1` / `AWECRAFT_WALL_SHOTS=…` | `wallshot` mode: before/after AC-0067 comparison shots | writes into `tasks/AC-0067/` |

## 3. Battery + known-stable values

### `AWECRAFT_BATTERY` (main.gd:207, post-AC-0061)
`AWECRAFT_BATTERY=a;b;c` = ONE process, ONE `Game.new_world` per mode, in order:
- each mode runs its normal harness body (per-mode `RESULT` printed verbatim, also
  written to `user://debug_result.json`);
- `BATTMODE <mode> ms=<n>` printed per mode;
- between modes: `_batt_reset_state` — `Game.mode="pause"` drop-freeze pre-settle,
  player freed, chunk `data`/`fl` zeroed, drops freed, light/fluid dirty maps cleared,
  `render_radius=4`, time 0.3, recenter + await build;
- final combined `RESULT {"battery": {mode: {…per-mode RESULT + ms}}, "ok": <all ok,
  genhash exempt>, "total_ms": …}`.
- Battery modes: `player`, `interact`, `light`, `fluids`, `buckets`, `genhash`,
  `settings`; anything else prints `BATTSKIP <mode>` (no RESULT, no failure).
- Per-mode reset adds wall cost vs standalone runs (≈+10 s per reset at r4);
  SMOKE tier (2–4 dependency-mapped modes + genhash) is the fast path.
- `AWECRAFT_SEED` applies to every battery mode (genhash arm included — use the
  full mode list, a truncated list that ends before genhash loses the seed arm).

Fresh smoke battery 2026-08-24 (`player;interact;light;fluids;genhash`): wall 33 s;
BATTMODE: player 2739 ms, interact 1349 ms, light 2879 ms, fluids 3763 ms, genhash 884 ms.

### Known-stable gate values (FRESH runs — RE-ESTABLISHED 2026-08-29 by AC-0091, H 80→384; AMENDED 2026-08-31 by AC-0152/0160 banded streaming + AC-0155 full-chunk-save)

AC-0091 raised world height 80→384 (sea y30→y126). **Every world-geometry-derived
baseline changed; the table below is the NEW set.** Values that are pure mechanics
(torch propagation, water-on-lava reaction, interact booleans) are unchanged and
carry their original provenance. **AC-0152/0160 amendment:** the streaming set at
harness r4 changed from the 121-chunk square to the 69-chunk banded set
(circle(4) ∪ diamond(5) ∪ 8-ring; band 0 = 21, band 3 = 48), so the fluids sea
counters and the boundary band moved (rows below); occlude/cave/MINFO wait on
`chunks.size() >= 81` (old square) and now hit their wait cap at r4 (expected
drift, outside the gate set).

<!-- BEGIN GENERATED: standing-values -->
| value | fresh result (2026-08-29, H=384) | established by |
|---|---|---|
| player `start` | **[8.5, 142.0, 8.5]** — the SEARCHED column on NATURAL terrain (AC-0314 removed the pad, re-measured 2026-09-18): the deterministic spawn search (dry / gently-sloped / un-vegetated, (taxi,x,z) order) picks the anchor column [8,8] as tier 0 — natural surface top 141 (MC Y 77), the pad centre's old 136 plateau is gone. `jump_peak_y` 142.43, `after_fly_y` 145.27 (re-measured with the natural spawn — the walk/jump traverse the natural undulations around the anchor column) | AC-0091 (was [8.5,35,8.5] @ H=80) → AC-0324 (search + re-measure) → AC-0314 (pad removal, natural column [8,8] top 141) |
| fluids `sea_surface_before`/`_final` | **2756 / 2756** (count over `world.chunks.keys()` — r4 resident set changed 121→69 chunks at AC-0152/0160, so the absolute count moved; before==after = the invariant). **SUPERSEDED at AC-0314 (2026-09-18): re-measured **0 / 0** + `sea_stable` false — the arm is DEGENERATE on the padless seed-44 spawn region (no data ocean water at y=126 in the r4 resident set; the natural terrain around the new spawn is all above sea). Identical to the AC-0321/AC-0324 documented state (pre-existing since the AC-0215 3D field, NOT re-baselined — the pad removal changed the spawn column's H, not the ocean) | AC-0068 invariant, values AC-0152/0160 (old 2730/888 VOID) → AC-0314 (degenerate state re-confirmed) |
| fluids `sea_backed_before`/`_final` | **914 / 914** (SUPERSEDED at AC-0314: re-measured **0 / 0** — same pre-existing degeneracy as the sea_surface row above) | AC-0152/0160 (was 888 @ AC-0091, 1406 @ H=80) → AC-0314 (degenerate state re-confirmed) |
| fluids `sea_stable` | **true** (`water_delta` 1, `shore_after` [5,7], `source_after` [5,8]). **SUPERSEDED at AC-0314: re-measured **false** (degenerate — no ocean in the spawn region to stabilize; fixture water also unplaced, `water_delta` 0) — pre-existing since AC-0215, documented at AC-0321/AC-0324, not re-baselined** | AC-0068 → AC-0314 (degenerate state re-confirmed) |
| fluids `water_on_lava_result` / `sideways_lava_result` | **25 / 9** (obsidian / stone). **AC-0314 re-measure: 0 / 0 — the reaction fixture cells read air (degenerate, no lava/water in the spawn region) — same pre-existing state as AC-0321/AC-0324, not re-baselined (the fluid REACTION lanes are untouched by the pad removal)** | M6 fluids port → AC-0314 (degenerate state re-confirmed) |
| interact `drop_spawned` / `place_ok` | **true / true** (`breakable_id` 2, `place_cell`/`target_cell` **[8,141,8]**, `after_place_cell` 2 — re-measured 2026-09-18 at AC-0314: the DDA target is the NATURAL surface top 141 of the spawn column, was [8,136,8] under the pad) | M4 interaction (place y 35→136 @ AC-0091) → AC-0314 (natural top 141) |
| light `torch_level` | **14** (`surface_eff` 15, `torch_far_after` 9). **AC-0314 re-measure: `surface_eff` still 15; the torch/cave probe values are the pre-existing DEGENERATE state (-1 — no qualifying pocket in the arm scan window near the new spawn top), identical to AC-0321/AC-0324 — not re-baselined** | M5 lighting → AC-0314 (surface 15 re-confirmed; probe degenerate pre-existing) |
| light `cave_eff` / `torch_far_before` | **10 / 1** (cave pocket re-scaled with terrain; dark-cave semantics hold). **SUPERSEDED at AC-0314: re-measured **-1 / -1** — the arm's pocket scan (top−6..top−30, ±16 around the spawn column) finds no qualifying pocket on the padless terrain; identical to the AC-0321/AC-0324 documented degenerate state (pre-existing since the AC-0215 3D field — the cave LIGHTING LANE is untouched by AC-0314, which changed terrain only) — not re-baselined** | AC-0091 (was 0 / 0 @ H=80) → AC-0314 (degenerate state re-confirmed) |
| basis (new arm `AWECRAFT_LOGIC=basis`) | AC-0314 re-measure (2026-09-18, pad removed): spawn_top **141** solid + air-above (natural surface of the anchor column — was 136 under the pad; `mc_y.spawn_top` 72→77), surface sky/eff 15/15 (unchanged). `ok` **false** for TWO PRE-EXISTING reasons (both red on pristine HEAD, proven by bisection — NOT caused by AC-0314): (a) `bedrock_y0` 0 — the arm's FIRST read races the boot burst landing of chunk (0,0) (AC-0263 made create_chunk node-only; the read happens before the burst handoff — a recenter-then-await read of the same cell is 11); (b) the sea check — the mirror (coarse 2-D) picks an "ocean" column whose C++ data is land (ocean_at [8,8] padless / [8,-4] with pad, stone at y=126 — the mirror/C++ divergence since the AC-0215 3D field). Named follow-ups: the bedrock-read race + the mirror/data ocean divergence | AC-0091 (MC Y = internal − 64 surface contract) → AC-0314 (spawn-top rebase 136→141; two pre-existing instrument reds documented with bisection evidence) |
| nightday `cave` / `torch` | **15.0 day / 3.0 night**, torch **14.19996** day==night | AC-0135 (EXACT, height-invariant) |
| genhash | **25/25** `GENHASH` lines, 32-hex MD5 each, deterministic (2 runs byte-identical, GENMS 51 — re-measured 2026-09-19 at AC-0289). **REBASED at AC-0289 (cave P1 spaghetti/noodle tunnels — INTENTIONAL GENERATION CHANGE, not a regression; the user's 2026-09-15 note: old-world save compatibility NOT required, fresh worlds only): all 25 hashes are NEW vs the AC-0288 reference — the P1 tunnel fields (spaghetti f_spag = fbm3 xz/14 seed+303 2oct + noodle f_nood = fbm3 xz/10.5 seed+304 2oct, gated by the rarity selector f_gate = fbm3 xz/56 seed+305 2oct over the window 0.52–0.58 — tunnel air where |f_spag−0.5|<0.16·w or |f_nood−0.5|<0.08·w, applied before the he/solidf scan and in the veg margin scan, full path only) re-carve the deep slabs of every chunk in the 5×5 window. The H distribution is UNCHANGED — proven byte-identical, not asserted: AweGen.column_heights16 over a 21×21-chunk window gives the same SHA-256 before and after (`dc0088b9…264c7e`, H∈[118,164]) — the tunnel fields are built and read ONLY on the full path (skip==0), so the far h-only payload and the lazy skip fill never touch them — and farab `h_mismatch` 0 keeps the far/full H bit-exact contract** — e.g. `GENHASH 0 0 692eb06c…` (AC-0288) → `673e1f231e3c4097a8b98332e8ee7c4c` new (full new 25 → `.scratch/AC-0289-gates/genhash-after2-1.log`; pre-change ref: `.scratch/AC-0289-gates/genhash-before-1.log`). Previous rebase (AC-0288 cave P0 tune): 25/25 new; before that (AC-0314 pad removal): 5 of 25 new. | AC-0082 gate / AC-0091 values → AC-0314 (pad removal rebase — 5/25 chunks) → AC-0288 (cave P0 tune — intentional, 25/25 new, H distribution byte-identical) → AC-0289 (cave P1 tunnels — intentional, 25/25 new, H distribution byte-identical, tunnel fields full-path-only) |
| starlighttest `mismatches` | **0 / 5,308,416 cells** — AweStarlight ≡ the legacy pull kernel (synthetic column + 3 edits + settled live region) | AC-0283 P1 (re-verified at every AC-0283..0286 closeout — the engine is exact) |
| brightslab C1–C6 | per case **`engine_vs_ref` 0 / `ref_vs_pull` 0 / mesh bakes exact / `max_bright_delta` 0.0**; C4 **bakes 73 / over-bright 0 / lver_drops 0** (the 93→73 FEED delta is the AC-0297 sanctioned change, not a regression); C2/C5/C6 each carry one documented under-lit ref quad (the scoped-bake margin) | AC-0283 P2 (93 bakes) → AC-0297 (73, star-native tex-refresh) |
| farab `slab_mismatch` | **0 / 1080** (1080/1080 byte-identical at BOTH grids — 45 cols × 24 slabs: the far census is 45 columns now (the 984/41-cols figure is the pre-rebase count, superseded), `h_mismatch` 0, 4a materialization HARD contract holds `mat_surf_diff` 0, water census non-vacuous (4/5/20)) — the far H-only payload (H u16 + biome + top, 129 B on disk at AC-0314 — the delta-codec byte count moved with the new H values, informational; v6 flag bit 1) emits byte-identical meshes through `AweMesh.h_avg_emit`. AC-0314 re-measured 4b: `mat_pierce_cells` **1233** (was 1218 — the padless natural surface opens a few more cells at the band, RECORDED) / `mat_pierce_cols` 33 (unchanged), `mat_uplift_cells` 8620 (was 8517, informational). AC-0288 (cave P0 tune) re-measure: `h_mismatch` **0** (the far H payload stays bit-exact with the full path — the tuned cave field never touches the H), 4a HARD contract holds `mat_surf_diff` 0, `slab_mismatch` 0/0; informational 4b: `mat_pierce_cells` **1344** (was 1233 — the re-carved caves open more cells at the band) / `mat_pierce_cols` **29** (was 33), `mat_uplift_cells` 8607 (informational). **AC-0331 (2026-09-19, the far-tier floor, re-measured): both emits now run at `yfloor=Data.SEA` — the floor path itself is byte-gated — and EVERY count is unchanged (0/1080 both grids, `h_mismatch` 0, `mat_surf_diff` 0, water 4/5/20 — the water surface survives the floor mask; the floor adds zero divergence to the identity gate)** | AC-0284b (H stays bit-exact with the full path so promotion never shifts terrain) → AC-0314 (re-measured; 0/984 holds) → AC-0331 (re-measured ON the floor path; 0/1080 holds) |
| halo (b) heightmap-sky re-emit | `ok` true — AC-0314 re-measure (2026-09-18) on the arm CURRENT shape (rebuilt AC-0312): (b) 84 cols / **821/821 slabs** ok (unseeded 84 = the far-band class), (b1) 20/20, (c) 0 mismatches over 9 promotions (settled, ref converged), (d)/(d2) ok (eviction + residency), (a) 1 boundary mismatch at taxi (4,0) — the documented under-lit-until-promotion class, ok true. The **192/192** number above was the pre-AC-0312 arm shape — superseded. **AC-0331 re-measure (2026-09-19, ON the far-tier floor): (b) **233/233 slabs** (slabs entirely below the floor are pruned from the far re-emits — both sides take `p_yfloor=Data.SEA`, byte-identity holds on the floor path), (b1) 20/20, (d)/(d2) ok (evicts 33), (a) **0** boundary mismatches (the documented class no longer manifests — the floor zeroes exactly the sub-floor margin cells), (c) **5 mismatches, col [5,0], first cell (15,11,0) current=1 starlight=0 — PRE-EXISTING deterministic red, byte-identical before and after the floor (NOT floor-caused; the floor cannot reach the promotion path's tier-0 build); follow-up named in `tasks/AC-0331/AC-0331-results.html`** | AC-0284b (AC-0284a was 0 — the 26 = the legacy-reference far-hole class) → AC-0314 (re-measured on the AC-0312 shape; ok true) |
| MINFO | **AC-0152/0160 DRIFT:** r4 resident set is now 69 chunks (circle(4) ∪ diamond(5) ∪ 8-ring), so the 121/81/40 shape no longer reproduces (boundary `resident_final` 101 incl. candidates); occlude/cave arms wait on `chunks.size() >= 81` and hit the wait cap → their numbers drift. OUTSIDE the gate set — do not chase | AC-0091 shape (`.scratch/AC-0091-gates/minfo_new.txt`) SUPERSEDED at r4 by AC-0152/0160 |
| boundary r4 one-shot | `ok`/`remesh_ok`/`marker_ok` true, walk `p95_ms` **≈ 42** (p50 ~13) — **re-measured 2026-09-20 at the AC-0337 closeout (per-slab collision census + stop-rebuilding-unchanged, the one-shot A/B): `ok`/`remesh_ok`/`marker_ok` true, `flap` 0, p95 **41** (was 42 — flat-to-better, NO walk-p95 trade-off), p50 **13** (flat), `max_ms` **524** (single worst-frame sample, in the documented noisy band 357–824), `built_final` 64, `resident_final` 93, `in_radius_built_final` 59, `staged_dropped` **1845** (now a dedicated standing row with threshold). The new per-slab census: `collision_slabs` 2079 (was 2236 before the change, −7%), `collision_slab_ms` 4450.6 (was 4837.8), `collision_n` batches 343 (was 517, −34%), `staged_drained` 216 (was 398, −46%), `reband_entry` 186 with `reband_rearm_slabs` **0** — every re-entered band-0 column's geometry stamp still matched, so no kept body was re-derived (the before run re-derived the re-entries' + light-only remeshes' slabs unconditionally). Pre-AC-0337: **re-measured 2026-09-19 at the AC-0331 closeout (the far-tier floor, one-shot self-check): `ok`/`remesh_ok`/`marker_ok` true, `flap` 0, p95 **42** (was 39 — within run noise, NO walk-p95 trade-off), p50 **13** (flat), `max_ms` **580** (a single worst-frame sample, in the documented noisy band 357–824; the recenter `burst_p95_ms` 8900 vs 7237 pre — burst-class, not walk-class), `built_final` 62, `resident_final` 93, `in_radius_built_final` 58 — the floor prunes below-floor far slabs, so the recenter bursts build slightly LESS far work yet the burst p95 rose: run-to-run burst scheduling noise, the walk tail (the gated metric) is flat-to-better. **Pre-AC-0331: re-measured 2026-09-18 at the AC-0314 closeout (pad removal, the one-shot self-check): p95 **39**, p50 13, `max_ms` 660, `built_final` 62, `resident_final` 93, `in_radius_built_final` 57, `marker_ok` true (marker on the natural spawn terrain, y=141), `flap` 0, `remesh_ok` true — NO walk-p95 trade-off from the pad removal (39 → 39). Pre-AC-0314: **re-measured 2026-09-17 at the AC-0313 closeout (default walk speed 16 b/s, the same invocation as the AC-0286 row below): p95 39, p50 13, `max_ms` **357–824** (a single worst-frame sample, noisy by nature — both values are far below the AC-0286-era 9.2 s burst class), `built_final` 61–62, `resident_final` 93, `in_radius_built_final` 56–57, `staged_dropped` 1518–1678, one multi-hundred-ms burst at a far crossing** — the inside-out FULL-COLUMN build order (AC-0313) raises the tail (p95 22 → 39, max 128 → 357) while building MORE of the inner radius (in-radius built 41 → 56, `built_final` 43 → 61); p50 is flat, so the steady walk is unchanged and the tail is the price of fewer/bigger dispatch units — an honest, user-directed trade-off (recorded in AC-0313's notes, not smoothed). **At `AWECRAFT_WALK_SPEED=8` the arm reads `ok:false` for a BUDGET reason (fewer crossings than `walk_lines`), while the same run's world values are clean (p50 14, p95 21, `marker_ok` true) — judged on the metric fields, never on `ok` off the default speed.** — **supersedes** ≈ 22 (AC-0286 closeout 2026-09-17: p95 22, p50 14, `max_ms` 128, `built_final` 43, `resident_final` 93, `queue_size` 81, `staged_dropped` 707, one 9.2 s burst — the AC-0283 P2/P3 streaming), **which superseded** ≈ 30–37 (AC-0178: p50 ~13, p95 30–37 — the group-consumption fix un-stuck the recenter 5×5 burst prune, front-loading work into smaller bursts and smoothing per-frame walk cost), **which superseded** ≈ 73–89 (AC-0155; re-verified 74/80 at AC-0158/0164), ≈ 54 (AC-0152/0160) and 267–401 / ~39.9k–47.6k VOID (banded streaming: circle ∪ diamond ∪ ring set, spawn-fast drain). **RESOLVED (AC-0178):** the engine-level `ERROR: Invalid Task ID` at `is_task_completed` from `recenter` (world.gd) is GONE — the 5×5 burst is a GROUP task; the recenter prune uses `is_group_task_completed` + `wait_for_group_task_completion`. Zero SCRIPT ERROR in all runs since | AC-0337 closeout (2026-09-20, p95 41 / p50 13 — no walk-p95 trade-off from the collision re-derive stop; new per-slab census + reband counters) supersedes AC-0331 closeout (2026-09-19, p95 42 / p50 13 — no walk-p95 trade-off from the floor) supersedes AC-0314 closeout (2026-09-18, p95 39 unchanged after pad removal) supersedes AC-0313 closeout (2026-09-17) supersedes AC-0286 closeout (2026-09-17) supersedes AC-0178 (2026-09-01) supersedes AC-0155 (2026-08-31) supersedes AC-0152/0160 supersedes AC-0091/AC-0143 |
| boundary r4 `staged_dropped` (the staged collision drain's oversubscription census) | **1845** (re-measured 2026-09-20 at the AC-0337 closeout — the staged lane drains ≤ 2 columns/frame; a dropped entry lost validity before its drain (eviction / band change / the mesh finished). The 2026-09-20 tree's band is **1746–1845** (the AC-0337 before/after A/B: 1746/1782 pre-change, 1845 post). **THRESHOLD: ≤ 2500** — a sustained reading above that means the drain is oversubscribed again (the AC-0313 re-measure band was 1518–1678 on that tree; the AC-0286-era reading was 707) or the lane has stalled; the AC-0340 per-frame cap is the remediation instrument. `staged_dropped` high with `staged_drained` low = the drain is losing the race to streaming; `staged_drained` at zero with pending entries = the lane is off (the `AWECRAFT_COLSTAGE=0` env disables it) | AC-0313 closeout (2026-09-17, 1518–1678 — inside the boundary one-shot row) → AC-0337 (2026-09-20, promoted to a dedicated standing row with the ≤ 2500 threshold; re-measured 1845) |
| sphere probe (new arm `AWECRAFT_LOGIC=sphere`) | AC-0325 re-measure (2026-09-19): **home identity on the NATURAL column unchanged** — `home_spawn_top_ok` true: data top **141** == the deterministic C++ flat-column top; `home_spawn_top_analytic` 140 = the C++ heightmap H; bedrock 11 @y0, surface solid + air-above (all green). Edge points 884/884 bitwise-identical, world↔face 300/300, neighbor_key 240/240, corners 32, `key_home_ok` true. Keying round-trip **240/240** (`key_bad` **0** — AC-0325 fixed the silent drop: `set_block` on a data-less column is now a RECORD-ONLY edit into `world.edits` (+ `get_block` reads recorded edits back on data-less columns), so the 38 face-0/1 samples on node-only far columns (|x|,|z| up to 4000 — all `state=dataless` in the arm's payload; pre-AC-0263 the sync materialize gave them data) round-trip, and the 2 samples on materialized columns (0,-1)/(-1,-1) pass as before). `ok` **false** for ONE pre-existing reason (red on pristine HEAD, proven by bisection — NOT AC-0325's): `home_sea` — the mirror (coarse 2-D) calls the centre column itself "ocean" (`home_sea_at` [8,8]) while the C++ data is land there (stone, cell 3 at y=126) — the mirror/C++ ocean divergence since the AC-0215 3D field (**AC-0326**, a separate ticket). Named follow-up: the mirror/data ocean divergence (shared with the basis arm's sea check) | AC-0143 (12-face convention, home pair = faces 0,1) → AC-0314 (home spot-check rebased to the natural analytic column; two pre-existing instrument reds documented with bisection evidence) → AC-0325 (keying round-trip GREEN: the data-less-column edit is record-only into world.edits; the home_sea red remains AC-0326's) |
| save v2 (arm `AWECRAFT_LOGIC=save`) | `ok` true — `SAVE_VERSION` **3** (AC-0155 full-chunk-save era; load stays shape-validated, v2 JSON still loads), `planets:[{id:0,R:4000,orbit:null}]`, edits re-keyed `"0:<face>:<ccx>:<ccz>:<local>"` (face 0 = ccx≥0 / face 1 = ccx<0), block+player round-trip exact, **v1 save soft-fails** (edits discarded, clear log line `SAVE SOFT-FAIL (old save format: …)`); continue arm re-validates+converts v2 keys (`edits_ok` true); R clamp [2000,8000] → `Game.planet_R` | AC-0143 (non-home faces never record edits in P1a — data-level only) |
| occlude (arm `AWECRAFT_LOGIC=occlude`, r4 seed 44) | `ok` true — `leaking_cells`/`leaking_faces` **0** (no interior face leaks), `full_solid_slabs` **25** = `occluders` **25** [xvfb node graph; headless 0 — gated], `underground_verts` **494864** / `total_verts` **698196**, `underground_faces` 123716, `interior_voxels` 2192711 [Stage-B pre-pass skip set], `cave` 1417 cells aabb [-64,67,-64]→[-54,85,-23], `cull_3d` + `use_occlusion_culling` true [canonical `rendering/` keys] | AC-0110 (Stage B = geometry NO-OP: AC-0106 greedy mesher already culls interior faces — MESHRECS 81/81 per-slab identical, genhash + SMOKE 5 exact; Stage A execution unprovable on this box [no forward_plus GPU] — node-graph + no-regression verified) |
<!-- END GENERATED: standing-values -->

### Known non-fatal log noise (do not chase; G0 still requires zero `SCRIPT ERROR` from OUR code paths — these transient classes are the documented exception)
- AC-0137-class threadgen transients + `SCRIPT ERROR: Invalid type in function 'threadgen_handoff' … Nil→PackedByteArray` (world.gd `threadgen_poll`) — 3–17 per battery run, pre-existing (stash-verified AC-0151); still seen at 1–25/run post-AC-0152/0160.
- ~~`ERROR: Pages in use exist at exit in PagedAllocator: WorkerThreadPool GroupE` — exit-time line when a burst group task is in flight at `quit()` (post-AC-0152/0160 spawn-burst machinery); harmless.~~ **RETIRED at AC-0293** — the 5x5 spawn burst (the only `add_group_task` caller) is gone, so this line class cannot occur; a fresh one is a regression.
- ~~`ERROR: Invalid Task ID` at `is_task_completed` from `recenter` (world.gd:2258) — see the boundary row above (follow-up recommended, non-blocking).~~ **RETIRED at AC-0293** — the group-task prune/consume that printed it is gone with the burst.

## 4. Run recipes (sandboxed, one godot at a time)

Sandbox note: `/tmp` is ephemeral per bash call; the real HOME is read-only — so every
godot call is ONE bash command that sets `HOME` first. Never run two godot processes
in parallel (`.godot` cache corruption). Scratch → project-local `.scratch/` (or
capture `RESULT` into your reply — /tmp files do not survive to the next call).

```bash
# common prefix for EVERY godot call (one bash command):
export HOME=/tmp/dsh_home; mkdir -p $HOME
cd /home/angrygiant/github_projects/AweCraft   # engine: ~/tools/godot/godot, always --path godot
S=/home/angrygiant/tools/godot/godot

# G0 — headless load, zero script errors (~5 s, timeout 300 s):
timeout 300 $S --headless --path godot --quit

# single logic mode (fast class, timeout 240 s):
timeout 240 env AWECRAFT_LOGIC=fluids $S --headless --path godot

# slow modes (boundary/perf at r≥8, r50) — timeout 3000 s, wall ~25+ min:
timeout 3000 env AWECRAFT_LOGIC=boundary AWECRAFT_RADIUS=50 $S --headless --path godot

# smoke battery (2–4 dependency-mapped modes + genhash; quote the list, ~30 s):
timeout 300 env 'AWECRAFT_BATTERY=player;interact;light;fluids;genhash' $S --headless --path godot

# full battery (pre-build gate, ~60–90 s):
timeout 600 env 'AWECRAFT_BATTERY=player;interact;light;fluids;buckets;genhash' $S --headless --path godot

# render (software GL under virtual X; R=1–2; timeout 300000 ms). NOTE: a PROXY
# renderer — gl_compatibility cannot show Forward+-only features (DOF) and differs
# in tonemap; the shipped renderer is forward_plus (AC-0241). See §2.
xvfb-run -a env AWECRAFT_SNAPSHOT=/tmp/shot.png AWECRAFT_CAM=top AWECRAFT_RADIUS=2 $S --path godot --rendering-method gl_compatibility

# env knobs that matter (any mode):
#   AWECRAFT_SEED=n   world seed (default 44)
#   AWECRAFT_RADIUS=n render radius (forces world.render_radius; default 4 under harness envs)
#   AWECRAFT_THREADGEN_N=n / AWECRAFT_TGDEBUG=1   (world.gd)
#   AWECRAFT_RECPROBE=1 / AWECRAFT_DRAIN_MS=ms / AWECRAFT_GEN_BUDGET=n   (world.gd streaming budgets)
#   AWECRAFT_TM_HO=n   (AC-0224/AC-0225) steady-state streaming handoff
#     burst per process frame — preloads the Settings "chunks_per_frame"
#     value the Options "Chunk meshes per frame" slider drives (world.gd
#     stream_ho_cap reads that setting every process frame; default 3,
#     was 1 at AC-0219; clamped 1..100; no save — the harness must not
#     clobber the user's cfg). R16 RESULT reports ho_cap = the cap in
#     force; the fly phases report ho_max = the burst actually used.
#   AWECRAFT_TICKTIME=1   (boundary: enable world tick timing)
#   AWECRAFT_WALK / AWECRAFT_WALK_SPEED   (boundary walk config)
#   AWECRAFT_VMITEM / AWECRAFT_VMSHOT / AWECRAFT_VMFRACTION   (viewmodel mode)
#   AWECRAFT_SETTLE_TICKS=n   (fluidfall/fluidsettle hard settle cap)
#   AWECRAFT_ONLY=cx,cz;…   chunk-visibility filter (NOT the battery)
#   AWECRAFT_FRUSTUM=engine|manual|0   (AC-0212) frustum-cull lane: engine = the
#     render server's automatic per-MeshInstance3D cull (NEW DEFAULT, zero script
#     cost); manual = the AC-0109 per-frame _frustum_cull_pass (A/fallback, 32 m
#     off-screen margin). Legacy AWECRAFT_FRUSTUM_CULL=0 still disables manual
#     when AWECRAFT_FRUSTUM is unset. AWECRAFT_ONLY always wins (manual defers).
#   AC-0208: AWECRAFT_MESHCPP / AWECRAFT_GENCPP / AWECRAFT_LIGHTCPP /
#     AWECRAFT_STRIPSCPP are GONE (the C++ extension is now REQUIRED — Game
#     fails fast if it is missing; see the nofallback arm above). The C++
#     probe arms were adapted: chunkiocpp's slab check = C++ decode_slabs vs
#     the runtime decode_column handoff (+ChunkIO.cpp_slab_decodes counter);
#     meshprobe = C++ full-nbs build vs C++ compact-ring build (the dispatch
#     invariant — the GDScript build_accs reference was removed); the
#     GDScript kernels that survive are probe-only (stripsprobe/pullprobe/
#     lightprobe references, sentinel-counted). AWECRAFT_FRUSTUM is UNTOUCHED.
```

## 5. Verification gates (per task)

- **G0**: `--headless --path godot --quit` exits 0 with zero `SCRIPT ERROR` lines;
  `git status --short` shows only the task's own new files.
- **G1**: dependency-mapped mode(s) green (each `RESULT` ok / expected values).
- **G2**: genhash 25/25 byte-identical to the last verified run **only if** `world/*`
  changed.
- **G3**: known-stable values (§3) unchanged after any fluids/world-touching change.
- New world/* or fluid change → also run `boundary` r4 (or the AC-0079 gate values).
