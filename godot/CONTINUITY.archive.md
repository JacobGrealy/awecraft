## 16. CHECKPOINT 2026-09-17 (r16) - **AC-0297 + AC-0286 CLOSED; the lighting series is done except AC-0287 (save filter); PAUSED for the user's AC-0299 answers.** AC-0297 (P5, 3e0b50b): the classic pull has ZERO live game consumers - (a) DIRTYFULL evidence = pull light byte-equal to the settled star payload 0/98304 but the edit-face slab has no independent lane coverage (uncovered_delete [8]) -> FEED settled per-slab payloads at dispatch (epoch-checked at landing), C4 bakes 93->73; (b) tex-refresh star-native (real = settled payload / halo = h_avg_emit re-emit), c.last_eff dropped from the path; permanent texrefresh arm + the Godot-4.7 ArrayMesh precision probe (normals 16-bit 1/65536, colours 8-bit truncated on the round-trip - every game mesh carries the signature). AC-0286 (aed3e4b): seamless far->sim promotion - three-pointed detection (recenter / disk load / in-band FAR landing - the gen-queue-lag bug: such a column was never owed and sat low-detail forever), retain-swap (never hides: far low keeps showing, slabs flip high-on/low-off atomically behind the settled-payload gate - no hole, never unlit; promo-originated late-landings counter expects 0), single flood (was_far re-seeds the whole column exactly once - seed-at-landing == 1 on all 181 landed promotions), burst (bypasses only the build-queue score). Measured: 0 hole-frames, 0 unlit, 1 promotion/residency, cost 2 ms regen + 2.6 ms re-seed + 22-frame swap at rest, sim disc walk 100% / flight median 42.8%; 14/14 gates + coordinator canaries. **USER DECISION PENDING (AC-0299, asked in-chat 2026-09-17):** the user's message re-frames (b) the halo look: beyond the sim radius the band should be HIGH-LOD 16x16x16 TEXTURED chunk meshes (not the 4x4 avg P3 shipped) out to the MEDIUM start distance, with heightmap-only light + no 3D noise (caves) - i.e. 284a's skip-slab representation MESHED FULL-LOD, not 284b's h-only + 4x4 avg. This partially reverses P3's "halo subsumes med/low" + 284b's h-only draw (the h-only DATA stays useful: cheap gen + tiny save; the slabs are materialized from H+biome+top at mesh time, or skip-slab gen is re-armed for the near-far band - decide with the user's answers). Queue head after me: AC-0302/AC-0303/AC-0300 (the user-session process/render tickets - left for their session; do not reorder). NEXT after the AC-0299 answers: the far-band draw change (new ticket), then AC-0287 (save filter), close-out design doc. **Windows build 20260917-12xx on :8082** (this one - includes promotion no-pop; stamp in the build log).

## 15. CHECKPOINT 2026-09-17 (r15) - **AC-0283 + AC-0284 CLOSED; AC-0284b (H-only far data) shipped d9bb3e6; P5 (AC-0297) is next in the queue.** User asked why AC-0283 wasn't done -> marked done (code-complete since P4; the halo-look confirmation + build re-test are tracked as AC-0299/AC-0300, nothing lost). **AC-0284b (subagent, all 13 gates green):** far columns (halo band + offscreen collar) now store NO slabs - just [H u16x256][biome][top] (~198 B on disk; v6 flag bit 1, old saves decode unchanged). Gen builds only the 3 coarse surface fields H depends on + the heights pass: **far 92 us/col (fields 57 + heights 34 + top 1) vs full 1678 (~18x)**. The ticket's literal <0.05 ms clause is 2x OVER - the 57 us surface fields are the bit-exactness floor (H must match the full path or promotion shifts terrain); the RP heights-only line (32 us) is MET (34 us). Recorded in the AC-0284 close note + the final design doc. `AweMesh.h_avg_emit` emits the 4x4 avg from the payload, BYTE-IDENTICAL to the slab emitter (farab arm 984/984); far H bit-exact vs full (h_mismatch 0). **Engine-seed contract (starlight):** a far column is NEVER seeded as all air + a far->full promotion re-seeds the WHOLE column top-down - a real bug the probe found (far columns were air-seeded at the recenter crossing/disk load, leaving a stale all-air seam that mis-carried sky on the promotion re-seed: deep stone kept sky 15). `_far_promo_owed` cap-drop retry (threadgen_enqueue now returns bool) + `_low_relower_owed` demote re-lower. skip arg semantics now 0 full / 1 slab-skip (A/B reference only) / 2 far h-only. Gates: genhash 25/25 byte-identical (coordinator re-ran), battery 22 identical + the exact P4 timing-jitter pattern (0 mesh/gen diffs; wallshot pre-existing hang), starlighttest 0/5.3M, r16/lightstate/ladder/meshprobe rebased+documented, brightslab C1-C6 exact vs 284a, halo a-d (b: 192/192 byte-identical; (a) 26 boundary = the documented deep-corner under-lit-until-promotion class), profgen walk 100% / flight 46.2% (beat 284a's 44.1%) - .scratch/ac0284b/report.md. ARCHITECTURE.md updated in-task (gen.cpp skip 0/1/2, mesh.cpp h_avg_emit, v6 bit 1, Far-data decision, engine-seed contract). **PARALLEL SESSION:** the user-side agent is active (AC-0298 doc reorg + new standing rules; filed AC-0299 open-items / AC-0300 forward_plus A/B / AC-0301 harness-data-single-source / AC-0302 parallel-work-isolation / AC-0303 gate-runner-and-build-ownership); its r12 boot blocker was this coordinator's subagent mid-WIP (transient, resolved). Queue order after me: AC-0297 (P5), AC-0302, AC-0303, AC-0300, AC-0286, AC-0287 - the user-session tickets sit ahead of the epic's 286/287 (do NOT reorder another session's tickets without asking). **NEXT: AC-0297 (P5 star-native dirtyfull)** - convert/kill the last two live classic-pull consumers: (a) the edit-fallback DIRTYFULL (verify redundancy vs the star remesh lane with the star_bake_probe evidence first - delete vs feed), (b) the tex-refresh rebuild (_drain_tex_refresh, c.last_eff -> capture settled star payloads at dispatch). Then AC-0286 (promotion no-flash on the final h-only representation), AC-0287 (save filter - far columns now ~198 B, the filter drops them entirely), close-out design doc. **Windows build for the user: 20260917-0059 on :8082** (http://192.168.0.224:8082/AweCraft-20260917-0059.zip - includes halo + both edit-lighting fixes + H-only far data; note: far columns now save ~198 B and a save made with THIS build can't be loaded by the older 20260916-1256 build).

## 14. CHECKPOINT 2026-09-16 (r14) - **AC-0301 SHIPPED: a doc is no longer a script's data file** (user principle: a .md must not be both an agent-facing reference and a hard requirement for a script to run; code may be scraped because it is a machine-readable artefact versioned with the behaviour it describes - prose may not). `tasks/harness_data.yaml` (NEW, 30.3 KB) is now the single MACHINE source for the two tables in `godot/HARNESS.md`: schema 1, mode_columns, **45 modes** (entry/tests/result_fields/envs/wall/notes), value_columns, **16 standing values**, battery tiers. The doc keeps every word of its prose and wraps section 1 and section 3 in BEGIN/END GENERATED markers (19,573 + 5,492 bytes; doc now 277 lines / 40.3 KB). NEW `tasks/scripts/harness_doc.py --render|--check` - `--check` is the anti-drift gate (exit 1 + the first differing line). `spec_template.py` parses NO markdown now: `_harness_rows`, the doc-reading form of `_known_stable_rows` and `_parse_md_rows` are DELETED; the exception is kept and documented in-file (`_parse_gd_constants` still reads data.gd/generator.gd). `test_tasks.py` 53/53 (+7 checks: doc<->data sync, data schema, battery-mode coverage, and a regression guard against the scraper returning). **MIGRATION PROOF:** every cell round-tripped through YAML with equality asserted, then the doc was re-rendered and diffed against the pre-migration copy - 3 removed / 7 added lines and nothing else (4 marker lines + 3 rows); the 16 standing-value rows are BYTE-IDENTICAL. Three findings the migration surfaced: the **`spin` row had SIX cells instead of seven** (its "what it tests" and "key RESULT fields" text were merged into one cell, so markdown rendered that row shifted AND the old scraper mis-split it) - repaired, the one content change; `chunkio`/`nofallback` each carried a stray leading space before their first pipe (normalized); and two generated-spec fallbacks still carried **pre-AC-0091 numbers** (sea 2730/2730, backed 1406/1406) - replaced with an honest pointer. Deliberate data-driven spec changes: the battery table now includes `settings` (the doc always listed it as a battery mode; the code had a hardcoded 6-mode list) and the value-table header comes from the data. AGENTS.md standing rule 3 now covers GENERATED blocks (edit the YAML, run --render; never the doc). NO GODOT: tooling/docs only, no arm/heavy gate/build. The tree boots again (AC-0284a landed in r13), so r12's "tree does not boot" caveat is historical.

## 13. CHECKPOINT 2026-09-16 (r13) - **AC-0284a SHIPPED (727f27e): the halo band (taxi > band0_r) now generates in lazy-skip mode** (AC-0216's C++ skip path extended taxi-only: no cave field, no top-down scan, solid 0..H; surface/ore/biome/top-block exact - the halo's 4x4 avg + heightmap sky never shows caves). **no_caves marker** on the column (chunk.gd) + **v6 save flag byte** (v5 layout + 1 byte in the MD5 head; old v1-v5 decode as full; round-trip verified). Triggers: recenter promotion of a no-caves column + in-band disk load -> FULL regen through the AC-0283 P2 late-landing machinery (AC-0286 polishes no-flash). **Bug found+fixed:** the is_regen per-slab merge kept a no-caves column's solid top slabs when the full regen's cave-opened surface (he < H) ended lower - full regens now replace the column whole. **Measured: skip path 1.06 ms/col vs 1.73 full (1.63x)** - the <0.05 ms ticket clause is UNMET under the slab data contract (fill 710us + fields 115us + pallet 157us); p_keep measured 0us saved (above-H rows are zero-writes) - NOT wired (YAGNI, documented). **Done-when gap:** the RP 1k-col horizon metric is single-threaded GEN cost (<50 ms = <50 us/col; heights-only 32us is the only way in) -> **AC-0284b = H-only far data** (far columns store 256 H + biome + top block, NO slabs; the coarse field builds must be amortized via a neighborhood cache to hit the budget; the halo draw gains an H-driven 4x4 avg emitter - data + emitter are one coherent unit). Gates all green on 284a (genhash 25/25 byte-identical - full path bit-exact; starlighttest 0/5.3M; r16 lost 0/0 rebased: cols_full 617->220 cols_skip 1673->2143 scan 473->174ms; lightstate canaries exact; battery EXACT P4 pattern 21/8/0 - the r4/sim4 arms are all-real so skip never triggers there; brightslab C1-C6 pass, C4 93 bakes 0 over-bright; halo (a) boundary mismatches 103->0; walk sim disc 100%, flight 44.1% (beat P4's 42.1%); 0 errors) - .scratch/ac0284a/report.md. ARCHITECTURE.md updated in-task per the new standing rule 1 (gen.cpp skip path, v6 flag, the Far-data decision). **PARALLEL SESSION NOTE:** a user-side agent ran AC-0298 (doc reorg: COORDINATOR.md to root, HARNESS.md/OPS.md in godot/, docs/ essays, monotonic checkpoint ids, ARCHITECTURE.md same-task rule) + filed AC-0299 (the four OPEN-for-user items) / AC-0300 (forward_plus A/B) / AC-0301 (harness-data-single-source); its r12 'boot blocker' was this coordinator's subagent mid-WIP in harness.gd (transient - fixed before 284a finished). **NEXT:** AC-0284b (H-only far data + H-driven halo emitter - the done-when closer), then AC-0286 (promotion no-flash on the final far representation), AC-0287 (save filter), AC-0297 (star-native dirtyfull). **Windows build for the user is still 20260916-1256 on :8082** (284a changes the far-band data - rebuild after 284b, not yet).

## 12. CHECKPOINT 2026-09-16 (r12) - **AC-0298 SHIPPED: the read-first doc set repaired and reorganized.** Layout is now explicit: root = entry point + role process (`README.md` NEW, `AGENTS.md`, `COORDINATOR.md` MOVED here from godot/); `godot/` = the game plus how to run/verify/build it (`ARCHITECTURE.md`, `HARNESS.md` MOVED here from tasks/, `OPS.md` NEW, `CONTINUITY.md`); `tasks/` = ticket system + tooling (TASKS.yaml, scripts/, templates/, AC-NNNN/) with NO prose docs; `docs/` = design essays + `INDEX.md` NEW. Verified by grep: all 11 live docs exist, no live doc points at a moved path, and `godot/OPS.md` holds ZERO godot command recipes (6 in HARNESS) so the recipe duplication is gone. DEAD POINTERS FIXED (each checked before/after): AGENTS.md "00o" (top checkpoint is 00Z r11, now id 11), "section 6" (no such section - the queue is `tasks.py next`), the CONTINUITY "footer" game semantics (no footer exists), and the claim that commands/env hooks/build/daemons live in CONTINUITY (they are in godot/HARNESS.md §2/§4 + godot/OPS.md); COORDINATOR.md "00p INFRA LESSON v3 + BUILD RECIPE" (that section was compacted away - its content survived ONLY in tasks/AC-0110/continuity.md and the AC-0282 handoff, now harvested into godot/OPS.md §2/§4/§7) and its path-less "HARNESS §3" refs (the file is godot/HARNESS.md); ARCHITECTURE.md's "CONTINUITY.md section 6" and its four-autoload VERIFY block. ARCHITECTURE.md CORRECTED against the tree: 4 -> **6 autoloads** (Settings, Save); the hand-authored hud/interaction/combat/manager/models node layout does NOT exist - main.tscn is a single `Main` node and main.gd builds the whole tree at runtime (the old M1 line even said "5 autoloads", contradicting its own section 2); the "**No comments**" rule was INVERTED (comments are the project doc style - 3,648 comment lines in world.gd alone; the port-era rule is retired); the M1-M13 checklist is retired; and **`Game.dimension` has ZERO consumers - the nether/second dimension was never built**, although the doc claimed two worlds + per-dimension saves + portals. NEW section 4 documents the C++ GDExtension (gdext/src gen/mesh/strips/chunk_io/lighting[test-only]/starlight, SCons -> gdext/bin, loaded via godot/res/libchunkio.gdextension -> godot/bin, git-ignored build outputs, boot-guarded by `cpp_ext_ok`). STANDING RULES now in AGENTS.md, enforced at closeout (COORDINATOR.md) and in the builder contract (tasks/templates/two-phase.md): (1) **godot/ARCHITECTURE.md is updated in the SAME task as any architectural change** (user-directed) - a task that leaves it wrong is not done; (2) TASKS.yaml only via tasks.py; (3) never hand-edit build outputs; (4) one godot at a time + HOME=/tmp/dsh_home; (5) one home per fact (placement test; never restate another doc); (6) harvest durable rules at closeout - that hole is what killed INFRA LESSON v3. CONTINUITY maintenance is now byte-based (>6 KB or >2 checkpoints), ids monotonic (the two "00Z" labels are now 10 and 11; r10 archived with its stale NEXT and its OPEN block), and OPEN-for-user items must be promoted to tickets - the four lighting/halo OPEN items are now **AC-0299**. RENDER CONTRADICTION (item 3) PARTIAL: gl_compatibility is the only path proven on this box and is a PROXY renderer - verified in-run that the engine logs "Depth of field blur is only available when using the Forward+ or Mobile renderer" (DOF is a shipped developer setting, AC-0281) while the game ships forward_plus (AC-0241) - so a render is evidence about geometry/layout/UI, never about final colour or DOF. Recorded in godot/HARNESS.md §2/§4 with the hooks and the two-phase render instruction updated. The empirical forward_plus A/B (lavapipe ICD present but untested; a fresh worktree also needs a full asset import first, since `WorldGen`/`VoxelMath` class registrations and the atlas `.ctex` files live in the git-ignored `.godot/`) moved to **AC-0300**. **BLOCKER FOUND, NOT TOUCHED (docs-only ticket; another agent holds the WIP): the main working tree does NOT boot.** Uncommitted AC-0284a work in `godot/scenes/harness.gd` has 3 parse errors (lines 1148/1175/1176 - type inference on untyped values, "Warning treated as error") which fail `main.gd` compilation and take the whole game down: G0 prints 5 SCRIPT ERROR lines and still exits 0 - exactly why the G0 gate counts errors instead of checking rc. AC-0298 therefore verified nothing against the live tree, ran no heavy gates and produced no Windows build; no game file was modified by this task.

## 11. CHECKPOINT 2026-09-16 (r11) - **AC-0283 COMPLETE (P1-P4) + both user-reported edit-lighting bugs fixed + P5 follow-up filed.** Commits this span: e4f88da (P4: dead legacy light removal + re-classification; full re-profile — the AC-0274 startup drain-scan hitch is GONE: steady 60 fps startup, late 0; late-landings 0; star confinement held), d3e11b7 (user's backlog-reorder tool work + AGENTS.md task-decomposition rule), bf93b9a (borderbright fix). **Borderbright bug (user report: breaking a block on a chunk border -> border chunk overly bright a few seconds then correct):** root cause = the edit-full DIRTYFULL self-light bake fed the legacy pull the AC-0129 sky-carry v-channel; the pull's eff-stage 3D flood spread that carried sky ACROSS the chunk x/z edge 1-2 blocks into the border column interior (+1-2 over final at ledge geometry — the star engine's column-pull never crosses x/z). Fix = 2 lines (edit_full dispatch feeds the block-only settled strip -> same model as the star engine); permanent brightslab cases C4/C5/C6 with per-bake over-bright replay (C4: 93 bakes, 0 over-bright, was 2 bakes/28 cells; conv 550 ms) + the `star_bake_probe` hook in world.gd (null-guarded, arm-only). All gates green (starlighttest 0/5.3M, r16 lost 0/0, lightstate demoted 41/hole 1813 canaries exact, ladder 0/965, meshprobe 1.0, battery 21/8/0 pattern, halo a-d, 0 errors) — .scratch/ac0283-borderbright/report.md. **NOTE for the final design doc:** the tex-refresh path still self-lights via the classic pull with the old v-channel (rare event, documented in the borderbright report scope notes) — that's exactly what AC-0297 (P5, queued pos 2 after AC-0283) fixes: star-native DIRTYFULL, eliminating the classic pull's last live consumers (the edit-fallback may be pure remesh-lane redundancy — verify with probe evidence before deciding delete vs feed). **AC-0283 status:** code-complete (P1-P4 + both edit bugs); kept in-progress until the user's in-game confirmation of the halo look (open question: halo = literal 4x4 avg beyond the sim square — the 8x8 med tier is gone as a draw tier; confirm that's the intended look at 64-128 m) — then `tasks.py set --id AC-0283 --status done` and queue runs AC-0284 (cheap far height-only data via p_keep, ~55x ratio measured: 1.75 ms full vs 31.6 µs heights) -> AC-0286 (promotion data side + no-flash) -> AC-0287 (save filter) -> AC-0297 (P5). **Windows build for the user: 20260916-1256 on :8082** (http://192.168.0.224:8082/AweCraft-20260916-1256.zip — includes halo + both edit-lighting fixes; test: boundary breaks should no longer flash bright; walking identical; far band = 4x4 avg + heightmap sky). **OPEN for the user (final design doc):** (a) real-disc radius = sim_dist=4 knob vs the epic's loose "80-200 columns"; (b) the literal-halo look (b); (c) the walk fix's 2.5 s crossing-period regime gate; (d) the Y-window's replacement (fresh-edge priority) as a scheduler-redesign item; (e) 1k-col horizon done-when already met by heights-only gen (31.6 µs) — AC-0284 makes the far band actually use it.

## 10. CHECKPOINT 2026-09-15 (r10) - **AC-0283 P3 SHIPPED (b0ff477, pushed) — the far-band HALO is live: everything taxi > band0_r (the sim_dist square = collision/fluid square) now draws 4x4 avg + HEIGHTMAP SKY light (sky 15 strictly above the terrain top per x,z, 0 at/below, no flood, no block light) with NO star seed / NO nibbles — the star engine's work is confined to the real band. The halo SUBSUMES the old med/low draw tiers (the med/low setting values still flow through the settings-clamp chain but select no draw tier — coordinator's literal reading of the epic's "drawn 4x4 avg"; flag for the user).** Measured (profgen R50, 60 s): walk sim-disc 100% held, star sections 27792->984 (= the real band exactly), flush bakes 1695->375 (-78%), flight 40.7% (up from P2's 38.6%), frames ~17 ms; PROMOTION (halo->real on recenter) = one full seed+flood per column (~23 promotions/60 s flight, 0 unlit frames, 0 mismatches vs reference); DEMOTION = evict_column (nibbles freed, high mesh stored free per keep-all-LOD). Documented boundary effect: real-band slabs adjacent to unseeded halo columns are under-lit by the missing margin (the engine's "unseeded faces drop" semantics — 61 cells at (2,-2) in the halo arm; 1 quad in brightslab's boundary-edit case; max_bright_delta 0) — the engine is the standard there, not the legacy kernel. P3 surfaced + fixed two live-game bugs the halo exposed: the E2->remesh wedge (a settled column's boundary change didn't re-arm neighbor re-bakes through the remesh lane) and the promotion re-bake arm (a demoted->re-promoted column never re-baked its OWN slabs). PERMANENT ARM: `AWECRAFT_LOGIC=halo` (a: real band exact vs reference + boundary ring reported; b: halo cols unseeded + payload == heightmap reference 190/190; c: promote 23, 0 unlit; d: evict 23, 0 unlit — 7.7 s). Gates: all 11 green — starlighttest still 0 mismatches / 5.3M cells; r16 lost 0/0 with the halo streaming (the arm now pins sim 2 to exercise it); battery EXACT P2 pattern (the r4 battery arms are all-real => halo is a byte-exact no-op: 21 byte-identical / 8 jitter / 0 struct / genhash exact); meshprobe 1.0 (r4/sim4 = all-real circle); lightstate R16 census rebased + documented (demoted 41, hole_traj flat 1812 — .scratch/ac0283-p3/report.md); ladder boundary_flip 126->0 by construction (documented); brightslab rebased (boundary case) + ok; 0 script errors.

## 00Z. CHECKPOINT 2026-09-15 (r9) - **AC-0283 P1+P2 SHIPPED — Starlight lighting is now in the live stream (commits ef28ac1 + f16606d, pushed; Windows build 20260915-2302 on :8082 for the user — user is back and will test in-game).** The "Flight-ready world" epic is the active plan: AC-0283 in progress (P3/P4 remain), queue AC-0284 → AC-0286 → AC-0287.

## 00Z. CHECKPOINT 2026-09-14 (r8) - **SESSION ENDS — PLANNING HANDOFF FILED (AC-0282)** — user rejected 83cfcfe's timer mechanism: the Y-window/moving-state mode switch is NOT the spec — "there should not be some kind of internal timer that changes what we do" — the deep-fill starvation must EMERGE from the recenter queue (new outer-edge work outranks owed deep work; stopping simply stops recenters). Also asked "we need to rescore all slabs every recenter, are we doing that?" — ANSWER: yes, scores are live per pick (nothing cached; pure function of the live recenter anchor + live player slab + probes) — the gap is the (layer, taxi) SHAPE (layer-major lets a passed deep slab outrank a fresh edge slab). **FILED: AC-0282 (p1, queue head) + `godot/HANDOFF-emergent-queue.md` (read this first in a fresh planning session — spec quotes, remove/keep list, code map, gates, open questions incl. the ahead-lead snapback timer).** 83cfcfe remains the shipped state (gated, user-tested OK in-game).
