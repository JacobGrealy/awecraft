# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6.

## 00H. CHECKPOINT 2026-09-09 - **USER RETURNED. Queue discipline: work the EXPLICIT queue only (tasks.py next/queue list) - NEVER pick backlog by judgment (AC-0053 etc. = backlog). AC-0243 stays at the head for the user real-pad retest; user chose to continue with AC-0191 meanwhile. LANDED THIS STRETCH (user-reported bug + enhancement + queue task): (1) AC-0244 (user bug 2026-09-09: water no longer flows) - root cause in world.gd tick_fluids (AC-0203 era): the sparse scan passed (cell & 255) as the per-slab pos to _data_at_slab; the per-slab index is (ly<<8)|(lz<<4)|lx (12-bit) so every above-slab water cell read its slab's ly=0 row (deep stone), failed is_fluid_id, and its fl level was zeroed every tick - placed/bucket water could never move (natural ocean unaffected: fl=0). Fix: full cell value + below_pos = cell - 256. New fluidflow harness arm (stone-box fall + stone-pit spread, terrain-independent) ok:true. (2) AC-0245 (user request: water boundary faces visible everywhere) - cull_disabled on both fluid shaders + the legacy StandardMaterial3D fluid material; AC-0036 near-cull shrunk 1.9 -> 1.2 (only the face inside the head is hidden - the surface a short distance above the eye stays visible from underwater). GOTCHA: GLSL comments are // not #. (3) AC-0191 (test range) - scenes/test_range.tscn + world/test_range.gd (RangeWorld: greybox floor with 3-deep water pit + lava trench + 10/25/50 m RangeTargets, NO worldgen; minimal world interface so the REAL player.gd runs unmodified; Game.world set in _ready; attach_entities - Godot 4.7 add_child does NOT reparent, remove_child first) + range_target.gd (center/hurt/range_target_dist); bow = data item 142 "tool":"bow" + player.gd _fire_bow placeholder hitscan 60 m over the Game.entities scan (no projectile/arrow consumption) + in_water_now/in_lava_now flags; main.gd start_range (Save.active_slot=-1 isolation, _create_game_nodes(range_mode)) + _run_game logic=="range" branch + menu Test Range button (after PackButton - gamepad nav unchanged); harness AWECRAFT_LOGIC=range arm + battery mode range LAST (swaps the world). Range arm ok:true (movement 2.87 m, sword hit, bow 50 m hit, swim, drown exactly 2.0, lava exactly 4.0, active_slot -1, console). Gates: syntax 0; SMOKE battery 15 modes ok (incl. fluidflow + range, 0 SCRIPT ERROR, 0 SHADER ERROR); genhash 80b904c0... held; console/gamepad/quitmenu green; meshprobe + R16; xvfb render; EXE rebuilt + :8080 byte-match. DebugInventory NOT built (console give covers items) - noted in the closeout. USER NEXT: retest with the new exe (fluid flow + underwater surface visibility + Test Range menu button); then satellite LOD planning TOGETHER (user asked to plan it jointly - do not start unilaterally). NEXT (queue): AC-0243 (user retest), then AC-0097.
## 00I. CHECKPOINT 2026-09-09 (late) - USER LIVE-TESTING from the local tree. LANDED SINCE 00H: (1) AC-0244 layer 2 (user: "it was broken in lake" - natural water): worldgen water is stationary by AC-0203 design (block 5, fl=0, never in fluid_wet which the scan is gated on; set_block wake only reset the global counter) -> world.gd _wake_fluid_around(x,y,z): 6 neighbors that are fluid-id with fl==0 get set_fl_at(7) + fluid_wet mark, called from set_block _fluid_near branch; fluidflow CASE 3 (natural pool: stays until support broken - then falls; side broken - spreads). (2) AC-0245 follow-up (user: "shadows flashing nonstop anywhere under water", screenshot at tasks/AC-0245/water-flicker-issue-screenshot.png - moving dark blobs on terrain ONLY when the eye is underwater, shape-changing while the camera moves, stopped when it stops): root cause = the fluid surfaces SHARED the opaque ArrayMesh, so the water sheet cast into the shadow map; the shadow-map footprint follows the camera, so the water shadow over the underwater floor kept re-shaping. Fix: fluid surfaces build into their own ArrayMesh on fluid_instance with cast_shadow = 0 (chunk.gd _assemble_slab) + MINFO (world.gd) restructured (opaque slot from the opaque mesh, fluid slots 1..3 from the fluid mesh); also removes the fluid double-draw (both instances drew the shared mesh). A back-face-static surface-animation experiment was tried and REVERTED at user request (the 300 ms flip is fine). VERIFIED: two xvfb underwater renders (AWECRAFT_AIM at ocean (-20,88), eye SEA-3) - flat terrain uniformly lit, no blobs; the dark cliff wall is per-face shading, stable across yaw (legit, not an artifact). (3) AC-0246 FILED (p1, NOT queued - user decides placement): transient native signal-11 core dumps in get_local (chunk.gd:464) slab reads at the dnlight-to-oceanfill battery transition (2 of 6 runs this stretch; pre-existing - other runs finished 15/15) - slab handoff lifetime race in gdext. GOTCHAS: this engine build STRIPS engine enum constants (GeometryInstance3D.CAST_SHADOW_OFF and BaseMaterial3D.SHADOW_DISABLED do not parse; disable_shadow_cast render mode missing; FACE_FORWARD / FRONT_NORMAL missing in fragment shaders) - use int assignment / derived math. Range battery leaked-world gotcha: long batteries can leak real ChunkWorlds whose terrain collision (ConcavePolygonShape3D at origin) pins the range-arm player - the range battery entry frees world/world.gd-scripted children before start_range. editsnap edit_ms 50.0 boundary flakes occasionally - one re-run before calling it. USER STATE: the user builds from the LOCAL TREE (their exe = working-tree state); their last build predates the mesh-split fix, which is why they still saw the blobs - they must rebuild (or fetch :8080) after the C1..C4 commits. NEXT: user retest (lake flow + underwater blobs gone + Test Range button); THEN satellite LOD planning TOGETHER (ask_user_question on the design forks - do not design unilaterally). Queue: AC-0243 (pad retest) at head, then AC-0185, AC-0097.


**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout), 00z moved 2026-09-07 (AC-0231-followup closeout), 00F+00G moved 2026-09-09 (AC-0244/0245/0191 closeout). -->