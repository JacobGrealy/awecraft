# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the queue in `tasks/TASKS.yaml` (`python3 tasks/scripts/tasks.py next`).

<!-- MAINTENANCE (AGENTS.md): compact by BYTES, not lines - when there are more than 2 checkpoints or the top checkpoint exceeds ~6 KB. Keep the newest 2, move the rest to godot/CONTINUITY.archive.md (newest first). Checkpoint ids are MONOTONIC (`## <n>. CHECKPOINT <date> (<rev>)`) and never reused. An OPEN-for-user item must be promoted to a ticket BEFORE the next compaction can drop it. -->

## 19. CHECKPOINT 2026-09-19 (r19) - **THE FAR-BAND/STREAMING CLUSTER IS DONE: AC-0329 (r16 sub-flags), AC-0287 (the save filter), AC-0293 (the 5x5 burst retired) and AC-0325 (the silent data-less edit drop) all closed with heavy stages + independent verification. Queue head is now the CAVE SERIES (AC-0288).** Commits: AC-0329 = 61ef0db (the three r16 reds I found were STALE ARM EXPECTATIONS, proven at the data level - 262/262 UV mismatches were alpha-only because the checker predated AC-0283 P3's sky-alpha, the 3 "air" slabs were hidden stale capsules, and the far census was blind to h-only demoted columns; fixed arm-side only, no game path). AC-0287 = 4b97a8d (region save = `taxi <= sim` OR edited; far columns never encoded - the 10,240-block flight A/B reads 0 bytes with the filter vs 4,149,708 bytes at 405 B/block on the save-all baseline; an edited far column persists WITHOUT a payload because save.gd:86 writes the whole `world.edits` dict into the JSON edits diff and the load path re-applies it, verified by code path). AC-0293 = 200b4dc (the 5x5 startup burst is GONE - state, worker, apply, drain hold, the `_exit_tree` group shim and the `_spawn_fast` latch with its nine gates, net -371 lines; the recenter pre-warm rides the normal queue; anti-fall is now AC-0313 clause 4's sim-diamond load gate, proven with a real player on 3 seeds; load-gate wall 1193 -> 1264-1270 ms at r4, recenter far-forward p95 7199 -> 8943 ms = the user-accepted "fraction longer", -25 MB memory). AC-0325 = f29b100 (a data-less/far column edit was silently dropped - `set_block` now records it in the edits dict + returns bool and `get_block` reads it back; sphere `key_bad` 38 -> 0; the contract "apply through the record/promotion path or fail loudly" is now an ARCHITECTURE.md convention).
**LESSONS THAT MATTER FOR THE NEXT SESSION:** (1) r16's top-level `ok` is HARDCODED true - always run it with its documented `--fixed-fps 600` and read the SUB-FLAGS (my AC-0322/AC-0314 heavy stages omitted the flag and read `ok`, which is how AC-0329's reds hid for two tickets; the row now says so). (2) The R50 `load` arm is the RELIABLE reproducer of the intermittent crash (3 of 3 attempts died: twice silently, once SIGABRT, once signal 11 with an empty backtrace, always in the load/drain window after the gate) - so the full-load wall stays UNMEASURED and the clause-4 gate is verified instead from the loader timeline (pct tracks the 41-column diamond and closes on it, now also proven WITHOUT the burst). (3) The battery compare script only diffs VALUES, so pass/fail status hid: three arms (craft/water/trees) have been `ok:false` since the archived baseline and four (atlas/buckets/player/interact) declare no `ok` at all - filed as AC-0330.
**OPEN TICKETS (all filed, none blocking):** AC-0326 mirror-vs-C++ sea divergence (the residual `home_sea_ok`/`sea_ok` red in sphere+basis - this is what keeps sphere `ok:false` after AC-0325), AC-0327 basis bedrock-y0 race, AC-0328 crash-repro campaign (now pointed at the R50 load arm as the cheapest frame capture, with raw stderr for a Rust panic message), AC-0330 battery arm-status debt. All three pre-existing reds were bisected on pristine HEAD with the HEAD `.so`, so none of them belong to the recent work.
**QUEUE HEAD: the CAVE SERIES AC-0288 (cave-p0-tune-single-field-variety) -> AC-0289 (spaghetti/noodle tunnels) -> AC-0290 (carvers/canyons) -> AC-0291 -> AC-0292, then the planet series AC-0306-0311/AC-0145, then the tail (AC-0326/0327/0328/0330 and the parallel session's AC-0315-AC-0318/AC-0320, whose relative order is untouched).** The user's 2026-09-18 directives are in force and documented: the sizing rule in COORDINATOR.md (split before you delegate - already applied: AC-0324 was split out of AC-0314, AC-0323 out of AC-0322, and the r16 reds/arm debts got their own tickets), and the image-input limitation (this agent's model cannot view images - screenshots are read numerically or described by the user). WINDOWS BUILD: the served :8080 build was 20260918-2156 (AC-0321 + AC-0322 + AC-0324 + AC-0314); AC-0287/AC-0293/AC-0325 landed since, so a fresh build is produced at this checkpoint.
## 18. CHECKPOINT 2026-09-18 (r18) - **FIVE TICKETS CLOSED IN ONE SITTING: AC-0312 (its two reds fixed by AC-0321 + AC-0322), AC-0324 (deterministic spawn search) and AC-0314 (the spawn plateau is GONE). The far band is three draw tiers off the same h-only data, band A textures correctly, the walk's data residency is back to 100%, and the player spawns on natural terrain found deterministically. A rare intermittent SIGSEGV is now the SECOND queue item (AC-0323).** Commits: AC-0321 = 0b71ae0 (band A's high mesh was attached with the SCOPED apply - `apply_edit_accs(..., false)` pins the plain 1024-tall atlas material while a full-column build emits merged-canvas UVs at 1888, so band A sampled below the tile rows; one-line fix = `apply_accs(res, _tm_ms_full)`, plus a permanent ladder gate asserting band-A UVs equal a fresh real-band emit's (5959 quads) and that the visible material samples the emitted canvas); AC-0322 = a85125d (AC-0313 clause 3's removal of the TG-empty data feed starved the data pass - a DISPATCH is one SLAB, so an entry frees once per 24 slabs and `u==0` almost never fired, leaving the TG pipeline idle (tg_inf 0 in 16/20 windows) and the walk's taxi-8 sim disc at 61.4%; the predicate is now `u == 0 or threadgen_inflight.size() == 0`, restored in STATELESS form - no timer, no mode; walk sim-disc 89/145 -> 145/145, frames unchanged, boundary p95 38 vs 39). AC-0324 = 39f1bee (world.spawn_point() + a `spawnsearch` arm: first column in (taxi, cx, cz) order that is dry, gently sloped (max |dH| <= 2 over 4 neighbours) and un-vegetated, with T1/T2 determinism fallbacks; while the pad existed every gate seed resolved to the pad centre, which is exactly what AC-0314 ended). AC-0314 = 533ef8f (the pad term + SPAWN_H/is_pad/padcol removed from gext/src/gen.cpp and mirrored in generator.gd; native lane rebuilt; spawn is now natural - seed 44 -> H 141 -> start [8.5,142.0,8.5], 1 -> 148, 7 -> 200; genhash re-baselined and INDEPENDENTLY VERIFIED: exactly 5 of 25 hashes differ from the pad-era reference and they are the plus-shaped footprint of the radius-10 disc ([-1,0] [0,-1] [0,0] [0,1] [1,0]) - the builder's first "10 new / 15 identical" was wrong and is corrected in the results page and the standing value; battery blast radius found by the coordinator and fixed in-task: hunger + survival bailed with "no flat <X> spot near spawn" because they needed pad geometry, now using an explicit 5x5 stone fixture with a corrected survival aim and a frame-based tool_ratio). **THREE PRE-EXISTING REDS** bisected on pristine HEAD with the HEAD `.so` are filed and left red: AC-0325 far-chunk-set-block-silent-drop (sphere key_bad 38), AC-0326 mirror-vs-cpp-sea-divergence (sphere home_sea_ok / basis sea_ok), AC-0327 basis-bedrock-y0-race (basis bedrock_ok). **THE ONE OPEN HAZARD: an intermittent SIGSEGV with an EMPTY C++ and GDScript backtrace** - seen as the r16 flake (AC-0312), as the R50 load arm's crash (AC-0322: twice silent, once SIGABRT), and as lightstate aborting 12 s into the AC-0314 heavy stage (Godot's handler prints the empty backtrace then aborts, rc=134); 8 follow-up runs (lightstate x5, r16 x3) were clean, so the rate is order 1 in 10 and it is NOT deterministic or tied to any one change. AC-0323 owns it and is queued SECOND because a mid-run crash silently invalidates a gate run's evidence (it did). The R50 `load` arm's full wall remains UNMEASURED for the same reason (the clause-4 load GATE itself was verified live at AC-0313 from the loader timeline). **PROCESS CHANGE (user directive, 2026-09-18): COORDINATOR.md now owns "Sizing - split before you delegate"** - the coordinator sizes work before handing it over, splitting anything that spans more than one subsystem/lane, more than a handful of files, carries an "and also", forces several arm rebases, or needs temporary instrumentation; AC-0312's oversize turn (three tiers + materialization + a water exception + five arm rebases + a profgen re-profile, returning with two unrelated reds) is the recorded precedent. It has already been applied: AC-0324 was split out of AC-0314 ahead of it (the search had to land before the pad went) and AC-0322's r16 segfault was split into AC-0323. QUEUE HEAD NOW: AC-0323 (crash), AC-0287 (save filter), AC-0293 (5x5 burst retirement), AC-0325 (the silent far-band edit drop), then the cave series AC-0288-0292, then the planet series AC-0306-0311/AC-0145, with AC-0326/AC-0327 at the tail. The parallel session's tickets (AC-0315-AC-0318, AC-0320) are at the tail in THEIR order - a coordinator mistake (assuming free ids) briefly moved AC-0315/0316 to the head; it was detected and they were restored exactly. WINDOWS BUILD: the served :8080 build is being regenerated after the pad removal (the previous stamped build 20260918-1303 carries AC-0321 only). Servers: :8080 is the one that serves the current export (the build script also prints :8082 URLs - nothing listens there). The user tested and reported the band-A texturing bug themselves and asked for the task-splitting discipline; both are now handled.
