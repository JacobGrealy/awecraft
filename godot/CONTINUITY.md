# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6. 

## 00W. CHECKPOINT 2026-09-11 (late) - **AC-0258 (lods-treat-clutter-as-air, effort S) COMPLETE + SHIPPED (AweCraft[20260911-1903], :8080 byte-matched 477c4c2c...). Queue head = AC-0112 (held-item-positioning-per-type-review).** Coordinator-implemented directly (user present, running the AC-0257 in-game look test concurrently — results not yet reported). WHAT: clutter blocks (rose 18 / dandelion 19 — the ONLY flora terrain gen places; torch 22 + banana 28 are cross blocks but NOT clutter = functional) now count as AIR in the avg-color LOD grids (low 4x4x4 / med 8x8x8): excluded from the count AND the avg color; an all-clutter sample emits nothing (empty). HIGH is untouched (the xquad cross path keeps the flora close-up). MECHANISM: derived slab field `nc` (clutter-cell count) — built in the SAME scan as nz/bs, never on the wire (disk Slab = {n,b,nz,p,i} unchanged; missing nc = pre-change behavior, same convention as bs; old saves re-derive on next landing). Sites: chunk_io.cpp `palettize_flat` (every data landing incl. load), gen.cpp `palettize_slabs`, chunk_io.cpp `slab_set` (±1 delta, all 4 branches, has-nc→maintain / missing→stay-missing), `slab_copy` (worker value copies). mesh.cpp `low_emit_avg_impl`: nc==0 → pre-AC-0258 path byte-for-byte (one int check); nc>0 → cell recount decodes with clutter excluded (an all-clutter cell = air); decode cost lands only on flower slabs. Clutter table: `awecommon::is_clutter_block` (awe_common.h) + GD twin `world._clutter_block`; GD `_avg_grid_from_rows` (harness twin) same rule. GD encode twins (chunk_io.gd/chunk.gd) lag by the `bs` precedent (A/B arms compare n/b/nz/p/i). "Optionally high far" DEFERRED (S effort). Gates (all green, .scratch/ac0258_gates.log): genhash EXACT 80b904c0...; slabops/slabwrite ok; ladder ok + NEW clutter evidence (GD all-rose rows → all-air grid; half-dirt/half-rose → solid at pure-dirt avg; C++ emit on a synthetic all-rose slab → empty; on half-dirt/half-rose → 6 quads, every vertex a dirt face-triple, no rose in the avg); meshprobe 1.0 (verts 321824 == baseline, high byte-identical); r16 ok (holes 0, air_ok — a fully-clutter slab is empty at BOTH avg tiers = monotonic, never a hole); battery ok (2639/2115/846/3497/415 ms); wprof ok (frame 3.9 avg, lost_chunks 0). Build: linux .so staged for gates (scons installed to .scratch/scons_pkg by pip — /tmp is per-call tmpfs, pip --user is sandbox-RO); build_windows.sh rebuilt both gdext targets (vendored scons at .scratch/scons-src); pidfile went stale → dup daemon on 8082 (8080/8081 = pre-existing unkillable daemons, 8080 = authoritative, serves the live exports dir). LESSON: fcc is per-FACE (18 = 6 faces × 3) — an avg-mesh vertex color is the ORIGIN cell's face triple for ITS face, so color assertions must match against the SET of all 6 face triples (the first ladder clutter failure was this test bug, not the code); plus one `get("empty", true)` default typo in the new test.

## 00V. CHECKPOINT 2026-09-11 (evening) - **AC-0257 (layered-sweep-streaming-with-baked-queue, effort L) COMPLETE + SHIPPED (AweCraft[20260911-1751], :8080 byte-matched b6ef67be...). AC-0256 (early-out-stale-lod-builds) closed CANCELLED = superseded by AC-0257. Queue head = AC-0258 (lods-treat-clutter-as-air).** Coordinator-implemented (user present, no-subagents override), option A one-shot. WHAT: (1) the AC-0234 vertical window (black caps, kept masks, owed-superset rebuilds, debt, re-entry regens, GEN band padding) is GONE — every slab in the horizontal radius is wanted at its band LOD, pending slabs render as NOTHING; (2) the BAKE is Y-LAYERED: recenter carries the player Y (`last_wy` on slab crossings — player.gd + main.gd), pick order = (layer rank, taxi) at every site via `_grid_score`/`_entry_best_pending` (rank 0 = player's slab, 1 = y-1, 2 = y+1, ...); (3) NEW Settings (Options > Developer submenu, code-built): `tier0_radius` (0-8 Chebyshev, default 0 — the ball skips fog/low lanes = full column straight to high; `note_tier0_radius` re-stamps), `worker_gen_threads`/`worker_mesh_threads` (0 = auto = cores split 6→2/4, live); "Chunk meshes per frame" slider moved into Developer; (4) stale-LOD CACHE-AND-KEEP (absorbs AC-0256): pre-start early-out in `_tm_worker_run` (wanted tier != queued tier -> no emit), handoff tier-mismatch -> finished mesh to `chunk.low_cache` {tier,dgen,fgen,v,i,n,c,mh} + the visible mesh STAYS (last tier-matching, no holes), flip-back = attach from cache (`_low_attach_cached`, no worker round-trip, dgen/fgen-guarded), all-air at a stale tier is NOT terminal, caches die with `drop_low` (r+2); (5) instance cap: the recenter column-free batch is bounded at `stream_ho_cap`/frame (the same "Chunk meshes per frame" number bounds attach AND detach/checkin) — over-cap frees deferred (`_deferred_free`, identity-guarded inst+col_gen per AC-0247 so a re-occupied key is never freed) + drained from `_process` at the cap. Gates (full battery, L): genhash EXACT 80b904c0e5f085eef1e839459058c706; meshprobe match_rate 1.0 (32/32); r16 ok:true (stream ascend/descend holes+settle, inr/immediate/gate/low_thread/height/air/uv ok, wave layer_monotone_ok, spin invariant, P4 counters 0/0/0 — the cache path only fires on in-flight boundary moves); ladder ok:true (med/low signatures 122/122 + 602/602, flip 67, air rule, NEW tier0 predicate + layer-rank gate); battery player;interact;light;fluids;buckets ok:true; wprof ok:true (frame avg 3.6/p95 6.3 flat, ahead band held, lost 0, gen queue deep = workers fed, recon 0%). NOTE: the old 8081 duplicate daemon is unkillable from the sandbox (pid outside the bwrap pid namespace; serves the same live dir — harmless, 8080 authoritative). PENDING USER GATE: the in-game look test (layered arrival while flying, edits leave no boundary holes, deep shafts see-through-then-fill, boundary moves swap med/low hole-free) + AC-0252 far-view + WORLD-PIPELINE overlay.



**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout), 00z moved 2026-09-07 (AC-0231-followup closeout), 00F+00G moved 2026-09-09 (AC-0244/0245/0191 closeout), 00J moved 2026-09-10 (AC-0250 filing), 00K moved 2026-09-10 (AC-0250 closeout), 00L moved 2026-09-10 (AC-0251 closeout), 00M moved 2026-09-10 (AC-0247 closeout + AC-0252 filing), 00N moved 2026-09-10 (AC-0252 closeout), 00O moved 2026-09-10 (AC-0248 closeout + AC-0253/0254 filing), 00Q moved 2026-09-10 (AC-0254 closeout), 00R moved 2026-09-11 (AC-0253 closeout), 00S moved 2026-09-11 (AC-0185 closeout), 00P moved 2026-09-11 (AC-0097 closeout), 00T moved 2026-09-11 (AC-0257 closeout), 00U moved 2026-09-11 (AC-0258 closeout). -->