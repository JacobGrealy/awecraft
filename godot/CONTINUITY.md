# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6.

## 00M. CHECKPOINT 2026-09-10 (late morning) - **AC-0250 COMPLETE (commit 973930d; shipped AweCraft[20260910-0645 973930d]; :8080 byte-matched). NEW TOP PRIORITY BEFORE AC-0247: INSTRUMENT THE WORLD PIPELINE (user: fps still tanks to ~20 while flying on the AC-0250 build).** AC-0250 landed: the priority cone is gone (4 tiers -> 3: under / sim radius / rest-by-taxi, look-independent; _look_dir/_look_yaw/PICK_LOOK_REFRESH_DEG/_refresh_look_dir + the look-snap rescore kick + the _pool_key look components deleted; _low_pick f20/f21 ahead-behind logic -> ring1 then rest) and the low-LOD main-thread sync fallback is gone (LOW_TASK_CAP hit = new verdict 2 = capped no-op, slab stays PENDING, re-picked next frame; -1 = null slab only; low_sync_fallbacks_n = always-zero regression gate; the harness spin-180 probe re-scoped into the LOOK-INVARIANCE regression). COORDINATOR GATES (all independent runs): genhash (0,0) 80b904c0... exact; meshprobe ok:true verts 321080 UNCHANGED; r16 ok:true (spin: invariant=true, common_n=658, t2common=642, flip_frames=0; low_sync_zero_ok=true); A/B vs HEAD: no NEW lod sub-field falses (air_ok/height_ok/immediate_ok false in BOTH runs - pre-existing since the AC-0240 fog-wave change); battery genhash+range ok:true; 0 script/shader errors. KNOWN PRE-EXISTING: the nightlot arm fails at HEAD (ok:False, baked_diffs=15, A/B identical before/after - era breakage, tracked separately); one native crash (AC-0246 class) hit during the baseline A/B. BUILD/SERVE: the user built the same tree themselves, then clarified: duplicate builds are FINE - "the build is quick so just always do it" - the coordinator ALWAYS runs ./build_windows.sh; the 0645 build found 8080+8081 busy -> spawned a dup on 8082 (died with the job; :8080 authoritative, BYTE-MATCHED: zip f6464985... exe c84e74b4...). USER FLEW THE AC-0250 BUILD: the expected frame-drop relief did NOT fully materialize - fps still tanks to ~20 while flying (user: "I thought after AC-0250 I would no longer have frame drops when flying but would see more pop in. However fps is still tanking to 20 which it shouldn't if we are properly multithreading and spreading the main thread work over multiple frames"). DIAGNOSIS DIRECTION: the remaining hitches are elsewhere in the main-thread world pipeline; suspects from the code (to confirm/refute with data, NOT to fix yet): the face-block light cascade on handoff (~1.2 s class), per-dispatch strip/nbs work (30-50 ms class), per-slab MeshInstance3D create/attach (AC-0247 targets this), the in-r low budget (up to 8 ms/frame), the blob IO commit, the burst handoff. NEXT STEP: file the instrumentation ticket (per-stage ms attribution across the whole world gen/streaming pipeline) - design IN PROGRESS with the user (open forks: output surface, ticket boundary instrument-only vs instrument+fix, the repro conditions of the 20 fps drops). QUEUE AFTER: AC-0247 (pool meshinstances/slab-arrays/columns) -> AC-0248 (prewarm) -> the rest in order (AC-0243 user-gated: record + skip). SATELLITE far-LOD stays ON HOLD until the user asks. DO NOT COMMIT: user scaffolds (unchanged list).
## 00L. CHECKPOINT 2026-09-10 (morning) - **LOD DISCUSSION LANDED: AC-0250 FILED (remove the priority cone + the low-LOD main-thread sync fallback - BOTH NOW, standalone ticket). Satellite design intact, forks still open.** After the user-initiated compaction the user returned and talked through the loader: "I think we need to remove the priority cone. We should also remove the fall back where low lod is constructed on the main thread." Scope settled TOGETHER (ask_user_question; my recommendation approved): both removals NOW as one standalone cleanup ticket, shipped on the current fog->low->high ladder BEFORE the satellite work (NOT folded into the satellite ticket, NOT split). AC-0250 filed to the intake registry (full schema, priority 2, open; NOT added to the execution queue - user-driven tickets go to intake per the AC-0244..0249 precedent; queue head stays AC-0243 user-gated, then AC-0185, AC-0097). CONE REMOVAL (godot/world/world.gd, the AC-0233/0239 4-tier system): tiers collapse 0/1/2/3 -> 0 under you, 1 sim radius (Chebyshev <= band0_r), 2 rest (taxi |dx|+|dz|). Remove: TIER_CONE_DOT (~382), the _tier_of dot branch (~435), the _pool_key look components (~415), _look_dir + _refresh_look_dir (~5044) + its drain call (~5427), the look-snap _rescore_kick (~5059); KEEP the re-stamp amortization + the kicks for the sim slider (~2819), the recenter rebuild (~6879) and the column cross (~7312). _low_pick (~2131): drop the f20 (ahead early-exit) / f21 (cone-behind) logic -> ring1 (f1) then rest by taxi rank. The order gate "tier >= 2" becomes "outside the sim radius" (expression unchanged). Harness: re-scope the AC-0233 spin-180 probe (~8310-8465) into the removal regression (a 180 deg look flip must leave the queue tier set INVARIANT); the tier-0 fall-through + dirty-edit gates (~6666/~6704) stay. FALLBACK REMOVAL (AC-0236 part 2): _low_dispatch_slab (~1890) drops the LOW_TASK_CAP (64) -> sync main-thread build branch (~1893-1895); a capped dispatch = NEW verdict 2 = no-op, the slab stays PENDING and the in-r pass / far wave re-picks it next frame (existing stale-drop semantics, no re-queue logic); -1 now means null slab ONLY (air bookkeeping, not a build); call sites ~2412 (_low_inr_step: capped must not count as in-r progress) + ~2506 (_low_step slab wave); low_sync_fallbacks_n (~652, already harness-read) stays and must now read 0 forever - that is the regression gate. GATES: genhash (0,0) baseline 80b904c0... stays green (pure scheduler change, no world-gen change); meshprobe unaffected (no C++ change); battery at least genhash + range; TASKS.yaml YAML round-trip after the edit. USER QUEUED THE WORK 2026-09-10 (morning) then WENT TO SLEEP: "go ahead and work on 250, then 247 and 248" + "once you finish 248 keep working on the queue in order. I'm going to sleep. Will let you know when I want to work on the satellite stuff." The user moved AC-0250/0247/0248 to the HEAD of the executive queue (queue list: AC-0250, AC-0247, AC-0248, AC-0243 (USER-GATED pad retest - implementation done, gamepad arm green at HEAD - if reached, record "user-gated, awaiting retest" and move on WITHOUT touching it), AC-0185, AC-0097, ...). OVERNIGHT MODE: work the EXECUTIVE QUEUE IN ORDER autonomously (tasks.py queue list, re-read each step; NEVER pick backlog by judgment); the SATELLITE far-LOD is ON HOLD until the user returns and asks (do NOT start it unattended). Per ticket: one blocking xhigh subagent implements (code only, no commits/docs), coordinator re-verifies ALL gates (genhash baseline 80b904c0..., meshprobe verts 321080, r16 arm, battery subset, 0 errors), ./build_windows.sh + :8080 byte-match, commit+push, docs. NEXT STEP: execute AC-0250 (subagent in flight when this was written).


**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout), 00z moved 2026-09-07 (AC-0231-followup closeout), 00F+00G moved 2026-09-09 (AC-0244/0245/0191 closeout), 00J moved 2026-09-10 (AC-0250 filing), 00K moved 2026-09-10 (AC-0250 closeout). -->