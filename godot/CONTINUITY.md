# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6. 

## 00Z. CHECKPOINT 2026-09-13 (overnight r2) - **AC-0266 + AC-0271 + AC-0267 (this round) COMPLETE + SHIPPED (AweCraft[20260913-1031], :8080 zip sha 9b797225... byte-matched). Queue head = AC-0247 (pool-meshinstances-for-slabs).** Autonomous round 2 of 12 (user asleep). AC-0278 (remove-candidate-flag): the c.candidate boolean is GONE - candidacy = a distance fact at the single recenter walk (in-set/out vs old center opcx/opcz); the stuck-mesh root cause (user: "candidate out of sync kept mesh from generating even at tier 0") = a build entry stripped on candidacy exit was only re-queued in the WANT walk, which DEBOUNCED recenters skip - fix: the walk (runs on EVERY recenter) re-queues a high-band chunk with data + no mesh + no build entry (idempotent; drain's high_only gate keeps it harmless); new arm AWECRAFT_LOGIC=candflag (T = stranded high-band chunk flies -17 to candidate, U = built chunk taxi 21 retained/freed/regenerated, 15 return moves, T re-enters high band at move 14 with queued=build). The old edgeretain arm is STALE (fails identically at HEAD - pre-band-3 model) - not a valid gate. AC-0277 (predictive-recenter-ahead-target): the AC-0213 debounced rebuild skip is GONE - a recenter within AHEAD_FAST_MS (3.5 s) of the previous PLAYER-chunk crossing = fast (sprint 2.9 s/chunk fast, walk 3.7 s slow) -> the recenter targets the chunk AHEAD_DIST (1) in front along the crossing direction (the queue LEADS); slow = current chunk; quiet AHEAD_FAST_MS -> _process snap-back to the chunk under the player. Every crossing rebuilds (the R16 walk = ~8 ms); the in-flight park generalizes AC-0233 (never discard an in-flight walk; finalize chains from the current center if still past REBUILD_COVER_L1; the quiet-stream settle stands down while _ahead_active). PITFALLS: the fast window measures from _last_cross_ms (last player-chunk crossing) - snap/Y-only recenters (_cross=0) must not re-arm it; _last_recenter_pcx must store the PLAYER chunk (an ahead target would read _cross=0 on the next 1-chunk cross); the arm's first crossing is necessarily slow (no rate to measure); load is stationary = structurally untouched (loadlog r30 3.8 s). New arm AWECRAFT_LOGIC=aheadtarget (fast [0,1,1], slow center==player, snapped after quiet). AC-0269 (respawn-controller-auto-select): the death-screen Respawn button (DeadBtn = a plain Control, NOT a Button - Godot does not auto-activate it on A) now has FOCUS_ALL + handles JOY_BUTTON_A in _gui_input + draws an amber focus border; _update_survival grabs focus on the show transition, releases on close. PRE-EXISTING GAMEPAD-ARM BUG FOUND: the D-pad release pressed 12 but released 13 - the sticky ui_down kept walking the GUI focus, so the A-accept landed run-to-run (sometimes Return-to-Menu = _free_game_nodes frees everything = every later sub-check sees null nodes - the symptom that LOOKS like the product). It now releases 12 (deterministic: one down = OptionsButton). LESSON: a GDScript parse error in a PRELOADED script (bad identifier - `rect` does not exist in 4.x, it is get_rect()) makes InventoryScript.new() throw "Nonexistent function 'new' in base 'GDScript'" at main.gd:212 while the REST of the game boots - the inventory is null for the whole run. Gamepad arm gained death_shown / death_focus_ok / pad_respawn_ok (A respawns, hp 20) / mouse_respawn_ok (synthetic click at get_global_rect().get_center() - mouse path kept). GATES: boot 0 err; gamepad arm ok (all sub-checks); candflag ok; r16 ok (fly50 lost 0, 25/25); battery all + GENHASH 80b904c0e5f085eef1e839459058c706 EXACT; loadlog r30 3.8 s 25/25 no stall; lightstate LS16 (demoted 48, stragglers 1, double_lod 0, holes pre/ring 0). DAEMON NOTE: build dup 8082 died with its sandbox (AC-0155 pattern; 8080 authoritative, byte-matched). AC-0268 (crafting-menu-controller-support): the crafting menu is controller-ONLY-capable - SlotCtl FOCUS_ALL + A = left-click (pick/place) + LB/RB = shift-click quick-move on the focused slot (presses only - the mouse release half is the held-item drop); open grabs the first craft cell (slot 9 - AFTER the visibility loop; the slots are hidden on the opening frame), close releases the focus; amber focus border. KEY FINDING: Godot's native GUI focus navigation does NOT walk controls inside a CanvasLayer (the inventory is the HUD CanvasLayer - two real Buttons on the layer never get the D-pad walk; the pause menu works only because it is a plain scene-tree Control) - so the inventory implements its own _pad_nav polled from _process on ui_*: nearest VISIBLE slot in the direction, cross-axis offset penalized x4 (inverted weights sent the focus down-right instead of same-row). Game._ready registers the ANALOG STICK on ui_left/right/up/down (defaults carry only the D-pad) = Bedrock-style stick nav; inert in gameplay (no focus). New arm AWECRAFT_LOGIC=craftpad (15 sub-checks, full A-only flow: seed 1 log - the {6:1}->{8:4} recipe matches the grid EXACTLY and A moves whole stacks - + 4 planks in storage[1] - inv_add fills the hotbar first and the strip is hidden while the panel is open; Y/stick/D-pad/A/LB/B; greedy nav = row-band first, then column). GATES: boot 0 err; craftpad 15/15; gamepad arm ok (no focus regression); battery all + GENHASH 80b904c0e5f085eef1e839459058c706 EXACT (mouse path untouched). AC-0266 (hold-right-trigger-continuous-swing): a held RT now feeds the _update_swing_loop predicate alongside _lmb_down (_pad_mining hold state, hysteresis 0.5/0.35) - the swing loops while held (parity with held-LMB, the spec's consistency baseline); the hit trace is untouched (mining already ticks per frame in the _mining state; mob/bow hits stay one-per-press as with the mouse); release -> clear_swing(). Gamepad arm: rt_held_swing_ok (swing_active ~0.5 s into the hold - a one-shot 0.2 s swing would be off) + rt_release_ok. GATES: boot 0 err; gamepad ok (all sub-checks); battery all + GENHASH 80b904c0e5f085eef1e839459058c706 EXACT (player.gd + harness.gd only). AC-0271 (craftable-wood-doors-open-close): wood doors - state by ID VARIANT (the grid is a pure id byte, no per-cell state): 26/27 CLOSED (solid box - mesh + concave collision free from the solid path) / 29/31 OPEN (cross panels - walk-through, no collision, passes light - lighting._tables and the C++ mesh tables both derive from Data.blocks, so ZERO C++ changes). Recipe 6 planks -> 3 doors (grid 3 = table; recompute reads table_grid NOT craft_grid). Placement = aimed face + 2-high air + no player intersection; use_selected door branch BEFORE the no-item guard (bare hand toggles); break either half -> pair clears, one drop (all four ids drop item 26); pair re-validated on every touch. Hinge side NOT stored (the cross-X open panel is symmetric - dead state otherwise). Atlas tile painted at (192,0) via PIL + JSON; NOT in atlas.gd MAP (the pack's 16x32 door textures would trip the stacked-frames heuristic; re-imports skip existing rects). Arm AWECRAFT_LOGIC=doors 8/8 (craft/place/solid-drop/bare-hand-open/walk-through/close/break/reject - drop tests need Debug.fly(false): fly freezes the CharacterBody). GATES: boot 0 err; doors 8/8; battery all + GENHASH 80b904c0e5f085eef1e839459058c706 EXACT; craftpad ok. AC-0266 (hold RT loops the swing - _update_swing_loop accepts _pad_mining alongside _lmb_down; gamepad arm rt_held_swing_ok/rt_release_ok). AC-0271 (wood doors - state by ID VARIANT 26/27 closed solid / 29/31 open cross; zero C++; recipe 6 planks -> 3 doors grid 3; bare-hand toggle before the no-item guard; pair clear on break; arm AWECRAFT_LOGIC=doors 8/8). AC-0267 (B toggles crouch - eye lerps 1.62->1.1 (the water/VM eye checks read camera.position.y LIVE now), capsule 1.8->1.2 shape at h/2, speed WALK*0.3, edge guard strips the velocity's forward component when the 0.45-ahead probe at foot level is air (on the VELOCITY not the lerp target); B stays the flight down key; arm AWECRAFT_LOGIC=crouch 6/6 - the edge finder MUST take a flat back side, a step-up behind is real physics not the guard). GATES: boot 0 err each; arms all green; battery all + GENHASH 80b904c0e5f085eef1e839459058c706 EXACT every round. RESUME: `python3 tasks/scripts/tasks.py next` (note: next can print a [done] top entry - scan `tasks.py list` for the first [open]) -> AC-0247 (pool-meshinstances-for-slabs, p2).

**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout), 00z moved 2026-09-07 (AC-0231-followup closeout), 00F+00G moved 2026-09-09 (AC-0244/0245/0191 closeout), 00J moved 2026-09-10 (AC-0250 filing), 00K moved 2026-09-10 (AC-0250 closeout), 00L moved 2026-09-10 (AC-0251 closeout), 00M moved 2026-09-10 (AC-0247 closeout + AC-0252 filing), 00N moved 2026-09-10 (AC-0252 closeout), 00O moved 2026-09-10 (AC-0248 closeout + AC-0253/0254 filing), 00Q moved 2026-09-10 (AC-0254 closeout), 00R moved 2026-09-11 (AC-0253 closeout), 00S moved 2026-09-11 (AC-0185 closeout), 00P moved 2026-09-11 (AC-0097 closeout), 00T moved 2026-09-11 (AC-0257 closeout), 00U moved 2026-09-11 (AC-0258 closeout). -->