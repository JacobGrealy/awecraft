# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6. 

## 00Y. CHECKPOINT 2026-09-13 (overnight) - **AC-0273/0274/0275 (+AC-0265 fold) COMPLETE + SHIPPED (AweCraft[20260913-0250], :8080 sha 7b1be050... byte-matched). Queue head = AC-0278 (remove-candidate-flag-single-orchestrator-check).** User asleep overnight (autonomous run; morning report delivered). (1) AC-0273 LIGHTING "ROOT CAUSE" (measured): the first-landing light is CORRECT (harness A/B: break surface block -> 294 common vertex colors, exactly 1 diff = the broken cell; eff arrays 98304 cells, 1 diff). The visible "checkerboard of washed-out/stuck-in-night chunks, not 100% regular, breaking a block fixes it" = DOUBLE-LOD state: high mesh + fog-box veil coexisting per slab; fog box color = DayNight.sky_display(t) -> bright blue by day ("washed out"), dark blue at night ("stuck in night"); random pattern = band-exit churn timing. (2) AC-0275 (user decisions A/B/C final): attach guards (_low_place_slab/_fog_ensure_slab refuse slabs with mesh_instance; low_failed[si]=data_gen terminal mark; counter low_on_high_n); band-exit DEMOTE on recenter (old center captured pre-update; column crossing taxi<medium_start -> >=medium_start with data: chunk.demote_high() frees mesh/fluid/flora/occluder pooled, high stamps -> data_gen, low_failed RE-OPENED (a guard-terminal mark = invisible slab once high gone), FOG VEIL on freed slabs (no veil = 1500+ see-through holes at r16, measured), probes invalidated; demoted_cols_n, DEMOBAND print); straggler guard (hslab landing at taxi>=medium_start takes _eff_landed-only). (3) AC-0274 r30 load stall 24-30s -> 3.6s (25/25 meshed): 4 layers - (a) drain deferred one column at a time (dedup break) -> frame-persistent skip set + LOAD_HSLAB_UNITS_PER_FRAME=16 + defer-reason counters + LOADWIN; (b) first fix attempt self-starved (per-pass skip set re-created in-loop: 1300 spins/100ms, q stuck 25) - the frame-persistent skip set IS the fix; (c) (layer,taxi) bake order starved the target -> _grid_score -5e9 prefix for loading-target cols while loading_active; (d) close waited on empty pools (never) -> decision B: target = tier0+sim band (_loading_band_count, 25 @ r30), close on _loading_band_meshed>=target, stop_loading restores tg/tm norms 2/4. Plus LOAD_HO_BUDGET_MS=100 handoff budget (loading only) + LOAD_TG_CAP 24->8. Throughput model: ~15ms/slab MAIN-THREAD dispatch (snap_rings x4 + _strips_for_scoped + slab_copy) = ~43 slabs/s cap; worker build ~2ms/slab (shared WorkerThreadPool, tg=2/tm=4 steady). (4) HARNESS arm fixes (product gates): interact arm was RACY at HEAD (5-frame wait < spawn data lead; mine wait = 60fps frame count but headless runs ~96fps -> prog_max 0.972, block never broke) -> waits for spawn column data + wall-clock mine wait (3s cap); r16 catch_up_ok now AC-0275 semantics (demoted>0 + far_low>100 + far_high<=20 - the old clause measured the pre-fix residue, baseline far1.far_high=1128) and lost_chunks hole-aware (re-tiered chunk covered by fog/low/claim != pop); lightstate move-phase census (double_lod + hole slabs w/ air-above-top excluded, demote ring taxi<=medium+2 vs far, pre-jump baseline); loadlog RESULT {load_secs,stalled,target,radius}. (5) GATES all green on final tree: boot/player(sprint_ok)/interact(place_ok)/light(surface_eff 15)/fluids(==HEAD, environmental)/buckets/gamepad(ok)/r16(ok, demoted 306, far_high 0, 0 pops)/ladder(ok)/meshprobe(1.0)/battery(ok, GENHASH 0 0 = 80b904c0e5f085eef1e839459058c706 EXACT)/wprof(ok, storm_peak 12.2 avg)/loadlog r30 (3.6s 25/25)/lightstate r16 (double_lod 0, demote-ring holes 0, pre-jump holes 0, demoted 48, low_on_high 0). PENDING USER GATE (morning): the in-game look - load a fresh save (3.6s window now), walk/fly across band edges day+night (no washed-out / stuck-night patches), break blocks at the band edge (no flash; AC-0265 folded here). RESUME: `python3 tasks/scripts/tasks.py next` -> AC-0278.

## 00Z. CHECKPOINT 2026-09-13 (overnight r2) - **AC-0278 (remove-candidate-flag-single-orchestrator-check) COMPLETE + SHIPPED (AweCraft[20260913-0421], :8080 sha d7f2144e... byte-matched, commit 45dd719). Queue head = AC-0277 (predictive-recenter-ahead-target).** User still asleep; autonomous round 2 of 12. WHAT: (1) AUDIT: c.candidate was read ONLY by the recenter walk + harness diagnostics - the spec's "scattered candidate checks in picks and fog/low/high gates" did NOT exist (those gates were already distance/band-based, per AC-0231's comment). (2) STUCK-MESH ROOT CAUSE (the user's "candidate out of sync kept mesh from generating even at tier 0"): a build entry is stripped when a chunk candidated on exit (_strip_candidate_builds); re-queueing happened ONLY in the WANT walk, which DEBOUNCED recenters skip (AC-0213: <1000ms + 1-chunk cross + cover<=REBUILD_COVER_L1(4); AC-0233 forces a full walk only every ~5 chunks). Sustained flight = debounced chain: the chunk re-enters the HIGH band on a debounced crossing, the player LANDS, and no recenter ever re-queued it - permanent hole. (3) FIX (the walk runs on EVERY recenter incl. debounced): in-set branch now re-queues a stranded entry - high-band (taxi < medium_start_r) chunk with data + no mesh + queued_keys != "build" -> _enqueue_build (idempotent; drain's high_only pool gate keeps over-queueing harmless; marker<->entry invariant holds across strip/cap-evict/merge - verified). Just-exited / just-re-entered transitions derive from opcx/opcz (old center, already captured for AC-0275 demote) - no flag. cand_since hysteresis (r+2 free after 2 events) + retain (hide-not-kill) unchanged. (4) NEW HARNESS ARM AWECRAFT_LOGIC=candflag (the gate): R=16 flying; T = high-band chunk with build entry stripped (the stranded state, _strip_candidate_builds) flies -16 to candidate; U = built chunk -> taxi 20 candidate, mesh KEPT (hide-not-kill), freed after 2 two-out events; 14 one-chunk return crossings (full walks m=5/10, rest debounced); T re-enters HIGH band (taxi 7) on a DEBOUNCED crossing (m=13/14); player lands; T must mesh from the standing drain (old code: no re-queue until the next FULL recenter = none while standing). RESULT ok: T re-entry queued=build + built 15 frames; U retained/freed/regenerated (11.8s). NOTE: the old edgeretain arm is STALE (fails identically at HEAD - pre-band-3 model; tracked chunks never stubbed) - NOT a valid gate, left in place. (5) GATES green: boot 0 err; candflag ok; battery all arms + GENHASH 80b904c0e5f085eef1e839459058c706 EXACT; r16 ok (fly50 lost 0); loadlog r30 3.7s 25/25 no stall (baseline 3.6s); lightstate LS16 double_lod 0 / holes pre 0 / demote-ring 0 / low_on_high 0 / demoted 47 (baseline 48) / stragglers 1. RESUME: `python3 tasks/scripts/tasks.py next` -> AC-0277 (predictive recenter ahead-target - replaces the AHEAD_RING_DEBOUNCE_MS/REBUILD_COVER_L1 debounce with a velocity-predicted ahead target; NOTE: AC-0278's re-queue lives in the walk that runs on EVERY recenter incl. debounced, so it composes with whatever AC-0277 does to the debounce).

**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout), 00z moved 2026-09-07 (AC-0231-followup closeout), 00F+00G moved 2026-09-09 (AC-0244/0245/0191 closeout), 00J moved 2026-09-10 (AC-0250 filing), 00K moved 2026-09-10 (AC-0250 closeout), 00L moved 2026-09-10 (AC-0251 closeout), 00M moved 2026-09-10 (AC-0247 closeout + AC-0252 filing), 00N moved 2026-09-10 (AC-0252 closeout), 00O moved 2026-09-10 (AC-0248 closeout + AC-0253/0254 filing), 00Q moved 2026-09-10 (AC-0254 closeout), 00R moved 2026-09-11 (AC-0253 closeout), 00S moved 2026-09-11 (AC-0185 closeout), 00P moved 2026-09-11 (AC-0097 closeout), 00T moved 2026-09-11 (AC-0257 closeout), 00U moved 2026-09-11 (AC-0258 closeout). -->