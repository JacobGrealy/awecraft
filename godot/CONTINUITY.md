# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6.

## 00D. CHECKPOINT 2026-09-07 (16:10) - **AC-0242 (forward-plus-srgb-display-match) DONE + COMMITTED (f423006) + PUSHED. The washed-out Forward+ look is FIXED at the pipeline level - PENDING user visual confirm on Windows (exe rebuilt in this session; daemon 8082). ROOT CAUSE (self-rendered A/B, see below): the gl_compatibility LDR path displays unshaded material output WITHOUT a final sRGB encode; Forward+ encodes correctly - so every unshaded 3D surface rendered one sRGB curve brighter than the old build = the washout. Evidence: compat grass frame (22,50,13) vs pre-fix Forward+ (95,130,87); the compat frame == sRGB-decode of the Forward+ frame (RMSE ~7/ch); atlas grass bytes 120-134 confirmed the direction (the OLD build was the darker one - that is the look the user likes). FIX (reversible, renderer-conditional, zero cost on compat): AeroLib.srgb_pre() = 1.0 under forward_plus / 0.0 under gl_compatibility; the 7 unshaded shaders pre-decode their final color (ALBEDO = mix(ALBEDO, pow(ALBEDO, vec3(2.2)), u_srgb_pre)): chunk_lit_opaque/_cutout/_fluid/_flower, fluid_anim, aero_sky, aero_wash (the additive wash in the linear HDR buffer was ALSO lifting darks 40-80 levels in blue - it needed the same decode; without it the match stalled at mean diff ~22). u_srgb_pre rides the per-frame _update_sky chunk push + one-time sets (Data.fluid_anim_mats, aero sky/wash). Ruled out with controlled renders (do NOT re-chase): glow/bloom (user A/B), env fog (pixel-identical on/off in BOTH renderers), grade/ACES (bare pair still showed the delta), mipmaps (import generate=false), the Linear-project-setting red herring (63729ad - env tonemap overrides it; ACES grade = the designed look, KEPT). HARNESS: AWECRAFT_TIME_FREEZE=1 env (the snapshot clock drifted up to a quarter day frame-dependently between software renderers and confounded every A/B before the freeze). SELF-RENDER CAPABILITY (NEW - the agent is no longer blind to renderer deltas): lavapipe Vulkan (Forward+) + llvmpipe OpenGL (gl_compatibility via --rendering-method) under xvfb; deterministic A/B recipe: AWECRAFT_SNAPSHOT=<path> AWECRAFT_RADIUS=2 AWECRAFT_TIME=0.25 AWECRAFT_TIME_FREEZE=1 AWECRAFT_SNAP_DRAIN=15000 AWECRAFT_SIZE=960,540 AWECRAFT_SEED=44 AWECRAFT_MENU=0 (+ AWECRAFT_AERO_GLOW/WASH/SKY=0, AWECRAFT_GRADE_* knobs, AWECRAFT_NO_FOG=1) xvfb-run -a godot --path godot [--rendering-method gl_compatibility]; verify with per-region PIL stats (grass/stone/hole boxes) - the world build at fixed frame count is nearly deterministic across renderers (same per-frame gen budget). GATES: syntax, SMOKE battery green (light arm torch 14/far 0-9/surface 15/cave 0 = baseline). EVIDENCE frames (gitignored): .scratch/ab3_ref vs ab5_fwd (bare, mean diff 4.25/ch, pre-fix ~60) + ab6_ref vs ab6_fwd (graded, 2.56). QUEUES: AC-0241 done+closed; AC-0239 (sim-distance-streaming-tier) NOW TOP; then AC-0175, AC-0087, AC-0238 (window-masked-light), AC-0235, AC-0140... (the queue was re-ordered by another session 2026-09-07 - AC-0175/AC-0087 notes NOT yet read; read when due). THREE-DISTANCE STREAMING (satellite far-LOD) still NOT ticketed - design parked (00C), awaiting user trigger decision. USER MODE 2026-09-07 16:05: mowing, full autonomy - finish look match -> AC-0239 -> continue down queue; if something absolutely needs user input (real-GPU visual verdict, design fork) PAUSE that task, document the question, move to the next.
LANDED (committed bf45a72, pushed, PENDING nothing - all gates green): **AC-0239 (sim-distance-streaming-tier)** - _tier_of tier 1 = SIM RADIUS (Chebyshev <= band0_r = the sim_dist slider, default 4) instead of the 8 ring-1 neighbors: the whole surround builds before the cone (turn/sidewalk never shows load-in next to the player). band0_r in _pool_key; Settings.apply_sim_distance/apply_world -> new public world.note_sim_distance() rescore kick (has_method guard; AWECRAFT_BAND0 override kicks); AC-0231 tier>=2 gate sites needed NO renumber (the sim circle drops out of the gate set = builds first = the feature). Gates: syntax/SMOKE green; R16 all sub-gates green (culls 643 = prior, build_ms 72802 faster, low 12017/12017, owed 0, gen_cov 0, fog_hides_pop true, drop_fps 0); SPIN flip_frames 0/overlap 0/post_bad 0/t2pre 51; ahead pre-warm intact (fly50 ahead_while_moving 830, fly4 253, pre_ahead_while 279 - the sim circle absorbed its expected ~41-chunk share); genhash 25/25 baseline held; settings arm ok (band0_r + sim_kicks proven). Also fixed a stale settings-arm literal (defaults json sim 1->4). NOTE: another (coordinator) session is editing tasks/TASKS.yaml CONCURRENTLY - the queue was re-ordered during this session (AC-0238 pulled from the queue, stale waiting_on AC-0237) and now reads [AC-0087, AC-0235, AC-0175, AC-0140, ...]; re-read the queue before grabbing the next ticket, and re-read after any TASKS.yaml edit (concurrent-writer hazard: duplicate-key clobber bit the AC-0239 closeout once - verify with yaml.safe_load after every edit).
LANDED (f33b1ff, pushed): **AC-0087 (Bedrock controller core)** - gamepad: left stick move (InputMap axes on move_*), A jump, RT hold-mine + LT tap-use (triggers = ANALOG AXES 4/5 in 4.7), LB/RB (9/10) hotbar, Y/X inventory/craft, START (6) pause, B cancel; right-stick look per-frame (2.5 rad/s); menus: native GUI focus nav (D-pad 11-14 + stick via the untouched built-in ui_*) + A confirm (pad_accept) / B cancel (pad_cancel) + per-box starting focus + modulate highlight. NEW ARM AWECRAFT_LOGIC=gamepad (headless, synthetic Input events, 14 checks ok:true). GATE LESSONS: project.godot comments use ; not # (a # line in [input] silently swallowed the next action); Godot 4.7 SDL joypad layout: 0 A,1 B,2 X,3 Y,4 BACK,5 GUIDE,6 START,7 LS,8 RS,9 LB,10 RB,11-14 DPAD, triggers axes 4/5; the enums are NOT GDScript constants (probe via `strings ~/tools/godot/godot | grep JOY_`). Regression green (SMOKE/settings/R16 all baseline).
LANDED (6faa336, pushed): **AC-0235 (skybox sphere removed -> sky pass + cloud layer)** - AeroSky dome deleted; gradient+sun in aero_sky_gradient.gdshader (shader_type sky, dome math 1:1, EYEDIR); clouds = cloud_layer.gdshader on a 4096 quad at CLOUD_H=160 following the player XZ, pattern world-anchored (parallax) + u_cloud_time wind scroll; AWECRAFT_CLOUDS=0 disables; AC-0242 srgb pre-decode on both (the SKY PASS needs it under Forward+ too). BUG CAUGHT BY USER ON REAL GPU (my fault - headless never compiles shaders): sky shader COLOR is vec3 not vec4; spatial shaders have NO WORLD_POSITION builtin (vertex-stage (MODEL_MATRIX*VERTEX).xz + varying). Verified: noon eyeup = blue sky + sun + white clouds, no z-fight, fwd==compat; t=0.25 pink is faithful (t=0.25 = SUNRISE: elevation=sin((t-.25)*TAU), noon = t=0.5); 30s wind scroll moved 48% of cloud-band pixels. SMOKE/settings green; exe rebuilt 19:26 (user retest PENDING).
**RESUME (new chat):** (1) read this 00D; (2) NEXT = AC-0243 (user-filed: double-tap A toggles fly mode on the controller, B lowers while flying - fly mechanics already exist: F key toggle, jump=up/shift=down, flight_speed setting) then down the queue (re-read tasks/TASKS.yaml - the coordinator session re-orders it; at 19:30 it was AC-0243, AC-0175, AC-0140, ...) one ticket at a time, standard gates (syntax/SMOKE + the arm the ticket names + R16 for streaming changes; ALSO grep gate logs for "Error parsing|Could not load"; AND for any shader work: render a real frame under xvfb - headless does NOT compile shaders), commit+push after each. Standing: godot ONE at a time; HOME=/tmp/dsh_home for headless godot (fresh /tmp per bash call) but NEVER for build_windows.sh (real HOME); long arms -> run_in_background + job_output; no single quotes in TASKS.yaml comments; commit+push after each task.

## 00C. CHECKPOINT 2026-09-07 (09:30) - **AC-0231/0234/0236/0237 ALL COMMITTED (b48c91b C++ pipeline + 36407d8 GDScript closeout) + CLOSED. USER RULE CHANGES (2026-09-07, committed 93e8e3a): (1) caps reuse the FOG material + shared box mesh (light sky-blue UNSHADED) - the black _vwin_cap_mat is DELETED (user: caps go to the fog color, not black); (2) OPEN-TOP BAND: the player band is [pys-4, TOP OF WORLD] - the full column ABOVE the player's Y is always kept, rendered normally, NEVER culled/capped (the +-4 band culled + capped the SKY slabs above the terrain in far columns = rows of placeholder boxes in the sky; user: fill goes DOWN not up). One-line impl: _vwin_recompute bhi = top slab - every band comparison (col_kept / keep_for incl GEN_BAND_PAD / sync) picks it up; cap logic untouched. R16 all-green (seed 44): owed 0, gen_cov 0, caps_ok, phase_ok, built_all, build_ms 74529->~70k, and the +-4 sky-cap churn collapsed (re-entry regens 24077->28, rebuilds 24805->224, uncaps 1322->0) - the R16 churn_ok gate was re-pointed to (culls_n>0 AND vwin_regens_n>0) since the old uncap requirement measured the REMOVED sky-cap churn. NOTE (user): this optimizes flight/above-ground; cave interiors may need a different algorithm later. USER caveat honored: the 4x4x4 LOW code path is NOT touched yet (satellite LOD may replace it - see below).
**RESUME (new chat):** (1) read this 00C; (2) NEXT = **AC-0238 (window-masked-light)** top of queue - the light pass (C++ AweLighting) must honor the same per-column keep mask as gen/build (full notes + gates in tasks/TASKS.yaml; the .so rebuild recipe is in this file's footer section, stage to godot/bin/ after any C++ change). THEN **AC-0239 (simulation-distance-streaming-tier)** - filed 2026-09-07: a sim-radius tier (Chebyshev <= the existing Settings.sim_dist Options slider, default 4) ahead of the FOV cone in _tier_of (currently 0 under / 1 ring1 / 2 cone / 3 rest); the slider is live-wired via Settings.apply_sim_distance (today: fluid_tick_radius + band0_r only); gate audit: the AC-0231 order gate fires on tier >= 2 = the SAME set after renumber (verify). ALSO LANDED 2026-09-07 (committed in the fog-wave batch): **AC-0240 (fog-wave-disable)** - FOG_WAVE_ON := false: the sky-blue fog boxes are GONE (restorable via the const, zero cost off - no fog MultiMesh nodes), caps unchanged (fog color). CRITICAL LESSON: the fog set was the ENTRY TICKET into the low lane's pending set (_low_pending_sis/_low_pending_si walk the fog/cap/low placeholder sets) - with fog off a VIRTUAL pending set (kept data slab, no placeholder/low/terminal) was added to both picks or the low lane starves (first R16: low=0, culls 0, build_ms 30s - the order gate never closed; the re-pointed inr_uncovered gate [kept-slab gap allowed] masked it). R16 final all green (culls 642, fog 0, build_ms 77339). KNOWN FOLLOW-UP (user-flagged): the low lane shares the TM high-priority pool with high builds - a dedicated low thread (AC-0187 precedent) would fix pool-contention starvation; urgency rose with fog off (the low is now the first visible representation). LANDED 2026-09-07 (committed, PENDING USER VISUAL VERIFY on Windows): **AC-0241 (forward-plus-migration-drop-dither)** - renderer flipped to forward_plus (gl_compatibility was web-era legacy; Windows-only per AC-0124; Bedrock reference = big distance + real LOD + modern API, PC range 4-96 chunks); DITHERED FADE DROPPED entirely (5 shaders: u_dither_start/end + DITHER44 + discard block; main.gd per-frame push/settings arm/R16 evidence/env hooks; daynight dither_start/end; settings keys dithering_enabled/dithering_start_pct; menu checkbox+slider; the dithersettings arm became the fogsettings arm). The FOG fade (fog_start_pct, per-frame env.fog_depth_end) is the only distance fade now. Gates: syntax, SMOKE, settings arm (fog round-trip), R16 all green (fog block: fog_far 236.64 <= 256 pop face, fog_hides_pop true, culls 643, build_ms 78836). NOTE: headless cannot SEE Forward+ - the user's Windows build is the render oracle; the xvfb render arm may be dead here (Forward+ = Vulkan on Linux, llvmpipe = OpenGL). NEXT after user verify: three-distance streaming (sim / mesh / satellite zones - the satellite far-LOD, GPU-native on this stack) as the next ticket; AC-0238 (window-masked-light) + AC-0239 (sim tier) queued. UNDER DISCUSSION with the user (NOT yet a ticket): SATELLITE FAR LOD - a per-chunk 16x16 top grid (C++ slabs_top + one palette read = top y + top block TYPE per 1x1 block column, LIVE edit-aware, re-baked on the existing 3x3 neighbor-sync edit hook) rendered as an unshaded 17x17 vertex-color heightfield at TRUE elevation (no stretch; MC relief shading additive; top-surface only, open underneath) - it takes the LOW's slot in the ladder (fog -> satellite -> high) with the low path left dormant behind a revert const; it is TERMINAL for far chunks (lazy heightmap-only gen = end state; no high build/light owed) - the CPU win is skipping full gen + high mesh + light for ~97% of the R50 circle, the bake itself is only ~5-25x cheaper than the per-slab low; the PLANET VIEW is the same top-grid cache max-pooled to coarser zooms (MC map scale 1/2/4), same unlit material. Design state: user confirmed the shape 2026-09-07; trigger (distance vs speed) + detail radius + ticket not yet decided.
**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout), 00z moved 2026-09-07 (AC-0231-followup closeout). -->