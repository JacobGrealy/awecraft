# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6. 

## 00Z. CHECKPOINT 2026-09-13 (r4) - **AC-0037 MOBS COMPLETE + SHIPPED** (build 20260913-1401, :8080 sha `bb907a22fe8689c38cded5dc5b3556a887e7dd8b899ca5e1181d6f903cd1495f` byte-matched) + user DoF PR **f3e93ae** pulled (camera attributes on player camera + base FOV 75->90) + AC-0247/AC-0253/AC-0248 closed (r3) + stale done AC-0264 removed from the queue. **QUEUE PAUSED BY USER: waiting for the joint LOD working session - DO NOT start AC-0093 or any other queue task until the user resumes.**
**QUEUE-ORDER RULE (BINDING, user-corrected r4):** the work order is the `queue` ARRAY in TASKS.yaml - `python3 tasks/scripts/tasks.py next` prints the head; `tasks.py list` (intake, ID-ascending open entries) is a SUPERSET, NOT the work order (r4 mistake: picked AC-0037 from the intake head - it was queue position 20). Current queue (23 live): AC-0093 (head, drop-spin-in-place-not-orbit p2), AC-0038 m12-particles, AC-0039 m13-sound, AC-0159, AC-0112, AC-0113, AC-0098, AC-0088, AC-0089, AC-0205, AC-0186, AC-0193, AC-0192, AC-0130, AC-0094, AC-0194, AC-0141, AC-0142, AC-0195, AC-0115, AC-0090, AC-0176, AC-0259.
**AC-0037 mobs (shipped this round):** 9 box-rig mobs (Data.mobs: pig/sheep/chicken/cow/wolf/zombie/skeleton/spider + bunny 0.5x0.6 with pink ears, same height as the chicken - the godot port uses procedural box rigs, no web-era PNG pipeline). `entities/mob.gd` (CharacterBody3D, BoxShape3D, Body/Head box meshes + 4 hip/shoulder PIVOT nodes, walk-cycle limb swing); AI: passives wander (2-4 s re-roll, 0.6x speed), wolf untamed flees <6 m / tamed follows holding ~5 m (FOLLOW_NEAR/FAR), zombie chases <16 m + melee 1.6 m/1 s cd via `damage_player`, skeleton HOLDS the 4-14 m band + volleys `entities/arrow.gd` (weak gravity 2.0, 8 s life, stops on first solid cell) every 2.5 s, spider hostile only when `DayNight.is_night`. Taming: bone id 144 used on an untamed wolf (player.use_selected -> aim_mob). Spawning: `world._mob_tick` - 24-44 m ring, day table passives only / night adds zombie+skeleton+spider, despawn >52 m, cap 20, 3 attempts per 0.5 s. GATES: boot 0 err; mobs arm 8/8 (rig, bunny geometry, fall, chase, spider day/night, wolf tame+follow, skeleton arrow, tables+cap, ring_ready); player arm green; battery green + GENHASH 80b904c0e5f085eef1e839459058c706 EXACT.
**MOBS PITFALLS (for any future mob work):** (a) `surface_top` = topmost SOLID - the spawn rejection must check ONLY the cell above it (the first version inverted the check and the tick never spawned); (b) `aim_dir`: POSITIVE pitch aims UP - to aim slightly down at a mob 3 m ahead use `p.look(atan2(-dx,0), -0.25)`; (c) the seed-40 spawn plateau is flat ONLY within 6 m - every cell at d>=8 is HIGHER (+1..+6), and the ring around the -x ridge bench is WATER (every spawn attempt rej_top) - mob AI test mobs spawn within 6 m; the wolf follow test teleports BOTH player and wolf onto the -x ridge bench (d10-18, top 140, found by an 8-ray same-level-run scan); the spawn-table test must run FROM THE PLATEAU; (d) the stream must deliver the ring chunks before the table window (`world.recenter` + ring_ready probe, 600-frame timeout; `ring_ready` in the result JSON); (e) a CharacterBody teleported INSIDE a solid is stuck forever (depenetration can't extract it) - never teleport a mob into unproven terrain.
**DoF PR f3e93ae:** `godot/camera_attributes/main-camera-attributes.tres` (CameraAttributesPractical: dof_blur_far at 82.62, amount 0.08) attached to the player camera; base FOV now 90 - the harness player-arm sprint check was updated (base 90, kick checks relative to base +4). ONE transient headless SIGSEGV on the combo tree after the pull did not reproduce (3/3 clean boots) - watch for recurrence.
RESUME: **WAITING ON USER for the LOD working session** (user 2026-09-13: "when you finish this though wait for me and we'll work on lod issues together"). The LOD question from r2 is still unanswered - which issue: pop-in, the demote veil, ahead-target interactions, or something seen in a build? When the user resumes: head = AC-0093 (queue array); any LOD findings get filed via `tasks.py add` + `queue add`.
**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout), 00z moved 2026-09-07 (AC-0231-followup closeout), 00F+00G moved 2026-09-09 (AC-0244/0245/0191 closeout), 00J moved 2026-09-10 (AC-0250 filing), 00K moved 2026-09-10 (AC-0250 closeout), 00L moved 2026-09-10 (AC-0251 closeout), 00M moved 2026-09-10 (AC-0247 closeout + AC-0252 filing), 00N moved 2026-09-10 (AC-0252 closeout), 00O moved 2026-09-10 (AC-0248 closeout + AC-0253/0254 filing), 00Q moved 2026-09-10 (AC-0254 closeout), 00R moved 2026-09-11 (AC-0253 closeout), 00S moved 2026-09-11 (AC-0185 closeout), 00P moved 2026-09-11 (AC-0097 closeout), 00T moved 2026-09-11 (AC-0257 closeout), 00U moved 2026-09-11 (AC-0258 closeout). -->