# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6.

## 00F. CHECKPOINT 2026-09-08 (18:10) - **AC-0175 + AC-0111 + AC-0140 LANDED + PUSHED + EXE REBUILT (110046472, :8080 byte-matched). USER STILL AWAY: working the queue autonomously per standing instruction.** (1) **AC-0175 DONE (3b387b5)**: region chunk saves - r.<rx>.<rz>.bin per 32x32-column region (AWCR + 1024-entry offset/size table + unmodified AWCC v1 blobs), main-thread _io_write_commit capped 4/frame, worker compaction with atomic rename, legacy per-column files read-only fallback. chunkio arm green (r=50 diamond = 12 region files = exact distinct-region count; task estimated ~8), lightcache green, SMOKE green. (2) **AC-0111 DONE (b0c2c1d)**: transparent blocks no longer occlude same-id neighbors - mesh.cpp s_faces now takes ktab/xtab and the same-id cull applies to OPAQUE blocks only (MC fancy-graphics semantics: leaf clusters emit interior faces, solids still occlude). New occl arm green (leaf cube in air = 162 faces, in stone box = 108 + stone >0, lone stone = 6, stone+leaf = 6/5); meshprobe self-consistent 1908/1908; R16 green. (3) **AC-0140 DONE (0bae0ff)**: the AWECRAFT_LOGIC/BATTERY harness extracted VERBATIM from main.gd into scenes/harness.gd (class_name Harness, extends Node, child of main): main.gd 17456 -> 1690 lines / 298 -> 54 funcs (thin coordinator: lifecycle, menu, sky/fog, snapshot), 258 funcs moved byte-for-byte (diff-verified after stripping the main. prefix), the _run_game dispatch cascade = harness.run(seed_env, logic, cam, snapshot_path, spawn), battery entry = run_battery(). Coordinator calls go through main.<fn>; 23 member reads/writes through TYPED get/set pass-through properties (assignments like player = x land on main). Shared waiters / snapshot fixtures / slot+fluidfall+breakable helpers / _occl_* cave tools / _StubWorld inner class STAYED in main (both sides use them); _ChunkScriptM + AeroLib consts duplicated on both sides. GDScript gotchas hit (keep): property setters must take an UNTYPED param (set(v) - set(v: Node3D) is a parse error); var x := main.fn() needs var main TYPED (var main: Main) or inference fails; a mutual class_name reference (main.gd var harness: Harness + harness.gd var main: Main) FAILS to parse ("Could not parse global class") - break it with a preload const (const HarnessScript = preload(...)); top-level class blocks are column-0 and must be their own parse block. Gates all green with values matching the pre-extraction baselines: syntax 0 parse errors, SMOKE battery ok:true (GENHASH prints bit-identical to the AC-0111 run), occl 162/0/108/25/6/6-5, meshprobe 1908/1908, chunkio 12 regions, lightcache ok, r16 built_all, save_test ok, quitmenu ok, gamepad ok. **QUITMENU GOTCHA (do NOT re-chase): the arm greps AWECRAFT_STDERR_FILE for SCRIPT ERROR - run it with AWECRAFT_STDERR_FILE=<log> or script_errors_seen defaults true and ok:false (that is an invocation artifact, not a regression).** Task notes: the ~400-lines main.gd target was stale (file was 8939 when filed, 17456 when worked); single-file harness.gd chosen over a harness/ folder to keep the move a pure verbatim relocation (splitting further = cosmetic follow-up). NEXT (re-read TASKS.yaml; the four done tasks above are still status:open for the coordinator to re-order): **AC-0121** (queue head after AC-0140), then AC-0122/AC-0170/AC-0171... Standing rules unchanged (godot one at a time; HOME=/tmp/dsh_home headless/xvfb ONLY - NEVER build_windows.sh; fresh /tmp per bash call; TASKS.yaml = YAML round-trip only + no apostrophes + safe_load check; push with GIT_SSH_COMMAND workaround; exe at :8080 authoritative / :8082 LAN; any mesh/shader work = meshprobe + R16 (+ xvfb render + SHADER ERROR grep for shaders); .so recipe + staging in the archived BUILD RECIPE below). USER RETEST ON RETURN: transparent-block look (leaf clusters now show interior faces - AC-0111) + save-folder region files (AC-0175) + whatever the exe rebuild delivers. **DO NOT COMMIT:** user scaffolds tasks/AC-0092|0125/plan.html|0140|0141|0142|0150|0154|0159|0177|0205/ + references/ + docs/halo-loot-brainstorm.html.
## 00G. CHECKPOINT 2026-09-08 (post-AC-0140) - **AC-0121 LANDED + PUSHED (e385d2e). USER STILL AWAY: working the queue autonomously.** In-game debug console: backtick/tilde or F3 toggles an overlay (godot/ui/console.gd, CanvasLayer 30) - green scroll-following RichTextLabel log + LineEdit command line; commands give/tp/time/fly/setblock/block/spawn/seedinv/help/quit dispatch to the Debug autoload; auto-closes when Game.mode leaves play. Input architecture: player.gd new _input handler does the toggle (runs before the focused LineEdit would swallow the key, marked handled); while Game.console_open the player gates EVERYTHING - LMB, the V/H/J/K/E/P/1-9 hotkeys, pad buttons, and every polled InputMap action (move/jump/sprint/fly/time/debug) - so typing never steers; physics keeps running. Game got console_open flag + console ref; main.gd _create_game_nodes creates it. harness AWECRAFT_LOGIC=console arm asserts the full contract (open+focus, typed give 126 5 lands, 60-command burst grows+scrolls the log, typed time 0.6 applies, held W no-move while open, close+mouse recaptured, W moves again) - ok:true. **GODOT 4.7 INPUT GOTCHAS (probed in isolation, do NOT re-chase): (1) TextEdit no longer exposes the text_submitted signal in 4.7 (LineEdit does) - console input is a LineEdit; (2) an injected InputEventKey needs .unicode set or it types nothing (headless never fills it from the keycode); (3) Input.parse_input_event queues the event OBJECT itself - a press then release on the SAME object rewrites the still-queued press (every key arrived pressed=false!) - use a fresh object per parse; (4) a LineEdit WITHOUT keep_editing_on_text_submit=true stops accepting keys after the first ENTER (focus still claims to be on it) - the console sets it; (5) headless cannot CAPTURE the mouse (the assignment is a no-op - the arm accepts VISIBLE there). Also 4.7 API: KEY_QUOTELEFT (no underscore) is the backtick key; RichTextLabel has no get_scroll() (use get_line_count vs get_visible_line_count). Gates: syntax 0 Parse Errors; console arm ok:true; SMOKE battery ok (player arm at baseline 2.82/138.43); gamepad ok; save_test ok; quitmenu ok 0 SCRIPT ERROR. **AC-0122 LANDED (9fcec63)**: the V/H/J/K debug keys are GONE from the player key handler and now live as console commands - swing (old H), holdswing [frac] (old J, default 0.5), clearswing (old K), seedinv (old V, pre-existing); P keeps pause until AC-0185. Console arm extended (cmd_swing/cmd_holdswing/cmd_clearswing assert player swing state; keys_dead = real H/J/V/K presses after close change nothing) - ok:true with 14 fields; quitmenu paused_ok (P still pauses); SMOKE + gamepad green at baseline. **AC-0170 LANDED (8dacafc)**: Options debug-logging toggle (settings debug_logging bool, default off) - Debug.log/Debug.error tee into the console overlay when on; console command log [on|off] drives the same setting; Options checkbox is code-created like the debug-stats one (arm path never boots the menu - the test instantiates res://scenes/menu.tscn directly; menu_ui is NULL in every arm run, _boot_menu only runs on the menu-first boot path). Console arm ok:true with 20 fields (log_tee_on/off, log_persist across disk reload, log_cmd, log_menu); SMOKE + quitmenu + mainmenuexit green. GOTCHA: a GDScript % format list with MORE values than specifiers is fine (extras ignored) but keep them matched; harness arm hangs to timeout (exit 124) when a script error aborts before get_tree().quit() - always check exit code. **AC-0171 LANDED (1f79d97)**: session log file - with the toggle on, Debug.log/error lines + a 2 s stats line (hook in _refresh_stats) append to a NEW user://logs/awecraft_<stamp>.log per run; prune keeps the 5 newest (6th run deletes the oldest). File-IO gotchas baked in as comments: create the file BEFORE prune (so it counts); same-second relaunch gets a fresh name (no truncation); open per line + close (a long-lived handle buffers writes - a second reader sees an empty file); this 4.7 build has NO FileAccess.APPEND flag and READ_WRITE fails on a missing file (WRITE to create, READ_WRITE + seek_end to append). Console arm ok:true with 24 fields. **AC-0172 LANDED (cf90562)**: one-click bug capture - F8 in play or the console bugreport command builds user://bugs/awecraft_bug_<stamp>.zip (report.txt seed/pos/chunk_origin/band/settings/stats/console+session-log tails, screenshot.png, session.log). New zipmini.gd (class_name ZipMini) = STORE-only zip writer + reader - this 4.7 build has NO ZipPack/ZipReader; cross-verified with system unzip. Zip layout gotchas (baked in): central header has NO compression-method field (local-header only) - 46 fixed bytes, name at 46; EOCD central offset at +16. bug_report() awaits frame_post_draw (headless NEVER emits it - probe-verified; skip in headless + placeholder screenshot). Harness: bugreport arm (both zips read back via ZipMini, key-set comparison not dict equality) + bugreport battery mode (quit guarded by _batt). GOTCHA: the harness _c_type/_c_submit helpers are COROUTINES - they MUST be awaited (unawaited = first character only). 12 fields ok:true; SMOKE battery ok incl. bugreport mode; quitmenu + gamepad green. **AC-0173 LANDED (5eadd60)**: crash/error capture - Debug.error (the central push_error path) writes user://logs/awecraft_<stamp>.log (same family as the AC-0171 session logs - pruned to 5 together, same-second numbered name) with the error + formatted stack + state block (godot build, seed, mode, time_of_day, player pos, chunk_origin, sim/render band, FPS/RAM/VRAM, console tail, session log tail), freezes the game (Game.mode = crash - the player gates input AND physics on mode == play), and raises a CanvasLayer 40 modal (dimmed rect + centered panel: message, saved path, Dismiss [restores play] + Exit Game [clean quit]; one dialog at a time) + [crash] console toast. 4.7 gotchas (baked in): get_stack() returns an Array of {source, function, line} dicts here, NOT a string; Node.find_children() returns an EMPTY array for every pattern/type combo (probe-verified) - walk the tree by hand. Harness: crash arm (ends by emitting the Exit Game button - process exit is the clean-exit check; battery mode dismisses instead) + crash battery mode. 11 fields ok:true; SMOKE battery ok incl. crash mode (256 ms); console/quitmenu/gamepad green. **AC-0174 LANDED (34a7f15)**: independent dev-overlay toggles (settings overlay_band/overlay_light/overlay_collision bools + 3 Options checkboxes + world.gd gizmo system). Band = chunk grid + sim/render boundary rects; light = per-lit-block vertical lines from ONE compute_light_split box (never per-block light_at); collision = slab collision AABB wireframes (player chunk + ring). Each overlay = own ImmediateMesh; all-off = meshes dropped (no cost). 4.7 gotchas (baked in): no ImmediateMesh.clear() (rebuild allocates fresh); meshes are RefCounted (assign null, never .free()); ConcavePolygonShape3D has NO get_aabb() and this AABB has no absorb/expand (fold faces with Vector3 min/max); ClassDB.class_get_method_list returns [] for builtins here. Harness: overlays arm (12 fields ok:true, menu scene instantiated directly for the checkbox check) + overlays battery mode. SMOKE battery ok incl. overlays/crash/bugreport; console/quitmenu/gamepad green. **AC-0204 LANDED (7a69a5f, VERIFICATION ONLY)**: day/night comes from the shader uniform, not the mesh - the AC-0128/0135 work already implements it (mesh stores s = clamp(eff/15, 0.08, 1.0), pure geometry: SKY_FULL is a compile-time 15 in gdext/src/lighting.cpp; shaders do L = 0.20 + 0.80 * max(u_day * r, g); main._update_sky pushes set_day_factor(DayNight.day(t)) every frame onto the shared cached ShaderMaterials). New harness arm dnlight (the pre-existing AWECRAFT_LOGIC=daynight stays the clock-cycle test): noon->midnight flip changes u_day 1.0->0.0 while mesh colors stay byte-identical; sky faces darken + 0.20 floor holds (block-light faces correctly NOT day-modulated - the L check must use g == 0 faces only); a world BUILT at midnight stores the same light values (needs world.recenter AFTER Game.new_world - the battery pattern - or no chunks build and the sample is empty); slab surface materials are the shared cache (LOD swaps keep the transition). 4.7 gotcha: MeshInstance3D.material is NOT accessible in this build - read surface_get_material(0). 11 fields ok:true; battery ok incl. dnlight mode; console/quitmenu/gamepad + old daynight clock arm green. No exe rebuild (harness-only). **AC-0092 LANDED (fc85df3, VERIFICATION ONLY)**: ocean columns are water from surface to floor - gen.cpp already implements it (the aquifer branch fills y in (he, sea] with B_WATER when the surface is below sea level). New harness arm oceanfill (AWECRAFT_LOGIC=oceanfill + battery mode): finds REAL ocean columns from the chunk DATA (a data scan only sees loaded chunks - recenters onto candidate spots until a water surface at sea level shows up), walks each column down from SEA: surface water at 126, air above, walk hits solid before any air (no fall-through). GOTCHA: the GDScript WorldGen.terrain_height does NOT track the C++ density-field gen (pointed at a column solid stone to the top) - ocean detection must be data-driven, never terrain_height. ok:true (12-13 columns checked); battery ok; genhash (0,0) = 80b904c0... (AC-0215 baseline held). **AC-0137 LANDED (545d7cf, VERIFICATION + probe made strict)**: no threadgen handoff strands a chunk - the drop was already fixed by the AC-0178 no-result re-queue; the spin probe now proves it strictly. The old frame-cap settle + 9x9-square count was stale (stream set = CIRCLE + band-3 feed: band 3 never meshes by design, the (4,4) corners are out of set by construction). New settle: wait until EVERY band 0/1 chunk is built (120 s wall cap) + 2400-frame stabilization tail (far feed keeps landing; instances born mid-spin read as flicker). Strict contract: circle_total >= 49 and circle_built == circle_total; q_nodata == 0 (the original drop signature - a queue entry whose chunk has no data); q_band01 == 0 (band-3 feed entries linger by design - both pools skip them, next recenter merge walk drops them - queue-to-0 is NOT the contract). No-pop gate: inst_transitions <= 2 (recorded baseline - 2 flora cross-quads on the yaw-edge frustum planes, deterministic steps 64/94 at seed 44, the engines own AABB test culls them = legitimate, not a pop). GOTCHAS: %d string formatting rejects bools in this build (prints raw + error - cast int); dynamic typed := inference fails (explicit types / as Array). spin ok:true (49/49, q_nodata 0, q_band01 0, inst 2); battery + console/gamepad/quitmenu green. No exe rebuild (harness-only). **AC-0199 LANDED (cdf823d, PRODUCT)**: retain-swap mesh edits + boundary-neighbor front-priority (supersedes AC-0187). (1) chunk.gd: build_mesh no longer frees old slab instances before the build; apply_accs / apply_edit_accs keep the old MeshInstance/occluder refs alive until the new refs are assembled, then free the old atomically; _assemble_slab assigns the four covered refs UNCONDITIONALLY (null when the Acc is empty) - a covered slab always reflects the new build; apply_accs drops the refs of slabs outside the build range (AC-0197 partial-build contract). (2) world.gd: a set_block at local x/z in [0,1] or [14,15] (the SNAP_W 18 ring reaches past the chunk edge) also dirties the E/W/N/S neighbor, jumped to the FRONT of the dirty queue (_dirty_front) - the neighbors face toward the edited column was stale until the ~500 ms light wave (the see-through hole); the neighbor build reads the edited chunk snap_rings from DATA (already updated by set_block) so it is correct independent of the edited chunk handoff. Unmeshed (far low / band-3) chunks keep their drop-by-design behavior. Harness arm editsnap (AWECRAFT_LOGIC=editsnap + battery mode): recenters onto candidate spots until it loads a border trunk (log 6) whose chunk AND boundary neighbor are both MESHED, breaks it, asserts neighbor remesh from the queue front (<= 6 frames), edited chunk remesh, slab instance ref LIVE every frame (retain-swap, no null frame), handoff < 50 ms, block gone. GOTCHA: an unmeshed edited chunk legitimately gets NO remesh (the dirty entry drops by design) - the arm must filter mesh_built on BOTH chunks. Verify: syntax 0; editsnap ok:true (edit 29-40 ms, both remeshed 2 frames, ref_always_live); meshprobe ok; R16 ok; battery ok incl. editsnap (0 script errors); genhash (0,0) baseline held; console/gamepad/quitmenu green. EXE REBUILD + :8080 byte-match pending in closeout (product code changed). **AC-0199 EXE REBUILT + :8080 byte-match VERIFIED (399d1fe, md5 c63bd827... == local).** **AC-0206 LANDED (RESEARCH, no code)**: the .so load crash is no longer reproducible - G0 (plain --headless --quit, fresh HOME) 13/13 exit 0 in the coordinator env; the whole 09-08/09 session ran dozens of godot launches across every task with zero signal-11; the crash was against the 09-04/06 .so builds (sites varied: slab_cell/snap_rings/generate_flat = a transient GEN race + first-load import race); the current .so (09-08, post-AC-0236) is staged byte-identical (gdext/bin == godot/bin md5 a04f16fb...), the .dll is PE32+ verified; the old crashing .so is not in git so no A/B is possible. **AC-0243 CLOSEOUT (no code; impl was 1d901ee)**: gates re-verified at HEAD - the gamepad arm ok:true carries all six AC-0243 fields (fly_toggle/climb/descend/land + hold_mine + jitter); stays OPEN for the user real-pad retest (AWECRAFT_PADTRACE=1 in the exe env if a symptom persists). **AC-0036 LANDED (399d1fe, PRODUCT)**: underwater view - (1) both fluid shaders (chunk_lit_fluid + core/fluid_anim) discard water fragments within 1.9 blocks of the eye while submerged (the Bedrock blue-cube look; the eye check = main._update_sky: camera eye block is B_WATER); camera position rides an explicit u_cam_pos uniform - GOTCHA: the headless DUMMY renderer REJECTS the CAMERA_POSITION builtin (SHADER ERROR in every headless log until switched to the explicit uniform); (2) full-screen ColorRect tint 0x2B6AB2 @ 50% (CanvasLayer 10) shown while the eye is in water; chunk.gd set_underwater pushes u_underwater on change to the lit fluid kind + Data.fluid_anim_mats (legacy no-atlas StandardMaterial3D skipped). Harness arm underwater: recenters until it finds a 3-DEEP water column (GOTCHA: surface water at SEA is often 1 block deep - shallow coastal water; the eye would land in terrain + depenetrate), asserts eye-in-water + tint both ways + u_underwater read back from a live fluid ShaderMaterial. Verify: syntax 0; underwater ok:true; meshprobe + R16 ok; battery ok incl. underwater (14 modes, 0 script errors, 0 SHADER ERROR); genhash baseline held; console (one time_cmd 0.02 flake after the battery - rerun green, day-drift not this change)/gamepad/quitmenu ok. EXE rebuilt + :8080 byte-match VERIFIED (399d1fe). **DO NOT COMMIT:** user scaffolds tasks/AC-0092|0125/plan.html|0140|0141|0142|0150|0154|0159|0177|0205|0230|0235/ + references/ + docs/halo-loot-brainstorm.html. NEXT (re-read TASKS.yaml): ALL p1s done (AC-0206 closed as research 2026-09-09; AC-0243 closeout written, status open for the user real-pad retest). Open p2 head per tasks.py = AC-0243 (closeout done - re-order), then AC-0038/AC-0039/AC-0053/... AC-0036 done (underwater view). Standing rules unchanged (godot one at a time; HOME=/tmp/dsh_home headless/xvfb ONLY - NEVER build_windows.sh; fresh /tmp per bash call; TASKS.yaml = YAML round-trip only + no apostrophes; push with GIT_SSH_COMMAND workaround; exe :8080 authoritative; mesh/shader work = meshprobe + R16). USER RETEST ON RETURN: backtick console (type help / give 126 5 / time 0.5), plus the AC-0111 leaf faces + AC-0175 region saves.

**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout), 00z moved 2026-09-07 (AC-0231-followup closeout). -->