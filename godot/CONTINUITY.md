# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6.

## 00z. CHECKPOINT 2026-09-04 (01:00) - **AC-0211 mesh-snap-and-instance-remaining-gdscript-to-cpp DONE (C++ change + .so/.dll REBUILT)** [the remaining mesh GDScript hot spots moved into AweMesh (gdext/src/mesh.cpp — same class/.so, registration+manifest unchanged): (1) NEIGHBOR SNAP RINGS — the dispatch snapshots the ONE 16x16 boundary slice per slab per edge neighbor (snap_rings, 256 B/slab) instead of _slabs_deepcopy of all 24 slabs; parse_nbs consumes the compact PackedByteArray nbs (the legacy paletted-slab nbs format still accepted — probes + the AWECRAFT_MESHCPP=0 parity); (2) OWN-COLUMN VALUE COPY — slab_copy (true byte copies, no COW sharing); (3) SYNC build_mesh LANE — runs the same C++ pipeline as the workers (light via the existing C++ pull -> snap_rings -> make_ctx -> build_accs -> apply_accs); sync_snap decodes the paletted slabs in C++, replacing the 5 x 98k-cell GDScript flat_data()/flat_fl() materialization (inputs read LIVE — main thread, synchronous); _build_snap now returns [snap, snap_fl]; (4) HANDOFF SCOPED STALE CHECK — rows_eq (256 B/row decoded in C++, no per-row 4096 flat materialization). STAYED GDScript (documented): the main-thread scene handoff (apply_accs -> MeshInstance3D/ArrayMesh/materials/collision — engine scene-tree + mesh upload) + the world==null get_world_block generator fallback + the legacy fills (AWECRAFT_MESHCPP=0 fallback, all intact). LATENT BUG FIXED (pre-existing): the legacy ro-scan read `var d: PackedByteArray = data` — a size-24 ZERO PBA on the AC-0203 paletted Array (Godot 4.7.1 silent dict->byte-0 coercion, empirically verified) -> now flat_data() + a snap-cell fallback for the data-empty generator case. meshprobe arm extended with the 4 new lossless checks (all gated inside ok).] — meshprobe **100% + the 4 NEW arms all 32/32** (compact-ring nbs -> byte-identical build_accs result vs legacy full-slab nbs; slab_copy == _slabs_deepcopy; rows_eq verdicts == GDScript row loop; sync_snap == legacy GDScript _build_snap byte-for-byte); nbs+own snapshot **213 -> 132 us/chunk (1.6x)** + 1/3 the bytes crossing the worker boundary; worker build_accs 2.88 -> 3.09 ms (unchanged band); perf r4: build_ms 120 -> 98, worker p50/p95 3/6 -> 3/4 ms, frame_max 38 == baseline; G0 **0 SCRIPT ERROR**; SMOKE battery **ok:true x4** (C++ lane AND AWECRAFT_MESHCPP=0 fallback — values byte-identical to the AC-0210 battery); chunkiocpp md5-identical (8a893557…); genhash **25/25** == AC-0188; light surface 15 / cave -1 / torch -1 (== pre-task on this machine; user-box standing 15/10/14 unchanged, cell-identity proven by pullprobe); stripsprobe 100% (512/512 + faces); pullprobe 100% (64/64 variants, 6.29M cells); editperf == baseline (98 vs 109 ms total); MINFO **88/89 exact** + chunk pos [-16,32] border jitter (1964 vs 1968 body verts) **PROVEN PRE-EXISTING** (flips run-to-run on the SAME binary: C++ lane 1964/1964/1968, legacy AWECRAFT_MESHCPP=0 lane 1964/1964 — the chunk builds before its north neighbor lands, air-edge bake; the controlled meshprobe A/B is byte-exact so the port changes no geometry for identical inputs). .so 1,080,648 B + .dll 3,386,302 B **rebuilt (MinGW, PE32+ verified) + staged godot/bin/**. **The mesh hot path (gen/light/pull/strips/mesh + the surrounding dispatch/snap/stale-check steps) is now fully C++.** Code + gates + tasks/AC-0211/AC-0211-results.html on disk — commit = coordinator's step (no git in this subagent env).
**RESUME (new chat):** (1) read this 00z; (2) **AC-0211 DONE + SHIPPED** (the last remaining GDScript mesh hot spot series is complete: AC-0165 foundation + AC-0188 gen + AC-0189 lighting + AC-0190 mesh + AC-0207 strips + AC-0210 pull wiring + AC-0211 mesh surroundings) — closeout: coordinator re-verifies gates, commits the explicit paths (gdext/src/mesh.cpp + godot/world/world.gd + godot/world/chunk.gd + godot/scenes/main.gd + godot/bin/libchunkio.{so,*.dll} + tasks/AC-0211/ + TASKS.yaml + this file), then `python3 tasks/scripts/tasks.py next` for the next user task. GDExt rebuild recipe (subagent-verified): `GODOT_CPP=<root>/.scratch/godot-cpp PYTHONPATH=<root>/.scratch/scons-src python3 -m SCons -C gdext platform=linux|windows target=template_release -j$(nproc)` then stage `gdext/bin/libchunkio.so` + `gdext/bin/libchunkio.windows…x86_64.dll` -> `godot/bin/` (the .gdextension points at res://bin/libchunkio.so). NEW GOTCHA: the meshprobe sync_snap arm pins `_ChunkScriptM._mesh_cpp = null` (harness-only, saved/restored) to force the legacy GDScript _build_snap as the reference. KNOWN/CAVEAT: (a) the .so crashes in the coordinator env only (AC-0206, research); (b) a one-off AweGen SIGSEGV (AC-0206); (c) the AC-0190 R50 queue-no-stall is PENDING (run when AC-0206 resolves); (d) r4 MINFO border jitter: chunk pos [-16,32] body verts ±4 between runs (pre-existing air-edge bake, both mesh lanes — see 00z note). Standing: godot ONE at a time (ps check + .scratch/.godot_busy); HOME=/tmp/dsh_home every godot call; LONG arms -> run_in_background + job_output; /tmp EPHEMERAL per bash call (diff files inside ONE command).

## 00y. CHECKPOINT 2026-09-03 (12:50) - **AC-0210 wire-aweLighting-flood-flat-in-pull DONE + SHIPPED (wiring-only — the C++ pull is now live)** [call AweLighting.compute_chunk_pull (AC-0189) from the pull entry in godot/world/lighting.gd (compute_light_flat_chunk_pull + _flood_flat x2), the GDScript kernel preserved verbatim as _pull_kernel_gd, toggled by AWECRAFT_LIGHTCPP=0; no C++ change, no rebuild — the AC-0207-built .so/.dll already carried it] — Flood p95 53.1→0.78 ms (68×), 100% cell-exact across 64 runtime call variants (6.29M cells) via the new pullprobe arm. No regression: chunkiocpp byte-identical, genhash 25/25 == AC-0188, meshprobe/stripsprobe 100%, G0 clean, both toggle states battery-green. **The pull kernel (the 20ms border light hitch) is now C++** — the incremental light update on chunk-border crossing is native. Committed (godot/world/lighting.gd pull wire + main.gd pullprobe + tasks/AC-0210/*).
**RESUME (new chat):** (1) read this 00z; (2) **THE ENTIRE AweCraft hot path is now in C++ AND the pull kernel is wired** — AC-0165 (foundation) + AC-0188 (gen) + AC-0189 (lighting) + AC-0190 (mesh) + AC-0207 (strips) + AC-0210 (pull wiring) are all DONE + SHIPPED. NEXT task = **AC-0211 — mesh-snap-and-instance-remaining-gdscript-to-cpp** (a new user task — read its notes in tasks/TASKS.yaml; likely the last remaining GDScript hot spots: the mesh snap + the instance path). The C++ pipeline is mature (gdext/SConstruct + .gdextension + godot-cpp master 05057de api 4.7 + MinGW; build_windows.sh auto-builds GDExt + stages the .dll; -ffp-contract=off; build godot-cpp from its own dir; a new .gdextension needs one `godot --headless --import`; registered classes: ChunkIOPalette/AweGen/AweLighting/AweMesh/AweStrips all in chunkio_library_init). KNOWN/CAVEAT: (a) the .so crashes in the coordinator env (signal 11) but not the subagent env — AC-0206 (research, non-blocking); (b) a one-off non-repro SIGSEGV at the AweGen.generate_flat frame (AC-0188 gen code, hit on first battery runs, passed after) is in AC-0206; (c) the AC-0190 R50 queue-no-stall is PENDING (run when AC-0206 resolves). Standing: godot ONE at a time; HOME=/tmp/dsh_home every godot call; LONG arms → run_in_background + job_output; check `uptime` load; GODOT 4.7.1 gotchas; harness AWECRAFT_RADIUS not in HARNESS_ENVS. GOAL ACTIVE: AC-0203 + the whole C++ series (AC-0165/0188/0189/0190/0207/0210) done + shipped this round; next = AC-0211.


**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout). -->