# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6.

## 00z. CHECKPOINT 2026-09-03 (23:55) - **AC-0229 dynamic-mesh-per-frame-scaling DONE + SHIPPED (stream_ho_cap = base x speed_factor x radius_factor, clamped 1-100, p95 stable, slider still bounds the burst)** [GDScript, no C++ touched: stream_ho_cap = clampi(roundi(base x speed_factor x radius_factor), 1, 100) where base = AC-0225 slider (1-100, default 3); speed_factor = 0.5 still (<0.5 m/s), 1.0 at the 4.3 m/s walk ref (s/4.3), 12.0 saturated - sampled from actual player position, 0.3 lerp; radius_factor = R/16 clamped [1.0, 2.0]. main.gd - harness reporting (ho_dyn/ho_cap/ho_cap_max) + an env-gated AWECRAFT_WALKLOG diagnostic] — Reference points (base 3): R16 still->2, walk->3 (AC-0225 no-op), 50x fly->36; R50 still->3 (0.5x2.0 = 1.0 no-op - AC-0227 profile untouched), walk->6, 50x fly->72. R16 walking (gate): ok:true, 797/797, build 13467 ms, p95 7/7 = baseline; fly4 279/8 s = 34.9 chunks/s, fly50 58/2 s = 29.0/s - byte-identical fill to baseline, 0 lost, recenter 9/27; ho_cap_max 3->8 (4x) / 3->36 (50x) - the dynamic budget tracks actual speed, ahead fills at the same rate with 2.7-12x headroom. R50 (gate): 7845/7845, drain 354.49 s = 22.1 chunks/s (baseline 21.4 - same profile), p95 59 = baseline, ho_cap 3; the targeted 50x-flight-at-R50 scenario gets cap 72 -> 4320 meshes/s vs ~430 chunks/s ahead demand (10x headroom vs the fixed cap 0.42x shortfall). Slider: TM_HO 1/3/100 -> still caps 1/2/50, fly caps up to 3-12/8-36/100-100 (monotonic, clamped at slider max) - slider still bounds the burst. p95 stable: R16 7/7 before->after, R50 59 before->after. G0: exit 0, 0 SCRIPT ERROR. SMOKE: all 4 modes ok:true (light 15/0/14 standing, sea stable). No regression: chunkiocpp byte-identical (md5 8a893557900a462fe4ab1c91f8589081); genhash 2x 25/25 == AC-0215 NEW baseline; light 15/0/14; mesh/strips/pull probes 100pct exact (234224 verts / 512 strips / 6,291,456 cells max_diff 0); nofallback ok:true (C++ lanes >0, GD sentinels 0). boundary r4 (insurance): ok:false (13/500 s) - A/B proves it environmental: original code identical; WLOG telemetry shows the walk sticks at (208.9, 161.0, 8.3) - a collision in chunk (13,8) that the fast C++ pipeline builds in place before the player arrives (pre-existing harness-vs-pipeline interaction). The dynamic 11-burst at the 16 m/s walk leaves p95 20 ms / max 42 ms - below every recorded standing - no slope re-tune needed. R50 RAN CLEAN IN THE SUBAGENT ENV (the .so loads clean there, only the coordinator env crashes - AC-0206). Committed (godot/world/world.gd dynamic scaling + godot/scenes/main.gd harness reporting + AWECRAFT_WALKLOG diagnostic + tasks/AC-0229/*).
**RESUME (new chat):** (1) read this 00z; (2) **THE GAME IS 100% C++ FOR THE HOT PATH + engine cull default + drain capped at 3 mesh/frame (tunable via Options slider 1-100 + dynamic scaling with speed and render_radius) + the forward-move 5fps tank is GONE + the terrain is MC 1.18 style + the queue priority is in + offscreen interior bands skip the 150-pt density eval free + the border compare is in + the bouncy banana is in + the Options slider is in + the fog is pulled in (0.95 -> 0.875) + the dithered fade is in (screen-space 4x4 Bayer checkerboard) + the dynamic scaling is in (stream_ho_cap = base x speed_factor x radius_factor, clamped 1-100)** — AC-0165 (foundation) + AC-0188 (gen) + AC-0189 (lighting) + AC-0190 (mesh) + AC-0207 (strips) + AC-0210 (pull) + AC-0211 (mesh surroundings) + AC-0212 (frustum gate) + AC-0208 (fallback removal) + AC-0219 (streaming cap) + AC-0213 (forward-move tank fix) + AC-0215 (MC 1.18 terrain) + AC-0222 (queue priority) + AC-0224 (streaming cap tuning 1->3) + AC-0216 (lazy skip interior bands) + AC-0218 (border compare) + AC-0040 (bouncy banana) + AC-0225 (Options slider) + AC-0226 (fog pull-in) + AC-0227 (dithered fade) + AC-0229 (dynamic scaling) are all DONE + SHIPPED. NEXT task = **AC-0217 — debounce-collect-pool-score-only-on-move-or-turn** (read its notes in tasks/TASKS.yaml). The C++ pipeline is mature (gdext/SConstruct + .gdextension + godot-cpp master 05057de api 4.7 + MinGW; build_windows.sh auto-builds GDExt + stages the .dll; -ffp-contract=off; build godot-cpp from its own dir; a new .gdextension needs one `godot --headless --import`; registered classes: ChunkIOPalette/AweGen/AweLighting/AweMesh/AweStrips all in chunkio_library_init; the ONLY remaining toggle is AWECRAFT_FRUSTUM; the game REQUIRES the C++ extension (no fallbacks); the drain is capped at 3 streaming mesh per frame (AC-0224, tunable via Options slider 1-100, AC-0225, + dynamic scaling with speed and render_radius, AC-0229); the box-light kernel is now C++-cached (473->13 ms); the terrain is MC 1.18 style (one density field); the queue priority is in (newest ahead entries to the front, build depth capped); offscreen interior bands skip the 150-pt density eval free (AC-0216); the border compare is in (only mark light_dirty when the 2-deep border light actually differs, AC-0218); the bouncy banana is in (banana trees at the shore, RigidBody3D bounce, pickup + eat, AC-0040); the Options slider is in (Chunk meshes per frame 1-100, persists cross-process, AC-0225); the fog is pulled in (0.95 -> 0.875, hides far-edge pop at R16+R50, scales with render_radius, AC-0226); the dithered fade is in (screen-space 4x4 Bayer checkerboard, complements the fog, AC-0227); the dynamic scaling is in (stream_ho_cap = base x speed_factor x radius_factor, clamped 1-100, p95 stable, AC-0229)). KNOWN/CAVEAT: (a) the .so crashes in the coordinator env (signal 11) but not the subagent env - AC-0206 (research, non-blocking); the R50 CAN run in the subagent env (the .so loads clean there); (b) a one-off non-repro SIGSEGV at the AweGen.generate_flat frame (AC-0188 gen code, hit on first battery runs, passed after) is in AC-0206; (c) the AC-0190 R50 queue-no-stall is PENDING (run when AC-0206 resolves, or delegate to a subagent env); (d) the ~21 ms p95 residual = the 500 ms light refresh itself (AC-0213); (e) the genhash is now the AC-0215 NEW baseline ((0,0)=80b904c0...) - NOT the AC-0188 baseline (the terrain changed). Standing: godot ONE at a time (ps check + .scratch/.godot_busy); HOME=/tmp/dsh_home every godot call; LONG arms -> run_in_background + job_output; check `uptime` load; GODOT 4.7.1 gotchas; harness AWECRAFT_RADIUS not in HARNESS_ENVS; NEVER put single quotes in TASKS.yaml closeout comments (the YAML string is single-quoted - use 100pct not 100pct, avoid apostrophes). GOAL ACTIVE: AC-0203 + the whole C++ series + AC-0212/0208/0219/0213/0215/0222/0224/0216/0218/0040/0225/0226/0227/0229 done + shipped this round; next = AC-0217.

## 00y. CHECKPOINT 2026-09-03 (12:50) - **AC-0210 wire-aweLighting-flood-flat-in-pull DONE + SHIPPED (wiring-only — the C++ pull is now live)** [call AweLighting.compute_chunk_pull (AC-0189) from the pull entry in godot/world/lighting.gd (compute_light_flat_chunk_pull + _flood_flat x2), the GDScript kernel preserved verbatim as _pull_kernel_gd, toggled by AWECRAFT_LIGHTCPP=0; no C++ change, no rebuild — the AC-0207-built .so/.dll already carried it] — Flood p95 53.1→0.78 ms (68×), 100% cell-exact across 64 runtime call variants (6.29M cells) via the new pullprobe arm. No regression: chunkiocpp byte-identical, genhash 25/25 == AC-0188, meshprobe/stripsprobe 100%, G0 clean, both toggle states battery-green. **The pull kernel (the 20ms border light hitch) is now C++** — the incremental light update on chunk-border crossing is native. Committed (godot/world/lighting.gd pull wire + main.gd pullprobe + tasks/AC-0210/*).
**RESUME (new chat):** (1) read this 00z; (2) **THE ENTIRE AweCraft hot path is now in C++ AND the pull kernel is wired** — AC-0165 (foundation) + AC-0188 (gen) + AC-0189 (lighting) + AC-0190 (mesh) + AC-0207 (strips) + AC-0210 (pull wiring) are all DONE + SHIPPED. NEXT task = **AC-0211 — mesh-snap-and-instance-remaining-gdscript-to-cpp** (a new user task — read its notes in tasks/TASKS.yaml; likely the last remaining GDScript hot spots: the mesh snap + the instance path). The C++ pipeline is mature (gdext/SConstruct + .gdextension + godot-cpp master 05057de api 4.7 + MinGW; build_windows.sh auto-builds GDExt + stages the .dll; -ffp-contract=off; build godot-cpp from its own dir; a new .gdextension needs one `godot --headless --import`; registered classes: ChunkIOPalette/AweGen/AweLighting/AweMesh/AweStrips all in chunkio_library_init). KNOWN/CAVEAT: (a) the .so crashes in the coordinator env (signal 11) but not the subagent env — AC-0206 (research, non-blocking); (b) a one-off non-repro SIGSEGV at the AweGen.generate_flat frame (AC-0188 gen code, hit on first battery runs, passed after) is in AC-0206; (c) the AC-0190 R50 queue-no-stall is PENDING (run when AC-0206 resolves). Standing: godot ONE at a time; HOME=/tmp/dsh_home every godot call; LONG arms → run_in_background + job_output; check `uptime` load; GODOT 4.7.1 gotchas; harness AWECRAFT_RADIUS not in HARNESS_ENVS. GOAL ACTIVE: AC-0203 + the whole C++ series (AC-0165/0188/0189/0190/0207/0210) done + shipped this round; next = AC-0211.


**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout). -->