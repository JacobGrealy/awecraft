# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6.

## 00E. CHECKPOINT 2026-09-08 (16:00) - **AC-0235 RETEST 5+6 + AC-0243 LANDED (1d901ee) + PUSHED + EXE REBUILT (15:56, :8080 byte-matched). USER AWAY: working the queue autonomously per standing instruction (note user-weigh-in items on tasks, move on).** (1) AC-0243 COMPLETE: double-tap jump action (Space/A) within 0.3 s toggles fly (2nd tap suppressed), A climbs / B (pad_cancel, kb Shift) descends / hold-both holds altitude; RT hysteresis mine (press >0.5, release <0.35) fixes the real-pad single-punch (trigger jitter 0.45-0.55 no longer drops the hold) + idempotent start_mine; AWECRAFT_PADTRACE=1 env-gated per-event pad trace KEPT in player.gd (debug tooling for the user real-pad retest). Gamepad arm DETERMINISTICALLY GREEN (godot/.scratch/gamepad_trace8.log ok:true) after fixing pre-existing-at-HEAD flakes: parse_input_event events flush on a LATER frame under slow xvfb frames -> arm asserts poll STATE (_await_state); one event object re-parsed in the same frame gap reads CURRENT values -> fresh object per parse (_pad_axis; also fixed the LT place check); 900+ ms flush stalls blew the 300 ms double-tap window -> arm re-pushes fresh press/release pairs every 60 ms wall clock until latch; fly test safe return (teleport + velocity reset + fall_start = -1.0 - fall tracking SURVIVES teleports, the +50 m return was killing the player). (2) AC-0235 retest 5: cloud noise 5-octave fbm2 + 3-frequency blend, 3-layer deck (256/160/110 block cells, varying height/coverage); star field per-star UV2 data (size/hue/brightness, dim-heavy, 4 hue classes, soft dot + tight point + cross sparkle). Retest 6: graded slow winds (0.005/0.012/0.025 blocks/s, top essentially static); star floor 0.45-1.0 display space; **star sRGB round-trip** (pow 0.4545 lift BEFORE the AC-0242 pow 2.2 pre-decode so Forward+ round-trips to the designed brightness). (3) **STAR INVISIBILITY MYSTERY (root-caused, ~20 xvfb runs)**: the r=320 star shell never drew on the llvmpipe harness. Proven facts: the exact mesh/shader renders in a standalone SceneTree probe (fog/tonemap/glow/sky/clouds all isolated clean); in the MAIN scene the star field only draws when AWECRAFT_NO_WORLD_VIS=1 (chunks hidden) - the chunk meshes (GDExtension, R=4) cull the star node on lavapipe; the user machine renders it normally with the world present (their retest 5 screenshots show the stars) so it is a renderer-specific culling quirk, NOT a game bug. Code fixes that ARE strictly-correct and shipped: 4.7 ArrayMesh.add_surface_from_arrays does NOT compute the mesh AABB (stays zero) -> explicit set_custom_aabb around the shell radius; star shader carries the cloud layer's proven empty fog_fragment() exemption (fog itself was ruled out: NO_FOG + fog_fragment + fog-disabled all byte-identical). HARNESS HOOKS (kept, env-gated debug): AWECRAFT_STAR_R=<f> (field radius), AWECRAFT_NO_WORLD_VIS=1 (hide chunks for star verification). Verified star evidence: godot/.scratch/night_final.png (production r=320, varied field) + star_noworld32.png (r=32 close-up: brightness/hue/size/shape all visible). DEBUG LESSONS (do NOT re-chase): 4.7 mesh API = surface_get_arrays(i)/surface_get_vertex_count; NO update_mesh_aabb (set_custom_aabb instead); no VERTEX_COLOR builtin in spatial shaders; ShaderMaterial.transparency property access = Invalid (do not probe it); a script error mid-_create_game_nodes leaves the scene half-built (the snapshot still saves a star-less frame - always grep the log for SCRIPT ERROR, not just SHADER ERROR); probe quads pitched-down with the camera go UNDER the terrain (aim probe offsets ABOVE the horizon: view axis is ~12 deg down at spawn); the stone wall right of spawn is NOT a world child (survives NO_WORLD_VIS); .local/share app_userdata is sandbox-READONLY (shader_cache undeletable from bash). GATES GREEN: SMOKE battery ok:true (settings;player;interact;light;fluids;buckets;genhash), 0 Parse Errors, 0 SHADER ERROR. EXE rebuilt 15:56 (build_windows.sh ~4 min this time; :8080 serves 200 byte-matched). NEXT (queue, re-read TASKS.yaml - the coordinator re-orders): AC-0175, then AC-0111/AC-0140... Standing rules unchanged (godot one at a time; HOME=/tmp/dsh_home for headless/xvfb ONLY - NEVER build_windows.sh; fresh /tmp per bash call; TASKS.yaml = YAML round-trip only, no apostrophes; push with GIT_SSH_COMMAND; exe at :8080/:8082 serves the live exports dir; any shader work = xvfb render + SHADER ERROR grep). USER RETEST ON RETURN: retest 5/6 clouds+stars look; double-tap A fly / B descend / RT hold-to-mine on the physical controller (if anything looks off: AWECRAFT_PADTRACE=1). **DO NOT COMMIT:** user scaffolds tasks/AC-0092|0140|0141|0142|0150|0154|0159/ + references/ + docs/halo-loot-brainstorm.html.

## 00D. CHECKPOINT 2026-09-07 (22:00) - **AC-0235 FOLLO-UP LANDED (commit this span) + PUSHED PENDING - the sky now actually renders. USER RETEST ROUNDS (both landed): (1) clouds too close + too fast + still no sun; (2) look: fewer but BIGGER/volumetric clouds, DARK at night. Changes: CLOUD_H 160->400 (layer ~260 above highest terrain), cloud quad 4096->6144 (reaches the 4096 far plane down to ~5 deg elevation), wind 6.0->0.7 blocks/s (drift: 60 s cloud-time offset now changes 13.7% of sky-band pixels vs 48%+ old), cloud shader re-tuned (base scale /64 not /26, 2nd octave 2.2, coverage window 0.74/0.44, soft-edge opaque-core alpha ramp), night cloud tint Color8(46,50,66).lerp(CLOUD, DayNight.day(t)) in main.gd. **THE REAL NO-SUN BUG (root-caused, fix = 1 line)**: with env fog ENABLED (always on, terrain depth fade), the engine fog path REPLACES the custom-sky background with the flat fog color - the sky pass output never reached the screen at all (shader-default changes, runtime uniform pushes, AND process_mode all LOOKED frozen because the fog color painted over everything). The cloud quad was immune (own fog_disabled render_mode) - that asymmetry was the clue. FIX: `render_mode disable_fog;` on aero_sky_gradient.gdshader (terrain fog untouched; only the sky pass is exempt). Also fixed: sky material u_srgb_pre was pushed BEFORE _srgb_pre was computed (always 0.0 under Forward+) - compute now precedes push in _ready. VERIFIED (xvfb Forward+/lavapipe, fog ON, R2, seed 44, frozen): sun-aimed t=0.35 = sun disc with white-hot core (255,255,243) + halo on the pushed day gradient; noon t=0.5 = day palette + sparse big soft clouds; night t=0.0 = near-black + stars (pixel-matched to the old dome-era night frames 12-13,17 range = no regression); drift pair above. SMOKE battery + settings arm + genhash + syntax all green; Windows exe rebuilt (8082 live). DEBUG LESSONS (do NOT re-chase): Sky has its OWN ProcessMode enum (Automatic/Quality/Incremental/Realtime - `Sky.PROCESS_MODE_REALTIME`) but process_mode was a RED HERRING (fog was the whole bug); a GDScript parser RECOVERY after a type error reports a misleading SECONDARY "Expected statement, found X" line further down (read ALL parse errors, not just the first); `res://` cannot load hidden dirs (`.scratch` fails) - scratch scenes go in a real dir (used godot/skytest/ temporarily); a bare-scene probe (WorldEnvironment+procedural sky+camera in a lone .tscn) is the fast way to prove Sky-pipeline vs project-scene; a sky with fog ON renders flat = the fog color, check that before shader math. HARNESS NEW: AWECRAFT_CLOUD_T0=<float> seeds the cloud drift clock (deterministic drift A/B); AWECRAFT_NO_FOG=1 (fog kill switch - keep). NEXT = **AC-0243** (user-filed: double-tap A (jump) within ~0.3 s toggles flying on the CONTROLLER, 2nd tap suppressed; B (pad_cancel btn 1) lowers while flying; keyboard parity exists: F toggle, jump=up, Shift=down, flight_speed setting, `var flying` in player.gd; gate = extend AWECRAFT_LOGIC=gamepad arm with synthetic double-tap + hold-A rise + hold-B fall + SMOKE). USER MODE: back at the machine, actively play-testing + giving pixel-level visual direction (sky/clouds) - respond fast, show renders, be honest about verified vs user-visible; after the sky retest lands they want to "work with you on some stuff" (expect interactive direction). Standing: godot ONE at a time; HOME=/tmp/dsh_home for headless/xvfb godot but NEVER for build_windows.sh; fresh /tmp per bash call (curl downloads vanish between calls - fetch+process in ONE call); no apostrophes in TASKS.yaml comments (safe_load + comment-id check after every edit - concurrent coordinator session re-orders the queue); push with GIT_SSH_COMMAND workaround; exe at :8082 serves the live exports dir; **for ANY shader work: render a real frame under xvfb + grep the log for SHADER ERROR - headless NEVER compiles shaders.**

**RESUME (new chat):** (1) read this 00C; (2) NEXT = **AC-0238 (window-masked-light)** top of queue - the light pass (C++ AweLighting) must honor the same per-column keep mask as gen/build (full notes + gates in tasks/TASKS.yaml; the .so rebuild recipe is in this file's footer section, stage to godot/bin/ after any C++ change). THEN **AC-0239 (simulation-distance-streaming-tier)** - filed 2026-09-07: a sim-radius tier (Chebyshev <= the existing Settings.sim_dist Options slider, default 4) ahead of the FOV cone in _tier_of (currently 0 under / 1 ring1 / 2 cone / 3 rest); the slider is live-wired via Settings.apply_sim_distance (today: fluid_tick_radius + band0_r only); gate audit: the AC-0231 order gate fires on tier >= 2 = the SAME set after renumber (verify). ALSO LANDED 2026-09-07 (committed in the fog-wave batch): **AC-0240 (fog-wave-disable)** - FOG_WAVE_ON := false: the sky-blue fog boxes are GONE (restorable via the const, zero cost off - no fog MultiMesh nodes), caps unchanged (fog color). CRITICAL LESSON: the fog set was the ENTRY TICKET into the low lane's pending set (_low_pending_sis/_low_pending_si walk the fog/cap/low placeholder sets) - with fog off a VIRTUAL pending set (kept data slab, no placeholder/low/terminal) was added to both picks or the low lane starves (first R16: low=0, culls 0, build_ms 30s - the order gate never closed; the re-pointed inr_uncovered gate [kept-slab gap allowed] masked it). R16 final all green (culls 642, fog 0, build_ms 77339). KNOWN FOLLOW-UP (user-flagged): the low lane shares the TM high-priority pool with high builds - a dedicated low thread (AC-0187 precedent) would fix pool-contention starvation; urgency rose with fog off (the low is now the first visible representation). LANDED 2026-09-07 (committed, PENDING USER VISUAL VERIFY on Windows): **AC-0241 (forward-plus-migration-drop-dither)** - renderer flipped to forward_plus (gl_compatibility was web-era legacy; Windows-only per AC-0124; Bedrock reference = big distance + real LOD + modern API, PC range 4-96 chunks); DITHERED FADE DROPPED entirely (5 shaders: u_dither_start/end + DITHER44 + discard block; main.gd per-frame push/settings arm/R16 evidence/env hooks; daynight dither_start/end; settings keys dithering_enabled/dithering_start_pct; menu checkbox+slider; the dithersettings arm became the fogsettings arm). The FOG fade (fog_start_pct, per-frame env.fog_depth_end) is the only distance fade now. Gates: syntax, SMOKE, settings arm (fog round-trip), R16 all green (fog block: fog_far 236.64 <= 256 pop face, fog_hides_pop true, culls 643, build_ms 78836). NOTE: headless cannot SEE Forward+ - the user's Windows build is the render oracle; the xvfb render arm may be dead here (Forward+ = Vulkan on Linux, llvmpipe = OpenGL). NEXT after user verify: three-distance streaming (sim / mesh / satellite zones - the satellite far-LOD, GPU-native on this stack) as the next ticket; AC-0238 (window-masked-light) + AC-0239 (sim tier) queued. UNDER DISCUSSION with the user (NOT yet a ticket): SATELLITE FAR LOD - a per-chunk 16x16 top grid (C++ slabs_top + one palette read = top y + top block TYPE per 1x1 block column, LIVE edit-aware, re-baked on the existing 3x3 neighbor-sync edit hook) rendered as an unshaded 17x17 vertex-color heightfield at TRUE elevation (no stretch; MC relief shading additive; top-surface only, open underneath) - it takes the LOW's slot in the ladder (fog -> satellite -> high) with the low path left dormant behind a revert const; it is TERMINAL for far chunks (lazy heightmap-only gen = end state; no high build/light owed) - the CPU win is skipping full gen + high mesh + light for ~97% of the R50 circle, the bake itself is only ~5-25x cheaper than the per-slab low; the PLANET VIEW is the same top-grid cache max-pooled to coarser zooms (MC map scale 1/2/4), same unlit material. Design state: user confirmed the shape 2026-09-07; trigger (distance vs speed) + detail radius + ticket not yet decided.
**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout), 00z moved 2026-09-07 (AC-0231-followup closeout). -->