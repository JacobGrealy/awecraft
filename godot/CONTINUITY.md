# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6. 

## 00Z. CHECKPOINT 2026-09-14 (r6) - **AC-0263 4-RULE QUEUE SPEC COMPLETE + SHIPPED** (commit 7f1888d, build 20260914-0640, :8080 sha `2813cb7e67e3a8be…` byte-matched, harness.gd pristine vs HEAD) — joint session: the user's 4 BINDING queue rules, framed as spec restoration + bug fixes, now implemented AND measured end to end. (1) every chunk crossing recomputes the ENTIRE queue (walk phases WANT→STUB→MERGE_OLD carry→MERGE_WANT→MERGE_RING + finalize swaps buckets, rebuilds queued_keys flag⇔entry); (2) highest-priority (tier-0) chunks build from the player's Y; (3) no chunk shows without calculated lighting (per-chunk `light_settled` + `flush_slabs` + settle-aware flush re-arm + `hide_all_high()` pre-flush, chunk.gd resets on set_local/data_landed/slabs_landed/clear_data/_pool_reset); (4) passed chunks stop building down until 3.5 s (AHEAD_FAST_MS) quiet.
**RULE-4 MECHANISM (the heart of this session) — the Y-window is PER COLUMN, not global:** `_lod_windowed_for(c)` (world.gd, after `_lod_player_moving`) = moving AND `_ahead_active` AND the column is PASSED (farther from the ahead center than from the player) AND not tier-0 (rule 2 outranks). Windowed probes only see slabs |dy|≤1 of the player slab. Both cached pick probes (`_entry_best_pending_cached`/`_hslab_best_pending_cached`) fingerprint `[data_gen, fl_gen, player_slab, tier, _lod_windowed_for(c)]` — **the probe CALL and the fingerprint MUST use the same predicate (defect below)**. Companion rules: windowed probe -1 is NOT a completion — `_grid_score` returns 1e30 (no candidate) for a windowed -1 entry, and the build/data/loadwin passes HOLD the entry on windowed -1 (only a full-probe -1 frees it). A global moving window (first cut) stranded spawn (the load recenter sets no ahead lead), tier-0 under a flying player, and ahead columns — all now full-probe.
**TWO measurement-caught defects (both fixed, both in the probe-cache class):** (a) cached LOW probe fingerprint used the per-column bit while the probe call used the GLOBAL moving bit → while moving without an ahead lead (snapback) a windowed -1 was cached under the full-probe fingerprint and served forever — the wave never re-picked demote-ring slabs (lightstate: 50 ring holes through the entire settle, `cached=-1 fresh=6` on the hole column); (b) the loading-window build pass fell through to a -1 SLAB DISPATCH on a windowed verdict (no `continue`) → 300 ms/frame spin → 15-min arm hang — a windowed -1 now breaks the pass (the pick scored it lowest ⇒ no window-visible candidate exists).
**GATES (this vs HEAD d99ffee):** boot clean; lightstate R16 `hole_slabs_now 1776` EXACT (= r5 baseline), `hole_traj [249,0,0,0,17]` (base [238,0,0,13,0] — the 17 = re-entry flip jitter class), demoted 48, double_lod 0/0, wash false, settle 2400 cap (baseline behavior), loh 0→4 (2-6 jitter); r16 `ok=true` (edit sub-test FIXED — the old "no built chunk near player" is gone; lost_chunks 0/0; under_gap 0; **far-region census RE-BASELINES BY DESIGN**: far data-only region is unmeshed by contract, and the window no longer pre-builds the PASSED far disc mid-flight — that pre-build was exactly the waste rule 4 removes. This run: far_holes 942 / far_pending 547 / far_visible 83 vs the old build-everything 0 / 0 / 564; `catch_up_ok=false` on the arm is informational, not the pass gate); ladder ok (126 boundary flips, base 128 jitter, boundary_flip_ok true); meshprobe match_rate 1.0 mismatch []; **battery 30 modes: EVERY ok flag and every ok-less RESULT byte-identical to a same-day HEAD-worktree baseline run** (`.scratch/basecol_battery` vs `.scratch/percol_battery`) — pre-existing non-greens reproduced identically on HEAD: wallshot 240 s timeout at "MSG Flying" (same 84-byte stop since Aug 22), genhash NO_RESULT (the arm prints GENHASH lines, no RESULT — expected), trees arm pre-trees-baseline gate fail at (2,2) (stale `.scratch/genhash_before_trees.log`, Aug 18, predates the tree feature's non-tree terrain changes); the 25 GENHASH lines are byte-identical to the AC-0248 gate run → generation unchanged.
**INFRA (this session, BINDING):** `./build_windows.sh` needs the REAL `$HOME` (engine `$HOME/tools/godot/godot` + templates `$HOME/.local`) — run it WITHOUT the /tmp/dsh_home override (headless runtime still wants `HOME=/tmp/dsh_home`); a fresh build started its daemon on :8082 (died on its own) and left the stale :8081 dup serving an old dir (404 on BUILD.txt) — :8080 authoritative, serving the new build from disk ✓. `.scratch/run_battery.sh` godot path is now `${GODOT_BIN:-/home/angrygiant/tools/godot/godot}` (the old `~` broke under HOME override). Baseline worktree `.scratch/.baseline` (d99ffee) still present — usable for A/B battery diffs (this is how the zero-regression proof above was made); remove when convenient.
**RESUME:** 4-rule spec DONE + shipped. In-game acceptance is on the user (they should re-fly the fast path: passed trail stops building down, ahead/tier-0 build from the player's Y, recenter recomputes the whole queue per crossing). Queue still PAUSED (head AC-0093) until the user resumes. Known tolerated (no action): census late-sample blips, stragglers 5-8 (timing-only), loh 2-6 jitter, spawn ground hidden until first flush, stored-high light staleness after low-band flush, far census per the re-baseline above.

## 00Z. CHECKPOINT 2026-09-13 (r5) - **AC-0263 LOD RESTORE COMPLETE + SHIPPED** (commit 51ce580, :8080 sha `bd3c1a1518c5486d…` byte-matched) — joint LOD working session with the user: AC-0273/0274/0275 (band-exit demote-FREE + fog veil) removed; the jointly written AC-0263 spec restored: **every generated tier stays stored per slab, exactly ONE visible, band crossings = atomic visibility flips, no fog**. User's fog-UI-toggle commit 8ec2b9b landed in between (keep).
**KEEP-ALL-LOD DESIGN (shipped — BINDING spec):** terminology user-fixed: slab = 16×16×16 (`slabs[si]`), chunk = full tower — all state is per-slab. Up to 3 meshes stored per slab: HIGH (full-res), MED (8×8×8, taxi [medium_start_r, low_start_r)), LOW (4×4×4, [low_start_r, render)). Flip = `chunk.high_slab_visible(si,false)` + `low_slab_visible(si,true)` in the same handoff (occluder+collision are LOD-independent, stay on). **`mesh_built` stays TRUE on demote** (a complete stored high IS complete) → drain, WAVE-3 catch-up, AC-0278 re-queue, `_enqueue_build` all skip demoted columns; re-entry is the FREE flip (`world._reentry_flip_high`, recenter-walk mirror branch `taxi_old>=ms and taxi_new<ms`); partial highs (mesh_built=false) rebuild via the normal re-queue. The LOW wave owns demoted columns' pending lows — band-aware gates: `_low_scan_slabs`, the wave dispatch guard, and `band_drained()` count a meshed WAVE-band column's pending lows as OWED (only meshed HIGH-band columns owe no low). Stored instances free only at column recycle (`_lod_free_all` free path `drop_low`; the upgrade path HIDES lows + keeps stamps — slabs stay low-complete). PERF LAW (user): generating the mesh is the cost; visibility flips are free; GPU idles — never pool-checkin on a tier transition.
**NO-FOG TRADE-OFF (user: "remove the fog blocks, I don't want them"):** a band jump leaves the not-yet-BUILT slabs of partially-high demoted columns instance-less for the wave's fill window — measured `ring_traj [58,0,0,0,15]` (fills in ~300 frames ≈3 s; the 15 = the snap-back's fresh micro-demote). The fog veil is GONE. The lightstate arm gates on the SETTLED census (band_drained wait, 2400-frame cap — open-world R16 never fully drains, hitting the cap is normal; the ring is 0 by then). `_lod_census` holes now exclude HIGH-COMPLETE slabs (stamped at current data): a faceless emit (buried slab, no exposed faces) is DONE, not a hole. double_lod = co-VISIBLE tiers (co-STORAGE — hidden high behind active low — is the design).
**GATES (all green):** boot clean; lightstate R16 (`AWECRAFT_LS_RADIUS=16` — the arm's default R=4 CANNOT exercise band crossings, the stream set is too small for a medium_start crossing while in-set; the baseline "demoted 48" was always an r16-env run): demoted 48 (= baseline), 6 flips, double_lod 0, settled demote-ring holes 0; r16 ok (demoted 321, far_visible 564, far_holes 0, catch_up_ok, fps steady); ladder ok (128 boundary flips); meshprobe ok (match 1.0); battery ok + GENHASH 80b904c0e5f085eef1e839459058c706 EXACT; build + :8080 sha byte-match.
**USER QUESTION ANSWERED (measured 2026-09-13, the flytrace A/B):** "when I fly fast and stop I see outer rings building TOWARDS me instead of out from my center" — it was the AC-0275 demote churn, now gone. A/B method (both builds, identical 10 s flight at 215 m/s +x, real-game R50, stop at chunk 107,0): OLD (18c26df) post-stop BUILD picks LOCK at taxi 7 for ~5 s (247 picks rebuilding the freed demote ring at the high-band edge — the visible "outer ring building"), data lane stalled behind it, then outward gen resumes (taxi 9→16). NEW (51ce580): picks flow taxi 5→6 immediately (the edge of the already-built disc — the player was inside the ahead disc in flight) expanding OUTWARD, data 7→8 concurrent, 548 keep-high demotes/484 silent flips, zero ring rebuild. Residual (by design, no action): the post-stop disc is centered ~3 chunks AHEAD (the last AC-0277 ahead target); the 3.5 s snap-back recenters on the player but finds nothing pending nearby (already built) — the fill still reads outward-from-the-player. Method notes: temp `flytrace` harness arm (settles 25 high-band cols, `_fly_phase(50, secs, +x)`, 900-frame post-stop) + `AWECRAFT_PICKLOG=1` (PICK b/fb/d lines with wall-ms) + temp PLR player trace every 100 sim frames — all REMOVED after the measurement; a git worktree of 18c26df ran fine with the CURRENT gdext .so (gdext unchanged since) but needs `--editor --quit` first (the .godot cache's extension_list.cfg or the GDExtension classes don't resolve in a fresh worktree).
**DIAG LESSON (LOD session):** the recenter walk is ONE inline `for key in chunks` inside `recenter()` (world.gd ~8186) — it runs on EVERY recenter call (incl. `Debug.teleport`'s direct call); demote/flip branches key off `opcx/opcz` (pre-move center) vs the new center. Harness arms run SMALL radii by default (battery 4, lightstate 4, r16 arm sets 16 + sim 2/medium 4) — LOD band gates only fire at R ≥ ~9.
**RESUME:** LOD session COMPLETE (shipped + the fly-fast question answered with the A/B measurement above). Next session with the user: report the before/after evidence (the user has seen the old behavior in-game and will recognize it); then resume the queue — head = AC-0093 (the queue ARRAY in TASKS.yaml is the work order; still PAUSED until the user resumes).
<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout), 00z moved 2026-09-07 (AC-0231-followup closeout), 00F+00G moved 2026-09-09 (AC-0244/0245/0191 closeout), 00J moved 2026-09-10 (AC-0250 filing), 00K moved 2026-09-10 (AC-0250 closeout), 00L moved 2026-09-10 (AC-0251 closeout), 00M moved 2026-09-10 (AC-0247 closeout + AC-0252 filing), 00N moved 2026-09-10 (AC-0252 closeout), 00O moved 2026-09-10 (AC-0248 closeout + AC-0253/0254 filing), 00Q moved 2026-09-10 (AC-0254 closeout), 00R moved 2026-09-11 (AC-0253 closeout), 00S moved 2026-09-11 (AC-0185 closeout), 00P moved 2026-09-11 (AC-0097 closeout), 00T moved 2026-09-11 (AC-0257 closeout), 00U moved 2026-09-11 (AC-0258 closeout). -->