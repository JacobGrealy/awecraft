# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6.

## 00K. CHECKPOINT 2026-09-09 (late night) - **TWO-PASS CAMERA-SIDE FLUID CULLING LANDED + SHIPPED + USER APPROVED. AC-0245 follow-up 3 (commit 9d8d691), :8080 build AweCraft[20260909-2141 9d8d691].** The user rejected the follow-up-2 cull_back trade-off ("removing the option to see the water surface from underwater is not an option, you just replaced one bug with another"; "not only the top from the bottom but any boundary where the water is against air"; "show/hide outside / inside faces based on camera position"). Design (user-specified): two-pass camera-side culling - every water-air boundary face is drawn by exactly one of two passes, the one whose side the camera is on, so the whole boundary (surface from above AND below, shore/side faces from the air side AND the water side) stays two-sided with no coincident fragments. CRITICAL PROBE FINDING (xvfb seed 44, byte-identical frame series): the engine render_mode cull_back AND cull_front are UNRELIABLE in this build - both rendered extra sides (the inward pass behaved like cull_disabled) - so the section-5-era mottle was the water sheet front+back drawn by two draw calls with a per-pixel depth tie (the water-vs-terrain coincidence theory was WRONG; the mottle region is a flat 126.875 sheet over an undulating floor, depths 1-8). Fix: explicit per-fragment camera-side selection - both passes cull_disabled + derivative face normal (cross(dFdx(world), dFdy(world))) vs direction to camera (u_cam_pos): fluid_anim.gdshader (OUTWARD) keeps the air side, new core/fluid_anim_bf.gdshader (INWARD) keeps the water side. Wiring: two surfaces per animated fluid category (chunk.gd _assemble_slab, same arrays); data.gd fluid_anim_bf_mats (mirrored _fluid_anim_mat + apply_atlas re-point); main.gd u_srgb_pre (startup) + u_cam_pos (per frame); chunk.gd set_underwater u_underwater - all pushed to both material sets. AC-0036 near-cull kept in both passes (in 1-block pools the surface above the head stays hidden by design; deep water 2+ blocks shows it from below). VERIFY: genhash (0,0) 80b904c0... held; meshprobe ok:true verts 321080 UNCHANGED (GD-side doubling does not touch the C++ accs); 0 SCRIPT/SHADER ERROR; xvfb subagent: mottle camera above - mottle + ghosting GONE, even tint, crisp shoreline; underwater at (0,124,88) (8-block-deep water) - surface underside visible, smooth clean blue sheet, scene readable through it; shoreline render clean. GOTCHA (new): an "underwater" render eye BELOW the floor (e.g. y=123 at (-20,88) where the floor top is 124) is INSIDE the solid - the camera sees the landscape through back-face-culled walls (sky/sun/trees at full brightness) and u_underwater reads false; probe water depth first (depthprobe method: world.get_block walk down from Data.SEA) and keep the eye 1.2+ below the surface AND above the floor. The .so is UNCHANGED (GD/shader-only fix) - the fixed .so md5 4c5f3447... still in gdext/bin + godot/bin. DAEMON FLAKE recurred (harmless): the 21:41 build found 8080 busy -> dup (pid 257) died instantly (curl 000); :8080 stays authoritative and BYTE-MATCHES (zip 8de2f557... + exe e367f287... == local); pidfile stale (257). PUSHED: 9d8d691 (code) + e1b46fc (docs closeout), master up to date. USER RETURNED 2026-09-09 (late night) to the LOD work - the satellite far-LOD is now the ACTIVE work item (planned TOGETHER with the user - do NOT design unilaterally; AC-0243 pad retest stays in the queue but is user-gated). SATELLITE FAR-LOD DESIGN STATE (user confirmed the shape 2026-09-07 - full text: archive 00C tail + TASKS AC-0236/0237 notes): per-chunk 16x16 top grid (C++ slabs_top + one palette read = top y + top block TYPE per 1x1 column; LIVE edit-aware; re-baked on the existing 3x3 neighbor-sync edit hook) rendered as an UNSHADED 17x17 vertex-color heightfield at TRUE elevation (MC relief shading additive; top-surface only, open underneath) - it takes the LOW slot in the ladder (fog -> satellite -> high) with the low path left dormant behind a revert const; it is TERMINAL for far chunks (lazy heightmap-only gen = end state; no high build/light owed - the CPU win is skipping full gen + high mesh + light for ~97% of the R50 circle; the bake itself is only ~5-25x cheaper than the per-slab low); the PLANET VIEW is the same top-grid cache max-pooled to coarser zooms (MC map scale 1/2/4), same unlit material; GPU-native (compute pass over the per-chunk top grids, CPU patches edits only) on the AC-0237/0241 forward-plus stack - phase 0 groundwork already landed (the fog-slot placeholder + the compute shaders are available). OPEN FORKS (ask_user_question, decide together): (1) the trigger - distance vs speed; (2) the detail radius - how much full/high detail stays around the player before the satellite zone takes over; (3) ticket boundaries - bake+render first, planet view first, or streaming integration first. NEXT STEP: file the satellite-LOD ticket after the design forks are settled + plan. QUEUE (tasks.py next, re-read): AC-0243 (user pad retest), then AC-0185 (Esc pause), AC-0097. DO NOT COMMIT: user scaffolds (unchanged list).
## 00J. CHECKPOINT 2026-09-09 (evening) - **WATER MOVING-SHADOW BUG FIXED + SHIPPED + USER-CONFIRMED. AC-0245 follow-up 2 (commit ca41d3c) + AC-0249 title-screen build stamp.** The "moving shadows" (camera-motion-dependent blocky mottle on water-covered terrain, from above AND below the water) persisted through the section-4 mesh-split "fix". Probe-confirmed root cause: NO shadow map exists (main.gd sun.shadow_enabled = false - the shadow theory was a red herring); the real cause = cull_disabled (AC-0245 section 3) rendering the water BACK faces exactly on the opaque underwater terrain - a depth-buffer tie flips the winner per block as the camera moves. Probe chain (xvfb seed 44 + subagent image compares): hide-all-fluid removed the mottle (water-related); cull_back probe removed it (cull_disabled is the cause); two-angle compare proved view-dependent (z-fight, not texture). Fix: (a) gdext/src/mesh.cpp emit_fluid skips fluid faces toward solid neighbors (the stab[nb]>0 rule opaque blocks use - the user-hypothesized "next to air" check: water emits faces only toward air/transparent); (b) core/fluid_anim.gdshader cull_disabled -> cull_back. Trade-off: the water surface underside (seen from directly underwater) is no longer drawn (that back face was the z-fight source); the AC-0036 near-cull + blue tint keep the underwater view clean; revisit with per-face cull / depth bias if the user wants it. AC-0249 (user request): the title-screen version label (menu.gd "AweCraft[" + Build.ID + "]") now carries the real stamp - build_windows.sh rewrites core/build_id.gd Build.ID to "<stamp> <git-short-sha>" before export + restores on exit (EXIT trap); shipped builds show AweCraft[20260909-1856 ca41d3c], dev runs show AweCraft[dev]. USER CONFIRMED: "I got the tagged release and it is fixed". Gates: meshprobe ok:true match_rate 1.0 verts 321080; genhash (0,0) 80b904c0... held; battery 15/15 across TWO runs (run 1: 10 modes ok then the pre-existing AC-0246 native flake at dnlight->oceanfill; run 2: remaining 5 modes all ok; 0 SCRIPT/SHADER ERROR); final xvfb render clean; :8080 byte-match (zip 37fa5156... + exe ff216ffd... sha256 == local). DAEMON FLAKE (known, AC-0155): the build found 8080 busy (the long-lived daemon; pidfile stale) and spawned a dup that never held a port - 8080 stays authoritative (live dir serve), no live dup (8082 dead); the pidfile may read stale (253) - a future build may spawn another dup: kill it, 8080 stays. GOTCHA (new): sandbox bash runs in a PRIVATE PID namespace (bwrap --unshare-pid) - host daemons (http.server pids) are invisible and unkillable from sandbox bash; verify daemons via curl, not /proc. PENDING: push the two commits (ca41d3c code + docs closeout). QUEUE (tasks.py next, re-read): AC-0243 (user pad retest) at head, then AC-0185, AC-0097. NEXT AFTER QUEUE ITEM: satellite LOD planning TOGETHER with the user (ask_user_question on the design forks - do NOT design unilaterally). DO NOT COMMIT: user scaffolds (unchanged list).


**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout), 00z moved 2026-09-07 (AC-0231-followup closeout), 00F+00G moved 2026-09-09 (AC-0244/0245/0191 closeout). -->