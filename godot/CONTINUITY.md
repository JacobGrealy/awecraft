# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the queue in `tasks/TASKS.yaml` (`python3 tasks/scripts/tasks.py next`).

<!-- MAINTENANCE (AGENTS.md): compact by BYTES, not lines - when there are more than 2 checkpoints or the top checkpoint exceeds ~6 KB. Keep the newest 2, move the rest to godot/CONTINUITY.archive.md (newest first). Checkpoint ids are MONOTONIC (`## <n>. CHECKPOINT <date> (<rev>)`) and never reused. An OPEN-for-user item must be promoted to a ticket BEFORE the next compaction can drop it. -->

## 15. CHECKPOINT 2026-09-17 (r15) - **AC-0283 + AC-0284 CLOSED; AC-0284b (H-only far data) shipped d9bb3e6; P5 (AC-0297) is next in the queue.** User asked why AC-0283 wasn't done -> marked done (code-complete since P4; the halo-look confirmation + build re-test are tracked as AC-0299/AC-0300, nothing lost). **AC-0284b (subagent, all 13 gates green):** far columns (halo band + offscreen collar) now store NO slabs - just [H u16x256][biome][top] (~198 B on disk; v6 flag bit 1, old saves decode unchanged). Gen builds only the 3 coarse surface fields H depends on + the heights pass: **far 92 us/col (fields 57 + heights 34 + top 1) vs full 1678 (~18x)**. The ticket's literal <0.05 ms clause is 2x OVER - the 57 us surface fields are the bit-exactness floor (H must match the full path or promotion shifts terrain); the RP heights-only line (32 us) is MET (34 us). Recorded in the AC-0284 close note + the final design doc. `AweMesh.h_avg_emit` emits the 4x4 avg from the payload, BYTE-IDENTICAL to the slab emitter (farab arm 984/984); far H bit-exact vs full (h_mismatch 0). **Engine-seed contract (starlight):** a far column is NEVER seeded as all air + a far->full promotion re-seeds the WHOLE column top-down - a real bug the probe found (far columns were air-seeded at the recenter crossing/disk load, leaving a stale all-air seam that mis-carried sky on the promotion re-seed: deep stone kept sky 15). `_far_promo_owed` cap-drop retry (threadgen_enqueue now returns bool) + `_low_relower_owed` demote re-lower. skip arg semantics now 0 full / 1 slab-skip (A/B reference only) / 2 far h-only. Gates: genhash 25/25 byte-identical (coordinator re-ran), battery 22 identical + the exact P4 timing-jitter pattern (0 mesh/gen diffs; wallshot pre-existing hang), starlighttest 0/5.3M, r16/lightstate/ladder/meshprobe rebased+documented, brightslab C1-C6 exact vs 284a, halo a-d (b: 192/192 byte-identical; (a) 26 boundary = the documented deep-corner under-lit-until-promotion class), profgen walk 100% / flight 46.2% (beat 284a's 44.1%) - .scratch/ac0284b/report.md. ARCHITECTURE.md updated in-task (gen.cpp skip 0/1/2, mesh.cpp h_avg_emit, v6 bit 1, Far-data decision, engine-seed contract). **PARALLEL SESSION:** the user-side agent is active (AC-0298 doc reorg + new standing rules; filed AC-0299 open-items / AC-0300 forward_plus A/B / AC-0301 harness-data-single-source / AC-0302 parallel-work-isolation / AC-0303 gate-runner-and-build-ownership); its r12 boot blocker was this coordinator's subagent mid-WIP (transient, resolved). Queue order after me: AC-0297 (P5), AC-0302, AC-0303, AC-0300, AC-0286, AC-0287 - the user-session tickets sit ahead of the epic's 286/287 (do NOT reorder another session's tickets without asking). **NEXT: AC-0297 (P5 star-native dirtyfull)** - convert/kill the last two live classic-pull consumers: (a) the edit-fallback DIRTYFULL (verify redundancy vs the star remesh lane with the star_bake_probe evidence first - delete vs feed), (b) the tex-refresh rebuild (_drain_tex_refresh, c.last_eff -> capture settled star payloads at dispatch). Then AC-0286 (promotion no-flash on the final h-only representation), AC-0287 (save filter - far columns now ~198 B, the filter drops them entirely), close-out design doc. **Windows build for the user: 20260917-0059 on :8082** (http://192.168.0.224:8082/AweCraft-20260917-0059.zip - includes halo + both edit-lighting fixes + H-only far data; note: far columns now save ~198 B and a save made with THIS build can't be loaded by the older 20260916-1256 build).

## 14. CHECKPOINT 2026-09-16 (r14) - **AC-0301 SHIPPED: a doc is no longer a script's data file** (user principle: a .md must not be both an agent-facing reference and a hard requirement for a script to run; code may be scraped because it is a machine-readable artefact versioned with the behaviour it describes - prose may not). `tasks/harness_data.yaml` (NEW, 30.3 KB) is now the single MACHINE source for the two tables in `godot/HARNESS.md`: schema 1, mode_columns, **45 modes** (entry/tests/result_fields/envs/wall/notes), value_columns, **16 standing values**, battery tiers. The doc keeps every word of its prose and wraps section 1 and section 3 in BEGIN/END GENERATED markers (19,573 + 5,492 bytes; doc now 277 lines / 40.3 KB). NEW `tasks/scripts/harness_doc.py --render|--check` - `--check` is the anti-drift gate (exit 1 + the first differing line). `spec_template.py` parses NO markdown now: `_harness_rows`, the doc-reading form of `_known_stable_rows` and `_parse_md_rows` are DELETED; the exception is kept and documented in-file (`_parse_gd_constants` still reads data.gd/generator.gd). `test_tasks.py` 53/53 (+7 checks: doc<->data sync, data schema, battery-mode coverage, and a regression guard against the scraper returning). **MIGRATION PROOF:** every cell round-tripped through YAML with equality asserted, then the doc was re-rendered and diffed against the pre-migration copy - 3 removed / 7 added lines and nothing else (4 marker lines + 3 rows); the 16 standing-value rows are BYTE-IDENTICAL. Three findings the migration surfaced: the **`spin` row had SIX cells instead of seven** (its "what it tests" and "key RESULT fields" text were merged into one cell, so markdown rendered that row shifted AND the old scraper mis-split it) - repaired, the one content change; `chunkio`/`nofallback` each carried a stray leading space before their first pipe (normalized); and two generated-spec fallbacks still carried **pre-AC-0091 numbers** (sea 2730/2730, backed 1406/1406) - replaced with an honest pointer. Deliberate data-driven spec changes: the battery table now includes `settings` (the doc always listed it as a battery mode; the code had a hardcoded 6-mode list) and the value-table header comes from the data. AGENTS.md standing rule 3 now covers GENERATED blocks (edit the YAML, run --render; never the doc). NO GODOT: tooling/docs only, no arm/heavy gate/build. The tree boots again (AC-0284a landed in r13), so r12's "tree does not boot" caveat is historical.
