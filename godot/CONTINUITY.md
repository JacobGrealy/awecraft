# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6.

## 00u. CHECKPOINT 2026-09-02 (02:53) - **AC-0187 priority-remesh-on-block-edit DONE + PUSHED** [edit_front queue + slab-scoped sync remesh] — a block break/place remeshes INSTANTLY (skips the ~7710 queued far chunks at R50), hole lands <50 ms, no drop/flicker. Design: set_block stashes the pre-edit eff cache entry, appends {key,si0,si1} (slab closure of the edited row) to the NEW edit_front priority queue, evicts the eff entry, marks the 3×3 light wave, and dispatches immediately via _mesh_dispatch_edit (defer-on-cap keeps it queued 1 frame when the pool is saturated). Slab-scoped build_accs(...,si0,si1,d_off) allocates only touched slabs + ymask pre-pass + _eff_stored_eq stale-light drop + apply_edit_accs (retain-until-swap, never an empty frame); full path byte-identical when scoped=false. Edit-yield: while edit_inflight_count>0 the far-queue drain/flush/tex/non-edit handoff are skipped (priority pass ungated → no deadlock; handoff was stalling up to 856 ms without it). Also FIXED a pre-existing flush_active stale-true bug (D2) that had silently inflated editperf counters (94-100 frames/303-715 ms → true 9/22/22). Files: world.gd (+595: edit_front queue + _mesh_dispatch_edit + edit-yield + _edit_front_add), chunk.gd (+212: build_accs scoped signature + apply_edit_accs + scoped strips + _s_face_light early-out), main.gd (+161: NEW AWECRAFT_LOGIC=editfront probe). **GATES ALL GREEN** (subagent delivered a FULL report this round — no output-cap death; coordinator re-ran HEAVY + genhash + G0 on the stable tree + trusted the light gates per COORDINATOR.md): G0 0; editfront ok:true (queue 8244, break 32.5 ms / place 37.3 ms, both <50, 0 drops, scoped 2, tm_cap 6 enforced, far stream kept draining queue_after 8242); editperf 9/22/22 = baseline (total_ms 494 vs 452 = edit-yield window); lodswap ok:true (AC-0182 regression holds); SMOKE all baselines; genhash 25/25 IDENTICAL (x2, world data untouched); HEAVY boundary r4 ×2 p95 31/27 (IN band 30-37 — no change), remesh/marker ok, perf p95 24; build 109,817,440 B sha d592a095 8080 byte-match + 5180=200 + 8443 down (8081 dup died on its own).
**RESUME (new chat):** (1) read this 00u + `godot/COORDINATOR.md` + HARNESS §1/§3; (2) `python3 tasks/scripts/tasks.py next` — user co-edits the queue LIVE (re-run at EVERY boundary; NEW tasks keep appearing at the head — AC-0187/0188 were added live this session); as of 02:53 queue head = **AC-0168 → AC-0169 → AC-0156 → AC-0175 → AC-0087 → …** [AC-0188 gdext-gen-fbm-caves is p2 waiting_on AC-0165]; (3) THE JOB = the head task — read its TASKS.yaml notes verbatim (User Story + Technical Details); ONE blocking subagent (xhigh for real work per the active goal; low only if genuinely trivial; describe END RESULT + gates + env + code map; subagent honors .scratch/.godot_busy; subagents MAY read_image PNGs, coordinator may NOT); (4) closeout per COORDINATOR.md: TRUST builder light gates (they are part of the contract) → coordinator HEAVY [boundary r4 ×2 + perf, STABLE tree] + a cheap genhash ×2 determinism re-check + G0 → build [OWN call per BUILD RECIPE symlinks FIRST] + 8080 byte-match + 5180=200 + 8443 note [down — do NOT start] + kill any 8081 dup → HARNESS §3 only if standing values moved → TASKS.yaml done + queue removal + closing comment → CONTINUITY → commit EXPLICIT PATHS [task folder + godot files + TASKS/HARNESS/CONTINUITY/archive; NEVER other users’ scaffolds or docs/halo-loot-brainstorm.html] + `GIT_SSH_COMMAND="ssh -F /dev/null" git push` → report localhost + LAN 192.168.0.224:8080/5180 → next. Standing: godot ONE at a time; HOME=/tmp/dsh_home every godot call (fresh /tmp per bash call); LONG godot arms → run_in_background + job_output (bash 60 s default kills a foreground long arm); a subagent’s LONG report can hit the LLM output cap (AC-0182 died this way — land code+gates+results on disk FIRST, then a ≤20-line report); xvfb render can hang at GL init (timeout -k 15 480, retry once, record; headless gates carry). GOAL ACTIVE: "complete all open tasks" — this round did AC-0182 + AC-0187 (2 tasks); next = AC-0168.

## 00t. CHECKPOINT 2026-09-01 (20:34) - **AC-0182 LOD transition DONE + PUSHED** [retain + hysteresis + cache both at the 12/13 line] — built on AC-0181 (0-12 full, 13+ coarse). Dead-band hysteresis (LOD_HYS_FULL_MAX 11 / LOD_HYS_COARSE_MIN 14: full->coarse commits @taxi>=14, coarse->full @taxi<=11, 12/13 keeps current — no per-frame flip), retain-until-ready (old mesh NOT killed; c.lod_pending re-enters the light_pending flush; worker builds the new LOD; handoff capture->apply_accs = one atomic main-thread swap), dual-LOD alt-cache for taxi 11-14 (per-slab alt_slabs [mesh/fluid/flora] + alt_data/alt_fl/alt_atlas; swap_to_cached = 0 ms re-crossing; staleness guarded by data/fl equality + atlas identity). BANDBREAK rerouted _enqueue_build->_tm_retrigger (no stranded lod_pending). Files: world.gd (consts + _rec_want_step dead-band + _reband + threadmesh_handoff capture/store + _enter_candidate clear), chunk.gd (alt-cache vars + capture/store/valid/swap/clear + drop_slab_bodies), main.gd (NEW AWECRAFT_LOGIC=lodswap probe + _lodband_test dead-band update + lodring cam preset). **SUBAGENT RUN HIT THE LLM OUTPUT CAP** before its final report (INFRA FINDING v2) — work landed on disk; coordinator re-ran ALL gates on the stable tree. **GATES ALL GREEN:** G0 0; lodswap ok:true (no_hole/hole_frames 0, retain_window, cross1 atomic 1-build, flicker 0 band/fid changes over 120 jitter frames, cross2 instant_swaps 2 lag 0 + cross3 instant 1, lod_builds 1 total, swap 0.08-0.12 ms); lodband ok:true @R50 (ahead11 full/ahead14 coarse every step, full_ok 265, transition 100, spawn_monotonic); SMOKE all baselines (player 135.43/136.78, interact place_ok, light 15/10/14, fluids 2756/914 + 25/9); genhash 25/25 IDENTICAL; bandmap {b0 41,b1 272,b2 7532,b3 408}=[8253] + [12,0] full/[13,0] coarse; HEAVY boundary r4 x2 p95 35/32 (IN band 30-37 — no change), perf p95 25; build 109,796,112 B sha aebb3128 8080 byte-match (8081 dup died on its own) + 5180=200 + 8443 down. RAM note: ring chunks hold a 2nd LOD (~+208 meshes @r50, AC-0184 anticipated). Standing rule (00s): read_image ONLY in subagents.
**RESUME (new chat):** (1) read this 00t + `godot/COORDINATOR.md` + HARNESS §1/§3; (2) `python3 tasks/scripts/tasks.py next` — user co-edits the queue LIVE (re-run at EVERY boundary); as of 20:34 queue head = **AC-0187 (NEW, user just added) → AC-0168 → AC-0169 → AC-0156 → AC-0175 → AC-0087 → …**; (3) THE JOB = the head task — read its TASKS.yaml notes verbatim (User Story + Technical Details); ONE blocking subagent (xhigh for real work per the active goal; low only if genuinely trivial; describe END RESULT + gates + env; subagent honors .scratch/.godot_busy; subagents MAY read_image PNGs, coordinator may NOT); (4) closeout per COORDINATOR.md: builder light gates (trusted) → heavy [boundary r4 x2 + perf, STABLE tree] + build [OWN call per BUILD RECIPE symlinks FIRST] + 8080 byte-match + 5180=200 + 8443 note [down — do NOT start] → HARNESS §3 only if standing values moved → TASKS.yaml done + queue removal → CONTINUITY → commit EXPLICIT PATHS [task folder + godot files + TASKS/HARNESS/CONTINUITY/archive; NEVER other users’ scaffolds or docs/halo-loot-brainstorm.html] + `GIT_SSH_COMMAND="ssh -F /dev/null" git push` → report localhost + LAN 192.168.0.224:8080/5180 → next. Standing: godot ONE at a time; HOME=/tmp/dsh_home every godot call (fresh /tmp per bash call); battery env = AWECRAFT_BATTERY=…; logs .scratch/<task>-gates/; LONG godot arms → run_in_background + job_output (the bash 60 s default kills a foreground long arm — learned this round); xvfb render can hang at GL init (timeout -k 15 480, retry once, record; headless gates carry). GOAL ACTIVE: "complete all open tasks" — AC-0182 done this round; next = AC-0187.

**RESUME (new chat - do these in order):** (1) read this 00p block + `tasks/HARNESS.md` §3 [standing values incl. NEW boundary p95 ≈73–89 + SAVE_VERSION 3 + chunkio row]; (2) `python3 tasks/scripts/tasks.py next` [user co-edits TASKS.yaml LIVE — re-run at EVERY boundary; as of 14:05 queue = **AC-0158 → AC-0159 → AC-0154 → AC-0109 → AC-0150 → AC-0137 → AC-0092 → AC-0144/0145/0146 [planet] → AC-0111 → AC-0036 → AC-0147 → AC-0161 → AC-0148 → AC-0149 → AC-0140/0141/0142 → AC-0121 → AC-0122 → AC-0097 → AC-0112 → AC-0113 → AC-0093 → AC-0125 → AC-0094 → AC-0098 → AC-0101 → AC-0037..0040 → AC-0087..0090 → AC-0115 → AC-0130**]; (3) THE JOB = **AC-0158 20hz-diamond-game-tick** (Bedrock Realms 20 Hz tick on Simulate 4 diamond 41 [taxi ≤4, 41 not 81] not 5 Hz square: 1 randomTick per 16³ per tick (~1/68 s per wheat), fluid 20 Hz, redstone 1-tick dust, mob spawn 24–44 circle + n−1 diamond; efficiency + correctness, half columns, far 13–50 never tick; spec.html present in tasks/AC-0158/); delegate as ONE blocking xhigh subagent [v3 — parent idle while child runs; describe the END RESULT + gates + env, the child plans its own implementation], then coordinator re-verifies ALL gates; (4) per-task closeout: verify gates → `./build_windows.sh` [symlinks FIRST per BUILD RECIPE] + 8080 byte-match + 5180=200 + 8443 note [down — do NOT start] → results.html → HARNESS.md [only if standing values moved] → TASKS.yaml done [proper entry, queue removal] → CONTINUITY 00p → commit EXPLICIT PATHS [NEVER the other users' scaffolds] + `GIT_SSH_COMMAND="ssh -F /dev/null" git push` → tasks.py next → report localhost + LAN 192.168.0.224. Standing: godot ONE at a time; `HOME=/tmp/dsh_home` on every godot call; **battery env = `AWECRAFT_BATTERY=…` (a `;`-list in AWECRAFT_LOGIC hits the unknown-logic path and ABORTs the process — learned 2026-08-31)**; logs → `.scratch/<task>-gates/`; /tmp EPHEMERAL per bash call; report ≤20 lines.
**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout). -->