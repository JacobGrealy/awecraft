# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6. 

## 00Z. CHECKPOINT 2026-09-13 (overnight r2) - **AC-0278 + AC-0277 COMPLETE + SHIPPED (AweCraft[20260913-0514], :8080 zip sha a49d2185... byte-matched). Queue head = AC-0269 (respawn-controller-auto-select).** Autonomous round 2 of 12 (user asleep). AC-0278 (remove-candidate-flag): the c.candidate boolean is GONE - candidacy = a distance fact at the single recenter walk (in-set/out vs old center opcx/opcz); the stuck-mesh root cause (the user's "candidate out of sync kept mesh from generating even at tier 0") = a build entry stripped on candidacy exit was only re-queued in the WANT walk, which DEBOUNCED recenters skip - fix: the walk (runs on EVERY recenter) re-queues a high-band chunk with data + no mesh + no build entry (idempotent; drain's high_only gate keeps it harmless); cand_since r+2 hysteresis + hide-not-kill unchanged; new arm AWECRAFT_LOGIC=candflag (T = stranded high-band chunk flies -17 to candidate, U = built chunk taxi 21 retained/freed/regenerated, 15 return moves, T re-enters high band at move 14 with queued=build, meshes from the standing drain). The old edgeretain arm is STALE (fails identically at HEAD - pre-band-3 model) - not a valid gate. AC-0277 (predictive-recenter-ahead-target): the AC-0213 debounced rebuild skip is GONE - a recenter within AHEAD_FAST_MS (3.5 s) of the previous PLAYER-chunk crossing = fast (sprint 2.9 s/chunk fast, walk 3.7 s slow) -> the recenter targets the chunk AHEAD_DIST (1) in front along the crossing direction (the queue LEADS); slow = current chunk; quiet AHEAD_FAST_MS -> _process snap-back to the chunk under the player. Every crossing rebuilds (the R16 walk = ~8 ms); the in-flight park generalizes AC-0233 (never discard an in-flight walk; finalize chains from the current center if still past REBUILD_COVER_L1; the quiet-stream settle stands down while _ahead_active). PITFALLS (all caught via the new arm/review): the fast window measures from _last_cross_ms (last player-chunk crossing) - snap/Y-only recenters (_cross=0) must not re-arm it; _last_recenter_pcx must store the PLAYER chunk (an ahead target would read _cross=0 on the next 1-chunk cross and cancel fast mode); the arm's first crossing is necessarily slow (no rate to measure); load is stationary = structurally untouched (loadlog r30 3.8 s). New arm AWECRAFT_LOGIC=aheadtarget (fast [0,1,1] leads, slow center==player, snapped after quiet). GATES (final tree): boot 0 err; aheadtarget ok; candflag ok; r16 ok (fly50 lost 0, 25/25); battery all + GENHASH 80b904c0e5f085eef1e839459058c706 EXACT; loadlog r30 3.8 s 25/25 no stall; lightstate LS16 (demoted 48, stragglers 1, double_lod 0, holes pre/ring 0, low_on_high 0). DAEMON NOTE: build dup 8082 died with its sandbox (AC-0155 pattern; 8080 authoritative, byte-matched). RESUME: `python3 tasks/scripts/tasks.py next` -> AC-0269.

