# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6.

## 00U. CHECKPOINT 2026-09-11 (afternoon) - **AC-0097 (held-item-still-occluded-not-on-top-regression) COMPLETE + SHIPPED (AweCraft[20260911-1254], :8080 byte-matched 1f331b1b...). Queue head = AC-0112 (held-item-positioning-per-type-review) — but the user wants to PLAN A LOD CHANGE WITH THE COORDINATOR NEXT (satellite far-LOD still ON HOLD).** AC-0097 was implemented DIRECTLY by the coordinator (user present/collaborating — the no-subagents override was in effect). Fix = `no_depth_test = true` on ALL viewmodel materials: AC-0067 had set only `depth_draw_mode = DEPTH_DRAW_DISABLED` (stops the depth WRITE + transparent pass) while the depth TEST still ran → occlusion when the hand pokes behind terrain. Touched: player.gd hbmat/fmat/tool-voxel mat; held_mesh.gd `_base_mat()` (source of the box/cross kind mats); the sprite MATERIAL `_vm_sprite_mat` (no_depth_test + depth_draw_mode) — **the `Sprite3D.no_depth_test` NODE property is a no-op in this build**. Bonus bug the arm found: the sprite's `material_override` DISCARDS the Sprite3D's internal texture binding → the held item sprite rendered as a flat WHITE quad; the override material now carries the tile (albedo_texture). `HELD_ITEM_SCALE` 0.70 → **0.33** (held block AND sprite) per user. Wallshot arm A/B (pre-fix state forced IN PLACE on the original materials — reassigning material_override to a fresh resource is NOT reflected in the rendered output in this build): block_vm_nonstone 68 → 16493; item_vm_cyan 0 → 191; sanity: wall stone fills ~91% of the frame in both runs. Arm repairs (its assertions were vacuous since AC-0067): (1) the 0-255 pixel predicates on 0-1 Colors were always false; (2) the player stood 1.5 m out, not 0.5 m — the viewmodel was never behind the wall (now the face is 0.4 m ahead of the eye, hand 0.3 m behind it); (3) meshing is worker-async → the arm awaits `main._await_world_build`; (4) see in-place force above. Evidence: `.scratch/ac0097/` (4 PNGs + results.txt). The user's own 12:18 build (20260911-1218, contains the depth fix; the sprite-texture fix landed just after) tested fine in-game. NEXT: LOD change planning with the user; then AC-0112.

## 00T. CHECKPOINT 2026-09-11 (morning) - **AC-0185 (Esc pause) COMPLETE + SHIPPED (AweCraft[20260911-0926 c7ac331], :8080 byte-matched d72014a9...). Queue head = AC-0097 (held-item-still-occluded-not-on-top-regression). NEW PROCESS RULE (from the users feedback after cancelling a full-battery subagent run): gates AND subagent effort scale with the change size - AC-0185 was gated with its single pausemenu arm (ok:true, all ten sub-assertions), no battery.** AC-0185: ui_pause now = P + Esc in project.godot (bound directly, no feature tag - Windows-only since AC-0124, the "browser owns Esc" concern is moot); Esc-while-a-UI-mode-is-open closes that UI first (moved above the pause branch so one press never both closes and toggles); Esc from play opens, Esc from the pause menu resumes, Esc from a submenu backs out ONE level, P still toggles both ways. Harness "pausemenu" arm (standalone + in-battery) synthesizes keys with a fresh InputEventKey per send (AC-0243 lesson) + _await_state polling: open/close Esc, P both ways, ArrowDown to Options (keyboard-reachable) + Options screen shows + back-out levels. ALSO NOTED: at 08:08 tasks/TASKS.yaml was rewritten by a NON-API writer (width-80 raw dump + new ticket AC-0256 "early-out-stale-lod-builds", source/created None - the other-chat agents style; unqueued). Data was intact; the next API write re-normalized it (257 entries, diff vs HEAD = just the new entry). The single-writer rule is in AGENTS.md, but the other chat is still doing raw dumps - the user may want to tell it to use the API too. NEXT: AC-0097 (read its notes - a held-item occlusion regression). SATELLITE far-LOD still ON HOLD. The users in-game look test (AC-0252 far view + WORLD-PIPELINE overlay at 4x) is STILL the pending user gate - the 20260911-0926 build carries everything. PROCESS: subagent (effort scaled to the change) -> coordinator diff audit -> the proportionate gates (agent runs them; coordinator does NOT re-run) -> ./build_windows.sh ALWAYS -> :8080 byte-match -> docs VIA THE API -> commit+push. ALL task-list mutations go through the API. DO NOT COMMIT: user scaffolds (unchanged).


**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout), 00z moved 2026-09-07 (AC-0231-followup closeout), 00F+00G moved 2026-09-09 (AC-0244/0245/0191 closeout), 00J moved 2026-09-10 (AC-0250 filing), 00K moved 2026-09-10 (AC-0250 closeout), 00L moved 2026-09-10 (AC-0251 closeout), 00M moved 2026-09-10 (AC-0247 closeout + AC-0252 filing), 00N moved 2026-09-10 (AC-0252 closeout), 00O moved 2026-09-10 (AC-0248 closeout + AC-0253/0254 filing), 00Q moved 2026-09-10 (AC-0254 closeout), 00R moved 2026-09-11 (AC-0253 closeout), 00S moved 2026-09-11 (AC-0185 closeout), 00P moved 2026-09-11 (AC-0097 closeout). -->