# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6.

## 00z. CHECKPOINT 2026-09-03 (08:45) - **AC-0203 paletted-sections-per-slab DONE + SHIPPED (per user decision; recenter cost tracked to the C++ series)** [per-slab palette MC1.18 style; lossless; real RAM win; the v4 GDScript decode is the slow recenter path, to be fixed by C++] — in-memory data/fl paletted (slab = null | {bits 1-8, palette[16], idx 4096 packed}); per-column flat 192 KB → ~4 KB; get_local/set_local paletted indirection; build_accs per-slab flat views; chunk_io v4 (back-compat v1/v2/v3); slab-aware light kernels. ALL GATES GREEN: G0 0; genhash 25/25 IDENTICAL (lossless, byte-identical to AC-0197 ref); codec selftest 30/30; slabwrite 3000; SMOKE light exact; R50 all_meshed 7845/7845 0 errors. RAM win: R50 build increment 5.924 GB → 4.471 GB (−1.453 GB / −24.5%). colbytes flat 4235 ≤ 4258 (zlib already crushes sparse slabs). Build: AweCraft.exe 109,881,504 B, sha c6c1a9c0, 8080 byte-match, 5180=200. KNOWN COST (accepted, tracked to C++): the mid-walk recenter is ~4x the AC-0197 baseline (60s vs 16s, confirmed NOT machine load) — the v4 paletted DECODE in GDScript is the slow path (build_worker p95 fine, 211/219). The user identified this as a slow-GDScript-loop cost to be fixed by the C++ series. Committed (5 godot files + tasks/AC-0203/* + docs).
**RESUME (new chat):** (1) read this 00z; (2) AC-0203 is DONE + SHIPPED (committed). NEXT task = **AC-0165 — GDExtension native chunk I/O codec** (the C++ FOUNDATION: sets up the SCons + .gdextension + Windows .dll build pipeline and ports the chunk I/O codec to C++ so the palette decode is a ~1ns int lookup, "fixes 0203 GDScript 74ms"; at the queue head, unblocked by AC-0203). The rest of the C++ series follows: AC-0188 (gen.cpp), AC-0189 (lighting.cpp), AC-0190 (mesh.cpp). **Before AC-0165:** check the C++ toolchain on the build machine (g++/MSVC, SCons, godot-cpp headers, Windows .dll) — the project was pure GDScript, so the C++ build infrastructure is NEW (the main risk). Standing: godot ONE at a time; HOME=/tmp/dsh_home every godot call (fresh /tmp per bash call); LONG arms (R4/R50) → run_in_background + job_output; check `uptime` load before trusting a p95; GODOT 4.7.1 gotchas: no OS.get_cpu_usage etc., no get_index/reorder_child (child-scan + Node.move_child), Performance.MONITOR_* → Performance.TIME_*; harness: AWECRAFT_RADIUS is NOT in HARNESS_ENVS (set AWECRAFT_LOGIC on a probe launch). GOAL ACTIVE: AC-0203 done + shipped this round; next = AC-0165 (the C++ foundation).

## 00y. CHECKPOINT 2026-09-02 (17:55) - **AC-0197 sparse-air-slabs-skip-empty-top DONE (with an honest caveat) + PUSHED** [empty top slabs now skipped; lossless; real R50 wins] — sky above the terrain (empty top slabs) is no longer scanned/stored/meshed/light-flooded. Per-column `top` (max non-air y) threaded generator→world→chunk/lighting/chunk_io; build_accs early-bounds si1 to the top slab (no 98k air scan); _bake_box row-scoped (153600 B → ~65k); lighting pull skips the flood above top+2 (eff=15 there); chunk_io v3 writes an offset table of only non-null slabs (v1/v2 decode kept). **LOSSLESS: genhash 25/25 byte-identical** (the #1 invariant). **REAL R50 wins** (same env, before/after): built_final 7845→7845, build_ms −7.7%, frames-to-full-mesh −10.4%, frame max −33%, mem −109 MB. **BONUS:** found + fixed a pre-existing _tm_slots/_tg_slots cross-thread race (Mutex; the R50 before-run segfaulted at 22 min without it — behavior-neutral, applied to both sides). **HONEST CAVEAT (targets miscalibrated in the notes, NOT an implementation failure):** (1) p95 build_worker <30 ms NOT met → measured 240 ms (full-24-slab-build granularity on a 6-core box; cannot be 30 ms at that granularity, and the metric did not exist on the before side); (2) 8KB/col→5KB NOT met → measured 4338→4258 B (−1.8%): zlib already crushes the zero slabs (the notes’ 8 KB assumption was wrong — real before was 4338 B). The R50 build is NOT dominated by the air-cell scan (non-air cells + meshing + collision + light dominate), so the ~45% the notes predicted is unreachable by skipping the air alone — **if a bigger R50 win is wanted, the bottleneck is the non-air-cell mesh/build path, not the air.** Files: chunk.gd (top + update_top + build clamp + row-scoped _bake_box + range-aware apply_accs), lighting.gd (top-truncated pull), world.gd (top propagation + _tg/_tm_slots Mutex fix), chunk_io.gd (v3 sparse slab codec, back-compat), main.gd (colbytes probe + perf arm upgraded). **GATES:** G0 0; genhash 25/25 IDENTICAL (subagent + coordinator ×2, byte-identical to AC-0156 ref); codec self-test 14/14; SMOKE all 5 arms (light 15/10/14 far 1→9); R50 built_final 7845 + real deltas; HEAVY boundary r4 ×2 ok:true (p95 35/40 under 3× load → clean re-run p95 32, in band, no regression) + perf r4 (arm upgraded) + fog_ok; build 109,855,584 B sha f87da865 8080 byte-match + 5180=200 + 8443 down.
**RESUME (new chat):** (1) read this 00y + `godot/COORDINATOR.md` + HARNESS §1/§3; (2) `python3 tasks/scripts/tasks.py next` — user co-edits the queue LIVE and keeps ADDING tasks (re-run at EVERY boundary; as of 17:55 queue head = **AC-0203 → AC-0204 → AC-0198 → AC-0199 → …**, 73 open; AC-0197 done this round); (3) THE JOB = the head task — read its TASKS.yaml notes verbatim (User Story + Technical Details); ONE blocking subagent (xhigh for real work per the active goal; low only if genuinely trivial; describe END RESULT + gates + env + code map; subagent honors .scratch/.godot_busy; subagents MAY read_image PNGs, coordinator may NOT); (4) closeout per COORDINATOR.md: TRUST builder light gates → coordinator HEAVY [boundary r4 ×2 + perf, STABLE tree] + genhash ×2 + G0 → build [OWN call per BUILD RECIPE symlinks FIRST] + 8080 byte-match + 5180=200 + 8443 note [down — do NOT start] + kill any 8081 dup → HARNESS §3 only if standing values moved → TASKS.yaml done + queue removal + closing comment → CONTINUITY → commit EXPLICIT PATHS [task folder + godot files + TASKS/HARNESS/CONTINUITY/archive; NEVER other users’ scaffolds or docs/halo-loot-brainstorm.html] + `GIT_SSH_COMMAND="ssh -F /dev/null" git push` → report localhost + LAN 192.168.0.224:8080/5180 → next. Standing: godot ONE at a time; HOME=/tmp/dsh_home every godot call (fresh /tmp per bash call); LONG godot arms (R4/R50!) → run_in_background + job_output (bash 60 s default kills a foreground long arm); a subagent’s LONG report can hit the LLM output cap (land code+gates+results on disk FIRST, then a ≤20-line report); R50 arms are SLOW (20+ min) — background + generous timeout + check `uptime` load (a 3×-loaded box inflates p95 — re-run clean before declaring a regression); GODOT 4.7.1 gotchas: no OS.get_cpu_usage etc., no get_index/reorder_child (child-scan + Node.move_child), Performance.MONITOR_* → Performance.TIME_*; harness: AWECRAFT_RADIUS is NOT in HARNESS_ENVS (always set AWECRAFT_LOGIC on a probe launch); some probes need R≥2. GOAL ACTIVE: "complete all open tasks" — this round did AC-0197 (1 task, +1 bonus race fix); next = AC-0203 (new head). The user is actively re-prioritizing + adding tasks (queue grew 61→73 over recent rounds).

**RESUME (new chat - do these in order):** (1) read this 00p block + `tasks/HARNESS.md` §3 [standing values incl. NEW boundary p95 ≈73–89 + SAVE_VERSION 3 + chunkio row]; (2) `python3 tasks/scripts/tasks.py next` [user co-edits TASKS.yaml LIVE — re-run at EVERY boundary; as of 14:05 queue = **AC-0158 → AC-0159 → AC-0154 → AC-0109 → AC-0150 → AC-0137 → AC-0092 → AC-0144/0145/0146 [planet] → AC-0111 → AC-0036 → AC-0147 → AC-0161 → AC-0148 → AC-0149 → AC-0140/0141/0142 → AC-0121 → AC-0122 → AC-0097 → AC-0112 → AC-0113 → AC-0093 → AC-0125 → AC-0094 → AC-0098 → AC-0101 → AC-0037..0040 → AC-0087..0090 → AC-0115 → AC-0130**]; (3) THE JOB = **AC-0158 20hz-diamond-game-tick** (Bedrock Realms 20 Hz tick on Simulate 4 diamond 41 [taxi ≤4, 41 not 81] not 5 Hz square: 1 randomTick per 16³ per tick (~1/68 s per wheat), fluid 20 Hz, redstone 1-tick dust, mob spawn 24–44 circle + n−1 diamond; efficiency + correctness, half columns, far 13–50 never tick; spec.html present in tasks/AC-0158/); delegate as ONE blocking xhigh subagent [v3 — parent idle while child runs; describe the END RESULT + gates + env, the child plans its own implementation], then coordinator re-verifies ALL gates; (4) per-task closeout: verify gates → `./build_windows.sh` [symlinks FIRST per BUILD RECIPE] + 8080 byte-match + 5180=200 + 8443 note [down — do NOT start] → results.html → HARNESS.md [only if standing values moved] → TASKS.yaml done [proper entry, queue removal] → CONTINUITY 00p → commit EXPLICIT PATHS [NEVER the other users' scaffolds] + `GIT_SSH_COMMAND="ssh -F /dev/null" git push` → tasks.py next → report localhost + LAN 192.168.0.224. Standing: godot ONE at a time; `HOME=/tmp/dsh_home` on every godot call; **battery env = `AWECRAFT_BATTERY=…` (a `;`-list in AWECRAFT_LOGIC hits the unknown-logic path and ABORTs the process — learned 2026-08-31)**; logs → `.scratch/<task>-gates/`; /tmp EPHEMERAL per bash call; report ≤20 lines.
**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout). -->