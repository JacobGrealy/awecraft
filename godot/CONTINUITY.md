# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6.

## 00C. CHECKPOINT 2026-09-07 (09:30) - **AC-0231/0234/0236/0237 ALL COMMITTED (b48c91b C++ pipeline + 36407d8 GDScript closeout) + CLOSED. USER RULE CHANGES (2026-09-07, committed 93e8e3a): (1) caps reuse the FOG material + shared box mesh (light sky-blue UNSHADED) - the black _vwin_cap_mat is DELETED (user: caps go to the fog color, not black); (2) OPEN-TOP BAND: the player band is [pys-4, TOP OF WORLD] - the full column ABOVE the player's Y is always kept, rendered normally, NEVER culled/capped (the +-4 band culled + capped the SKY slabs above the terrain in far columns = rows of placeholder boxes in the sky; user: fill goes DOWN not up). One-line impl: _vwin_recompute bhi = top slab - every band comparison (col_kept / keep_for incl GEN_BAND_PAD / sync) picks it up; cap logic untouched. R16 all-green (seed 44): owed 0, gen_cov 0, caps_ok, phase_ok, built_all, build_ms 74529->~70k, and the +-4 sky-cap churn collapsed (re-entry regens 24077->28, rebuilds 24805->224, uncaps 1322->0) - the R16 churn_ok gate was re-pointed to (culls_n>0 AND vwin_regens_n>0) since the old uncap requirement measured the REMOVED sky-cap churn. NOTE (user): this optimizes flight/above-ground; cave interiors may need a different algorithm later. USER caveat honored: the 4x4x4 LOW code path is NOT touched yet (satellite LOD may replace it - see below).
**RESUME (new chat):** (1) read this 00C; (2) NEXT = **AC-0238 (window-masked-light)** top of queue - the light pass (C++ AweLighting) must honor the same per-column keep mask as gen/build (full notes + gates in tasks/TASKS.yaml; the .so rebuild recipe is in this file's footer section, stage to godot/bin/ after any C++ change). THEN **AC-0239 (simulation-distance-streaming-tier)** - filed 2026-09-07: a sim-radius tier (Chebyshev <= the existing Settings.sim_dist Options slider, default 4) ahead of the FOV cone in _tier_of (currently 0 under / 1 ring1 / 2 cone / 3 rest); the slider is live-wired via Settings.apply_sim_distance (today: fluid_tick_radius + band0_r only); gate audit: the AC-0231 order gate fires on tier >= 2 = the SAME set after renumber (verify). UNDER DISCUSSION with the user (NOT yet a ticket): SATELLITE FAR LOD - a per-chunk 16x16 top grid (C++ slabs_top + one palette read = top y + top block TYPE per 1x1 block column, LIVE edit-aware, re-baked on the existing 3x3 neighbor-sync edit hook) rendered as an unshaded 17x17 vertex-color heightfield at TRUE elevation (no stretch; MC relief shading additive; top-surface only, open underneath) - it takes the LOW's slot in the ladder (fog -> satellite -> high) with the low path left dormant behind a revert const; it is TERMINAL for far chunks (lazy heightmap-only gen = end state; no high build/light owed) - the CPU win is skipping full gen + high mesh + light for ~97% of the R50 circle, the bake itself is only ~5-25x cheaper than the per-slab low; the PLANET VIEW is the same top-grid cache max-pooled to coarser zooms (MC map scale 1/2/4), same unlit material. Design state: user confirmed the shape 2026-09-07; trigger (distance vs speed) + detail radius + ticket not yet decided.
## 00B. CHECKPOINT 2026-09-06 (22:50) - **AC-0234 vertical-lod-window-per-column v2 DONE + VERIFIED, UNCOMMITTED (coordinator commits + closes)** [USER v2 RULES (in-loop 2026-09-06, FINAL, supersede the v1 design in TASKS.yaml comment id 2): (1) NEVER replace a built mesh with black (KEEP-HIGH for culling) — a slab with a built HIGH mesh is never capped/dropped/re-meshed by the window; black caps cover NEVER-BUILT slabs only (drop_slab_high REMOVED); (2) the kept range is PER TOWER — each column span = MIN/MAX of its OWN top slab + its 8 neighbors' tops (its 3x3, c.top/16); kept per column = that tower span UNION the player band [pys-4,pys+4] (always). v2 machinery: C++ unchanged (id-2 p_mask works per-column; .so as staged). chunk.gd: + vwin_mask (the exact keep mask the last build covered; EMPTY = full); vwin_full gates the owed check. world.gd AC-0234 section: _vwin_span(cx,cz) + _vwin_col_kept(c) (per-tower; the global kept set is GONE), _vwin_mask_for(c) = kept UNION already-built (built range STICKY across swap/edit/tier transitions), _vwin_recompute BAND-ONLY (Y recenter no-op unless the band changed — X/Z walks free), _vwin_tier0 (EVERY recenter: new player column drops caps + window-masked build under the player force-re-queues the FULL build — the vwin_full_build entry flag honored by the 4 drain guards), _vwin_neighbor_sync (data landing / edit re-syncs the 8 neighbors whose spans moved; hooked at the TOP of _low_fog_for so edited built columns count), _vwin_owed (a BUILT chunk keeping a non-air slab its vwin_mask never built — band slid back / neighbor span grew / mid-flight window move — re-queues the SUPERSET build kept+built; a cull NEVER re-queues; convergence guarantee), _vwin_apply (band change: sync + owed + re-queue per chunk + low-pick cache invalidation), _vwin_sync RETURNS a kept cap — a re-entered cap on a NON-BUILT chunk RE-ISSUES its queue entry (the low lane only sees ENTRY-HOLDING chunks: far out-of-circle chunks hold none — band 3 is data-only, build entries consumed at handoff) so the in-r pass / far wave swaps it cap→low→high (WAVE 3) = the uncap path. The v1 window-stale re-queue is GONE (only owed-superset + tier-0 forced). player.gd unchanged (id 2). main.gd: _vwin_state per-tower span; R16 vwin = caps_ok + churn_ok (culls AND uncaps) + owed_ok (invariant: zero built chunks owe kept slabs at final sample — rebuilds_n COUNT is 0 by construction in this arm: no build dispatches while a slab is culled) + phase_ok. EVIDENCE (R16 seed 44, clean run): top ok:true; vwin caps_ok (7928 caps/932 chunks), churn_ok (culls 637 up / uncaps 233 down — the re-entered caps swap via the re-queue; phase caps 6649→8168 altitude→7949 after descent), owed_ok (owed_n 0), phase_ok (inr_uncovered 0 at all 4 samples), trace ver 2→13→24 band [7,15]→[18,23]→[7,15] span [9,9]; legacy ALL green (inr/immediate 1382+1583/gate/wave/height/cap both slab samples/no_downgrade/low_thread stale 0/catch_up), fps static p95 29ms moving 5ms drop 0, fall ok, edit 13.6ms, spin ok. meshprobe 1926 match 1.0 (unchanged .so). SMOKE ok:true. Syntax 0. DEBUG NOTE: the v2 churn_ok:false (uncaps 0) root cause = re-entered caps sat on entry-less far chunks (VDBG probe: eligible=0 at phase end) — fixed by the _vwin_sync kept-cap re-queue + _vwin_owed; the owed-kept re-queue fires on mid-flight moves/span growth, not in this arm's geometry. RESUME:** (1) AC-0234 v2 DONE + VERIFIED + UNCOMMITTED — the coordinator commits + closes it (full v2 evidence in TASKS.yaml comment id 3; id 2 = v1 historical); (2) NEXT = **AC-0235** (remove-skybox-add-procedural-clouds-shader; notes in tasks/TASKS.yaml). CAVEAT: (a) AC-0236 ALSO uncommitted (both parts; evidence in its notes + 00A below); (b) the .so is REBUILT for AC-0234 (build_accs +mask) — staging rule stands (hand-built gdext/bin .so needs cp to godot/bin/; build_windows.sh auto-stages); (c) the AC-0206 one-off SIGSEGV class persists (2026-09-06 sites: slab_cell/snap_rings/generate_args/throwaway-camera cull — all clean-retry passed). Standing: godot ONE at a time; HOME=/tmp/dsh_home every godot call; LONG arms -> run_in_background + job_output; NEVER put single quotes in TASKS.yaml closeout comments.]
**RESUME (new chat):** (1) read this 00B; (2) **AC-0234 + AC-0236 are CLOSED** (2026-09-07, user direction). **AC-0237 (window-scoped-generation) is DONE + VERIFIED + UNCOMMITTED** (2026-09-07; closeout evidence = TASKS.yaml AC-0237 comment id 3): phases 0/1a/1b/1c complete + phase 2 a documented no-op (DRAIN_UNITS_FRAME_CAP 4->6 tested: owed_n 0->4, no fill gain, reverted). Keep-mask generation: C++ gen_flat takes a 24-byte keep mask (empty = full column = bit-identical, genhash A==B + (0,0)=80b904c0... baseline held); keep = per-tower span UNION player band (GEN_BAND_PAD=4 for gen only); re-entry = scoped TG regen + neighbor RE-MESH only when the regenerated boundary has cave air (C++ slab_boundary_air); ungenerated slabs = SOLID for the snap (snap_rings genkeep) + capped in _vwin_sync; v5 codec = the 24-bit gen_mask at header bytes 13-15, written ONLY when range-generated (full columns stay v4). The OWED-BUILD LEAK full mechanism set (all in godot/world/world.gd, all permanent): the 4 drain scan sites accept vwin_remesh entries of built chunks; remesh entries are order-gate-exempt (AC-0231) + eviction-exempt (AC-0222 cap); the recenter merge PRESERVES them (_rec_merge_old_step WANT branch + _rec_merge_ring_step finalize stale-drop - the main leak was entries wiped every recenter); re-entry rechecks (recenter candidate re-entry + _reband 3->0/1) re-queue _vwin_owed/_vwin_gen_owed; the _drop_queued exemption (the handoff no longer deletes the debt it just re-queued); and the dedicated remesh debt lane (vwin_debt_keys + _pick_vwin_debt + up to 2 nearest-ready remesh dispatches/frame before the scored pass, bypassing PICK_POOL_CAP=512 starvation). Band-3 chunks are excluded from the owed gate count (deferred debt, healed by the reband hook). HANG ROOT-CAUSE (the post-cleanup freeze of every streaming arm): a temporary line-deletion patch had swallowed the stale-drop tail of the _rec_merge_ring_step finalize while loop (arrf.remove_at(i)+continue + i += 1), so the recenter merge finalize spun forever on the first non-remesh entry - restored; that was the only code-level cause. ALL gates green on the clean tree: syntax 0 parse errors; player arm RESULT; SMOKE battery ok:true; meshprobe 1926/1926; genhash 25/25 A==B + baseline; continue arm edits_ok/pos_ok/saved_ok; v5 selftest ok; R16 (seed 44, 235.8 s) ok:true owed_n 0 owed_ok:true gen_owed_n 0 gen_cov_bad 0 gen_range_n 915/934 built_all true static p95 31 ms moving 5 ms build_ms 74529. **AC-0231/0234/0236/0237 are COMMITTED + CLOSED** (2026-09-07, coordinator closeout). **NEXT = AC-0238 (window-masked-light, top of queue)** - the light compute (AweLighting) is still full-column: it should honor the same per-column keep mask as gen/build (AC-0237 comment id 1 phase list + the task notes carry the design). Standing: godot ONE at a time; HOME=/tmp/dsh_home every godot call (fresh /tmp per bash call - mkdir -p each time); long arms -> run_in_background + job_output; NEVER put single quotes in TASKS.yaml closeout comments; the .so staged at godot/bin/libchunkio.so carries ALL C++ changes (gen mask + snap_rings genkeep + slab_boundary_air + timing counters) - rebuild recipe in 00A, stage after any C++ change.

## 00A. CHECKPOINT 2026-09-06 (18:30) - **AC-0236 (TWO PARTS, user-directed) DONE + VERIFIED, UNCOMMITTED (coordinator commits)** [Part 1 = chunk-io v4 codec encode/decode to C++; Part 2 = low-lane threading (user: "just add it as a second part of AC-0236"). PART 1: the C++ twins (encode_section/decode_section, chunk_io.cpp) ALREADY SHIPPED in the .so — the fix was pure GDScript rewiring in godot/core/chunk_io.gd: _encode_head v4 branch + decode_column v4 dr/fr now call io_cpp().encode_section/decode_section (new counters cpp_encode_sections/cpp_decode_sections); v1-v3 legacy GDScript paths kept (decodable, never written); the GDScript v4 twins remain the chunkiocpp A/B reference. NOTE: latent legacy typo kept verbatim (out of scope): decode_column v1/v2 fl fallback reads de_at where fe_at expected. PART 2: C++ AweMesh.low_emit(slabs, si, ms) (gdext/src/mesh.cpp — port of _low_emit_slab: 3-slab 4x4x4 grid sample, 6-face viewing-side shell scan, slab-boundary culling, greedy merge key id*16+(k+1) H=1, strip+plain tile UVs, face tables VoxelMath.FACES verbatim) + the GDScript lane in godot/world/world.gd: _low_dispatch_slab (tristate 1 dispatch/0 dedupe/-1 sync-fallback, LOW_TASK_CAP 64), _low_handoff (main-thread attach: the full _low_build_slab bookkeeping incl. all-air terminal fog mark), _low_poll(dt) (own queue — NEVER threadmesh_inflight, so the high lane keeps stream_ho_cap; cap clamps to frame dt), worker branch in _tm_worker_run, handoff routing in threadmesh_handoff, in-r pre-low + far wave rewired (fallback = old sync build), ms snapshot cache (dirty at the 3 _tm_ms_full reassigns), shutdown clear. R16 evidence keys: low_emit_cpp/low_enqueue_n/low_handoff_n/low_drop_stale_n/low_sync_fallbacks_n/low_thread_ok. Evidence: nofallback ok:true — Part 1 counters 2/2 + torch 15 + roundtrip; Part 2 A/B low_emit_pairs 3/3 MATCH (C++ vs GDScript _low_emit_slab: v/u/i/mh EXACT; n within 1e-4 + c within 1/255 = ArrayMesh STORAGE artifacts, probe-verified: add_surface_from_arrays re-normalizes normals ~1.5e-5 + uint8-quantizes colors). R16 gate run all-green: ok + inr0/inr1 uncovered 0, gate_ok (firstbuild 4 <= 24), wave/height/air/no_giant/uv_repeat/no_downgrade/immediate/catch_up all true, static p95 32ms (vs 33 baseline — the intended post-stop high fill), moving p95 5ms = baseline, drop_fps 0; LOW-LANE THREADING PROOF: low_enqueue_n 26331 = low_handoff_n 26331 (EVERY dispatch handed off), low_emit_cpp 26326 (pool-carried; the 5 delta = in-flight at snapshot), low_drop_stale_n 0, low_sync_fallbacks_n 0 (the pool NEVER saturated — the main-thread fallback never fired once in 26k slabs). Battery: SMOKE ok:true light 15/0/14 (the FIRST run hit a one-off SIGSEGV at generate_args generator.gd:127 on the battery world reset — AC-0206-class, 4th distinct .so site this campaign after slab_cell + snap_rings + the real-bug face recursion; clean retry green); chunkiocpp ok (md5 8a893557900a462fe4ab1c91f8589081 == orig); genhash A==B byte-identical; meshprobe 1926 accs 100pct; stripsprobe 32/32; pullprobe 6,291,456 cells max_diff 0. BUILD NOTES: no scons in the agent env — the .so was built with g++ directly (SConstruct flags: -O2 -ffp-contract=off -std=c++17 -fPIC -shared -fvisibility=hidden, static lib .scratch/godot-cpp/bin); the game loads res://bin/libchunkio.so = godot/bin/ — the gdext/bin build MUST BE STAGED (build_windows.sh does it; the user ran windows builds this session which staged it — the first low-lane run crashed against the stale staged .so: missing low_emit -> worker error storm -> secondary SIGSEGV no frames; clean after staging). GDScript API gotchas hit: ArrayMesh 4.x = surface_get_arrays (NOT get_arrays/get_surface_arrays); String % [args] with EXTRA args = silent ERROR + template returned verbatim (count tokens); Vector3.is_equal_approx takes NO tolerance param in this build (manual per-component absf). UNCOMMITTED (coordinator commits): godot/world/world.gd, godot/scenes/main.gd, godot/core/chunk_io.gd, godot/world/chunk.gd, gdext/src/mesh.cpp, tasks/TASKS.yaml, godot/CONTINUITY.md(+archive).]
**RESUME (new chat):** (1) read this 00A; (2) AC-0236 (codec + low-lane threading) is DONE + VERIFIED + UNCOMMITTED — the coordinator commits + closes it (its TASKS.yaml notes carry the full verify evidence; status stays open until the commit). NEXT = **AC-0234** (top of queue after AC-0231/AC-0236; read its notes in tasks/TASKS.yaml). The C++ pipeline is COMPLETE (gdext/SConstruct + .gdextension + godot-cpp; build_windows.sh auto-builds GDExt + stages the .dll/.so to godot/bin/ — REMEMBER: a hand-rolled g++ build in gdext/bin does NOT reach the game until staged; -ffp-contract=off; registered classes: ChunkIOPalette/AweGen/AweLighting/AweMesh/AweStrips). KNOWN/CAVEAT: (a) the .so crashes in the coordinator env (signal 11) but not always in the agent env — AC-0206 (research, non-blocking); crash SITE varies (2026-09-06: slab_cell, snap_rings, generate_args — all clean-retry passed; do NOT confuse with the same-day face-recursion stack underflow, a real gate-burst bug, fixed by FACE_PULL_MAX_DEPTH); (b) a new .gdextension needs one godot --headless --import; (c) the ~21ms p95 residual = the 500ms light refresh (AC-0213); (d) genhash baseline = AC-0215 ((0,0)=80b904c0...); (e) godot-cpp build loses raw ptrw() writes on PackedByteArray handles shared with a Dictionary. Standing: godot ONE at a time; HOME=/tmp/dsh_home every godot call; LONG arms -> run_in_background + job_output; check uptime load; NEVER put single quotes in TASKS.yaml closeout comments.


**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout), 00z moved 2026-09-07 (AC-0231-followup closeout). -->