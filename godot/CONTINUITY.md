# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6.

## 00q. CHECKPOINT 2026-08-31 (18:05) - **AC-0158 20hz-diamond-game-tick + AC-0164 thread-chunk-io DONE + PUSHED** [combined closeout; prior cae0c8e AC-0155] — **AC-0158:** Bedrock Realms 20 Hz simulation clock: fixed-step accumulator (`TICK_INTERVAL` 0.05, `TICK_MAX_CATCHUP` 4 drop, play/pause gate) in `_process`, `tick_index` = deterministic seed; per tick: 1 random tick per 16³ subchunk over the band-0 diamond 41 ONLY (band 1–3 / far 13–50 never tick) — cell = pure function of (world seed, tick idx, col, sub) via splitmix64; fluids now run INSIDE the game tick (5 Hz Timer removed; `fluid_wet` sleep kept) = 20 Hz; region/rate contracts `REDSTONE_DUST_DELAY_TICKS=1` + mob-spawn circle 24–44 ∪ (n−1) diamond (pure predicates — feature work deferred: no redstone/mobs in codebase). New arm `AWECRAFT_LOGIC=tick` (HARNESS §1): settles quiet r=4 41-set, 120 ticks — **hz2 ≈20** (gate on last-half window: full-window hz is dragged by catch-up drops on >200 ms frames), scope 41 cols/984 subs min=max, band123_ticks 0, recompute_mismatch 0/118080, **window_md5 `d81250cd…` byte-identical across fresh runs** (determinism), tick p95 ≈4–7 ms. Deviations: "half columns" = 41≈81/2 (diamond vs square); 1 tick/16³/tick ⇒ wheat cadence ~205 s (note's ~68 s ⇒ 3 ticks/16³/tick = Bedrock rts=3 — recorded for the crops task). **AC-0164:** threaded column I/O on the threadgen pool (AC-0082 slot pattern): write = snapshot+**slot captured at enqueue** → encode+FileAccess on worker (in-flight dedup); read = `file_exists` main → read+decode worker, fail-closed → threadgen; in-flight registry guards every data-landing site (drain pick, `_materialize_chunk_data`, recenter 5×5 burst `had_data` — the ~500 ms sync burst is GONE); spawn column (0,0) stays sync (spawn contract); `_exit_tree` drains I/O. **Main-thread cost: save 32.6→0.05–0.11 ms, read 30.5→0.03–0.06 ms.** AWCC v1 blob + SAVE_VERSION 3 unchanged; chunkio still 41/41 byte-identical (+`io` counter block). **GATES:** builder light gates all green (SMOKE5 exact, genhash 25/25, chunkio ×2, save/continue, tick probe, render) — coordinator TRUSTS light gates per `godot/COORDINATOR.md`; heavy on STABLE tree: boundary r4 ×2 **p95 74/80 in-band (73–89 HOLDS — no band change)**, perf p95 54, build 109,760,568 B [sha 9a4cf966…] 8080 BYTE-MATCH + 5180=200 + 8443 down. Non-fatal: threadgen_handoff transients + `Invalid Task ID` + crossing-16 burst 33.3/40.1 s. **PROCESS CHANGE (user 2026-08-31, BINDING) — `godot/COORDINATOR.md`:** trust builder light gates; coordinator = heavy + build + chain next. **NEW INFRA LESSON: a heavy godot stage must NOT overlap the subagent's FILE-EDIT window** (the `.scratch/.godot_busy` lockfile gates the subagent's godot RUNS but not its edits — first heavy run on a mid-edit tree measured p95 102; stable-tree re-run → 74/80; check file mtimes vs the stage window and re-run heavy after the subagent returns if they overlapped). Chain: launch the next subagent while the previous heavy stage runs (builder's first action = reads/plan, no godot; lockfile protects its runs).
**RESUME (new chat):** (1) read this 00q + `godot/COORDINATOR.md` + HARNESS §1/§3; (2) `python3 tasks/scripts/tasks.py next` — user co-edits the queue LIVE (≈50 items; new AC-0165..0178 family); as of 18:05 queue = **AC-0178 → AC-0167 → AC-0168 → AC-0169 → AC-0154 → AC-0156 → AC-0175 → AC-0121 → AC-0122 → AC-0170..0174 → AC-0159 → AC-0109 → AC-0150 → AC-0137 → AC-0092 → AC-0165 → AC-0144/0145/0146 → AC-0111 → AC-0036 → AC-0147 → AC-0161 → AC-0148…**; (3) THE JOB = **AC-0178 loading-screen-bypass-throttles** (loading screen on first spawn + render_distance change; while visible bypass the world-gen throttles — read its TASKS.yaml notes verbatim); ONE blocking xhigh subagent (end result + gates + env + code map — child plans its own implementation); (4) closeout per COORDINATOR.md: heavy [boundary r4 ×2 + perf, STABLE tree] → build [symlinks FIRST per 00p BUILD RECIPE] + 8080 byte-match + 5180=200 + 8443 note [down — do NOT start] → HARNESS §3 only if standing values moved → TASKS.yaml done + queue removal → CONTINUITY 00q → commit EXPLICIT PATHS [task folder + godot files + TASKS/HARNESS/CONTINUITY/archive + any user infra edits] + `GIT_SSH_COMMAND="ssh -F /dev/null" git push` → report localhost + LAN 192.168.0.224:8080/5180 → next. Standing: godot ONE at a time; `HOME=/tmp/dsh_home` every godot call; battery env = `AWECRAFT_BATTERY=…`; logs `.scratch/<task>-gates/`; /tmp EPHEMERAL per bash call; **xvfb render can hang at GL init (environmental — run under `timeout -k 15 480`, retry once, record; builder's render stands)**; DO NOT COMMIT: other users' scaffold folders + `docs/halo-loot-brainstorm.html` (never).

## 00p. CHECKPOINT 2026-08-31 (14:05) - **AC-0155 full-chunk-save DONE + PUSHED** [prior 30d4662 AC-0152+0160]: full-column save shipped — every 16×384×16 column saved WHOLE on evict (recenter to_free → `_queue_chunk_save`, drained 1–2 cols/frame ~26 ms encode, main-thread, pure stubs never written) via NEW `godot/core/chunk_io.gd` [static codec: per-16³ palette+bitpack ×24 subchunks + `fl` fluid array, "AWCC" v1 blob (u32 seed + u16 height + 16B MD5), deflate mode 0 [only int modes 0–3 round-trip in 4.7.1 GDScript], ~8.3 KB/col, ~26 ms enc / ~20 ms dec, lossless; files `user://chunkdir_<slot>/<face>_<cx>_<cz>.bin` — slot isolation via Save.active_slot (set before world creation on every path), Save.clear wipes the slot dir]; revisits READ not re-gen: disk-first `_try_disk_load` at every data-landing point (create_chunk / recenter(0,0) / 5×5 burst construction / drain [1 inflate/frame via `_gen_last_disk`]) + provenance counters `disk_reads`/`disk_read_ms`/`gen_count`/`gen_ms_total`/`chunk_origin`{disk|gen}; threadgen handoff has NO disk-first (safe: worker only ever enqueues for no-file chunks — a file can't appear while its chunk is alive); SAVE_VERSION 3 [load is shape-validated, NOT version-gated → v2 JSON still loads; the JSON edits diff is now redundant-but-idempotent on load — kept for the save/continue arms]; NEW harness arm `AWECRAFT_LOGIC=chunkio` (HARNESS §1 row). Gates ALL GREEN (coordinator re-ran EVERYTHING independently): G0 0 err; SMOKE5 EXACT [player 135.43/136.78, interact place_ok true, light 15/10/14/9, fluids 2756/2756 + 914/914 stable + 25/9, genhash 25/25 byte-identical to AC-0091 baseline ×3 runs]; chunkio ok:true wall ~33 s [41/41 files, 41/41 byte-identical (MD5 of 98304 B), r4 revisit origin 41 disk/0 gen = "second-visit GENMS 0", r50 revisit disk 41/gen 0, phaseA gen 53–54/disk 0]; save + continue ok:true (iso intact, v1_softfail); render R=1 PNG clean. HEAVY: boundary r4 ×2 ok:true [walk p95 **73/89** — NEW standing band [HARNESS §3 amended], old ≈54 VOID, upward drift = the write-on-evict cost, predicted at handoff; crossing-16 burst now 37.8/38.7 s, same non-fatal class]; perf r4 ok [total 40 s, frame p95 49 ms, drains clean]; build 20260831-0955 109,747,304 B + 8080 BYTE-MATCH [sha 87a57846…; NOTE: build script saw a STALE pidfile and spawned a DUP daemon on 8081 which died on its own — 8080 single-daemon state restored, see BUILD RECIPE] + 5180=200 + 8443 DOWN pre-build [do NOT start]. KNOWN LIMITS (not in gate set): revisit recenter does a SYNC 5×5 disk-load (~500 ms one-time hitch per recenter-to-saved-area — chase a paced variant if it matters in play); battery-as-one-run ~275–295 s on this box (inter-mode mesh-drain waits; values exact; NOT a regression — the battery never evicts so the save queue is a no-op there).
**RESUME (new chat - do these in order):** (1) read this 00p block + `tasks/HARNESS.md` §3 [standing values incl. NEW boundary p95 ≈73–89 + SAVE_VERSION 3 + chunkio row]; (2) `python3 tasks/scripts/tasks.py next` [user co-edits TASKS.yaml LIVE — re-run at EVERY boundary; as of 14:05 queue = **AC-0158 → AC-0159 → AC-0154 → AC-0109 → AC-0150 → AC-0137 → AC-0092 → AC-0144/0145/0146 [planet] → AC-0111 → AC-0036 → AC-0147 → AC-0161 → AC-0148 → AC-0149 → AC-0140/0141/0142 → AC-0121 → AC-0122 → AC-0097 → AC-0112 → AC-0113 → AC-0093 → AC-0125 → AC-0094 → AC-0098 → AC-0101 → AC-0037..0040 → AC-0087..0090 → AC-0115 → AC-0130**]; (3) THE JOB = **AC-0158 20hz-diamond-game-tick** (Bedrock Realms 20 Hz tick on Simulate 4 diamond 41 [taxi ≤4, 41 not 81] not 5 Hz square: 1 randomTick per 16³ per tick (~1/68 s per wheat), fluid 20 Hz, redstone 1-tick dust, mob spawn 24–44 circle + n−1 diamond; efficiency + correctness, half columns, far 13–50 never tick; spec.html present in tasks/AC-0158/); delegate as ONE blocking xhigh subagent [v3 — parent idle while child runs; describe the END RESULT + gates + env, the child plans its own implementation], then coordinator re-verifies ALL gates; (4) per-task closeout: verify gates → `./build_windows.sh` [symlinks FIRST per BUILD RECIPE] + 8080 byte-match + 5180=200 + 8443 note [down — do NOT start] → results.html → HARNESS.md [only if standing values moved] → TASKS.yaml done [proper entry, queue removal] → CONTINUITY 00p → commit EXPLICIT PATHS [NEVER the other users' scaffolds] + `GIT_SSH_COMMAND="ssh -F /dev/null" git push` → tasks.py next → report localhost + LAN 192.168.0.224. Standing: godot ONE at a time; `HOME=/tmp/dsh_home` on every godot call; **battery env = `AWECRAFT_BATTERY=…` (a `;`-list in AWECRAFT_LOGIC hits the unknown-logic path and ABORTs the process — learned 2026-08-31)**; logs → `.scratch/<task>-gates/`; /tmp EPHEMERAL per bash call; report ≤20 lines.
**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00q+00p), archive rest; 00o moved 2026-08-31 (AC-0158/0164 closeout). -->

