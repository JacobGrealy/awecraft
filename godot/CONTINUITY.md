# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6. 

## 00Z. CHECKPOINT 2026-09-15 (r10) - **AC-0283 P3 SHIPPED (b0ff477, pushed) — the far-band HALO is live: everything taxi > band0_r (the sim_dist square = collision/fluid square) now draws 4x4 avg + HEIGHTMAP SKY light (sky 15 strictly above the terrain top per x,z, 0 at/below, no flood, no block light) with NO star seed / NO nibbles — the star engine's work is confined to the real band. The halo SUBSUMES the old med/low draw tiers (the med/low setting values still flow through the settings-clamp chain but select no draw tier — coordinator's literal reading of the epic's "drawn 4x4 avg"; flag for the user).** Measured (profgen R50, 60 s): walk sim-disc 100% held, star sections 27792->984 (= the real band exactly), flush bakes 1695->375 (-78%), flight 40.7% (up from P2's 38.6%), frames ~17 ms; PROMOTION (halo->real on recenter) = one full seed+flood per column (~23 promotions/60 s flight, 0 unlit frames, 0 mismatches vs reference); DEMOTION = evict_column (nibbles freed, high mesh stored free per keep-all-LOD). Documented boundary effect: real-band slabs adjacent to unseeded halo columns are under-lit by the missing margin (the engine's "unseeded faces drop" semantics — 61 cells at (2,-2) in the halo arm; 1 quad in brightslab's boundary-edit case; max_bright_delta 0) — the engine is the standard there, not the legacy kernel. P3 surfaced + fixed two live-game bugs the halo exposed: the E2->remesh wedge (a settled column's boundary change didn't re-arm neighbor re-bakes through the remesh lane) and the promotion re-bake arm (a demoted->re-promoted column never re-baked its OWN slabs). PERMANENT ARM: `AWECRAFT_LOGIC=halo` (a: real band exact vs reference + boundary ring reported; b: halo cols unseeded + payload == heightmap reference 190/190; c: promote 23, 0 unlit; d: evict 23, 0 unlit — 7.7 s). Gates: all 11 green — starlighttest still 0 mismatches / 5.3M cells; r16 lost 0/0 with the halo streaming (the arm now pins sim 2 to exercise it); battery EXACT P2 pattern (the r4 battery arms are all-real => halo is a byte-exact no-op: 21 byte-identical / 8 jitter / 0 struct / genhash exact); meshprobe 1.0 (r4/sim4 = all-real circle); lightstate R16 census rebased + documented (demoted 41, hole_traj flat 1812 — .scratch/ac0283-p3/report.md); ladder boundary_flip 126->0 by construction (documented); brightslab rebased (boundary case) + ok; 0 script errors.
**NEXT (epic order):** AC-0283 P4 — remove the legacy AweLighting flood + the star=false classic pull path + flush-loop remnants: the legacy kernel must survive in TEST-ONLY scope (starlighttest/brightslab use it as the gold reference — move kernel + Lighting tables to a test-only file, remove every game-code consumer — check the viewmodel light_at, the saved-light capture, the LOD avg light, the fog/ambient paths), then the full re-profile (RP §g arms: startup/flight/walk/horizon, before/after). Then AC-0284 (cheap far DATA: the dormant p_keep slab-mask + heights-only gen, target <0.05 ms/col vs 1.8 ms — the halo light already works off H, so this is the data-side twin), AC-0286 (promotion DATA side: full gen on real-band entry + the no-flash guarantee on top of P3's seed-side promotion), AC-0287 (save filter: write only taxi ≤ sim or edited — register/verify the 10k-flight no-bloat). Windows build for the user is 20260915-2302 on :8082 (= pre-P3: brightslab + walkfix — the bug-fix build to test the break-bright complaint); rebuild after P4.
**OPEN (design doc, for the user):** (a) the real-disc radius — sim_dist=4 is the knob, halo = everything beyond; the epic's "80-200 columns" was loose; (b) the halo is LITERAL 4x4 avg beyond the sim square (the 8x8 med tier is gone as a draw tier) — confirm that's the intended look at 64-128 m; (c) the walk fix's 2.5 s crossing-period regime gate; (d) the Y-window's eventual replacement (fresh-edge priority) stays a scheduler redesign item.

## 00Z. CHECKPOINT 2026-09-15 (r9) - **AC-0283 P1+P2 SHIPPED — Starlight lighting is now in the live stream (commits ef28ac1 + f16606d, pushed; Windows build 20260915-2302 on :8082 for the user — user is back and will test in-game).** The "Flight-ready world" epic is the active plan: AC-0283 in progress (P3/P4 remain), queue AC-0284 → AC-0286 → AC-0287.
**P1 (ef28ac1):** `gdext/src/starlight.cpp` — the C++ `AweStarlight` single-queue light engine (sky+block one queue, 16 level-ordered deques, per-section nibbles, two-phase removal, stateless = zero load cost, edit = queue the pos, sky column-pull + top-down open-carry seam = current behavior reproduced exactly). PROVEN EXACT vs the legacy AweLighting: 5,308,416 cells, 0 mismatches, 0.77x cost — permanent arm `AWECRAFT_LOGIC=starlighttest`. Shared `awelight::Tables` moved to `awe_common.h` (one source of truth).
**P2 (f16606d):** integration — the flush wave is GONE: build-once-at-settle under a per-BOX settle gate (a slab dispatches only when its 3x3x3 bake box = 27 sections is settled; the payload is final by construction; the worker is light-blind, LIGHT phase 0.08 ms), epoch-drop at landing (stale bake -> drop + re-arm, never apply), late-landings via engine reseed (the binding late-landing fix semantics preserved), a remesh lane (1 slab/key/frame) replaces the 1-col/frame flush, edits = on_edit + scoped re-arm. Measured: late-landing churn 5.3->0 and 13.1->0 per s, frames 24->17 ms flight / 25.7->16.6 walk, worker bake 2.5-3x cheaper.
**Two post-merge bugs found+fixed+gated in-tree before the P2 commit:** (1) USER-REPORTED (live test of the P2 tree): "breaking a block, the slab with the broken block re-rendered extremely bright" — root cause: `set_block` bumps `data_gen` BEFORE `_star_rearm_edit`, so its own-column arm condition can never match — the edited column was never re-armed and kept the legacy dirty-lane bake FOREVER (the bright case = the pre-edit glow flood); fix = scoped edit-landing stamp+re-arm over the whole on_edit window + the C++ payload `y_hi` top-margin rows (worker under-lit top-row max probes); permanent arm `AWECRAFT_LOGIC=brightslab` (surface break / cave break / torch place — all exact, max_bright_delta 0.000). The user ALSO confirmed the generation-time random bright slabs are GONE (engine exactness, corroborated in-game). (2) WALK feed stall (sim disc frozen 36-44% vs baseline 100%): P2's slab-unit dispatch + the Y-window — windowed columns only ever bake their player-slab ±1 slabs while moving, so their FULL probe never drains -> `mesh_built` never flips -> `_startup_pending` stuck -> `_spawn_fast` latches -> recenter WANT/STUB slice + the drain data feed both die (`_tg_enq=0` for 60 s). Fix (GDScript, walk-regime-gated, no new timer — the predicate reuses the recenter crossing timestamps: walk ~3.2 s vs flight ~0.8 s crossing period): a startup-3x3 full-probe completion pass (14 ms budget) + break-on-defer cap 16 + a TG-empty data-feed interleave. Walk back to 100% sim disc at t≈8.9 s (baseline ~9 s) and holding; FLIGHT improved 38.6% -> 44.1% (P2's flight was latch-affected too — it idled at 1.9 ms frames).
**Gates (all green, logs + reports in `.scratch/ac0283-p2/`):** boot · starlighttest 0 mismatches / 5.3M cells · r16 ok, lost 0/0 · lightstate R16 census 1685 (inside the 83cfcfe scatter band) + canaries exact (double_lod 0/0, demote-ring 0, demoted 95, wash false) · ladder 128 (126±3) · meshprobe 1.0 · battery 21 byte-identical / 8 jitter / 0 struct-diff / genhash 25 lines byte-identical · brightslab exact · 0 script errors.
**NEXT (epic order):** AC-0283 P3 — far-band heightmap light: taxi > sim_dist gets NO seed / NO nibbles, a heightmap sky bake (15 above top / 0 below / no flood) drawn as 4x4 avg; needs the far flag plumbed scheduler -> seed -> mesh; re-tune the drain governor against the new walk data. P4 — remove AweLighting + flush-loop remnants, rebase the lightstate census. Then AC-0284 (cheap far height-only data via the dormant `keep` slab-mask, target <0.05 ms/col vs 1.8 ms full), AC-0286 (far->sim promotion: ONE full gen + ONE flood, no bright->dark flash), AC-0287 (save filter: write only taxi ≤ sim or edited — a 10k-block flight must not bloat the save). Re-profile after each (baseline `.scratch/worldgen-research/RP-profiling.md`; epic done-when: 1k-col horizon <50 ms, sim disc fed while sprinting, no light pop on promotion, 10k flight no save bloat, walking identical).
**OPEN ITEMS for the user (go into the final design doc):** (a) sim_dist=4 disc vs the epic's "80-200 columns" flight radius — which radius is intended for the REAL disc (R50 disc ≈ 7.8k cols); (b) the walk fix's crossing-period regime gate (2.5 s) — derived from crossing cadence, not a new timer, but review against "no internal timer changes what we do"; (c) the Y-window (83cfcfe, the user-rejected mechanism) was the trigger of the latch chain — its replacement (fresh-edge priority) stays a scheduler item for the redesign, not in these tickets.

## 00Z. CHECKPOINT 2026-09-14 (r8) - **SESSION ENDS — PLANNING HANDOFF FILED (AC-0282)** — user rejected 83cfcfe's timer mechanism: the Y-window/moving-state mode switch is NOT the spec — "there should not be some kind of internal timer that changes what we do" — the deep-fill starvation must EMERGE from the recenter queue (new outer-edge work outranks owed deep work; stopping simply stops recenters). Also asked "we need to rescore all slabs every recenter, are we doing that?" — ANSWER: yes, scores are live per pick (nothing cached; pure function of the live recenter anchor + live player slab + probes) — the gap is the (layer, taxi) SHAPE (layer-major lets a passed deep slab outrank a fresh edge slab). **FILED: AC-0282 (p1, queue head) + `godot/HANDOFF-emergent-queue.md` (read this first in a fresh planning session — spec quotes, remove/keep list, code map, gates, open questions incl. the ahead-lead snapback timer).** 83cfcfe remains the shipped state (gated, user-tested OK in-game).

<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout), 00z moved 2026-09-07 (AC-0231-followup closeout), 00F+00G moved 2026-09-09 (AC-0244/0245/0191 closeout), 00J moved 2026-09-10 (AC-0250 filing), 00K moved 2026-09-10 (AC-0250 closeout), 00L moved 2026-09-10 (AC-0251 closeout), 00M moved 2026-09-10 (AC-0247 closeout + AC-0252 filing), 00N moved 2026-09-10 (AC-0252 closeout), 00O moved 2026-09-10 (AC-0248 closeout + AC-0253/0254 filing), 00Q moved 2026-09-10 (AC-0254 closeout), 00R moved 2026-09-11 (AC-0253 closeout), 00S moved 2026-09-11 (AC-0185 closeout), 00P moved 2026-09-11 (AC-0097 closeout), 00T moved 2026-09-11 (AC-0257 closeout), 00U moved 2026-09-11 (AC-0258 closeout). -->