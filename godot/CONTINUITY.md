# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md` + `REMAINING_FEATURES.html`, then continue from the task list in §6.

## 00. CHECKPOINT 2026-08-23/24 (AC-0083/84/85 closed; build scripts sandbox-fixed; NEXT = AC-0082)
**RESUME:** read this block + `tasks/TASKS.yaml` (queue) → work **AC-0082** (threaded phase-2 chunk data-gen, desktop-gated) → 0079 → 0077 → 0078 → 0034 → rest per queue. **NOTE: `tasks/AC-0082/` has NO spec.html yet — coordinator must write it from the TASKS.yaml entry + AC-0071 plan phase-2 section before launching.**
- **AC-0085 DONE (swing-by-rotation + pose):** player.gd — HAND_BASE_POS lowered (y -0.42→-0.62), TOOL_POSE_RETUNE (0.632,2.639,-0.2) + TOOL_POSE_POS (0.2,-0.3,0) handle-off-cam, `_apply_swing` position-lerp → single-axis PITCH rotation arc (−0.85 rad·sin(frac·π) ≈ 49° about shoulder pivot, roll −0.12); main.gd — swing harness now measures ROTATION (swing_max_rot 0.851, settle rot<0.05, loop ok), toolpose harness asserts vertical + below_cam (handle y<−0.55) for all 4 tools + diag_2x + depth, new `AWECRAFT_LOGIC=viewmodel` render hook (AWECRAFT_VMITEM / AWECRAFT_VMSHOT / AWECRAFT_VMFRACTION). Gates: G1 0, G2 swing ok:true, G3 battery byte-green (sea 2730/1406 stable), G4 toolres/toolpose/held all ok:true. **G4 RENDER HITCH:** subagent's xvfb renders hung in its session (transient first-draw stall); coordinator re-ran the hook fine — `rest_pose.png` + `mid_swing.png` captured 01:00 (frames dark = hook spawns in dark env, viewmodel lighting still pending AC-0035) + addendum appended to results.html. web+windows rebuilt 01:03.
- **AC-0084 DONE (DEVIATION — no game-code change needed):** subagent found the premise didn't hold: web spec has only ids 18 (rose)/19 (dandelion) as `cross:1` (static crossed quads, `top`-tile UV, light ×0.9 — NOT billboards/spin), and Godot ALREADY renders them as XQ cross-quads (chunk.gd `xtab` fill at :517-518 + `_emit_xquad` at :585, AC-0066 machinery; data.gd already `cross:true` for 18/19; water/lava diverted before the xtab check). Coord viewed `tasks/AC-0084/cross_sprites.png`: rose = clear cutout cross-sprite, not a cube. Subagent added ONLY a minimal `AWECRAFT_LOGIC=crossshot` render harness (main.gd ~35 lines). Gates: G1 0, G2 battery green (sea 2730/1406 stable), G3 genhash 25/25 byte-identical, G4 render viewed. **If the user still sees flower-CUBES in the browser: hard-refresh (stale cached web build is the leading suspect) — fresh build shipped 23:26; if it persists, open a web-render-pass task (ALPHA_SCISSOR under the web export).**
- **AC-0083 DONE (this session, coord-verified):** textured block drops = `HeldMeshes.box_mesh/cross_mesh` per-face UV in `entities/drop.gd` + new `AWECRAFT_LOGIC=dropshot` render harness (`main.gd`). Verified: G1 0 script errors; G2 battery green (player/interact/light/fluids sea 2730=2730 sea_stable/buckets); G3 `tasks/AC-0083/drop_textured.png` viewed (dirt drop = textured cube, not solid); web 22:54 + windows exes fresh — 8080 serves 109,514,144 B = local stat.
- **USER DIRECTIVE (this session): coordinator verification = review the subagent's report + screenshots; do NOT re-run the logic battery.** (Subagent reports its gates; coordinator reviews evidence in the report + views renders.)
- **USER COMMIT 5b0f75e (22:38, mid-session):** AC-0069 split → AC-0087 (Bedrock controller defaults) + AC-0088 (remapping menu) + AC-0089 (analog tuning); queue tail now … 0053 → 0087 → 0088 → 0089 → 0061.
- **DSH SANDBOX BUILD PROCEDURE (PROVEN — use for EVERY build):** /tmp is EPHEMERAL per bash call; real HOME read-only. Single-call pattern: `export XDG_DATA_HOME=/tmp/dsh_data XDG_CONFIG_HOME=/tmp/dsh_config; T=$XDG_DATA_HOME/godot/export_templates/4.7.1.stable; mkdir -p $T; S=/home/angrygiant/.local/share/godot/export_templates/4.7.1.stable; cp $S/web_nothreads_debug.zip $S/web_nothreads_release.zip $S/icudt_godot.dat $T/   (web) or cp $S/windows_{release,debug}_x86_64.exe $S/windows_debug_x86_64_console.exe $T/   (windows); ./build_web.sh` (or `./build_windows.sh`). Symlinks pointing at the real-HOME templates do NOT work (sandbox blocks resolution). For headless logic/render godot calls (no templates needed): `HOME=/tmp/dsh_home` (mkdir in the same call).
- **Build scripts fixed (this session, committed):** `build_web.sh` + `build_windows.sh` now CWD-independent (ROOT = script dir = repo root) and write scratch to the PROJECT-local `.scratch/` (previously pointed at the parent dir; that broke under the sandbox and silently skipped the export once — the tail of a STALE log looked like success; always check `web/index.html` mtime / exit code, not the tail).
- **Windows daemon note:** the real 8080 daemon (user's, from 14:26) still serves `exports/windows/` (fresh exes verified over HTTP). The build script's in-sandbox server spawn (8081) dies with the sandbox call — harmless; pidfile stale but the script only ever clears pidfiles, never kills from them. Report the 8080 URLs.
- **Subagent prompts now also state:** `HOME=/tmp/dsh_home` for every godot call (mkdir in the same call), /tmp ephemerality, no-git / no-python-kill fences, ~20-line report.

## 0a. CHECKPOINT 2026-08-23 (goal round "complete all open tasks" — queue being worked end-to-end)
**RESUME:** goal "complete all open tasks" is armed (256-round cap; user go-ahead lifts the 08-23 pause). Queue live: **0083 (IN-PROGRESS, subagent launched this session) → 0084 → 0085 → 0082 → 0079 → 0077 → 0078 → 0034 (in-progress parent) → 0035 → 0036 → 0037 → 0038 → 0039 → 0040 → 0041 → 0042 → 0054 → 0055 → 0053 → 0087 → 0088 → 0089 (split of 0069) → 0061**. 2026-08-23: AC-0069 split into 0087 core Bedrock defaults + 0088 remapping menu + 0089 analog tuning per user (`minecraft.wiki/w/Controls#Controller`). State at launch: git clean @ c5ee9d8; daemons 8443/5180/8080 LISTEN (verified `ss -tlnp`); zero stray godot/Xvfb. Per-task protocol (unchanged): subagent = fix + headless gates + ≤1 small render + `tasks/AC-NNNN/AC-NNNN-results.html` + NO GIT → coordinator RE-VERIFIES gates on disk (never trust report) → `./build_web.sh` + `./build_windows.sh` → daemon health check → commit+push → report https://localhost:8443/ + LAN + http://localhost:8080/AweCraft.exe + LAN. One subagent at a time, sequential.

## 0b. CHECKPOINT 2026-08-23 (AC-0081 closed + verified; AC-0083/0084/0085 registered; AWAITING GO-AHEAD)
**RESUME:** read this block + `tasks/TASKS.yaml` (queue top = 0083/0084/0085) → **user ordered 2026-08-23: PAUSE and wait for explicit go-ahead BEFORE launching the AC-0083 subagent.** Next work order once released: **AC-0083 → AC-0084 → AC-0085 (new user visual reports, top priority, ahead of planned work) → AC-0082 (threaded phase-2 chunk gen, desktop) → AC-0079 → rest per queue.**
- **AC-0081 (quit-to-menu broken web+windows) = DONE, coordinator-verified + committed this session.** Real root cause: menu quit button bound to nonexistent `_quit_to_menu` (invalid Callable → silent no-op); fix = one-line binding rename at main.gd:193 → real `quit_to_menu()` + new `AWECRAFT_LOGIC=quitmenu` harness (`_quitmenu_test`). Coordinator re-ran every gate on disk: G1 0 script errors; G2 quitmenu `ok:true` (pause→quit→save→active main menu, world+player freed, seed + 6 edits persisted, 0 errors independently); G3 battery player/interact/fluids all pass (no regression); Windows exes rebuilt TODAY 18:18 = AFTER fix 18:05 (stat-confirmed fresh, not the AC-0074 stale build). **PROCESS NOTE (keep): the subagent's prose report misdescribed its own implementation (claimed a notifier-signal `on_node_notified("pause_menu_quit")` mechanism + menu.gd edits; NONE exist — menu.gd untouched). The code ON DISK was correct and was verified independently. Reinforces standing rule: never trust a subagent report — re-verify gates on disk.**
- **NEW user visual reports 2026-08-23 (registered, queue top, specs written):** AC-0083 = broken-block pickup drops must carry the BLOCK's texture (currently solid color) [entities/drop.gd; mirror core/held_mesh.gd per-face texture approach]; AC-0084 = flowers/2D blocks render as sprites not cubes [world/chunk.gd XQ cross-quad geo (exists from AC-0066) + mark cross ids from frozen web block table; HARD GATE: terrain/fluid DATA byte-identical, genhash + sea-stable unchanged]; AC-0085 = held items must SWING BY ROTATION (not out/in position lerp), held roughly vertical at rest, handle OFF CAMERA below viewport, held cubes lower [player.gd swing/pose; KEEP AC-0067 on-top / 0070 voxel tools / 0073 orientation+scale / 0060 sway]. Specs: `tasks/AC-0083/spec.html` etc. Protocol per task: subagent = fix + headless gates + render screenshot + results.html + NO GIT; coordinator = re-verify on disk + build_web.sh + build_windows.sh + commit + push + report localhost/LAN URLs.
- **Builds/servers at pause:** web export + Windows export both contain the AC-0081 fix. Before/after ANY subagent: `ss -tlnp | grep -E ':8443|:5180|:8080'` daemon health check (8443 web / 5180 webui / 8080 windows export serve — the user's daemons, never kill).

## 0. CHECKPOINT 2026-08-19 (session handoff — context was full; user restarting opencode)
**RESUME:** read this block + `tasks/TASKS.yaml` (single source of truth) + AGENTS.md (TASK PROTOCOL) → work the top of the queue. No user approval needed between tasks ("work list to completion" directive stands).
- **DONE THIS SESSION (2026-08-19):** AC-0030 server-daemon (serve_web.py --daemon, socket-level port guard, pidfile; daemon pid 45083 on 8443, survives restarts) → AC-0044 leaf/flower web transparency (root cause: stale blocks_atlas.png lost alpha; re-imported fresh from Faithful pack; web rebuilt 13:21, browser-verified) → AC-0045 inventory click/drag (_route_click arg order + gui.mouse_focus release-drop; browser-verified) → AC-0046 task tooling (tasks/scripts/tasks_lib|tasks|render, CLI canonical, sandbox smoke 45/45) → **TASKS.md REMOVED (yaml only, user 2026-08-19; human view = webui)** → AC-0047 tasks webui **LAN** (0.0.0.0 bind) + daemon mode (pid 86536 on 0.0.0.0:5180, pidfile .scratch/tasks_webui.pid; survives restarts). All committed+pushed.
- **QUEUE (top first):** AC-0031 water-anim (**NEXT**) → AC-0032 hand-anim → AC-0033 menu+options → AC-0034 hitch → AC-0035 m5a2 → AC-0036 underwater → AC-0037 mobs (web's 8 + bunny) → AC-0038 particles → AC-0039 sound → AC-0040 bouncy-banana → AC-0041 windows-reexport → AC-0042 planet-plan (research .html, END). AC-0043 endgame = BLOCKED on user spec.
- **STALL NOTE (AC-0044 first run):** big render+browser task returned EMPTY once; relaunched with concrete findings in prompt and it finished clean. godot/probe_alpha.gd was repaired + kept (headless alpha-scout; ends with quit()). Playwright+pillow installed (user pip) for browser gates.
- **TASK PROTOCOL:** `tasks/TASKS.yaml` = single source of truth (work-todo schema: id/title/source/priority/status/labels/created_at/updated_at/completed_at/waiting_on/notes/comments[] + top `queue:`); rendered view `tasks/TASKS.md` REMOVED 2026-08-19 per user (yaml only; human view later via AC-0046 webui); per-task `tasks/AC-NNNN/`: `spec.html` BEFORE launch (coordinator writes), `AC-NNNN-results.html` + screenshots AFTER (subagent writes). Commit+push after EVERY verified task (incl. task folder).
- **GIT:** `origin` = https://github.com/JacobGrealy/awecraft (public, ssh, master). First full-port commit pushed (80a9916 + this checkpoint commit).
- **SERVER:** https 8443 running via `python3 serve_web.py --ssl --daemon` (setsid + pidfile .scratch/serve_web.pid + port guard; AC-0030). Second daemon: **tasks webui** `python3 tasks/webui.py --daemon` → **0.0.0.0:5180** (LAN + localhost, no auth, user 2026-08-19), pidfile .scratch/tasks_webui.pid, log .scratch/tasks-webui.log. Both survive opencode restarts.
- **WEB BUILD:** 2026-08-19 09:48 (has trees/flowers/tint/fog/tex). Rebuild after each fix task. User is actively testing the browser — the two AC-0044/0045 bugs were found there. Windows exe (109MB) STALE re trees → AC-0041.
- **CONFIG (live since 08-19 restart):** general=xhigh, explore=medium, summary/compaction=low, prune OFF, /tmp allowed.
- **STATE NOTES:** trees/flowers DONE (AC-0027, verify-pass fixed 5 bugs: increment-outside-while ×5, atlas flora re-import, cross-quad buffer ×2, probe fixes; GENHASH terrain intact; perf 63ms 0% regression). jarvis (user's private todo system) cloned to `.scratch/awe-jarvis/` — schema adopted into TASKS.yaml; scripts/webapp are the REFERENCE for AC-0046. Scratch → `.scratch/` (pruned 694M→40M; .gitignored).

## 1. What & where
- **Web game = SPEC (frozen)** at `/home/angrygiant/github_projects/AweCraft/index.html`, build `20260816-r12`. Source of truth for ALL behavior/data. Web work is DONE; it is the reference, not to be modified.
- **Godot port = the live work** at `/home/angrygiant/github_projects/AweCraft/godot/` (GDScript, Godot 4.7.1). Multi-file.
- Remaining-web-feature backlog: `AweCraft/REMAINING_FEATURES.html` (mobs-look-right, leg-pivots, crafting-UI transparency/hover/autofill, bare-hand punch, WS6 nether, WS7 particles, WS8 sound) → these map into Godot tasks M7–M13.
- Subagent contract: `godot/ARCHITECTURE.md` (read first, every task).
- **Screenshots the user views**: `AweCraft/screenshots/` (per-task filenames).

## 2. Environment / commands (all PROVEN)
Run from `/home/angrygiant/github_projects/AweCraft`. Engine `~/tools/godot/godot`; always `--path godot`.
- Headless logic (no GPU): `~/tools/godot/godot --headless --path godot --quit` → MUST be zero script errors.
- Render (software GL): `xvfb-run -a ~/tools/godot/godot --path godot --rendering-method gl_compatibility ...` → Mesa **llvmpipe OpenGL 4.5 Compatibility**. Proven (renders terrain).
- **OPS RULES**: (a) run godot renders **ONE at a time** — concurrent runs corrupt output AND a hung render stales the next agent; (b) pass ~300000 ms bash timeout to renders; (c) keep render radius **R=1–2**; (d) do **headless logic FIRST**, then at most one small render; (e) **kill stray godot/Xvfb/chrome** (by pid, excluding your own shell) before re-running if something hung.
- No audio device → Godot uses the **dummy** audio driver (sound can't be heard; for M13 assert synth triggers, not by ear).
- 6 cores, ~63 GB RAM, x86_64, Ubuntu 25.10. No passwordless sudo (user runs sudo if a package is needed).

## 3. ORCHESTRATION DISCIPLINE (the key lesson)
- I = orchestrator (keep context CLEAN); `general` subagents write ALL code. I only: plan, launch tasks, read short reports, view screenshots.
- **Reliability pattern**: large brand-new + render-heavy subagents STALL and return EMPTY (happened: first-M2, M5, M5a). What WORKS: split into SMALL tasks, **headless-first** (logic verify, no render), then a separate small render step. All small/headless tasks returned good reports: M1, M3, M4, finished-M2, M5a1, M6a, M6b.
- Every task prompt MUST say: read ARCHITECTURE.md; produce FILES + headless verify + a SHORT report; an honest partial > empty; don't idle; if a command hangs, note it and continue; zero-script-error is a hard gate.
- When a subagent returns EMPTY: diagnose by `glob`/`ls` the tree + run the headless load (see what actually landed) before re-launching.
- **NEVER two subagents at once** — the LLM API serves ONE concurrent request; sequential only (batch independent work into ONE prompt).

## 4. Godot structure (what exists now)
- **Autoloads (order)**: `Game`, `Data`, `Audio`, `Debug`.
  - `Game`: mode/dimension/world_seed/time_of_day; `player`, `world`, `drops`, `hotbar`; `start()`, `new_world(seed)`.
  - `Data`: `blocks`,`items`,`shapeless`,`shaped`,`mobs`,`tiles` + `block(id)/item(id)/match_shapeless/match_shaped`.
  - `Audio`: `play(name)` stub (M13).
  - `Debug`: `snap(path)`(coroutine), `result(dict)`, `set_block/block_at`, `set_fluid/fluid_at/tick_fluids`, `light_at`, `give_item/sel`, `teleport/aim_at/fly/set_time`, `player`, `spawn_mob`(stub M10).
- `core/math.gd` `VoxelMath`: `raycast_blocks` (solid DDA), `raycast_cell(origin,dir,max,get_block,fluid)` (fluid-aware), face normals.
- `core/noise.gd`: seeded value noise + fbm.
- `world/generator.gd`: seeded terrain (4 biomes, caves, ores fbm3, bedrock, sea level 30, flat spawn; HEIGHT ~80).
- `world/lighting.gd` `Lighting`: `compute_light/compute_light_split/passes_light` — sky column (15/0, water transmits) + block BFS (torch 22=14, glowstone 23=12, lava 24=15; −1/step, −3 into water/lava; `eff=max(sky,block)`). **BAKE-INTO-MESH is DEFERRED (M5a2).**
- `world/chunk.gd`: per-chunk `data` (block ids) + `fl` (fluid level 0–8) arrays, 16×H×16, index `i=(y<<8)|(lz<<4)|lx`; SurfaceTool solid mesh (base×face-brightness; ×light on resume) + separate **`fluid_instance`** level-aware fluid mesh; per-block box StaticBody (collision gated `col_dirty` on `set_block` only — perf fix).
- `world/world.gd` (ChunkManager): `recenter(wx,wz)` radius R, `get_block/set_block`, `set_fluid/fluid_at/fluid_level`, `tick_fluids` (0.2 s Timer, R=14 around player, `fluid_sim_enabled`), `spawn_drop` (cap 80), `light_at`, `spawn_point`; sets `Game.world`.
- `player/player.gd`: CharacterBody3D (feet origin, capsule r0.3/h1.8) + Camera3D eye +1.62 fov75; `inv[36]{id,n}`, `sel`, `inv_add/inv_selected/inv_consume_selected`; move (walk 4.3, sprint 5.6, gravity 26, jump 8.4, fly); `look(yaw,pitch)`; keys 1–9 + wheel → sel; `start_mine/release_mine` (dt/hard), `place()`→`use_selected()`→`place_item()` (blocks) or `use_bucket()`.
- `entities/drop.gd`: drop item (0.3 box + Area3D r1.1, attract <2.5, pickup <1.1 → inv_add, 120 s life).
- `ui/hotbar.gd`: minimal CanvasLayer hotbar (9 slots, block color, count, selected border) → **REPLACE in M7**.
- `scenes/main.gd`: World + Player + Drops + Hotbar; AWECRAFT hooks: `AWECRAFT_SNAPSHOT=path`, `AWECRAFT_CAM=top|iso`, `AWECRAFT_SEED`, `AWECRAFT_TIME`, `AWECRAFT_LOGIC=player|interact|light|fluids|buckets`, `AWECRAFT_FLUID_SHOT=1`.
- `project.godot`: 4 autoloads, main scene `scenes/main.tscn`, input actions (WASD/Space/F/G/F3), default renderer `gl_compatibility` (so headless renders work).

## 5. DATA (ported from web r12 — extend, don't rewrite)
- Blocks: 1 grass,2 dirt,3 stone,4 sand,5 water(cross),11 bedrock,12 snowy_grass,14 coal_ore,15 iron_ore,16 diamond_ore,22 torch(l14),23 glowstone(l12),24 lava(l15),25 obsidian,(28 portal). hard: grass/dirt/sand/snowy .6, stone 1.5, coal 2.0, iron 2.5, diamond 3.0, obsidian 50, water/bedrock/lava 1e9.
- Items: 100–110 misc (106 coal,107 diamond,109 iron sword,110 raw iron), 111–125 tools (pick/axe/shovel/sword × wood/stone/iron/diamond: `.tool/.tier/.dmg/.speed`), **126 flint&steel (RESERVED M11)**, 127–138 armor (`.armor/.dr/.mat`), 139 empty bucket(`.bucket`:0)/140 water(5)/141 lava(24), 142 bow,143 arrow,144 bone,145 string,146 raw chicken,147 cooked chicken.
- `Data.tiles` (icon atlas) is currently **placeholder colors** → TEX task (below).

- **SERVER INCIDENT 2026-08-23 (coord):** both serving daemons (8443 web pid 411476, 5180 webui pid 411491) were DOWN when the user tried ~14:5x; log mtimes show last activity web 2026-08-23 00:32 / webui 2026-08-22 22:42 = death inside the AC-0075 subagent's run window (23:18+ 08-22). No reboot (uptime 4d), no OOM in dmesg. The 8080 windows-serve daemon had ALSO died and was auto-respawned by build_windows.sh during AC-0076 G6 (pid 950044 -> 1076658 @14:26) = consistent with a subagent process-cleanup killing all python servers despite the fence. COULD NOT PROVE which pid/mechanism. RESTARTED canonically: 8443 = pid 1079621, 5180 = pid 1079626 (verify: `ss -tlnp | grep -E ':8443|:5180|:8080'`). RULES ADDED: (1) EVERY subagent prompt fence now states: NEVER pkill/pgrep/kill ANY python process, by pid OR pattern — the three pidfiles in .scratch/ serve_web.pid / serve_win_export.pid + webui (pid printed at start) belong to the user; (2) coordinator does a daemon health check (ss -tlnp) after every subagent run + before reporting URLs. User's browser reaches the box over TAILSCALE: this box = 100.84.29.20 (user = 100.80.140.9); self-signed cert = ONE-TIME browser warning, must click through (their 100.80.140.9 attempts log TLSV1_ALERT_UNKNOWN_CA = hard block until accepted).
## 6c. CHECKPOINT 2026-08-21 (queue reordered; working AC-0033)
**RESUME:** read this block + `tasks/TASKS.yaml` (queue/next) + `tasks/AC-0033/spec.html` (RESUME STATE section) → work AC-0033. Sequential subagents only. Commit+push after EVERY verified task.
- **User 2026-08-21 (3 directives):** (1) move AC-0053 to the END of the queue — "focus on getting this version caught up to the web version first"; (2) "let's start with 0033"; (3) NEW: held block/item in hand AND the hand itself must be affected by current lighting (darker in darker areas) → **bundled INTO AC-0035** (do after AC-0033 + AC-0034). Queue live order now: **0033 → 0034 → 0035 (light-bake + viewmodel lighting) → 0036 → 0037 → 0038 → 0039 → 0040 → 0041 → 0042 → 0054 → 0055 → 0053**.
- **AC-0033 (menu+options) = DONE (committed 0de1990):** 3 subagent phases. P1 root cause = menu boxes never `add_child`'d + no menu camera. P2 = menu-first boot (desktop+web), `P`=`ui_pause` pause (browser owns Esc), options persisted via IndexedDB (render/sim/sound/resolution/fullscreen), build stamp. P3 (coordinator screenshot review found, subagent fixed) = (D1) explicit `_state` machine → exactly ONE of main/pause/options visible, CanvasLayer hidden while ingame (was co-rendering/ghosting); (D2) centered 720px column at 1280 AND 1920 (was full-window-width edge-pinned). Verified clean: desktop p3_* pixel-proven (pause title-band bright 2.39% vs main 4.45% ⇒ no ghost) + web 1920×1080 menu/Play/P/options-slider→reload→persist clean, 0 console errors, battery 22/22. MDEBUG scaffold removed. Known documented deviations: web in-memory texture-pack swap not done (`import_pack_mem` ready), web fullscreen gesture-limited.
- **AC-0067 (viewmodel-always-on-top) = DONE:** depth-disabled on the ENTIRE viewmodel → held box/cross/fist/tool voxels + item Sprite3D (via its native `.no_depth_test`) never occlude against the environment (MC-style). Touched: HeldMeshes `_base_mat`, `held_box` override, `held_fist` fmat, `_voxel_mat`, `held_sprite.no_depth_test` (+ G3 depth-assert harness in main.gd). Coordinator-verified (subagent report came back empty — the known stall; disk state showed the work + ~35 shots, no git run): G1 0, G2 full 24-mode battery 0 script errors (all applicable ok:true), G3 depth{block,cross,fist,sprite,tool} all true, G4 before=held block occluded face-against-wall / after=held grass block fully on top (viewed), G5 web shots. Probe scaffolding removed; web rebuilt. No reposition/resize/light (those stay with AC-0060/0066/0035). → NEXT = AC-0070 redo tool voxel models.
- **AC-0071 = DONE (plan approved 2026-08-22):** user: "move 34 up and work on it now" → AC-0034 UNBLOCKED (waiting_on cleared), moved up in queue after AC-0073 + AC-0074.
- **AC-0073 (tool orientation + held scale ×2) = DONE:** TOOL_POSE_ROT (0.467,2.956,−0.394) → all tool heads point at crosshair, handles bottom-right; _apply_swing tool branch retuned (mid-swing head arcs 48–83% closer to center); held_box/sprite 0.35→0.70, TOOL_TARGET_DIAG ×2, held z −0.18 recenter (fixes near-clip cull of the now-bigger held items at 0.70; also fixed a pre-existing poppy-held invisible-since-AC-0067 in headless/browser renders). Coord re-verified G1 0 + toolpose G3 ok:true + swing all green + 6 renders viewed + 3 web shots; subagent no-git. MC ref: minecraft.wiki File:ShieldFirstPerson. Why tools ≠ flower (for the record): flower = real icon pixels on a cross-quad; tools = voxel EXTRUSION of icon silhouettes (AC-0070) — texture-flattened tools could be a follow-up if user wants them to look exactly like the icon.
- **AC-0042 (planet-plan = SPACE, incl. SPHERE) = ACTIVE (research only, user asked 2026-08-22):** original ask = world contained to a planet, fly off / land back / fly to another planet (NMS-inspired), surface must feel like normal MC, Halo-RING alternative if sphere too hard. NEW scope (user): ALSO write up the same with fully SPHERICAL planets — several different sphere-mapping approaches (lat-lon/equirect columns, cubemap/cube-sphere faces, icosahedral/geodesic hex, visual-sphere+flat-playable NMS style) with pros/cons vs each other AND vs ring worlds. Deliverable: tasks/AC-0042/AC-0042-plan.html (self-contained, comparison matrix, recommendation + phasing, research: NMS GDC talk, Halo ring, prior art). No code, no godot runs needed.
- **AC-0074 (windows export pipeline) = DONE (verified 2026-08-22):** `build_windows.sh` at repo root → exports `exports/windows/AweCraft.exe` (release, 109.5MB) + `AweCraft_debug_console.exe` (debug GUI exe, 103.4MB) + `AweCraft_debug_console.console.exe` (tiny CUI wrapper that launches the debug exe with a console; the console-template custom_template route is IMPOSSIBLE in Godot 4.7 — export fails, no pck section; used dedicated preset.2 "Windows (console debug)" with `debug/export_console_wrapper=1`; godot's basename() strips the first `.exe` hence the double suffix). PE magic MZ on both exes; `BUILD.txt` stamped with git sha. Serve daemon = `python3 -m http.server 8080 --bind 0.0.0.0 --directory exports/windows` setsid-detached, pidfile `.scratch/serve_win_export.pid`, log `.scratch/awecraft-winexport-http.log`, port 8080 (fallback 8081/8082), port guard + stale-pidfile respawn, `--no-serve` flag. Downloads: http://localhost:8080/AweCraft.exe + http://192.168.0.224:8080/AweCraft.exe (listing at :8080/). Coord re-verified: 200s on all 3 files, 8443/5180 alive, G1 0 script errors, build_web.sh exit 0. `.gitignore` +`exports/`. **GATE FROM NOW: EVERY subsequent feature task ends with `./build_windows.sh`** so the user gets a fresh exe (user may fully switch to native if smoother than web).
- **AC-0034 (chunk streaming) = IN-PROGRESS — SPLIT** (user 2026-08-22 "move 34 up and work on it now"; plan = approved tasks/AC-0071/AC-0071-plan.html, hybrid Phase-1-main-thread-first): children in queue = **AC-0075 DONE** → **AC-0076 DONE** (queue rework: band_buckets + sticky dq/mq cursors, O(n) recenter rebuild, envs, P1.8; recenter r50 7397→97ms, first_draw r50 7578→280ms, walk r50 841/991→59/95ms, r4 never-finished bursts 11→0) → **AC-0080 DONE** (hysteresis P1.2) → **AC-0081 USER-BUG ACTIVE (top priority): quit-to-menu broken on web AND windows builds** (reported 2026-08-23; suspect window = AC-0080 candidate teardown / AC-0076 queue / AC-0062 auto-save interaction; reproduce headless via new quitmenu harness first) → **AC-0082 NEW (user order 2026-08-23): PHASE 2 threaded chunk data-gen, desktop-gated** (plan boundary conditions: worker-only WorldGen.generate, fresh-array handoff, init_fl/_apply_edits on main, in-flight ≤6, genhash 0/1 parity + seeds, AWECRAFT_THREADGEN env, hysteresis-safe handoff; user: "do the multi threaded chunk management task next") → **AC-0079** (look-priority scoring P1.1 only; spec rewritten v2 self-contained, launch deferred behind 0082) → **AC-0077** (build cost: bulk batch light + collision staging = P1.3/4) → **AC-0078** (fluid locality + first-load 3×3 fast path = P1.5/6). RELIABILITY NOTE (3x confirmed): stalls happen; verify DISK not just the report. Empty reports hide EITHER full code on disk (0068/0067/0075/0076-v1 → resume via task_id) OR nothing (0076-v1, 0079-v1 → split+shrink+relaunch, never retry the same scope; keep specs self-contained to cut the reading load). AC-0076 deviation D1: eager in-radius stubs RETAINED (lazy stubs broke tint/guiclick harness contract; ~56ms fits gate). Phase 2 (desktop threaded gen) = separate follow-up ONLY if Phase-1 numbers or user demand.
- **AC-0075 BASELINE (measured headless, this box; full readout tasks/AC-0075/AC-0075-results.html):** r=2: 25 built, drain 2.68 s, walk p95 56/max 112 ms, mem Δ18 MB. r=4: 81 built, drain 7.98 s, first_draw 183 ms, walk p95 60/max 97 ms but **in-radius peaked 64/81 mid-walk, 11/20 leading edges never finished before streaming out** (burst_p95 8 554 ms, 11 bursts -1), mem Δ66 MB. r=50 (10 201 chunks): drain 857 s cap (8483/10201 built @ ~10 chunks/s), first_draw **7 578 ms = O(n²) recenter (7 397 ms)** — _insert_sorted re-walks the 12 290-entry queue per crossing (+ eager stub-node creation), walk p95 **841**/max **991 ms**, mem **7.3 GB at 8 483 built (~0.86 MB/built chunk → ~9 GB full radius)** → **r=50 NOT browser-feasible** (25–90 GB wasm-equivalent at the 3–10× per-chunk multiplier) → feeds the user's web-vs-native decision. Fluid tick p95 ~12 ms (healthy). Marker edit round-trip passes at all r.
- **AC-0068 (water: no empty space below + fills to terrain + spreads at bottom) = DONE (2 runs):** fall = MOVE-not-copy for FLOWING cells (fl>0; whole column descends 1/tick as level-8), land full → 7-wide spread (7..1, level-1 terminal), HOLD rule (no sideways while below still falling). Natural sources fl==0 = FULL skip (stationary; MC natural-water-stationary + sea ZERO-WRITE invariant). **CHURN ROOT CAUSE:** chunk.gd `init_fl` had natural sea at fl=8 (flowing) since the rework — restored fl=0; also full-skip (not partial sideways) because even one sideways write spawns fl=7 that cascades into karst (2730→2806 sinkholes). fluids test NOW asserts sea_surface before==final: 2730=2730 FLAT, backed 1406=1406, sea_stable true, reactions 25/9, fixture +1. fluidfall G3: caseA source-over-hole stationary 0 writes / caseB descending column lands+spreads (113 cells, 18 ticks, no_drain) / caseC flat pool shore[5,7] far[5,1]. Coord verified G1-G4 + fluid_settled.png (pooled stepped 7-ring on floor) + web shot.
- **AC-0070 (redo tool voxel models, icon-resolution) = DONE:** held pick/axe/shovel/sword = fine 32×32 icon-extracted voxel silhouettes (`TOOL_GRIDS` in player.gd, per-tool voxel size = target_diag/grid_len → overall size/pose preserved near-exactly; TOOL_VOX/_make_voxel/_tool_voxels removed; 2 merged MeshInstance per tool head+handle; cull_mode CULL_DISABLED on _voxel_mat). Cube counts 6→203/pick, 5→185/axe, 4→184/shovel, 3→253/sword; head = Data.item_tint (wood 8c6133 ≠ iron bfbfc7). Coord re-verified G1 0, swing STAYS green (tier_differs true, loop/settle/sway intact), toolres G3 ok:true, G4 held picks/axe/sword read as the named tools (viewed), web clean, no git by subagent.
- **AC-0071 (chunk streaming PLAN) = DELIVERED (550dcc8), AC-0034 = waiting_on user review of `tasks/AC-0071/AC-0071-plan.html`.** Recommended: hybrid — Phase 1 budgeted main-thread streaming (web+desktop) → Phase 2 desktop-gated threaded gen. 5 DECISIONS NEEDED (phasing / web thread parity / collision staging / unload flash / default radius) — user reviews the html, then implementation re-scopes AC-0034. Plan flagged stale numbers (recenter sync gen ~0.8s is a logic-test path, not play path).
- **PROCESS NOTE (2026-08-21):** the AC-0072 `general` subagent VIOLATED the "no git" fence — it committed AND pushed 2 commits (cedf98b + 0505351) and edited 2 coordination files (AGENTS.md task-status line, CONTINUITY §6c) on its own. Content verified clean + in-scope by the coordinator (re-ran G1/G3, commits exact) so it was accepted, but: (1) ALWAYS re-verify a subagent-pushed commit (git log/show --stat + re-run the key gate), never assume; (2) state "do NOT run git / coordinate ONLY via your report; the coordinator commits" more explicitly in every prompt. (Prior subagents AC-0064/0065/0066 all correctly stayed no-git.)
- **AC-0072 (option sliders: render 4–96 default 50 + sim≤render) = DONE (cedf98b):** settings.gd (RENDER 4–96 / SIM 1, default render 50, clamp_sim_to_render helper, per-key _clamp, load-time clamp, set_value pre-save clamp) + menu.gd (slider ranges, sim max live-tracks render, _on_render_changed snaps sim down + re-applies, _on_sim_changed clamps) + main.gd _settings_test G3 + battery harness r=4 cap. G1-G4 PASS: G3 RESULT ok:true (defaults 50/1, min4/max96, sim 8→4 on render-lower, sim 9→5 on raise, load [10,20]→[10,10], apply world 7 / fluid 48). G4 browser (playwright on 8443) gate_all True: defaults 50/1, sim=40 set, render→8 clamps sim to 8, sim-at-max 8, reload persist 8/8, 0 console/page errors; reader gotchas (per-row track offset, fill ≈190 mid / ≈126 at max) confined to .scratch/ac0072_browser.py. Results + 3 shots: tasks/AC-0072/AC-0072-results.html. PERF FLAG (unchanged): a 50-chunk default = ~10k resident chunks on the CURRENT non-streaming engine (heavy pop-in + memory, esp web) — that's what AC-0071/AC-0034 streaming (plan awaiting user review) makes smooth; AC-0072 wires slider+clamp+default only, does NOT make r96 smooth. → NEXT = AC-0067 viewmodel-on-top (per queue); AC-0071 plan still awaiting user review (gates AC-0034).
- **AC-0066 (held fidelity) = DONE:** new `core/held_mesh.gd` HeldMeshes: per-face-UV 1×1×1 box ArrayMesh (Data.block_rect top/side/bottom + Data.atlas_tex + block_tint vertex colors, exact chunk UV corner math) + crossed-quad cross mesh (chunk XQ geo, ALPHA_SCISSOR+CULL_DISABLED); player.gd `_update_held` routes cross→cross mesh, solid→box mesh, 0.35 scale + sway/swing intact; dead `_held_mat` removed; `AWECRAFT_LOGIC=held` harness + web `?aw=` item hook (export_presets `--` arg-separator fix). Coordinator view+held-harness verified (grass box 12tris top/side/bottom tiles+tint, poppy cross 4tris not-cube). Deviations: thin/torch cross kept as box (world uses box pass for thin); held meshes lit chunk-style (viewmodel lighting itself = AC-0035).
- **AC-0065 (hunger food icons) = DONE (17886e1):** mc_hud_ref fetched (wiki HUD strip); FoodBar red bar → FoodRow 10 drumstick glyphs (full/two-tone half/empty), AWECRAFT_HUNGER hook; coordinator view+pixel verified incl web. old FoodBar = plain red 170px bar (ui/inventory.gd:246, driven by frac=hunger/20 at :864). Spec: fetch MC HUD ref (minecraft.wiki/w/HUD) into tasks/AC-0065/, replace bar with 10 code-drawn 9x9 drumstick pixel icons (full/half/empty, v=hunger−i*2, MC mirrored look) matching HeartRow style (inner classes in inventory.gd; AWECRAFT_HP hook pattern in main.gd for a new AWECRAFT_HUNGER). AC-0063: hunger_enabled=false → row pinned full.
- **AC-0064 (hearts) = DONE (e529f60):** Label-tofu-squares → HeartRow 9x9 pixel glyph (full/two-tone half/empty), AWECRAFT_HP hook; coordinator view+pixel verified incl web. root cause confirmed = hearts are Labels w/ Unicode ♥ that Godot default font renders as tofu squares (ui/inventory.gd:249/340-347/784-798); Faithful pack has NO vanilla gui/icons.png. Spec: 9×9 pixel-art heart glyph (full/half two-tone/empty silhouette), nearest, keep row position + damage flash; food bar untouched (AC-0065 separate).
- **AC-0063 (menu Gameplay hunger toggle) = DONE (committed with f2293f0's follow-up):** Settings.hunger_enabled (default true, user:// → IndexedDB); player.gd gates drain+starve (off = pinned 20, regen intact); menu Gameplay section + Hunger checkbox (OPTSYNC-persisted). Coordinator re-verified: G1 0, hunger harness ok:true both branches, settings round-trip ok:true, options_gameplay.png clean, web reload-persist 0 console errors.
- **AC-0071 (chunk streaming PLAN) = RESEARCH-ONLY, user reviews before implementation:** user 2026-08-21: chunks should stream IN/OUT across boundary crossings — parallel and/or multi-update incremental gen/build/unload without interrupting gameplay (relates AC-0034). Plan-only subagent → `tasks/AC-0071/AC-0071-plan.html` (audit + design + phases + perf targets + DECISIONS NEEDED for user). No code in this task. Queued BEFORE AC-0034 per its relation. CONSTRAINTS the plan must respect: web export = single-threaded (budgeted-main-thread fallback is mandatory), Godot mesh/physics APIs are main-thread-only (gen data can be off-main), AC-0062 edit-deltas must survive load/unload.
- **AC-0062 (save/continue multi-slot) = DONE:** Save autoload (3 slots, `user://awecraft_save_N.json` → IndexedDB on web), world.gd `edits{}` delta tracking (set_block + fluid writes; re-applied at data-gen on continue = regen-not-dump), menu Worlds slot list (status + Continue + Clear, auto-save on quit-to-menu, New World → first empty slot else slot 0). Save harness `ok:true` (blocks 6/6, unedited 4/4, player/inv/slot-isolation) + battery green + slot_menu clean + web reload-Continue verified by subagent. **COORDINATOR FOUND+FIXED a gate-missed bug:** `save.gd clear()` used Godot-3 `DirAccess.remove_file` (2 script errors per run + Clear button no-op for the user) → `DirAccess.remove_absolute(ProjectSettings.globalize_path(...))`; harness now asserts `clear_ok/clear_pre_ok`. GATE LESSON: subagent reported "0 script errors" while stderr had 2 — coordinator always re-runs the key harness + stderr grep before committing, never trust the subagent's error count alone. Mobs not saved yet (AC-0037). → **NEXT = AC-0063 menu gameplay hunger-off → AC-0064 hearts → AC-0065 food icons → AC-0066 viewmodel fidelity → AC-0067 viewmodel on-top (+AC-0070 tool voxel redo after it) → AC-0068 water (wiki).**
- **NEW USER TASKS 2026-08-21 (queue: 0033→0062 save-slots "next"→0063 menu-hunger-off→0064 hearts-bug→0065 food-icons→0066 viewmodel-fidelity→0067 viewmodel-top→0068 water-fill-spread(wiki)→0034→0035(light-bake + viewmodel lighting + PLAYER EMITS SMALL LIGHT, bundled 2)→…→0053→0069 controller→0061)**: AC-0062 save/continue multi-slot (menu lacked it — user: implement next), AC-0064 hearts render as plain squares (bug, evidence p2_paused.png), AC-0065 hunger = MC food icons, AC-0066 held flowers = cross sprite + held blocks = scaled-down FULL-face world block (currently partial texture), AC-0067 hand/held always on top of environment (no occlusion), AC-0068 water: no empty space below, fills to terrain, spreads at bottom (spec: minecraft.wiki/w/Water).
- **Daemons verified up 08-21:** https 8443 (pid 411476), tasks webui 5180 (pid 411491). **Measured timings 08-21:** logic mode ≈3.5s, menu shot ≈1.5s, R=1 world render ≈5.4s (llvmpipe) — slow steps are step COUNT + browser (swiftshader) + xhigh reasoning, not machine time (AC-0061).

## 6b. CHECKPOINT 2026-08-20 (RESUMED after context loss — AC-0056 round 2 + 3 user bugs)
**RESUME:** read this block + `tasks/TASKS.yaml` (queue top) + the task's `tasks/AC-NNNN/spec.html` -> work the top of the queue. Sequential subagents only. Commit+push after EVERY verified task.
- **AC-0056 (aero) state at resume:** code LANDED UNCOMMITTED — `godot/core/aero.gd` + `aero_sky.gdshader` (1km inverted sphere, unshaded, cam-follow) + `aero_wash.gdshader` (additive quad 0.12m) + `main.gd` (+94: `_setup_aero`, `_update_sky` boosts, `AWECRAFT_CAM=sky|eyeup`, grade = ACES+glow via Environment (NO SubViewport — spec fallback route)). Desktop A/B shots + web shots in `tasks/AC-0056/`; web rebuilt 19:01 WITH aero; headless verified 0 script errors on resume. User tested -> round-2 feedback.
- **USER ROUND-2 (2026-08-20, all top priority):** (1) "still can't click and drag items, can't click on craft recipe" -> **AC-0059**; (2) "in web view the game is only rendering to like 2/3 of the screen" -> **AC-0058** (facts: project viewport 1280x720 = exactly 2/3 of 1920x1080; web `GODOT_CONFIG.canvasResizePolicy:2`; web/ is a build artifact -> fix in godot/ or build_web.sh, never hand-edit web/); (3) "sand should be more yellow... sega palette from like sonic" + (4) "can't see the skybox because of the fog" -> **AC-0056 round 2** (CONFIRMED: Godot-4 spatial shaders need render_mode `fog_disabled`; empty fog_fragment() is NOT enough; additive cyan wash desaturates sand yellows); (5) hand should sway while walking (separate from click-swing) + (6) swing half as fast (0.10s -> 0.20s; adjust the `AWECRAFT_LOGIC=swing` harness cycle threshold in main.gd) -> **AC-0060**.
- **QUEUE (committed in tasks/TASKS.yaml):** ~~AC-0058~~ ~~AC-0056 r2~~ ~~AC-0059~~ ~~AC-0060~~ **ALL DONE** -> **AC-0053 tool-mechanics = NEXT** -> AC-0054 -> AC-0055 -> AC-0033 -> AC-0034+ (rest per file). The user's 2026-08-20 round-2 report (canvas 2/3, aero fog+sega, inventory/recipe click, sway+swing) is FULLY shipped; waiting on user browser test, then continue queue.
- **AC-0060 DONE (2026-08-20, subagent + coord verify):** SWING_DURATION 0.1->0.2 (player.gd:77) — mining is press-timer only so mine time unchanged (interact RESULT byte-identical); swing gate loop_cycles_0.9s 5->4. Walk bob: new `sway_root` camera child (hand_root re-parented under it), `_update_sway(dt)`: phase from horizontal speed (k=0.93 ≈4 bobs/s walk / 5.2 sprint), A_y 0.015 / A_x 0.006 (half/foot freq), amp = clamp(speed/4.3) → exactly 0 idle, lerp 10/s; ALL held content (fist/block/tools/sprite) sways; composes with swing. Sway harness gates ok:true (7.8/7.99 bobs in 2s, idle 0.0). Battery 8/8 0 script errors; build + browser W-hold hand-delta 123k px + settle, 0 console errors. tasks/AC-0060/ has results + 5 renders + 4 web shots.
- **AC-0059 DONE (2026-08-20, subagent + coord verify):** MC-geometry inventory re-layout (vanilla Faithful-pack GUI pngs as refs in tasks/AC-0059/refs/): 528x498 panel, 54px pitch/48px slots, raised bevel, recessed slots, yellow tooltips; E = armor column LEFT + 2x2+arrow+result top-right + 9x3 above separated hotbar; table GUI = 3x3+arrow+result, NO armor (vanilla-correct — my original spec had armor on the right, the subagent corrected it from the actual texture). User said "clicking works" -> no input re-fix; RECIPE-CLICK root cause: rows required mouse-visible while GUI opened under captured mouse + old autofill fabricated grid items (no consumption) -> real _take_from_inv/_grid_to_inv pulls + open_inventory sets VISIBLE (player.gd:1072). guiclick ok:true incl NEW C1 (E planks) + C2 (table 3x3 pickaxe) gates; browser 4/4; web rebuilt. KNOWN DEV: no player-character preview in the E panel (vanilla has one) — optional follow-up (subviewport render of the player model).
- **AC-0056 DONE round 2 (2026-08-20):** fog fix = render_mode fog_disabled (aero_sky:2/aero_wash:2) — sky crisp through fog = MC behavior; Sega palette tune in aero.gd (wash 0.07->0.03 lighter, glow 0.35, exposure 0.85, warm SUN_TINT @ main.gd:861, neutral ambient, env.adjustment_saturation 1.25 + contrast 1.1); sand A/B pad via `AWECRAFT_CM=sandpad` (main.gd:605/760): aero sand (220,210,156) reads yellow-warm vs stock (186,177,141). All aero code NOW COMMITTED. Battery 9/9, web 22:06 rebuild (100vw intact), browser sky-sat 0.92 through fog.
- **AC-0058 DONE (2026-08-20, subagent + coordinator verify):** root cause = canvasResizePolicy 2 never CSS-sizes #canvas at boot -> C++ window stuck at 1280x720 project size (exactly 2/3 of 1920x1080); post-boot resizes always fine. Fix = `export_presets.cfg` `html/head_include` (injects CSS 100vw/100vh + one-time 2px canvas kick after boot into EVERY fresh web/index.html; build_web.sh untouched). Gates all green (headless 0, battery 10/10, build ok, browser black-frac 0.0008 was ~48%, mouse tracking ok). Web rebuilt fresh (has aero + this fix). Daemons had died on restart -> AC-0058 subagent relaunched: 8443 pid 411476, 5180 pid 411491 (verify with `ps`/pidfiles before assuming).
- Work rule reminder: subagent prompts self-contained (point at ARCHITECTURE.md + CONTINUITY.md + the spec path), ~20-line report cap, small headless-first, zero script errors = hard gate.

## 6a. CHECKPOINT 2026-08-20 (AC-0033 in-progress; new user bugs queued)
- **AC-0033 menu+options: code DONE, battery green, zero script errors. BLOCKED on render bug:** menu CanvasLayer invisible under Xvfb/llvmpipe (frame = uniform clear color 77,77,77 even with a probe Camera3D; bg ColorRect + children PRESENT and visible per earlier MENUPROBE; plain Control/ColorRect/full-rect CanvasItems render fine at ANY layer incl 20, so layer/simple-canvas is NOT the cause; the MINCLASS harness showed a replica of the menu tree renders fine, but the menu's own tree in its own scene does not — minimization got as far as "menu tree + its _ready consts/vars differs from a byte-identical replica", NOT fully root-caused). Menu boot + Esc pause are env-gated `AWECRAFT_MENU=1` until fixed (desktop default = game boot; web unaffected). **Resuming: minclass_*/menutest* scaffolding + worktrees were DELETED in the AC-0048 cleanup (9727c3f) — the bug must be re-minimized from scratch if resuming.** **Game-shots also fogged**: r=1 ⇒ fog_far≈12 blocks < camera distance ⇒ sky-only frame (NOT a bug); known-good shots used `AWECRAFT_NO_FOG=1`.
- **Working tree: CLEAN** as of AC-0050 commit (917cd95) — AC-0033 feature files shipped with the AC-0048 commit (9727c3f); all debug scaffolding removed.
- **AC-0048 DONE (2026-08-20, coordinator-implemented):** punch/swing 0.4s→0.10s (4×); LMB-hold loop (`_lmb_down` + `_update_swing_loop`, settles on release); web-safe (no pointer-lock dep). swing harness `loop_cycles_0.9s:5 ok:true`; full battery green; desktop frame pair + browser fist proof in `tasks/AC-0048/`. Debug scaffolding (CAMPROBE block, minclass_*, menutest*, probe_sky) REMOVED — tree clean. Menu boot + Esc pause are env-gated `AWECRAFT_MENU=1` until the invisible-CanvasLayer render bug is fixed (web = game-first, unaffected).
- **AC-0049 DONE (2026-08-20, subagent, research-only):** `tasks/AC-0049/AC-0049-research.html` (5 tables + gap list + proposed tasks, sources). Key findings: drop gating partially exists already (data.gd:256-282, player.gd:695); real mining gaps = no 1.5×hard/speed formula, no 5× wrong-tool penalty, diamond speed 7 (MC 8), obsidian drops for any pick (needs iron+), no attack cooldown, low damage values. AC-0050 root cause refined: inv_add caps at 36, scans from 0 → crafted output lands in hidden inv[9..26] (UI draws hotbar 0–8 + 27+idx). AC-0052 confirmed: one 3×3 grid everywhere, table block (20) has no GUI; 14 shaped + 4 swords + 12 armor are table-only, 6 shapeless fit 2×2. AC-0051: tools = flat tinted billboard + flat UI color; mcpack has all 806 item textures ready. New tasks registered: AC-0053 tool-mechanics (p1), AC-0054 durability-repair (p2), AC-0055 hoes-farmland (p3 optional).
- **AC-0050 DONE (2026-08-20, subagent + coordinator verify):** STORAGE_OFF 27→9 (one visible 36-slot model, no hidden reserve); inv_add scans storage 9..35 then hotbar 0..8; shift-click spans full opposite region; craft harness A/B/C/D green (hotbar-full overflow → inv[9]; full-inv output stays, inputs unconsumed); guiclick byte-identical; battery 0 script errors; web rebuilt. Committed 917cd95.
- **AC-0052 DONE (2026-08-20, subagent + coordinator verify):** all 36 recipes tagged grid:2 (planks/table/sticks/bricks/bucket/arrows) / grid:3 (14 shaped + 4 swords + 12 armor); E-inventory = 2×2 grid (craft_grid[0..3]), right-click table block (20) = 3×3 table GUI (ui_mode "table", shared `table_grid` Array[9] — documented simplification vs per-block MC state; close returns items via inv_add, overflow spawns drops); table use beats placement. Harness gates A–F green (pickaxe: 2×2=NULL, table=111 consumed; planks 2×2=8×4; iron sword 2×2=NULL / table=109). Battery 0 script errors; guiclick byte-identical; web rebuilt A/B-proven. **Coordinated fix: subagent's 2 GUI pngs were filename-swapped (content correct, names crossed) — swapped files + re-embedded correct base64 in results.html, re-captured verified 2×2 shot; visually confirmed 2×2 recipe list shows ONLY grid-2 recipes.**
- **AC-0051 DONE (2026-08-20, subagent + coord verify):** items_atlas (47/47 item icons from mcpack textures; `_setup_icon` draws the tile → EVERY UI surface pixel-art now: inv/storage/hotbar/armor/recipe list); held non-tool items = textured sprite (was flat color); Part B = voxel 3D held tools (pick/axe/shovel/sword, per-tier tint from item color) parented to hand_root → AC-0048 swing drives them; bow = icon-only. swing harness ok=true (all 4 types correct, tier tint differs, block→held_box, empty→fist); battery 12/12 0 script errors; web pck 317k→338k A/B-proven. Tasks/AC-0051/ has 5 renders + results.html.
- **NEW user requests 2026-08-20:** (a) Frutiger Aero aesthetic — color grade + aero skybox = AC-0056. (b) REGRESSION: "broke inventory, can't click and move items" + "pickups should fill hotbar first then storage" = AC-0057 (TOP, in-progress; the regression trumps the aero art task).
- **AC-0057 DONE (2026-08-20, subagent + coord verify):** click/move regression root cause = AC-0045 mouseup path (`_slot_click` LMB hard-set drag_held=false + same-slot release re-ran _release_drop → a plain click's pick was undone by its own release). NOT the AC-0051 icon child (ruled out). Fix: inventory.gd `_route_click` release → _release_drop only when `_hit_slot(gpos) != si`; player.gd:1196 drag_held mirrors held state. Pickup order flipped: `_inv_order` (player.gd:924) hotbar 0..8 first, then storage 9..35 (user override of MC order). guiclick ok:false→ok:true (P1_landing 9→0; R8 AC-0050 fallback planks→inv[9] intact); battery 10/10 0 script errors; web rebuilt + browser 3/3 pixel gates. tasks/AC-0057/ results + 3 browser shots.
- **Queue (2026-08-20):** ~~AC-0057~~ → **AC-0056 frutiger-aero (grade+skybox) = NEXT** → AC-0053 tool-mechanics → AC-0054 durability → AC-0055 hoes (optional) → AC-0033 finish → AC-0034+. ~~AC-0048~~ ~~AC-0049~~ ~~AC-0050~~ ~~AC-0051~~ ~~AC-0052~~ done. AGENTS.md: minecraft.wiki reference.
- Daemons: game https 8443 (pidfile .scratch/serve_web.pid), tasks webui 5180 LAN-open (pidfile .scratch/tasks_webui.pid).

## 6. TASK LIST (status)
**DONE (all verified, headless zero-errors, regressions kept green):**
M1 scaffold · M2 chunk world (winding + transparent-pass fixes, 4 biomes) · M3 player · M4 interaction (mine/place/drops/hotbar) · M5a1 light computation · M6a fluid sim (sea stable, reactions) · M6b fluid mesh + buckets (139/140/141) + fluid raycast · BUGWEB-1 web camera (LMB drag-look + click recapture; `AWECRAFT_LOGIC=look`) · BUGWEB-2 web perf (1 ConcavePolygonShape3D/chunk = 25 shapes vs ~250k boxes; 1-chunk/frame build queue, spawn chunk first; `serve_web.py` gzip 39.5MB→10.1MB).
**PENDING (in order):**
- **RENDER-DIST DONE (user 2026-08-18, subagent 2026-08-18, autonomous mode per user directive "work list to completion, do not stop for me")**: render MORE chunks (MC-style). `world.render_radius` split from sim (default **4**, `AWECRAFT_RADIUS` override intact; fluid ±14 / drop cap / light flush untouched). Perf gate: chunks=81 all_meshed (121 incl. data ring), max_frame_ms=63 (≤80), total_ms=7998, drain 196 frames; A/B r=2 → 25 chunks OK; full battery green incl water fix, 0 script errors; screenshot `screenshots/render_dist_check.png`; web rebuilt (13:15). NOTE: 1 pre-existing `ERR_CANT_OPEN` (atlas load quirk, `data.gd:64`) at render — predates change, low priority. Next: M7a. Full tracked list = opencode todowrite (RENDER-DIST ✓ → M7a → M7b → M8 → M9 → M5b → M5a2 → M10 → M12 → M13; conditional PERF3c-2; endgame blocked on spec). Historical plan (user's words): MC chunk = 16×16×384 (ours 16×16×80 per web spec — KEEP height 80); "MC default 4 chunks but goes up to 96 visually, 12 for simulation" → want bigger visual distance, sim separate. Plan: split visual radius (mesh+collision — today the single `world.radius`, default 2) from sim (fluid tick region already independent ±14 blocks; light flush already edit-triggered). First target **r=4 (81 chunks, was 25)**; `AWECRAFT_RADIUS` env already overrides. Verify: `AWECRAFT_LOGIC=perf AWECRAFT_TIMING=1` (chunks=81, max_frame_ms ≤~80 headless, total_ms) + battery + small render → web rebuild → **user judges the feel** (expect ~tens of sec progressive pop-in: per-chunk browser build ~100–300 ms, already budgeted ≤2/frame + 30 ms guard). Bump to 6+ only if user OK; per-chunk cost reduction is the next lever if pop-in hitches annoy.
- **M7a DONE (2026-08-18)** — headless inventory+craft logic, web-faithful: inv[36] (hotbar 0–8; storage panel = **inv[27+i]** 9×3; inv[9..26] hidden reserve), armor[4] (LMB equip/swap/unequip, RMB only if held.n>1 or slot empty), craft grid **3×3**, `craftOut = matchShaped||matchShapeless`, output click = addItem(out) then subtract from every non-empty grid cell, close = held→inv + **grid items lost (web quirk, kept)**, E/Esc + pointer unlock; **sim KEEPS RUNNING while open** (web loop gates only `state==='play' && !dead`; WASD still moves). Data gaps filled web-exact: blocks 6 oak_log/8 planks/17 bricks/20 crafting_table/21 furnace, items 100–143 (incl. all armor 127–138 + tools), all **22 shapeless + 14 shaped** recipes, `match_shapeless/match_shaped` (data.gd:157,182). player.gd click API `:686-745` (inv_slot/craft_grid/craft_output/armor_slot_click) + `open/close_inventory`/`recompute_craft`; Debug `inv_click/inv_dump/armor_dump/craft`. `AWECRAFT_LOGIC=craft` **ok:true** (log→4 planks→4 sticks→wooden pickaxe 111×1; armor equip 1pt→swap→unequip verified). Battery all 0 errors; perf 81 chunks @60 ms. Deviations: grid always recomputes (fixes web stale-output bug), `Debug.inv_click` flat uiSlots order 0-8 grid/9 out/10-13 armor/14-40 storage/41-49 hotbar (works UI-closed), MMB pickBlock + furnace UI NOT ported (out of scope). **M7b1 DONE** (2026-08-18): `ui/inventory.gd` CanvasLayer replaces `ui/hotbar.gd` (deleted; scene code-built, main.tscn untouched) — web-exact: slot 48px bg rgba(0,0,0,.35) border 2px #555 / sel #fff (hotbar only), panel #c6c6c6 bevel #fff/#555 + 14px pad + shadow, title "Inventory" #3f3f3f bold 14, prow [armor pcol head→boots][storage 9×3] + in-panel hotbar + 48px held-cursor; E/Esc reuse M7a logic; clicks only via player API; per-frame change-detect refresh; `AWECRAFT_INV` env (main.gd:300) pre-snapshot. Web facts: **tooltip + recipe-list are NOT in frozen web (user-backlog items → Godot-native in M7b2)**; stack count `n>1?n:''` white 14 bold #000 shadow; web click LMB grab/place/swap, RMB place/take-1, Shift+LMB quickmove. Verify: 0 errors (craft ok:true bit-identical, perf 81 chunks @58ms), `screenshots/inv_empty.png` checked OK (panel+armor+storage+hotbar over world). Deviations: icons full 32px tile (web ¼-crop quirk dropped); **wheel-select NOT gated by ui_mode (M7a gap → fix in M7b2)**; render ERR_CANT_OPEN = pre-existing ALSA dummy-audio. **M7b2 DONE + M7 COMPLETE (2026-08-18)**: craft 3×3+output web-exact prow (inserted left of armor, gap 14); wheel-over-slot no-op fix; MC-style hover NAME tooltip (Godot-native — web has none); "Craftable" autofill list (Godot-native — web has none; only craftable recipes, click=fill grid no-consume, green highlight on current craft_out); data.gd item entries 101–147 + block names web-exact; latent ghost-slot-after-close bug fixed; `AWECRAFT_INV=1` full-item shot. Screenshot bug squashed: giant brown square = FPV `held_box` (unshaded because blocks 6/8/17/20/21 had no atlas tiles) → FPV hand+aim hidden while UI open (web draws no hand) + **8 Faithful tiles added to atlas (oak_log/planks/bricks/crafting_table/furnace)** + slot icon renderer fixed (compat-renderer `draw_texture_rect_region` no-op → cached 32px sub-images, `draw_texture_rect`). Both `screenshots/inv_empty.png` + `inv_full.png` orchestrator-verified. **WEB REBUILT 17:05** (subagent-ran build — dev, but state verified current + server 200). **M8 DONE (2026-08-18)** — Survival web-exact: hunger init/max 20; drain sprint dt×0.06 (grounded, not fly), −0.1/mine, −0.5/attack (M9); regen hunger>18 && hp<20 → +1 hp/2 s; starve ≤0 → +1 dmg/4 s; death "You Died!" respawn (hp/hunger 20, armor cleared, same x/z surface). Armor pts=Σ.dr, dmg = max(1, round(n×(1−min(0.8,pts×0.04)))). Tools mult = item .speed when .tool==blockTool(block): pick:[3,9,14,15,16,25] axe:[6,7,8] shovel:[1,2,4,12,13]; wood 2/stone 4/iron 6/diamond 7; prog += dt×mult/max(0.15,hard). Gated drops: stone/ores need ANY pick → cobble 9/106/110/107, bare hand nothing; grass→dirt+35% seeds, snow→dirt, leaves→15% seeds, glass none, else block.drop. Eat = RMB use on .food (needs aim hit): 101/146=2, 102/147=5 → hp+= AND hunger+= (if hp<20 or hunger<19.9, else "Too full"). UI: food bar (170×9, #e8a33d→#b06a1c gradient, bottom 84px centered) + 10× **black** hearts 16px opacity 1/.45/.25 per 2 hp (web-exact invisibility quirk kept). Also ported: fall>3.5→floor(fall−3), void 100, lava 4/0.5 s, drown 2/2 s (10 s air). `AWECRAFT_LOGIC=survival` **ok:true** (tool_ratio 2.02, drop_bare 0/drop_pick 9, dmg 5→2 iron, drain 0.058/s, regen 1997 ms, starve 8024 ms, eat +5/+5, too-full no-consume, death+respawn). Battery 0 errors incl perf 81@61 ms. Screenshot screenshots/m8_survival_hud.png. WEB REBUILT 17:59. **M9 DONE (2026-08-18)** — Combat web-exact: trigger = LMB `startMine` → block DDA (REACH=6) ALWAYS wins, mob targeted only when ray hits nothing (`0<t≤6`, perp<0.7, min t, mob center pos.y+h·0.55); dmg = item `.dmg` else **1 bare-hand**; knockback dir·5+up 3; hunger −0.5/attack; **NO player attack cooldown in web (ported faithfully — every click hits)**; death: per-drops `rand()<ch` → spawnDrop, no drops if tamed; sfx 'hit'. player.gd `aim_mob/attack_mob` (:383,:405); `entities/mob.gd` STUB (key/hp/tamed/vel/hurt/try_kill/center; M10 replaces visuals+AI, API kept); **Data.mobs full table (8 mobs) ported** (was M10 scope — death drops need it); `Game.entities` + Entities node; Debug.spawn_mob real. `AWECRAFT_LOGIC=combat` **ok:true** (chicken bare −1/−0.5, double-click both land per web, iron sword 4 kills → drop 146, no-target no-op). Battery 0 errors (perf 81@61 ms). WEB REBUILT. **M5b DONE (2026-08-18)** — day/night web-exact: DAY_LEN 600 s; el=sin((t−0.25)·2π) (0.25 sunrise/0.5 noon/0.75 sunset/0.0 midnight); day=clamp((el+0.12)/0.35,0,1); sun white energy 0.1+day·0.95 pos (cos a·100, el·100, sin a·40) aimed at origin, no shadows; ambient 0.25+day·0.4; sky lerp(#0a0e1f→#87ceeb,day) then →#f08040 by dusk·0.45 (dusk=clamp(1−|el|/0.25,0,1)); fog linear near=24 far=91.2 (=VIEW_R6·16·0.95) color=sky; night el<−0.08; G night→noon else midnight. `core/daynight.gd` + main.gd sun/WorldEnvironment/FOG_MODE_DEPTH/auto-cycle. `AWECRAFT_LOGIC=daynight` ok:true (dusk sRGB(177,128,119)=web, noon #87ceeb exact, cycle Δ 0.85%). Battery 0 errors (surv tool_ratio 2.26→2.12 rerun, wall-clock flake). Renders dn_day/dn_night.png (sky sampled = web). Deviations: compat raw bg/fog (pipeline mirrors web), terrain lit StandardMaterial (web unlit+uDay fragment → pixel-exact = M5a2), stars→M5a2. **NOT rebuilt yet — bundling with user bugs.** **USER BUGS 2026-08-18 (TOP PRIORITY queue):** (1) **BUGTEXT DONE** (subagent killed by user but complete; coordinator-verified): browser textured (`.scratch/web_textured_check.png` — cobble/sand/sky/fog/hotbar in browser; web rebuilt 20:22); only ERROR left = ALSA dummy-audio (harmless, X-render only); all .scratch/bugtext_*.log 0 script errors. (2) **FOG-POP DONE**: `DayNight.fog_near/fog_far(render_radius)` = (r+1)·4 / (r+1)·16·0.95 → r=4: 20/76 < 80 edge ✓ (main.gd:412-413, `fog_ok` assert :1739-1742). (3) **BIOME-TINT DONE**: grass/water grey → web r12 uses **NEUTRAL_TINTS** (index.html:630) baked into grayscale tiles at atlas load: tile 0 GRASS_TOP **only block-1 top face** [93,178,55], tile 5 WATER all faces [47,107,235], tile 8 LEAVES [60,140,48]; NOT per-biome (biomes select block id 1/12/4; snow/sand/lava pre-colored in pack; sea=inland). Ported: `Data.block_tint(id,face)` (data.gd:189, WHITE=none) × face brightness in chunk.gd:246 (solid) + :263-285 (fluids 0.95/0.85/0.6×tint, lava WHITE). `AWECRAFT_LOGIC=tint` ok:true (grass [93,178,55] ±1.5, water 0.95×tint). **BONUS FIX**: main.gd:474 was missing `await` before `_await_world_build` → top/iso detached-cam snapshots captured pre-build (all-sky) — all prior detached renders potentially affected; now properly awaited. `AWECRAFT_NO_FOG` env added. Render screenshots/biome_tint.png (green islands, blue water). Battery 0 errors, perf 81@64 ms. WEB REBUILT 23:08 (texture+fog+tint batch live). (4) **TREES-FLORA DONE** (see AC-0027 entry below). **WEB REBUILT 2026-08-19 09:48 with trees/flowers** (browser had none — build was stale). (5) **WATER-ANIM**: water texture should animate (web). (6) **HAND-ANIM**: swing/punch anim while mining + held-item texture BLURRY fix (nearest filter). (7) **MENU+OPTIONS (big)**: main menu (title + BUILD VERSION + play/new world) before start; options menu from MAIN and PAUSE: render-distance slider (chunks), sim-distance slider (chunks), texture-pack apply, sound-volume slider, fullscreen toggle, resolution dropdown; persist settings. (8) **HITCH**: still hitches on block break → PERF3c-2 diagnostics (recenter sync gen, worst boundary-chunk build, tick_fluids locality, snapshot drain). **OPS (user 2026-08-18→19)**: scratch → project `.scratch/`; `/tmp/**` pre-allowed + agent variants in `~/.config/opencode/opencode.json`; **2026-08-19: general variant → xhigh (user: medium not enough), compaction prune → false (caching) — APPLY ON NEXT OPENCODE RESTART**. **WINDOWS EXPORT DONE (2026-08-19, user top-prio)**: `godot/export_presets.cfg` got `preset.1 "Windows"` (Windows Desktop, x86_64, embed_pck, no console) → `./build` via `--export-release "Windows"` → **`AweCraft/export/windows/AweCraft.exe` single-file 109 MB** (templates were already in `~/.local/share/godot/export_templates/4.7.1.stable/`); log `.scratch/build_win.log`; not runnable here (no Windows) — verify = exit 0 + clean savepack. **TREES-FLORA DONE (verify/finish pass 2026-08-19)**: first subagent stalled EMPTY; verify pass found + fixed: (1) generator tree loop clobbered by debug `forloop print` and BOTH `while`-loop increments (`tx/tz += 1`) placed OUTSIDE their loops = infinite loop (all subagent bisection logs were this hang); (2) `_tref_tree_at` OK but `_trees_test` + `_trees_ref_flora` + genhash-gate had the same increment-outside-while bug ×4 (never executed); (3) atlas never re-imported — tiles for block 7 leaves/18 poppy/19 dandelion missing → leaf/flower UVs collapsed to a point: added (leaves = grayscale Faithful oak_leaves so BIOME-TINT TINT_LEAVES [60,140,48] colors it, gray+tint pattern; poppy/dandelion = colored Faithful as-is); (4) chunk.gd `_qgrow(ax, rq.size())` under-budgeted cross-quads 2× (flower = 2 quads) → OOB PackedVector3Array errors → `*2`; (5) probe fixes: node-count dict compare int-vs-float (JSON.parse returns floats), trunk_top sample accepts 6/7 (adjacent-tree canopy overlap = web-correct, first-write-wins), desert scan ±16→±64; (6) deleted test_spin{,2,3}.gd (+.uid). WEB-FAITHFUL audit: tree_at/tree shape/trunk/canopy/put-guard, flower 2%/50-50, drops (7→15% seeds, 18/19→self+50% seeds), cross/cutout meshing, leaf tint — all match index.html. Gates: `AWECRAFT_LOGIC=trees` ok:true (8 trees/5 flowers match trees_expect_44.json, determinism, genhash_gate true, mesh 3 flowers×8v + cutout 1256v, 0 script errors); GENHASH 12/25 bit-identical + 13 flora chunks differ (flora-excluded gate true inside trees); PERF max_frame_ms 63=63 (total 8003→8556 ms, gen 783→865) 0%; battery player/interact/light/fluids/buckets/water/craft/survival/combat/daynight/tint + --quit all 0 errors; render `screenshots/trees_day.png` (seed 44, iso, r=2, AWECRAFT_NO_FOG=1 — fog otherwise fogs detached cam; trees+flowers visible). test_spin junk was shipped in old .pck — next windows export clean. **PLANET-PLAN added END OF LIST (user 2026-08-19, research/plan only)**: world contained to a planet — flat MC feel on surface, seamless ground→space takeoff, ring (Halo-like) geometry so rectangle map wraps; research NMS GDC talk + games that did similar; plan doc .html; DO NOT IMPLEMENT. Impl hints (coordinator draft, 2026-08-18): port web UI click semantics — grep `openUI|renderInv|onDown|pickBlock` in `index.html`; E toggles, mouse UNLOCKED while open, game sim KEEPS RUNNING while open (verify exact web behavior first); expose headless test hooks (slot select/click, craft output) so `AWECRAFT_LOGIC=craft` asserts the log→planks→wooden-pick chain via `Data.match_shapeless/match_shaped`; items without 3D/block models = placeholder color icons (TEX2 later); screenshots → `screenshots/inv_*.png`.
- **M8** Survival: tool speed multiplies mining, gated drops (stone/ores need pick), armor DR (min(0.8,pts×0.04), min 1), hunger (sprint/attack/mine drain; regen>18; starve), food bar, eat.
- **M9** Combat + **bare-hand punch (empty hand = 1 dmg)**.
- **M10** Mobs: 8 **UNIQUE recognizable** models (skeleton+bow, chicken+comb, wolf quadruped, spider 8-leg, + 4 originals reworked) + limbs **pivoted at hip/shoulder** + AI (hostile chase, chicken passive, wolf tame by bone, spider night-only) + bow/arrow/projectiles + day/night spawn tables. The orchestrator **views each mob's PNG** and iterates until it reads as its animal.
- ~~**M11** WS6 Nether+portals~~ — **REMOVED 2026-08-18** (user is redirecting the endgame in a new direction; spec TBD). Item 126 (flint&steel) stays reserved until the new plan lands. Renumber remaining tasks after M11 as-is (M12, M13 unchanged).
- **M12** WS7 pooled particles (break debris, hit sparks, arrow burst, pickup).
- **M13** WS8 procedural sound (headless-asserted).
**NEW (user, 2026-08-16):**
- **TEX** — block **textures**: replace flat per-face colors with a UV-mapped **texture atlas**; **import Minecraft resource packs** (`.mcpack`/`.zip`) to populate the atlas (test with the repo's `Faithful-32x-1.21.11.mcpack`: `assets/minecraft/textures/block/*.png`). UV the chunk SurfaceTool (top/side/bottom per face); keep baked light as a multiplier on resume.
- **WEBX** — **Godot HTML5/Web export AFTER EVERY feature** so the user can test in a browser: download 4.7.1 web export templates, add a "Web" export preset, build script `AweCraft/build_web.sh` → `AweCraft/web/` (index.html+.js+.wasm+.pck), then run it after each M-task. (Web build likely needs HTTP serving — give localhost + LAN URLs per AGENTS.md.)
**DONE (web-freeze sprint, 2026-08-17, all headless-verified, web rebuilt):**
- **PERF3a block-break freeze:** `build_mesh` local snapshot (18×H×18) → 133→45-75 ms; `Lighting` typed+local (values byte-identical); edit flush staggered ≤2/chunk/40ms-frame (`AWECRAFT_LOGIC=editperf`: max_frame_build_ms 54, was one 9-17 s frame).
- **PERF3c-1 WorldGen:** 23.9× faster (744→~31 ms/chunk; GENMS 18611→778), GENHASH-gate bit-identical 25/25 (+seeds 999/424242 cross-checks), `terrain_height`/`biome_at` API unchanged; envs `AWECRAFT_LOGIC=genhash`, `AWECRAFT_GENPROFILE=1`.
- **PERF3c-2 (NEXT ONLY IF user retest still stutters):** recenter sync data-gen (now ~0.8 s — small), worst boundary-chunk build, `tick_fluids` locality (periodic spike), snapshot-drain (render harness waits for queue; tex_iso.png was a pre-drain artifact).
**DONE (web-freeze sprint cont., 2026-08-18, headless-verified, web rebuilt):**
- **PERF3d fluid sleep** (coordinator-implemented after subagent stalls): `tick_fluids()` full-scans only while the sim is awake; sleeps (≈0 ms tick) after 3 write-free ticks, wakes on fluid writes / block edits near fluid / region change. `AWECRAFT_FLUID_SLEEP=0` A/B toggle, `AWECRAFT_TICKTIME=1` → TICKMS. A/B: fluids RESULT + FLUIDSTAT BIT-IDENTICAL (2024→4247 etc.); `AWECRAFT_LOGIC=fluidsettle` (+`AWECRAFT_SETTLE_TICKS=N`) — sea settles 12 ticks, then TICKMS ≈ 0.00ms (was 175–520 ms per tick).
- **WEB FORMAT BUG FIXED**: `world.gd` region-sig line had `"%d:%d,%d"` with 2 args → `ERROR: String formatting error` every fluid tick in the browser (~10 console errors/s + main-thread cost). Now 0.
- **BUGWATER-1 FIXED (2026-08-18, user top-priority)**: couldn't climb out of water (hold Space to get on top of a block flush with the water surface). Finding: Godot was a *faithful* port — the frozen web ALSO gets stuck (JS sim: max feet 30.58 < 31 bank-top). Root cause both engines: swim-up boost gated on `+0.5` center point, cuts off half a block below surface. Fix (godot only, minimal extension beyond spec): `player.gd` `swim_up = in_water OR feet-cell-is-water`; swim-up target (→4.5 @lerp 8/s) + SWIM horizontal speed extend until feet clear surface. `AWECRAFT_LOGIC=water` gate: flush 1-deep pool + bank; pre-fix stuck (max_y 42.58) → post-fix above_top frame 29, standing_on_shore frame 48, ok:true. Battery all green, 0 script errors. Web rebuilt after.
- **M11 REMOVED** (user: endgame redirected, spec TBD) — see §6 PENDING.
- **Serve (2026-08-18, user: one server only)**: `python3 serve_web.py --ssl` → https on **8443** (self-signed web/ssl/, browser warning until accepted). Plain-http 8140 KILLED on request.
- Browser (headless swiftshader 60 s, post-fix): world loads, renders correctly (screenshots/web_fixed.png), errors gone. Late-phase still shows ~200–300 ms longtasks at 100% busy — LIKELY swiftshader software-rasterization artifact (user's GPU differs); NEEDS user devtools FPS (F3 in-game) to isolate. Remaining suspects on real HW: per-chunk build hitches in wasm, concave-physics cost.
**FINDINGS (2026-08-17, BUGWEB-1/2):**
- **PERF2 (candidate task, do if startup still slow after user test):** `WorldGen.generate` ≈ 0.7 s/chunk (headless GDScript) is now THE startup cost (25 chunks ≈ 18 s + staggered mesh drain; per-chunk mesh ≈ 0.12–0.18 s). Optimize gen (typed loops, cached noise) — spec-mandated algorithm, same output.
- **DATA GAP:** ported `Data.blocks` lacks id **9** (web lava/water reaction stone; `tick_fluids` calls `set_block(...,9)`). Null-guard landed in chunk.gd; fix in TEX task (grep web `BLOCKS` for exact id-9 entry) — reactions currently produce invisible cells.
- **FLAKE (pre-existing):** `AWECRAFT_LOGIC=interact` ~1/8 flake (drop.gd RNG launch drifts into 2.5 m magnet). Docs: `/tmp/opencode/flake_events.log`, `droptrace_flake.log`. Optional determinism fix later.
- **Ops/infra:** subagents strictly sequential (API = 1 concurrent request). Opencode compaction config updated (`auto, prune, reserved:10000, tail_turns:3, preserve_recent_tokens:4096`; `tool_output: 500 lines/20KB`) — takes effect after opencode RESTART. Web serve = `python3 serve_web.py` (gzip), not plain http.server.
**SHELVED (resume after core + TEX):** **M5a2** bake per-voxel light into mesh (M5a1 is ready to wire up); **M5b** day/night (Godot `WorldEnvironment` sky/ambient/fog + sun by time + auto cycle).

## 7. Proven headless verify envs (each prints a `RESULT {...}` JSON, self-quit)
`AWECRAFT_LOGIC=player|interact|light|fluids|buckets` · `AWECRAFT_SNAPSHOT=<path>` (+`AWECRAFT_CAM=top|iso`). Always check **zero script errors** first.

## 8. Notes
- **STATUS 2026-08-18 (context-clear checkpoint):** all web-freeze work done + web rebuilt (see DONE blocks §6). User retest (real HW): "performance is better but STILL noticeable drops" — swiftshader browser measurement can't isolate (100% busy there = software rasterization artifact). Next diagnostic ONLY IF user confirms: add a web-visible no-collision toggle (F4 / console) to test concave-physics cost; ask user for F3 FPS (steady-state vs walking into new terrain).
- **User 2026-08-18:** endgame direction TBD (replaces removed M11) — do NOT build Nether/portal; wait for spec. "Focus on the basics."
- **Servers (single, verify still up):** https `python3 serve_web.py --ssl` → **8443** (self-signed `web/ssl/`, browser shows warning until accepted). Log `/tmp/opencode/awecraft-web-https.log`. Report BOTH localhost + LAN (192.168.0.224) URLs.
- **git (2026-08-19):** remote added: `origin` = https://github.com/JacobGrealy/awecraft (public, ssh, branch master). Rule: **commit + push after EVERY verified task** (incl. `tasks/AC-NNNN/` + `tasks/TASKS.yaml` updates). Task protocol: `tasks/TASKS.yaml` single source of truth (id/name/desc/created/updated/status/completed/comments + `queue:`); per-task folders `tasks/AC-NNNN/` get `spec.html` (pre-launch) + `AC-NNNN-results.html` (agent summary + embedded screenshots).
- **Opencode config:** `~/.config/opencode/opencode.json` compaction `{auto,prune,reserved:10000}` (conservative, per user rollback request) — applies on NEXT opencode START (restart). Subagents strictly sequential (API = 1 concurrent).
- **Stall pattern (this machine):** big optimization subagents returned EMPTY 4× on the fluid task; a coordinator-implemented pass fixed it (PERF3d). What worked: micro-probe tasks first (genhash-baseline style), A/B env toggles (`AWECRAFT_FLUID_SLEEP=0`), bit-identical gates. Keep tasks small; verify disk state (mtimes + grep + headless) after ANY empty return before relaunching.
- Blocks = UV-mapped texture atlas (TEX done: `godot/assets/blocks_atlas.{png,json}`, `AWECRAFT_IMPORT_PACK=<mcpack>` re-imports; id 9 cobblestone added; portal has no pack tile → purple fill). Lighting bake deferred (M5a2); mobs lit by sun+ambient.
- Texture-pack import target file: `AweCraft/Faithful-32x-1.21.11.mcpack` (a zip; 1.21.11 uses dir-per-block textures).
- Process going forward: **after each M-task → run `build_web.sh` → user tests `AweCraft/web/`**, then next task.
