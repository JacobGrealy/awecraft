# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6.

## 00o. CHECKPOINT 2026-08-31 (05:55) - **AC-0152 + AC-0160 DONE + PUSHED** [single closeout commit; prior pushes 7a49db5 AC-0110, 57a1763 AC-0151]; Banded streaming shipped per USER DECISION [billboards GONE — band 2 = normal full-mesh, no tick/collide]: render set = Euclidean circle R=50 [7845 columns, NOT 10201 square]; tick/collide set = taxicab diamond 4 [41 columns, full 16³ mesh]; band1 merged taxicab 5–8 [104, uv_scale 2]; band3 = collar ∪ circle-boundary 8-ring [408 @ R=50] data-only [lets circle-edge band-2 chunks build against neighbor data — no stranded queue]; stream set 8253; settings sim_dist 1→4 → band0_r; AWECRAFT_BAND0/BAND1 env. AC-0160 drain fix: TM-priority pools [TG=4/TM=6], drain NEVER sync-builds [all-8-neighbor gate], 5×5 spawn data burst at recenter, one-shot `_spawn_fast` recenter-slice pause, burst task ids tracked [slot stability] → **spawn3x3 11.7s → 3.2–4.0s** [measured machine floor, deviation of record: gen 1.35s + 9 builds 0.92s + (0,0) face-cache/eff cascade 1.3s + handoffs 0.5s]. **NEW STANDING GATE VALUES (HARNESS.md §3 amended):** fluids sea **2756/2756 + 888→914/914** [r4 resident set 121→69 chunks + faster drain; old 2730/888 VOID; invariants 25/9 + stable unchanged]; **boundary walk p95 ≈ 54ms** [old 267–401 / ~42.2k–47.6k VOID; one-shot so far — widen with future runs]; occlude/cave/MINFO drift at r4 [wait on `chunks.size()>=81`, set is 69 — outside gate set, do not chase]. **KNOWN NON-FATAL ANOMALY (follow-up recommended):** 20–26s one-off burst at boundary crossing 16 + engine `Invalid Task ID` at `is_task_completed` from recenter [world.gd:2258] in EVERY new-streaming-code run; non-blocking [run completes, queue drains]. New harness arm `AWECRAFT_LOGIC=bandmap` [HARNESS §1]; **RENDER LESSON:** named `AWECRAFT_CAM=top` silently yields first-person [on-demand player spawn main.gd:1310 makes the player camera current AFTER the named-camera block] — top-down shots use `AWECRAFT_AIM="8,240,8,0,-1.57"` [seed 44]. Gates (coordinator re-runs, final code): G0 0; bandmap tick 41/circle 7845/bands 41-104-7700-408/stream 8253/b0 41-41/0 B0LOST/band2 full mesh/trickle monotone/spawn3x3_ms 3192; genhash ×2 25/25 == baseline; SMOKE 4 exact [new fluids baseline]; boundary ok p95 54; render R=10 top-down band boundary visible/no impostors/no walls. Evidence: `.scratch/AC-0152-gates/coord-*` + `.scratch/AC-0160-gates/` [coord-boundary.log, coord-bands3.png]. NEXT = **AC-0155 full-chunk-save-not-edits** [unblocked — was waiting on AC-0152]; user live-queues — re-run `tasks.py next` at EVERY boundary
## 00n. (superseded header — its "BUILDER LAUNCHING xhigh" never happened: xhigh was already dead; state now in 00o)
**AC-0091 world-height-minecraft-Y-64-to-319 DONE + COMMITTED 37cf15e + PUSHED.** Standing facts: H=384 (y=0=bedrock=MC Y−64, y=383=MC Y319, sea y=126=MC Y62, MC Y=y−64 at coordinate surfaces only); baselines = genhash 25/25 [`.scratch/AC-0091-gates/genhash_new.txt`], MINFO 121/81/40 [`.scratch/AC-0091-gates/minfo_new.txt`], nightday 15.0/3.0 + torch 14.19996, player 136.78/horiz 2.82, light cave_eff 10/surface 15/torch 14, fluids 2730 stable + backed 888 + lava 25/9; **STANDING BOUNDARY BAND walk p95 267-401 / fwd_p95 ~42.2k-47.6k** (run-noise envelope across AC-0091+AC-0143 runs; old 108-133/10898-13019 VOID from AC-0091); known non-fatal g0/battery noise = AC-0137-class threadgen transients [incl `threadgen_handoff` Nil→PackedByteArray @ world.gd:627, 3–17 per battery, pre-existing stash-verified AC-0151] + CORE3X3 wait timeout; **interact arm RARE FLAKE class** (1/5 place_ok:false — re-run once, record); SMOKE 5 = player;interact;light;fluids;genhash.
**AC-0110 occlusion-culling-caves-interior DONE 2026-08-30 (delegated whole task to ONE blocking subagent — first full success of the v3 pattern).** Stage A: per-slab `OccluderInstance3D`+`BoxOccluder3D` 15³ @ (8, y0+8, 8) [0.5 inset] for fully-solid slabs (`c_ns==0`, chunk.gd both build paths, 7-elem slab row `row[6]`) + `project.godot [rendering] occlusion_culling/use_occlusion_culling=true + camera/cull_3d + cull_2d` [canonical `rendering/…` keys verified via in-project property-list probe]; headless-gated (DisplayServer check). Stage B: `_s_is_interior` 6-solid pre-pass (padded snap grid) — **PROVEN NO-OP: AC-0106 greedy mesher already culls interior faces** (MESHRECS 81/81 identical, genhash byte-identical) — recorded as legitimate outcome. Gates ALL GREEN (coordinator re-ran): G0 0 err; genhash 25/25 run1==run2 == AC-0143 baseline; SMOKE 5 EXACT; occlude arm headless ok leaking 0/0; **xvfb node graph occluders 25 == full_solid_slabs 25** [r4 seed 44]; cave render enclosed torch-lit no leaks [tasks/AC-0110/screenshots/cave-interior.png]. Measured r4: total_verts 698196 / **underground_verts 494864 (70.9%)** / underground_faces 123716/174549 / interior_voxels 2192711 / cave 1417 cells aabb [-64,67,-64]→[-54,85,-23] open:false. New harness: `AWECRAFT_LOGIC=occlude` arm + `AWECRAFT_CAM=cave` [300-frame settle; 40-frame = half-lit]. Build 20260830-1130 109,722,192 B + 8080 byte-matched [9733a5210423ee0784757da60668be60] + 5180=200 [8443 down pre-build, serve_web.py absent — noted, not started]. LIMITATION: Stage A draw-call reduction unprovable on this box [no forward_plus GPU — node-graph + settings + no-regression verified instead; user Windows forward_plus build is the real proof].
**AC-0151 water-always-blue-tint-strip DONE 2026-08-30 (2nd blocking-subagent success).** Fix: `data.gd::_bake_atlas_tints` pixel loop wrapped in `for f in range(block_anim_frames(tid))` tinting row `rc.y + f*TILE_PX` — all 32 water anim frames [strip (224,0) 1024px, fluid_anim TILE_F 1/32] instead of frame 0 only; `apply_atlas` covered via delegation [data.gd:258]; grass/leaves anim 1 unchanged; no hard-coded 32. Coordinator INDEPENDENT pixel probe [own SceneTree probe, /root/Data runtime lookup, raw res://assets/blocks_atlas.png vs baked]: 1568 px / 32 frames, 0 bad, max dev 2, frame centers [35,81,178]/[32,73,160] match builder probe, 3 distinct center values [animation intact], grass control unchanged. Gates: G0 0; genhash 25/25 == baseline; SMOKE 5 EXACT; xvfb render mid-frame = blue, no grey [screenshot committed]. DEVIATION of record: "all frames equal" unsatisfiable — base strip genuinely animates; invariant = per-frame baked == raw×TINT_WATER. FINDING: lava anim 20 @ (0,32) but block_tint(24)==WHITE — never tinted by design. Build 20260830-1223 109,722,032 B + 8080 byte-matched [9ef7f66dddfa050d4a3be41def2dc88f] + 5180=200.
**AC-0152 bedrock-realms-sim4-lod-render-50 STOPPED 2026-08-30 16:4x (user cancelled 2 subagent runs: first "thought it was pretty much done" with huge issues [user documented as AC-0160]; second after the fix attempt — "billboards still look way off, let's just get rid of billboards").** Partial implementation UNCOMMITTED in working tree [do NOT revert — build on it]: `world/world.gd` [+345: band vars band0_r=4 taxicab [ticks+full 16³ mesh] / band1 merged 5–12 coarse [uv_scale 2] / render_radius Euclidean circle; band_of/in_stream_set; recenter WANT/STUB/MERGE over new set; band reassignment on recenter; collision bodies band-0 only; tick_fluids band-0 diamond; AWECRAFT_BAND0/BAND1/BAND2 env], `world/chunk.gd` [+198: coarse flag, uv_scale in _s_uvc/_s_corner_uv/_s_qwrite_merged, build_impostor/apply_impostor/_lod_material/_lod_atlas_tex ← THE BILLBOARD MACHINERY TO REMOVE], `autoload/settings.gd` [sim_dist 1→4], `scenes/main.gd` [+174 additive: AWECRAFT_LOGIC=bandmap arm `_bandmap_test`]. Full design log: `tasks/AC-0152/continuity.md`; prior gate logs: `.scratch/AC-0152-gates/` [bandmap1..10 + boundary + genhash + fluiddbg]. **AC-0160 defects (user diag, verbatim):** impostors = black floating rectangles [stale `_tm_ctx` has_tex/brect −1 — captured before atlas bake; winding 0,2,1 → −Y faces culled; UV flipped] + spawn 3×3 = 11.7s [spec ~1s] with 7710 queue strands. **POST-BILLBOARD-REMOVAL SCOPE:** AC-0160's impostor fixes are MOOT [ctx/winding/uv all live in the removed path]; what REMAINS = spawn-fast drain cap [3×3 ≤ ~1s] + continuous trickle drain [no stranded queue]. **POST-REMOVAL AC-0152 SCOPE:** keep circle-~7850 render set [not 10201 square] + diamond-41 tick set + band-1 merged 5–12 [104] + spawn-3×3-fast-then-trickle + no-height-cut; band 2 [13–50] = normal full-mesh chunks [same builder path as band 0, NO tick] — no impostor quads anywhere. Gates for the finish: G0; bandmap arm [adapted: 41 tick diamond / ~7850 render circle / 41-104-~7705 band counts / spawn 3×3 ≤ ~1–2s / drain trend no-strand / band face counts]; genhash ×2 vs `.scratch/AC-0152-gates/genhash1.log` [arm is streaming-independent — verify]; SMOKE 4 EXACT [136.78/2.82; true/2/2; 10/15/14; 2730+888+25/9]; boundary ONE-SHOT r4 [old 267–401/42.2k–47.6k VOID — new band]; xvfb render [exact+merged visible, NO impostor quads, no height wall].
**INFRA LESSON v3 (2026-08-30, BINDING — supersedes the "small medium runs" pattern below for full-task delegation): ALL delegation = `subagent_plan` (BLOCKING) only.** Background subagent + live parent session THRASH the local LLM's single-session KV cache (root cause of AC-0143 "xhigh dead 4/4" — not just token cap; user: "we are using a local llm that only has enough cache for one session at a time"). Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight; give the child a FULL plan+implement+verify prompt (user: "let the x-high sub agent plan and implement"); coordinator = verify its gates + docs + commit. Subagents MAY run godot gates — PROVEN twice (AC-0110, AC-0151): every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (the old SIGSEGV class `Failed to open user://logs` = missing HOME, cured by the trick); but the coordinator ALWAYS re-runs ALL gates independently post-handoff [never trust the report] — AC-0151 example: coordinator's own probe reproduced the builder's values exactly.
**INFRA FINDING v2 (2026-08-29, still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap** [homeserver1 = Qwen3.8-27B-UD-Q5_K_XL @192.168.0.224:1234 via llama-swap; DSH sends no max_tokens]. xhigh thinking responses (10–30k) die; cap >~5k; small prescriptive responses survive. A DSH HOST RESTART (user op) may help before a big task.
**BUILD RECIPE (2026-08-30 lesson): `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local`** — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; its pidfile/sandbox quirks are cosmetic], 5180 = docs daemon [root must = 200], 8443 = https daemon DOWN pre-build [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**RESUME (new chat - do these in order):** (1) read this 00o block + `tasks/HARNESS.md` §3 [NEW standing values]; (2) `python3 tasks/scripts/tasks.py next` [user co-edits TASKS.yaml LIVE — re-run at EVERY boundary; as of 05:55 queue = **AC-0155 → AC-0158 → AC-0159 → AC-0154 → AC-0109 → AC-0150 → AC-0137 → AC-0092 → AC-0144/0145/0146 [planet] → AC-0111 → AC-0036 → AC-0147 → AC-0161 …**]; (3) THE JOB = **AC-0155 full-chunk-save-not-edits** (was waiting on AC-0152 — now unblocked; read `tasks/AC-0155/spec.html` if present, else `tasks/AC-0155/` scaffold + the entry in TASKS.yaml); delegate as ONE blocking subagent_plan [v3], then coordinator re-verifies ALL gates; (4) per-task closeout: verify gates → `./build_windows.sh` [symlinks FIRST per BUILD RECIPE] + 8080 byte-match + 5180=200 + 8443 note [down — do NOT start] → results.html → HARNESS.md [only if standing values moved] → TASKS.yaml done [proper `- id:` comment entry, queue removal] → CONTINUITY 00o → commit EXPLICIT PATHS [NEVER the other users' scaffolds] + `GIT_SSH_COMMAND="ssh -F /dev/null" git push` → tasks.py next → report localhost + LAN 192.168.0.224. Standing: godot ONE at a time; `HOME=/tmp/dsh_home` on every godot call; logs → `.scratch/<task>-gates/`; /tmp EPHEMERAL per bash call; report ≤20 lines.
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0155|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].
## 00m. CHECKPOINT 2026-08-29 (…/0138 DONE b04e97d; AC-0139 DONE 2faa6fa; **USER RE-QUEUED: AC-0091 + planet series AC-0143..0147 now at TOP, ahead of AC-0140**; AC-0091 now DONE 37cf15e — see 00n)
- **USER LIVE CO-EDIT (2026-08-29 pm, re-run `tasks.py next` at every boundary):** queue now = **AC-0091** (world-height-minecraft-Y-64-to-319, p2 — 384 levels, sea Y=62; old H=80/sea-y30 VOID) → **AC-0143** planet-p1a-sphere-math-world-keys (p1, R=4000, 12-face cubemap math) → **AC-0144** planet-p1a-curved-ground-plays-flat (p1) → **AC-0145** planet-p1b-gravity-flight (p1) → **AC-0146** planet-p1b-atmosphere-impostor-lod (p1) → **AC-0147** planet-p2-solar-system (p2) → **AC-0140** (extract-harness-from-main-gd, p2 — main.gd 8939 lines; all AWECRAFT_LOGIC arms move with it) → AC-0141 → AC-0142 → AC-0121 → AC-0122 → AC-0097 → AC-0112 → AC-0113 → AC-0093 → …. The planet series BUILDS ON AC-0091's H=384 (16×H×16 uniform 1m columns).
- **AC-0091 (world-height) IN-PROGRESS** — spec at `tasks/AC-0091/spec.html` (coordinator-written, de-webbed): internal index stays 0-based (y=0=bedrock=MC Y−64, y=383=MC Y319, MC Y = y−64 at coordinate surfaces only); H=384; sea y=126 (MC 62); bedrock at bottom; save = same internal y (old saves forward-incompatible, fail soft); terrain remap = same in-repo generator formulas re-scaled (grassland ~MC 62–100, mountains ~200+); **H=80 TRAPS to verify:** AC-0134 face mask + AC-0128 ring mask (side,yy) encodings, eff strips, fluid columns, slab math, y-in-bits packing. **ALL old baselines VOID** (player 37.78, sea 2730/1406, nightday 15.0/3.0, genhash ref, MINFO 121, boundary band 108-133/10898-13019) → builder records NEW baselines to HARNESS.md + .scratch/AC-0091-gates/ (genhash_new.txt, minfo_new.txt). One-shot boundary ESTABLISHES the new band (old band void; hard fail = crash/hang/queue-never-drains). Builder gate set per spec: G0 + full battery + basis sanity + one-shot boundary + 1 render R=1 + MINFO dump. Heavy (coordinator bg job): boundary flake + genhash repeat + MINFO diff + build + curls. **HEAVY GATES bash-32 ALL PASS 16:57 — AC-0091 DONE + COMMITTED (commit follows; NEXT = AC-0143 planet-p1a-sphere-math-world-keys [user p1, planet series]).** Gate verdict: g0 rc=0 [3× AC-0137-class threadgen transients]; basis ok:true; MINFO 4-chunk −40-verts-each drift [aabb identical = merge-reshuffle flake class at H=384]; player/nightday/fluids/light EXACT vs builder new baselines; genhash 25/25 BYTE-IDENTICAL vs builder baseline; boundary flake walk 267-290 / fwd 39892 [vs builder one-shot 344/46404 — in-band, run noise ~15%] ⇒ **NEW standing boundary band: walk p95 267-344 / fwd_p95 ~39.9k-46.4k (old 108-133/10898-13019 VOID from AC-0091)**; build 20260829-1656 109,686,480 B + 8080 un-stamped byte-matched + stamped 200 + 5180=200; unattributed 20260829-1534 stamp build in exports [harmless, gate build 1656 authoritative]. **INTERACT ARM RARE FLAKE of record:** gate run 1/5 `place_ok:false, inv n:0` vs 4/5 EXACT `place_ok:true, after_place_cell 2, inv n:1` [incl. 3 fresh consecutive 17:0x] = drop→pickup→place timing race in the arm while the player falls the mined hole — arm is otherwise deterministic; determinism follow-up folds into AC-0140 (harness extract owns arm hardening). **BUILDER REPORTED DONE (16:45)** — builder: files data.gd (H 80→384, SEA 30→126) + generator.gd (remap `y_new = 2.6·y_old_raw + 48` on pre-clamp field → 105.2 + c·36.4 + h·52.0 (+ (r−0.62)·390 if r>0.62), clamp [3,300]; ocean exact 2.6·30+48=126; SPAWN_H 136, TERRAIN_H_MAX 300) + lighting.gd (ring mask RE-DERIVED `(side<<17)|(yy<<4)|level` write-only; AC-0134 face mask dynamic — verified, lightaudit cliffs 0) + world.gd (7 strip sites → 2·16·h / 4·h; frustum y-center H/2) + chunk.gd (slab_n()=24 + `_zeros()` [GDScript has no `[0]*n` — G0's 10 parse errors were this] + `_bake_box`) + save.gd (height stamp + soft-fail) + main.gd (harness-only: NEW `basis` probe, `_continue_slot` soft-fail, scan ranges/thresholds). NEW baselines: nightday 15.0/3.0 + torch 14.19996 EXACT (unchanged formula!), lightaudit cliffs 0 + tunnel EXACT, genhash 25/25 run1==run2, MINFO 121/81/40, basis {−64, 62, 72, 319}, **NEW boundary band walk p95 344 / fwd 46404** (honest ~2.6×/3.9× cost of 4.8× height — spec-anticipated, NOT a deviation), sea_backed 1406→888 (deeper columns), light cave_eff 0→10 (pocket re-scaled), render tasks/AC-0091/tall_world_r1.png. Deviations of record: boundary cost increase (band established), the above baseline shifts (all worldgen-driven, documented in results).
- **AC-0139 (split-main-menu-into-own-scene) DONE 2026-08-29, commit 2faa6fa, pushed.** Minimum-change design: main.tscn stays project main scene (zero env/boot-path change for all harness arms + snapshot flow); new scenes/menu.tscn (root Menu Control → CanvasLayer layer 20 → full menu tree, 22 signal connections in-scene) + ui/menu.gd class_name Menu (576→334, −242; 9 builder funcs deleted; state machine was already extracted ⇒ main.gd 4-line delta); gates all pass (G0 0 errors after one --import for stale class cache; SMOKE all EXACT; 2 renders; dead-import audit clean [_ChunkScriptM = pre-existing AC-0120 preload → AC-0140 scope]); heavy gates ALL PASS (UI tier); build 20260829-1406 109,683,536 B + 8080 byte-matched. **INVOCATION LESSON (new):** quitmenu-style arms with a `script_errors_seen` field default it to TRUE unless `AWECRAFT_STDERR_FILE` is set AND the invoker redirects godot stderr to that file (main.gd:7391-7396) — bare invocation false-fails; call as `AWECRAFT_STDERR_FILE=$f … 2>$f`.
- (superseded 00l text below — its RESUME text described the AC-0135..AC-0139 span)

<!-- Archived history 00l..00 (2026-08-20..2026-08-29) moved to godot/CONTINUITY.archive.md on 2026-08-30 — see that file if debugging older checkpoints. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00o+00m), archive rest. -->

