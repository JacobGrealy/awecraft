# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6.

## 00w. CHECKPOINT 2026-09-02 (07:40) - **AC-0169 debug stats overlay DONE + PUSHED** [CPU/RAM/VRAM/FPS overlay + Options toggle + settings persistence] — a player toggles "Show debug stats" in Options and the top-left overlay stays on during play (no per-frame key). debug_stats setting (default false, bool-clamped) via Settings.PATH (user://awecraft.cfg); Options CheckBox (programmatic in menu.gd _ready, after HungerCheck, no .tscn edit) -> Settings.set_value; top-left CanvasLayer(25)+Label visible iff debug_stats, refreshed every 0.25 s from _process, stays on during gameplay + above the pause menu (layer 25>20), mouse-filter-ignored (no HUD/crosshair/inventory interference). **GODOT 4.7.1 API AUDIT (deviation): the task-suggested OS.get_cpu_usage/get_process_memory_usage/get_total_memory do NOT exist in 4.7** -> FPS=Performance.TIME_FPS (4.7 renamed MONITOR_FPS), CPU=ΔPerformance.TIME_PROCESS as % of 1 core (labeled), system RAM=OS.get_memory_info() (physical/free), process RAM=OS.get_static_memory_usage() (debug-builds only, 0 in release — labeled "engine static"), VRAM=Performance.RENDER_VIDEO_MEM_USED (real renderer stat, no n/a). Files: settings.gd (default+clamp), menu.gd (programmatic CheckBox + move_child — 4.7 has no reorder_child), main.gd (CanvasLayer+Label overlay + _refresh_stats + NEW AWECRAFT_LOGIC=debugstats two-run-persistence arm + render hooks). **GATES ALL GREEN** (subagent full report; coordinator re-ran HEAVY + genhash ×2 + build + G0): G0 0 (after 1 parse fix: 4.7 has no get_index/reorder_child -> child-scan + Node.move_child); debugstats ok:true (visible_when_on:true, HIDDEN-when-off:true [field misnamed visible_when_off], text_has_fields:true, persisted_after_restart:true across 2 runs, cfg_saved:true); SMOKE all baselines (137/135.43/136.78 · 15/10/14 · 2756/914+25/9); genhash 25/25 IDENTICAL (world/* untouched); RENDER 2 PNGs (overlay top-left 4 fields live under llvmpipe VRAM 51MB; options new ☑ toggle after Hunger) verified via read_image; HEAVY boundary r4 ×2 p95 27/30 (in band, no change), remesh_ok+marker_ok, resident 101, perf r4 p95 23 (the always-on 0.25s tick — runs even hidden — added no measurable cost) + fog_ok; build 109,837,456 B sha d35e66c8 8080 byte-match + 5180=200 + 8443 down.
**RESUME (new chat):** (1) read this 00w + `godot/COORDINATOR.md` + HARNESS §1/§3; (2) `python3 tasks/scripts/tasks.py next` — user co-edits the queue LIVE (re-run at EVERY boundary; NEW tasks keep appearing at the head); as of 07:40 queue head = **AC-0156 → AC-0175 → AC-0087 → AC-0154 → AC-0186 → …** [AC-0169 done this round]; (3) THE JOB = the head task — read its TASKS.yaml notes verbatim (User Story + Technical Details); ONE blocking subagent (xhigh for real work per the active goal; low only if genuinely trivial; describe END RESULT + gates + env + code map; subagent honors .scratch/.godot_busy; subagents MAY read_image PNGs, coordinator may NOT); (4) closeout per COORDINATOR.md: TRUST builder light gates → coordinator HEAVY [boundary r4 ×2 + perf, STABLE tree] + genhash ×2 + G0 → build [OWN call per BUILD RECIPE symlinks FIRST] + 8080 byte-match + 5180=200 + 8443 note [down — do NOT start] + kill any 8081 dup → HARNESS §3 only if standing values moved → TASKS.yaml done + queue removal + closing comment → CONTINUITY → commit EXPLICIT PATHS [task folder + godot files + TASKS/HARNESS/CONTINUITY/archive; NEVER other users’ scaffolds or docs/halo-loot-brainstorm.html] + `GIT_SSH_COMMAND="ssh -F /dev/null" git push` → report localhost + LAN 192.168.0.224:8080/5180 → next. Standing: godot ONE at a time; HOME=/tmp/dsh_home every godot call (fresh /tmp per bash call); LONG godot arms → run_in_background + job_output (bash 60 s default kills a foreground long arm); a subagent’s LONG report can hit the LLM output cap (land code+gates+results on disk FIRST, then a ≤20-line report); xvfb render can hang at GL init (timeout -k 15 480, retry once, record; headless gates carry); **GODOT 4.7.1 gotchas found in AC-0169: no OS.get_cpu_usage/get_process_memory_usage/get_total_memory, no get_index/reorder_child (use child-scan + Node.move_child), Performance.MONITOR_* renamed to Performance.TIME_* (TIME_FPS/TIME_PROCESS) — check the 4.7 API before assuming a suggested method exists.** GOAL ACTIVE: "complete all open tasks" — this round did AC-0169 (1 task); next = AC-0156.

## 00v. CHECKPOINT 2026-09-02 (06:05) - **AC-0168 r+1 retain (hide not kill) DONE + PUSHED** [candidate keeps mesh+visible at r+1; fog hides; free only at r+2] — walking the render edge no longer flickers/pops on the back-step. AC-0080 hysteresis kept node+data+edits at r+1 but KILLED the mesh (null slabs, freed collision+occluders, forced col_dirty); stepping back re-queued a build and the worker re-mesh gap read as a flicker/pop at the distance line. Fix: _enter_candidate no longer kills (mesh_built + slabs retained; pending-work erases kept); recenter re-entry now calls _stage_check (a band-0 body still pending at candidacy would never re-stage under the retained-mesh path); _enter_candidate_slabs() removed from chunk.gd. Free timing byte-identical (2-recenter-event hysteresis untouched). Fog was ALREADY (R+1)*16*0.95 (daynight.gd:11-12, applied main.gd:1760-1761) — verified, untouched. Files: world.gd (retain path + re-entry guard + 3 stale-comment refreshes), chunk.gd (removed _enter_candidate_slabs, 20 lines), main.gd (NEW AWECRAFT_LOGIC=edgeretain R=8 probe: 3 tracked edge chunks). **GATES ALL GREEN** (subagent full report; coordinator re-ran HEAVY + genhash ×2 + build + G0): G0 0; edgeretain ok:true (T1 axial band-1 held through 5 one_out events NEVER freed, remeshed:false, vis_min 10; T2 band-2 freed exactly at 2 events; T3 re-enters on back-step remeshed:false; killed_while_candidate:0, back_hole/hidden_frames:0, two_retained 6→13→0, fog 136.8 ≥ ring face 136.0); SMOKE all baselines; genhash 25/25 IDENTICAL (x2 + diff-empty vs AC-0187 A+B); lodswap ok:true (AC-0182 regression; T never a candidate → no-op); HEAVY boundary r4 ×2 p95 31/27 (IN band 30-37, no change), remesh_ok+marker_ok, resident 101, perf r4 p95 23 + fog_ok; build 109,826,640 B sha f341a964 8080 byte-match + 5180=200 + 8443 down (8081 dup died on its own).
**RESUME (new chat):** (1) read this 00v + `godot/COORDINATOR.md` + HARNESS §1/§3; (2) `python3 tasks/scripts/tasks.py next` — user co-edits the queue LIVE (re-run at EVERY boundary; NEW tasks keep appearing at the head); as of 06:05 queue head = **AC-0169 → AC-0156 → AC-0175 → AC-0087 → AC-0154 → AC-0186 → …** [AC-0168 done this round; AC-0188 gdext-gen-fbm-caves is p2 waiting_on AC-0165]; (3) THE JOB = the head task — read its TASKS.yaml notes verbatim (User Story + Technical Details); ONE blocking subagent (xhigh for real work per the active goal; low only if genuinely trivial; describe END RESULT + gates + env + code map; subagent honors .scratch/.godot_busy; subagents MAY read_image PNGs, coordinator may NOT); (4) closeout per COORDINATOR.md: TRUST builder light gates → coordinator HEAVY [boundary r4 ×2 + perf, STABLE tree] + genhash ×2 + G0 → build [OWN call per BUILD RECIPE symlinks FIRST] + 8080 byte-match + 5180=200 + 8443 note [down — do NOT start] + kill any 8081 dup → HARNESS §3 only if standing values moved → TASKS.yaml done + queue removal + closing comment → CONTINUITY → commit EXPLICIT PATHS [task folder + godot files + TASKS/HARNESS/CONTINUITY/archive; NEVER other users’ scaffolds or docs/halo-loot-brainstorm.html] + `GIT_SSH_COMMAND="ssh -F /dev/null" git push` → report localhost + LAN 192.168.0.224:8080/5180 → next. **AC-0187 black-lines BUG FIXED 2026-09-02 (06:40, pushed this round):** scoped edit remesh emitted opaque faces with PLAIN-atlas UVs but the opaque material was ALWAYS the MERGED atlas (chunk.gd:1957) → wrong texels → black lines until the wave’s full (merged-UV) rebuild. Fix (chunk.gd apply_edit_accs only): pass a plain-ms (tex:null) to _assemble_slab so the scoped opaque material uses the plain atlas (cutout/flower/fluid already plain — only opaque was mismatched). Verified: editmat probe ok:true opaque_mat_is_plain:true (A/B: revert → false); render no black lines; editfront ok:true; SMOKE ok; genhash 25/25 IDENTICAL; lodswap ok:true; HEAVY boundary r4 ×2 p95 29/28 + perf 23; build 109,832,112 B sha 9abd0179 8080 byte-match. See tasks/AC-0187/AC-0187-fix-blacklines.html. NEXT = chain AC-0169. Standing: godot ONE at a time; HOME=/tmp/dsh_home every godot call (fresh /tmp per bash call); LONG godot arms → run_in_background + job_output (bash 60 s default kills a foreground long arm); a subagent’s LONG report can hit the LLM output cap (land code+gates+results on disk FIRST, then a ≤20-line report); xvfb render can hang at GL init (timeout -k 15 480, retry once, record; headless gates carry). GOAL ACTIVE: "complete all open tasks" — this round did AC-0168 (1 task); the AC-0187 black-lines fix is next.

**RESUME (new chat - do these in order):** (1) read this 00p block + `tasks/HARNESS.md` §3 [standing values incl. NEW boundary p95 ≈73–89 + SAVE_VERSION 3 + chunkio row]; (2) `python3 tasks/scripts/tasks.py next` [user co-edits TASKS.yaml LIVE — re-run at EVERY boundary; as of 14:05 queue = **AC-0158 → AC-0159 → AC-0154 → AC-0109 → AC-0150 → AC-0137 → AC-0092 → AC-0144/0145/0146 [planet] → AC-0111 → AC-0036 → AC-0147 → AC-0161 → AC-0148 → AC-0149 → AC-0140/0141/0142 → AC-0121 → AC-0122 → AC-0097 → AC-0112 → AC-0113 → AC-0093 → AC-0125 → AC-0094 → AC-0098 → AC-0101 → AC-0037..0040 → AC-0087..0090 → AC-0115 → AC-0130**]; (3) THE JOB = **AC-0158 20hz-diamond-game-tick** (Bedrock Realms 20 Hz tick on Simulate 4 diamond 41 [taxi ≤4, 41 not 81] not 5 Hz square: 1 randomTick per 16³ per tick (~1/68 s per wheat), fluid 20 Hz, redstone 1-tick dust, mob spawn 24–44 circle + n−1 diamond; efficiency + correctness, half columns, far 13–50 never tick; spec.html present in tasks/AC-0158/); delegate as ONE blocking xhigh subagent [v3 — parent idle while child runs; describe the END RESULT + gates + env, the child plans its own implementation], then coordinator re-verifies ALL gates; (4) per-task closeout: verify gates → `./build_windows.sh` [symlinks FIRST per BUILD RECIPE] + 8080 byte-match + 5180=200 + 8443 note [down — do NOT start] → results.html → HARNESS.md [only if standing values moved] → TASKS.yaml done [proper entry, queue removal] → CONTINUITY 00p → commit EXPLICIT PATHS [NEVER the other users' scaffolds] + `GIT_SSH_COMMAND="ssh -F /dev/null" git push` → tasks.py next → report localhost + LAN 192.168.0.224. Standing: godot ONE at a time; `HOME=/tmp/dsh_home` on every godot call; **battery env = `AWECRAFT_BATTERY=…` (a `;`-list in AWECRAFT_LOGIC hits the unknown-logic path and ABORTs the process — learned 2026-08-31)**; logs → `.scratch/<task>-gates/`; /tmp EPHEMERAL per bash call; report ≤20 lines.
**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout). -->