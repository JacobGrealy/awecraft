# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6.

## 00Q. CHECKPOINT 2026-09-10 (night) - **AC-0248 CLOSED (commit e2e4b0c; AweCraft[20260910-2057 e2e4b0c], :8080 byte-matched zip d74b9d1b...) + the TASK-LIST CLOBBER SAGA ends with AC-0254 (the task API) filed at the queue head. Users new ticket AC-0253 (per-slab air-bitset for meshing) is queued #2, then AC-0185.** AC-0248: the pools prewarm to the streaming demand (2 recenter rings = provably 2R columns: 32@R16 / 100@R50; the R16 target = 64 columns / 640 MI / 128 MM pairs), a top-up runs in recenter() (required - the radius applies AFTER _ready), one-time cost 3.7 ms @R16 (6.7 ms worst @R50 incl. the self-healing refill). EVIDENCE: the initial burst grows the pools ONCE by design (col 868 / mi 5498 / mm 804 - it fills the whole 797-column circle, far past the 2-ring prewarm) and the sustained 4x + 50x fly-forwards show EXACTLY 0 growths (steady_zero_ok - steady state fully recycles). Gates: genhash exact, meshprobe 1908/32 + avg A/B 8/8, r16 ok (falsy set identical + spin), wprof flat, battery ok, 0 errors. THE SAGA (why 0254 exists): on 2026-09-10 a coordinator git-checkout of tasks/TASKS.yaml (intended to erase an assumed subagent corruption) actually ERASED the users freshly-filed task - their other-chat agent lost the same three entries (0247/0248/0253) and recreated them; root cause = 4+ writers (coordinator edit-tool, the users editor, two agents) doing raw read-modify-write on one YAML with no lock and different dumper styles. LESSON FOR 0254: the other agents schema is RICHER (a per-entry comments: list with {id, author, text}; pyyaml double-quote style) - the API must preserve ALL fields incl. unknown ones, and its first write normalizes the format once. AC-0243 = USER-GATED, recorded + skipped (queue record now in its notes). PROCESS: the users "do not re-run what the subagent already ran" stands (diff audit + spot checks). NEXT: AC-0254 (the task API - the single-writer CLI: flock + atomic replace + canonical dump + a data-preservation check that every OTHER entry is byte-identical before/after; the first write normalizes the format one time), then AC-0253 (the per-slab air bitset: a 512-byte solid/air bitset per 16x16x16 slab built at gen fill, the greedy walk checks the bitset first for the air/solid fast path + early-outs + neighbor checks, palette-decodes only on quad emit; gate = mesh ms/slab down), then AC-0185 (Esc pause). SATELLITE far-LOD still ON HOLD. The users in-game look test (the AC-0252 far view + the WORLD-PIPELINE overlay at 4x) is still pending - any build from 20260910-1915 carries both. DO NOT COMMIT: user scaffolds (unchanged).
## 00P. CHECKPOINT 2026-09-10 (evening) - **AC-0252 (medium-LOD avg-color ladder) COMPLETE + SHIPPED (commit d7b8585; AweCraft[20260910-1915 d7b8585], :8080 byte-matched, zip 2d185bb3..., fresh libchunkio dll in the export). THE FAR VIEW HAS CHANGED - the users in-game LOOK TEST is the next gate (plus the WORLD-PIPELINE overlay reading for the fps-fix ticket - one flight gives both).** The far zone now renders: MED 8x8x8 (each sample averages 2x2x2 = 8 real blocks) where the 4x4x4 textured low used to be, and LOW 4x4x4 (64 blocks/sample) from the NEW Low LOD start slider (default 58 = render_dist + 8, taxi, clamped to the far ring [R+1, ~R*1.42]) out to the render edge. Both tiers UNTEXTURED vertex-color avg (per face direction: the average of the cached per-block-face average colors of the non-air blocks in the volume; AIR RULE: more than half the volume air -> not emitted) + the STATIC per-fragment noise shader (godot/core/lod_avg.gdshader, +/-6% hash-of-world-pos jitter - no flicker; the day uniform tracks the sky). The FACE-COLOR CACHE (256 blocks x 6 dirs) builds once when loading into a world (~22 ms). ALL SLAB EMIT IS WORKER-THREADED (the C++ low_emit_avg on the TM pool; the GD twin is TEST-ONLY for the harness A/B; _low_build_slab = dispatch only; the only low-lane main-thread work left is the pick/dispatch/handoff-attach machinery - the scene-tree work that must stay on main). This answers the users offload directive (2026-09-10: stop main-thread chunk generation); the AC-0251 LOW stage (~3 ms) is now that machinery, not generation - if it ever gets hot, the targets are the pick scans + dispatch bookkeeping (separate ticket). GATES (agent ran, coordinator diff-audited: zero game-path twin references, all lanes dispatch/air based): genhash exact; meshprobe 1908/32 + the avg A/B pair 8/8; r16 ok (falsy set exactly the pre-existing trio, spin invariant, low_thread 11310 emits/11313 handoffs/0 stale); ladder ok (122 MED @taxi 9, 602 LOW @taxi 10-13, boundary flip 62 slabs, air rule synthetic + in-world); wprof LOW 2.9 avg flat, frame 4.3; battery genhash+range ok; 0 script/shader errors. C++ rebuilt (scons -O2) + re-staged; the build script re-stages the final dll into the export (zip dll md5 beb4f3fd... = fresh from the 14:45 source). STILL ALLOCATES (the next pool target if needed): the per-slab ArrayMesh vertex data (the C++ emit side). QUEUE AFTER: AC-0248 (prewarm), AC-0243 (user-gated = record + skip), AC-0185, ... SATELLITE far-LOD still ON HOLD (the user will ask). The 4x-flight fps fix ticket(s) still await the users live WORLD-PIPELINE overlay reading (any build from 20260910-1004 has the overlay). DO NOT COMMIT: user scaffolds (unchanged).


**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout), 00z moved 2026-09-07 (AC-0231-followup closeout), 00F+00G moved 2026-09-09 (AC-0244/0245/0191 closeout), 00J moved 2026-09-10 (AC-0250 filing), 00K moved 2026-09-10 (AC-0250 closeout), 00L moved 2026-09-10 (AC-0251 closeout), 00M moved 2026-09-10 (AC-0247 closeout + AC-0252 filing), 00N moved 2026-09-10 (AC-0252 closeout), 00O moved 2026-09-10 (AC-0248 closeout + AC-0253/0254 filing). -->