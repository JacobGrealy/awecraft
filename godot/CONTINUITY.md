# AweCraft → Godot migration — CONTINUITY checkpoint
If context is compacted, read THIS + `godot/ARCHITECTURE.md`, then continue from the task list in §6.

## 00O. CHECKPOINT 2026-09-10 (afternoon, late) - **AC-0247 CLOSED (commit 0e6c227; shipped AweCraft[20260910-1328 0e6c227], :8080 byte-matched) + AC-0252 (medium-LOD avg-color ladder) FILED at the queue head. PROCESS CHANGE (user 2026-09-10): "do not re-run what the subagent already ran" - the coordinator now TRUSTS the subagents gate runs and does a DIFF AUDIT + spot cross-checks instead of full re-runs (this cycle: audited the column reset field enumeration against every declared state + the col_gen guards; spot-ran r16 independently -> 272 leaked instances, matching the subagents A/B 272=272).** AC-0247 (slab/column pooling): MeshInstance3D pool (_mi_pool, prewarm 32; checkout resets to fresh-instance defaults, checkin drops the mesh REF only) + MultiMesh pool (_mm_pool, 8 [mmi,mm] pairs; checkin keeps the capacity buffer, the shared pre-baked fog box never freed) + COLUMN OBJECT pool (_col_pool, prewarm 16; _pool_reset covers every declared state field incl. the 24 Slab objects reset in place and the perf arrays re-sized to slab_n() - the subagent caught a real OOB there during r16) + the col_gen LOGICAL IDENTITY TOKEN (Godot 4 instance_id is fixed per object - a pooled reuse would blind every inst-based staleness check; captured at ALL dispatch sites, checked at ALL handoffs, mismatch = drop the stale result). SLAB RAW-ARRAY POOL: honest partial kept on alloc (the TM slab_copy is C++-side; the save-queue copy lifetime spans the compaction-defer re-queue). Gates (subagent runs + coordinator audit): genhash exact, meshprobe ok:true verts 321080, r16 ok:true falsy set identical + spin invariant + leak A/B 272=272, wprof MESHATTACH avg 0.1 (max 4.0) with frame avg 4.4 flat vs 4.2, battery genhash+interact+editsnap+range ok:true, 0 errors. STILL ALLOCATES: the per-slab ArrayMesh vertex data (needs the C++ emit side - the follow-up target if alloc pressure remains), collision bodies, occluders. AC-0252 (queue head, UNSTARTED): a NEW MED tier 8x8x8 (each sample averages 2x2x2 = 8 real blocks) takes the place of the current 4x4x4 low band; the LOW (4x4x4 = 64 blocks/sample) moves further out; a NEW CONFIGURABLE low-start distance (the sim_dist slider pattern in menu.gd:~383-443); BOTH tiers UNTEXTURED AVERAGE-COLOR (per face direction: the average of the cached per-block-face average colors of the non-air blocks in the volume; AIR RULE: more than half the volume air -> the sample is air); the FACE-COLOR CACHE (block id x 6 dirs) built once when loading into a world (atlas rects: godot/autoload/data.gd + godot/core/atlas.gd); the EMIT = C++ + GD TWIN (house pattern: C++ for the far-wave TM pool, the GD twin for the in-r lane, an equivalence check; the existing textured low emit STAYS in the tree, dormant); the NOISE SHADER = a slight STATIC per-fragment world-pos-hash brightness jitter (~5-8%, no flicker) on the med/low instances only. Gates: genhash exact, meshprobe ok, r16 falsy set identical + spin invariant, a harness LADDER arm (med near / low far / configurable boundary / air rule), wprof LOW flat-or-better, battery genhash+range, 0 errors. NEXT STEP: launch the 252 subagent (C++ ALLOWED - read the gdext/SConstruct header comment for the scons command; rebuild + re-stage the .so to godot/bin + exports/windows). After 252: AC-0248 (prewarm), then the queue (AC-0243 user-gated = record + skip). SATELLITE far-LOD still ON HOLD. The 4x-flight fps fix ticket(s) still await the users live WORLD-PIPELINE overlay reading. DO NOT COMMIT: user scaffolds (unchanged).
## 00N. CHECKPOINT 2026-09-10 (afternoon) - **AC-0251 WORLD-PIPELINE INSTRUMENTATION COMPLETE (commit fdee3a1; shipped AweCraft[20260910-1004 fdee3a1], :8080 byte-matched). FIRST DATA: at the 4x repro, LOW (the low-res slab lane) is the dominant main-thread stage on the server CPU. AWAITING the users live overlay reading on their Windows machine, then the fix ticket(s) follow the data.** Instrument-only (zero behavior change, +506/-1 across world.gd/chunk.gd/console.gd/harness.gd): world.gd = a per-stage timing ring before _process (180 frames x 10 stages, pre-allocated, main-thread-only, no hot-path alloc/locks) - TOP-LEVEL DISJOINT partition DRAIN (_drain_build_queue) / LOW (_low_step) / HANDOFF (threadgen_poll + threadmesh_poll) / IO (io_poll) / RECENTER (_recenter_slice) + MISC = exact frame remainder (recon 0% by construction, misc_neg_max_us guard); SUB-STAGES (detail, not in the partition): FACELIGHT (_eff_landed face-block cascade, isolated), RESCORE (_rescore_step, DRAIN subset), MESHATTACH (per-slab instance attach incl. chunk.gd _assemble_slab - the AC-0247 suspect - isolated); exposed via the world.profiler getter (rolling p50/p95/max/avg + active frames + tg/tm/low occupancy; recomputed on read only). console.gd = the live WORLD-PIPELINE table in the AC-0244 debug overlay (backtick/F3; 9 stage rows + FRAME row with p95 + approx fps + occupancy; ~500 ms refresh while open). harness.gd = NEW standalone AWECRAFT_LOGIC=wprof arm (NOT in the battery): r16 settle + 17 s sustained 4x flight in 3 segments (Engine.max_fps=60 pacing so headless measures true 16.7 ms frames), ring read after each segment; RESULT JSON with per-stage tables + recon + flight evidence. ALL 6 COORDINATOR GATES GREEN (independent runs): genhash (0,0) 80b904c0... exact; meshprobe ok:true verts 321080 in BOTH of 2 runs; r16 ok:true with the falsy-leaf set IDENTICAL to HEAD (only the pre-existing air/height/immediate) + the spin invariance holding = the SCHEDULE-TRANSPARENCY proof; wprof ok:true reproducing the subagents headline; battery genhash+range ok:true; 0 script/shader errors. HEADLINE DATA (4x flight, busiest 3 s window, SERVER CPU): frame avg 4.2 / p50 3.7 / p95 10.5 / max ~25 ms; LOW dominant (avg 2.8 / p95 7.6 / max 10.0); HANDOFF spiky (avg 0.2 / max 12.0); MESHATTACH cheap (max ~0.9, active 86/180 frames - the AC-0247 pooling suspect is NOT the biggest cost on THIS machine); FACELIGHT rare (2 frames) but the most expensive isolated event (max ~5.7); quiet windows read ~0.7 ms frame avg. CAVEAT: server CPU at 60 fps pacing - the users 20 fps Windows profile is a DIFFERENT machine (slower CPU, real GPU, vsync): the live overlay reading on their machine is the missing half of the evidence. NEXT: user flies AweCraft[20260910-1004 fdee3a1] at 4x with the overlay open (backtick/F3) and reports the WORLD-PIPELINE table; the coordinator then files the fix ticket(s) against the biggest stage(s) on the USERS profile, and continues the queue (AC-0247 -> AC-0248 -> ...). SATELLITE far-LOD still ON HOLD. PROCESS NOTE: the coordinator ALWAYS runs ./build_windows.sh after a ticket (user 2026-09-10: "the build is quick so just always do it" - duplicate builds fine). DO NOT COMMIT: user scaffolds (unchanged list).


**INFRA LESSON v3 (BINDING — supersedes all older delegation patterns): ALL delegation = ONE blocking subagent (xhigh for real work, low for trivial — user 2026-08-31).** Background subagent + live parent session THRASH the local LLM's single-session KV cache. Parent MUST be idle while child runs; NEVER run godot while a subagent is in flight. Give the child the END RESULT + gates + env + code map — it plans its own implementation (user: "we just need to describe what we want the end result to be, it can plan out how it wants to implement"). Coordinator = re-verify ALL gates independently + docs + commit (NEVER trust the report — AC-0155 example: coordinator's own battery/chunkio/boundary re-runs all reproduced the builder's values exactly). Subagents MAY run godot gates — every invocation in ONE bash command `mkdir -p /tmp/dsh_home && HOME=/tmp/dsh_home /home/angrygiant/tools/godot/godot --headless --path godot …` (missing HOME = SIGSEGV class).
**INFRA FINDING v2 (still true): subagent "token limit" deaths = the llama.cpp server's per-response output cap [homeserver1 = Qwen3.8-27B-UD @192.168.0.224:1234 via llama-swap; cap >~5k]. Small prescriptive responses survive; a DSH HOST RESTART (user op) may help before a big task.**
**BUILD RECIPE:** `./build_windows.sh` resolves the engine as `$HOME/tools/godot/godot` + export templates from `$HOME/.local` — with the ephemeral `HOME=/tmp/dsh_home` you MUST first `ln -sfn /home/angrygiant/tools /tmp/dsh_home/tools && ln -sfn /home/angrygiant/.local /tmp/dsh_home/.local` (fresh /tmp per bash call). 8080 = long-standing winexport daemon [serves the live exports dir — byte-match curl; **the pidfile can go STALE → the script then spawns a DUPLICATE daemon on 8081 (kill it; 8080 stays the authoritative serve contract — happened at AC-0155, dup died on its own**)]; 5180 = docs daemon [root must = 200]; 8443 = https daemon DOWN [serve_web.py absent from repo — user decision pending, do NOT start unrequested].
**DO NOT COMMIT:** user scaffolds `tasks/AC-0092|0140|0141|0142|0150|0154|0158|0159/` [until their tasks run — the task being worked on commits its own folder], `docs/halo-loot-brainstorm.html` [never].


<!-- Archived history: 00l..00 moved 2026-08-30, 00n+00m moved 2026-08-31 (AC-0155 closeout) → godot/CONTINUITY.archive.md. Trigger: compact when CONTINUITY.md >100 lines or >3 checkpoints; keep latest 2 (00r+00q), archive rest; 00p moved 2026-09-01 (AC-0178 closeout), 00o moved 2026-08-31 (AC-0158/0164 closeout), 00z moved 2026-09-07 (AC-0231-followup closeout), 00F+00G moved 2026-09-09 (AC-0244/0245/0191 closeout), 00J moved 2026-09-10 (AC-0250 filing), 00K moved 2026-09-10 (AC-0250 closeout), 00L moved 2026-09-10 (AC-0251 closeout), 00M moved 2026-09-10 (AC-0247 closeout + AC-0252 filing). -->