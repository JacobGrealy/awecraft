# AweCraft — Agent Guide (read first, every session)

Repo = voxel Minecraft-like (Godot 4.7.1, `godot/`, engine `~/tools/godot/godot` always `--path godot`; **Windows-only** since AC-0124).

## After any restart/compaction, read in this order
1. `godot/CONTINUITY.md` — state checkpoint: commands, ops rules, structure, task list.
2. `godot/ARCHITECTURE.md` — architecture + subagent contract (mandatory before ANY code task).
Then continue the task list in CONTINUITY.md §6. Do not re-derive state; the checkpoint files are authoritative.

## Commands (from repo root, all PROVEN)
- Headless logic check (MUST end with zero script errors): `~/tools/godot/godot --headless --path godot --quit`
- Logic tests (print `RESULT {...}` JSON then quit): `AWECRAFT_LOGIC=player|interact|light|fluids|buckets ~/tools/godot/godot --headless --path godot`
- Render (software GL under virtual X, llvmpipe): `xvfb-run -a ~/tools/godot/godot --path godot --rendering-method gl_compatibility` (+ env hooks: `AWECRAFT_SNAPSHOT=path.png`, `AWECRAFT_CAM=top|iso`, `AWECRAFT_SEED`, `AWECRAFT_TIME`, `AWECRAFT_RADIUS`, `AWECRAFT_FLUID_SHOT=1`)
- Windows build (run AFTER EVERY feature/fix, since AC-0074 2026-08-22): `./build_windows.sh` → `exports/windows/AweCraft.exe` (+ `AweCraft_debug_console.exe` + `.console.exe` wrapper) and (re)starts the LAN serve daemon (http 0.0.0.0:8080, pidfile `.scratch/serve_win_export.pid`, `--no-serve` to skip)
- When reporting any running server, ALWAYS give both localhost and LAN: windows exes `http://localhost:8080/AweCraft.exe` + `http://192.168.0.224:8080/AweCraft.exe`

## Ops rules
- **AFTER EACH completed + verified task: `git add -A` (`.gitignore` excludes .scratch/export/exports) + commit (incl. `tasks/AC-NNNN/`) + PUSH to `origin`** (user rule 2026-08-19). Remote added 2026-08-19: https://github.com/JacobGrealy/awecraft (public, ssh, branch `master`). **Push workaround (2026-08-25, PROVEN)**: on this host plain `git push` (ssh) fails — `/`+`/etc` are read-only and `/etc/ssh/ssh_config.d/20-systemd-ssh-proxy.conf` is owned by `nobody` → OpenSSH fatal "Bad owner or permissions". Primary (proven 08-24, re-verified 08-25): `GIT_SSH_COMMAND="ssh -F /dev/null" git push` (skips the bad user-config parse path). Fallback (proven 08-25): `gh` CLI is authed as JacobGrealy, push over HTTPS with the gh credential helper via env (no `~/.gitconfig` write needed): `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=credential.https://github.com.helper GIT_CONFIG_VALUE_0='!gh auth git-credential' git push https://github.com/JacobGrealy/awecraft.git master`.
- **Serving daemons (setsid-detached, SURVIVE opencode restarts — plain `nohup &` dies on every session restart):** (1) Windows-export download = `python3 -m http.server 8080 --bind 0.0.0.0 --directory exports/windows` (pidfile `.scratch/serve_win_export.pid`, log `.scratch/awecraft-winexport-http.log`, spawned/restarted by `build_windows.sh`); (2) Tasks webui (AC-0046) = `python3 tasks/webui.py --daemon` (pidfile `.scratch/tasks_webui.pid`, http 0.0.0.0:5180). Both have port-in-use guards. NEVER kill any of these python server pids from subagents (they only kill specific godot/Xvfb pids).
- Godot runs **ONE at a time** (concurrent runs corrupt the `.godot` cache and hang). Never run two godot processes in parallel, including subagents.
- Renders: ~300000 ms bash timeout, keep render radius R=1-2, always headless-verify FIRST, at most one small render after.
- Kill stray godot/Xvfb/chrome pids (by pid, never pkill your own shell) before re-running if something hung.
- Machine: 6 cores, ~63 GB RAM, Ubuntu 25.10, no audio device (dummy driver), no passwordless sudo.
- Screenshots: subagents save final PNGs into their task folder `tasks/AC-NNNN/` and embed them in `AC-NNNN-results.html` — user reviews via that HTML. (Legacy renders already in `screenshots/`.) Scratch (logs/intermediates) goes in project-local `.scratch/` — prefer it over `/tmp`. `/tmp/**` is pre-allowed in `~/.config/opencode/opencode.json` but `.scratch/` is the default.

## Orchestration discipline (coordinator = the main agent)
- **TASK PROTOCOL (user 2026-08-19)**: every task = `AC-NNNN` in `tasks/TASKS.yaml` (single source of truth — work-todo schema: id/title/source/priority/status/labels/created_at/updated_at/completed_at/waiting_on/notes/comments[] + top-level `queue:` = committed work order; TASKS.md removed 2026-08-19 per user — YAML is the only registry file, human view later via AC-0046 webui). BEFORE every subagent launch: create `tasks/AC-NNNN/` + write `spec.html` (requirements, web refs, verify gates, deliverables) and set status in-progress. The subagent saves its screenshots into that folder and writes a final `AC-NNNN-results.html` (self-contained summary, embedded screenshots where they make sense). On completion the coordinator updates TASKS.yaml, then commit+push. Once AC-0046 lands: use `tasks/scripts/tasks.py` CLI + webui instead of hand edits.
- **Subagents do ALL code AND research/implementing.** Coordinator only: plans, launches tasks, reads short reports, views screenshots, edits coordination files (this file, CONTINUITY.md, plans).
- The coordinator must NOT open large source files or run exploratory greps in its own context — put the question in the subagent prompt instead.
- **User-reported problems go at the TOP of the todo list (high priority), ahead of planned work**, and are researched+fixed via subagents.
- Subagent report limit: **MAX ~20 lines** (files changed, RESULT values, deviations). Anything longer goes into the task's `AC-NNNN-results.html`. This protects the coordinator's context.
- Subagent prompts must be self-contained: point at ARCHITECTURE.md + CONTINUITY.md (paths, not inlined content), state the task, the verification, the report limit. Report `plan.html` by path only — do NOT inline plan gates verbatim. Reliability pattern: SMALL tasks, headless-verify first, honest partial > empty, zero script errors is a hard gate.
- **SINGLE CONTINUITY LOG (user 2026-08-25, updated 2026-08-28)**: every single subagent (`subagent_plan` xhigh) MUST start with `0. First action: append "## RUN — $(date) — AC-NNNN" to tasks/AC-NNNN/continuity.md` before any edit or godot call, then append after each milestone (files changed, gates green/red). It is the handoff record: if the run is interrupted, the relaunch is a RESUME — read it first and continue. The coordinator checks the log before and after any relaunch. (Template lives in `tasks/templates/two-phase.md`.)
- **NEVER launch more than one subagent at a time.** This machine's LLM API serves ONE concurrent request only — parallel `task` calls will stall or collide. All subagents run strictly sequentially.
- **SUBAGENTS ARE LLM-BLOCKING (user 2026-08-24; relaxed 2026-08-27 to LLM-only): while a subagent is running, do NOT run OTHER LLM work** — no second subagent launch, no heavy model work (the local LLM serves ONE request at a time; a parallel launch stalls or collides). **Shell/bash work IS allowed while a subagent runs** (background gate jobs, builds, file ops, small targeted reads/greps): the sanctioned overlap is N's heavy-gate bash job + N+1's Run-1 in parallel. Godot still runs **ONE at a time** regardless — before any coordinator godot call while a subagent is live, check `pgrep` + the gate-job marker; never run godot under a live subagent that may itself call godot.
- **SUBAGENT EFFORT IS ROUTED BY TOOL NAME (user 2026-08-25).** The `$DSH_HOME` plugin `subagent-reasoning` pins each child's reasoning effort from the **tool used to launch it** (subagent tools take no per-run effort flag): `subagent_plan` → **xhigh** (single — plan+implement), `subagent` / `subagent_implement` → **medium** (trivial fast-path only); anything else falls back to the patch's `subagentEffort` config (currently `medium`). So in the single-phase workflow (see `tasks/templates/two-phase.md`): **non-trivial tasks launch with `subagent_plan` (xhigh, single), trivial tasks with `subagent` (medium).** The mapping lives in `~/.dsh/cordis.patch.yml` + `~/.dsh/plugins/subagent-reasoning.js`, is composed **at DSH host start** (an edit takes effect on the NEXT restart), and a host restart **KILLS in-flight subagents** — after one, inspect `tasks/AC-NNNN/continuity.md` + the task folder + `.scratch/AC-NNNN/` for partial progress and relaunch with a resume message pointing at the log's last entry (don't redo finished steps).
- Sequential godot work is therefore automatic; batch independent subagent work into ONE task prompt if possible.
- **HEAVY-GATE PIPELINE (user 2026-08-26, sliced 2026-08-27, single-phase 2026-08-28):** the single (`subagent_plan`, xhigh) verifies ONLY **G0** + **SMOKE** (2-4 modes + genhash when world/* touched) + task probe (≤60s) + **≤1 render when visual**, then exits. **ONE-SHOT BOUNDARY:** at most one r4 self-check for world/*|lighting.gd; trade-off → honest deviation, hard fail → bounce. Coordinator runs heavy gates as background bash job (boundary×1 + flake×1 + genhash, only for world/*; r50 nightly). Commit only after heavy pass.

- **Checkpoint rule**: before launching any large task, update `godot/CONTINUITY.md` (§6 task statuses + new findings) so a mid-task context loss is recoverable.
- Long unattended runs: work the task list end-to-end (verify → `./build_windows.sh` → next task) **until the list is empty**, checkpointing between tasks. Do NOT pause for approval or feel-checks — user directive 2026-08-18: "work through the task list until they are all complete, do not stop for me". Only stop if blocked or at a genuine decision point.
- Bash hygiene for the coordinator: capture long output to `/tmp/opencode/*.log` and grep/tail it; never dump full logs into context.

## Windows test loop (user's primary surface)
After each Godot feature or fix: `./build_windows.sh` → ensure the 8080 daemon is running (the script (re)starts it) → report `http://localhost:8080/AweCraft.exe` + `http://192.168.0.224:8080/AweCraft.exe` → user downloads & runs the exe → next task. Exe bugs are first-class: `godot/` is the source of truth.

## Task status (canonical)
`godot/CONTINUITY.md` top checkpoint block is authoritative — run `python3 tasks/scripts/tasks.py next` for the live next task. As of 2026-08-25 (pm): AC-0086→AC-0105 done; **AC-0106 greedy-meshing DONE as-delivered** (opaque verts −67.2%); **AC-0117 greedy-mesh-texture-mismatch DONE 2026-08-25** — user-reported wrong textures (tree trunks = dirt): root cause was `_merge_atlas()`'s cache-hit return missing the `"h"` (extended-atlas height 1760) key → merged y-UVs ×1.71875 scale on every chunk after the first → foreign tiles; 1-line fix (`godot/world/chunk.gd:47`) + fallback hardening, all gates PASS (genhash 25/25, battery exact, MINFO 125552 exact, seed-16 A/B 0.694% no foreign tiles, coord-verified ON==OFF). **AC-0099 main-menu-exit-button DONE 2026-08-25** (main-menu Exit button: desktop `get_tree().quit()`, web no-op; `mainmenuexit` harness; all gates PASS, menu render coord-verified; builds shipped 18:02); **AC-0107 threaded-mesh-light-build DONE 2026-08-25 as-delivered** (workers run pure-static mesh+light on fresh `duplicate()` copies → fresh Acc buffers; main thread assembles only; kill switch (deleted in AC-0124); build_ms −96.5% spec-gate PASS, total_ms −6.6% honest deviation (feed-paced drain + AC-0082 re-pick churn — follow-ups = AC-0118/0119/0120); p95 62→21; AC-0034 hitch kept open); **AC-0118 recenter-budgeted-amortized-rebuild DONE 2026-08-25** (frame-budgeted 5-phase sliced recenter, 8ms/≤2048-unit steps, byte-identical final band arrays; r50 recenter p95 41→12.2 ms, r4 unchanged; G5 queue_size==0 gate corrected — data_only residual 95==95 A/B'd); **AC-0077 chunk-stream-buildcost DONE 2026-08-26 as-delivered** (P1.3 one batched per-chunk-contained light pass per crossing via `compute_light_flat_batch` — eff cached, workers skip own light; P1.4 collision staging ±1 immediate / far deferred, `AWECRAFT_COLSTAGE=0` revert; fwd_p95 r4 5216→4832/4291 PASS but walk-frame sub-line honestly missed (main-thread light 17–34 ms/chunk — follow-up: batch light off-main, not registered — user co-edits queue); **AC-0119 get-block-air-on-miss-no-sync-gen DONE 2026-08-26** (air-on-miss on all read paths like web `world.block` :882; read_sync_gen hard 0; MINFO 121/121 convergence; D1 signed: `_data_landed` hook removed on evidence); **AC-0124 drop-browser-support-windows-only DONE 2026-08-26** (web export + web-compat paths removed — Windows-only; WorkerThreadPool always on, RENDER_MAX 96 feasible, ENet reserved for MP); **AC-0129 cave-lighting-propagation-fix DONE 2026-08-27 as-delivered** (user-directed Minecraft-semantics correction, documented deviation iii: cross-chunk block-light propagation — combined sky+blk pull kernel + 8 blk-only strips + D1 sky-carry strip + E2 boundary-frame re-enqueue [`AWECRAFT_E2=off` kill switch]; lightaudit A/B: hard cliffs 1711→0 [natural lava 14/0 boundary pairs were the PRE source], torch tunnel seq==ref EXACT, reproducible in heavy gates; battery 5 arms EXACT + genhash 25/25; HONEST G3: boundary r4 walk p95 62-63→85/83 + fwd 3022-3035→4019/4437 [E2 wave + pull kernel on the crossing path — one-shot rule (user 2026-08-27): deviation + follow-up **AC-0131** walk-path-relight-cost-recovery]; F1 initial-load 5875→6978 ms p95 215; F2 MINFO +1844 verts 73-chunk merge reshuffle); **AC-0128 night-darkness-sky-scale DONE 2026-08-28 as-delivered** (web :746 night-scale per face: vColor repack + 4 new unlit chunk_lit shaders + tints-to-atlas + u_day push; one BOUNCE: Run-2's per-bake chamfer mask sweep caused a real 3× fwd regression (104/10902 + 11.4 s stall) — control-A/B worktree at dd070f3 proved attribution (83/3587) → RUN 3 rework: mask = the pull kernel's blk-flood visited set (exact; sparse per-chunk `last_blk_ring`, +1.6 MB at r50, fence holds) + darkside 24401 resolved by the fix [residual 5168 = cross-boundary SKY leak over-count, correctly dark]; gates: nightday reproducible [cave 15.0/1.8, torch 14.11996 day==night EXACT], battery 5 EXACT, genhash 25/25, MINFO F2 −4 verts 1 chunk [64,−16] merge reshuffle, r50 fence HOLDS, build RC=0 + daemons 200; HONEST trade-off deviation: boundary r4 walk p95 92 [vs 85/83] + fwd_p95 4588 [vs 4019/4437; same-machine control 3587] — residual = per-glow-chunk-bake flood+mask+ring in GDScript → recovery on **AC-0131** (C-level candidate = AC-0125 plan); process lessons: control-A/B attribution via git worktree + no-subagent-during-timing-gates hygiene [00k]); **AC-0035 m5a2-light-bake DONE 2026-08-28 as-delivered** (M5a2 bundle complete: R1 web stars [500 billboard quads + core/star.gdshader, verbatim mulberry32(42) port, u_opacity=1−day, eye-follow] + R2 small player light [L_p 5, d0 0.4133, per-fragment add in all 4 chunk_lit shaders, u_player_light=0 default = inert] + R3 viewmodel lighting [unshaded player-local mat copies + per-frame albedo from TTL-cached World.light_at at the EYE cell]; gates: G1 viewlight EXACT [cave0 0.12→0.41333 STRICT lift, outdoor 1.0, torch 0.94133, star 500 @1.0/0.0], G2 nightday EXACT vs AC-0128, G4 MINFO BYTE-IDENTICAL to AC-0128 state, battery 5 EXACT, genhash 25/25, flake EXACT, build RC=0 + daemons 200, midnight render shows stars + halo + hand; HONEST trade-off deviation (smallest of the three): boundary r4 walk p95 96-99 [vs 92] + fwd_p95 typical 5056 [gate 8141 spike outlier; same-moment control 4471; per-crossing TOTAL work NEUTRAL] → mechanism candidate: per-frame additions erode the frame-budgeted drain → throttle fix folded into **AC-0131** [now owns AC-0129 E2 wave + AC-0128 flood cost + AC-0035 drain-erosion; target fence 65/3500]; AWECRAFT_AIM snapshot hook kept; 17 GL renders vs plan ≤1 accepted [vertex-flag A/B]; transient threadgen flake world.gd noted); **AC-0132 torch-crafting-recipe DONE 2026-08-28** (shapeless coal+stick → 4 torches, grid:2 = BOTH grids [T3 table arm proves]; PRE ok:false / POST ok:true reproduced in gates, genhash 25/25, smoke3 EXACT, build RC=0 + daemons 200; findings: recipe tables = FULL 1:1 web port [subset assumption stale], web has NO torch recipe = user-directed class ii, charcoal skipped [item absent], ids 9=Cobblestone/143=Arrow/table=block 20; no UI change needed); **AC-0133 stars-move-with-sky DONE 2026-08-28** (user "dead pixels": main.gd:1576 copied the camera's FULL transform to the star field every frame [AC-0035 shipped position+rotation instead of web :3248's position-only] ⇒ field orientation tracked the camera ⇒ stars screen-fixed; fix = one line global_transform→global_position + star.gdshader world-space camera-basis billboard; PRE ok:false [rot [0,90,0] after 90° yaw] / POST ok:true reproduced fresh; viewlight + smoke2 EXACT; night render dots-verified; new AWECRAFT_LOGIC=stars yaw-A/B probe guards this class); **AC-0134 ac128-regression-random-lit-chunks-at-night DONE 2026-08-29 as-delivered (Run-2 RESUME @ xhigh per user directive — prior medium run stopped mid-run [probe v2 + PRE captured, fix unapplied]; root cause = AC-0128 RING MASK bit-collision at HEIGHT=80 [3072/5120 (side,yy) pairs + 3/4-side decode offset] mis-marking boundary block-light at night — NOT the sky-carry eff strip [MC-correct cross-chunk skylight; AC-0129 shipped it at 0 cliffs; fix-5/6 blk-only strips killed it → 1173 false cliffs, retracted]; Fix-7 = eff import sky-carry verbatim + lossless per-side face mask `_compute_face_blk` [memoized seed→flood→inject→re-flood] + nightlot probe keyed on STALE; gates: lightaudit cliffs 1173→0 + tunnel EXACT, nightlot ok:true [gate-run ok:false = initial-settle 80/81 measurement artifact — clean re-run recenter 0/0, leak_stale 0, bright no_source 0, materials uniform], battery nightday/viewlight/light EXACT, genhash 25/25 byte-identical, MINFO byte-identical to AC-0129, midnight render uniformly dark; HONEST one-shot deviation: boundary r4 walk p95 85→125 + fwd_p95 4019→12497 [fence 65/3500 — lossless face costs more than the ring; MINFO identical ⇒ pure cost this seed] → recovery on AC-0131 [now 4 cost components incl. AC-0134 face-mask; C-level candidate = AC-0125 plan]; g0 1 known PRE-EXISTING transient threadgen_handoff [world.gd:474, not introduced]; build RC=0 exe 109,655,928 B + 8080 serves fresh bytes)** → **AC-0100 build-scripts-datetime-release-names DONE 2026-08-29 (trivial bypass, medium builder; build_windows.sh +71/−19: STAMP=$(date +%Y%m%d-%H%M) → stamped trio AweCraft-$STAMP.exe / _debug_console.exe / .console.exe + un-stamped latest copies [sha256-identical, die on mismatch] + prune_stamped() retention-3 + BUILD.txt/print_urls stamped URLs; gates G1–G5 PASS [gating build STAMP=20260829-0059: planted fakes deleted, RC=0, sha256 ×3, PE GUI+pck/CUI, 8080 un-stamped+stamped 200 byte-matched]; bug caught+fixed in-run: bare $STAMP_debug_console var-expansion → ${STAMP}_ braces; 5180 webui = env constraint [sandbox-spawned daemons die at call end; host restart: python3 tasks/webui.py --daemon])** → **AC-0135 moon-ambient-5-percent DONE 2026-08-29 (Run-1: spec premise stale — in-repo AC-0128 L-formula already floors eff-0 at 0.12=1.8/15 ⇒ no shader change + AWECRAFT_LOGIC=floor probe, v1 gates PASS → USER: 0.12 looks pitch black ⇒ floor = ~20% of sunlight (0.20=3.0/15) + web spec no longer referenced [spec_template.py boilerplate updated too] ⇒ Run-2 (xhigh resume): 4× chunk_lit shaders `L = 0.20+0.80*max(u_day*sky, blk)` [const+gain=1.0 ⇒ noon full-sky EXACT 1.0, eff-0 floor 0.20 day==night] + floor probe spec 0.20 + player.gd vm mirror + chunk.gd comment; PRE ok:false 0.12 / POST ok:true 0.20 [torch 0.94667, open_noon 1.0 EXACT]; NEW baselines: nightday cave 15.0/3.0 + torch 14.19996, viewlight cave0 vm_no_pl 0.2 [halo d0 0.41333 preserved]; battery 5 EXACT + genhash 25/25 byte-identical [eff array untouched]; heavy gates v2: g0 0 errors, boundary walk 126/fwd 11129 in-band [no cost change], build 20260829-0548 + 8080 byte-matched; 1 render: cave visibly moonlit; artifacts de-webbed per user directive)** → **AC-0136 texture-filtering-sharp-nearest-regression DONE 2026-08-29 (xhigh single — user-reported regression: block textures bilinearly blurry after AC-0128; root cause = shader-hint layer only [Godot 4.7.1 bare `sampler2D` = LINEAR default; runtime ImageTextures expose no filter API at all — sharpx-verified `filter_api_exposed=false` — ⇒ `: filter_nearest` is the sole authority; chunk.gd:87 "NEAREST default" comment was false]; fix = 4× chunk_lit shaders ×1 line ONLY + AC-0135 frozen L-line byte-identical; audit: fluid_anim.gdshader already had the hint [fluids were sharp — documented, no fix], viewmodel NEAREST preserved; sharpx probe PRE blended_linear off=7 [5–7 px ramp] / POST pure_nearest off=0 [hard 1-px step]; 3 renders [PRE closeup blurry / POST closeup sharp / midnight cave = G2, only diff = sharp texels]; heavy gates ALL PASS: g0 0 errors [2× known threadgen transient = AC-0137 class], genhash 25/25 byte-identical, boundary walk 123/fwd 11675 in-band [AC-0134 125/12497], player flake byte-identical, build 20260829-0809 + 8080 byte-matched; deviations: MINFO −48 verts merge-reshuffle flake [rerun-verified], micro-cave probe face absent pre-existing [informational])** → **AC-0109 frustum-culling DONE 2026-08-29 (xhigh single, spec de-webbed pre-launch: per-frame 6-plane frustum from camera xf/fov/aspect; per-slab AABB test [culled ⇔ ∃ plane d+span < −M, M=32.0 m], column early-out, visible set-on-change, world-level fixed scratch [no per-chunk state, no per-frame allocs, no O(chunk²)], kill-switch `AWECRAFT_FRUSTUM_CULL=0`, radial candidate path untouched; world.gd +141 / main.gd +173 + spin probe `AWECRAFT_LOGIC=spin`; gates: spin PRE ok:false verts identical / POST ok:true **28% vert drop [268096/371444]** + transitions 0 over 360° + queue flat, MINFO 121 lines/81 chunks all_meshed [drift A/B-attributed PRE-EXISTING — kill-switch run [AC-0109 fully inert] still drifts [16,48], merge-reshuffle class = AC-0136 flake; cull only writes instance.visible], SMOKE 5 EXACT, genhash 25/25 byte-identical, boundary walk p95 133 [band 108-126; builder one-shot same code 120 → run noise] / fwd 12076 mid-band, build 20260829-1042 109,674,968 B + 8080 byte-matched; gate MINFO invocation = `AWECRAFT_LOGIC=perf AWECRAFT_MESH_INFO=1` headless — MINFO dump lives in `_perf_test` main.gd:7858, NOT the quitmenu or snapshot arms])** → **in-progress: AC-0138 leaves-always-black-regression (user p1 — tree leaves [block 7, cutout] render black day AND night; regression from AC-0128/0134/0135/0136 window; spec pre-written + de-webbed at tasks/AC-0138/spec.html; root-cause candidates: cutout L path / vColor repack / lighting occluder-check / atlas tint bake / face-shade; leaf probe + 2 sanctioned day+night renders; xhigh single via `subagent_plan`)** → AC-0139 split-main-menu-into-own-scene [user p1] → AC-0140 extract-harness-from-main-gd [user p2; main.gd 8939 lines — all AWECRAFT_LOGIC arms incl. AC-0109 spin move with this] → AC-0141 split-blocks-items-into-json [user p2] → AC-0142 split-mobs-into-json [user p2] → AC-0121 → AC-0122 → AC-0097 → AC-0112 → AC-0113 → AC-0093 → … → AC-0131 walk-path-relight-cost-recovery [INTAKE ONLY, not queued] → AC-0137 threadgen-handoff-nil-pba-chunk-holes [p2 — pre-existing transient kills in-frame chunk meshes ⇒ visible sky-through-chunk holes] — web spec NO LONGER referenced (user 2026-08-29): provenance = in-repo formula, values = user-directed; user co-edits TASKS.yaml live [2026-08-29: queued AC-0138..AC-0142 ahead of the perf tail], re-run `tasks.py next` at every step**) per the committed `queue:`.

## Game semantics reference (user 2026-08-20)
- Minecraft wiki is the canonical reference for how tools/crafting/blocks should behave — AweCraft must match it **exactly unless the user specifies otherwise**: https://minecraft.wiki/w/Tool , https://minecraft.wiki/w/Block (plus related: Tool_durability, Mining, Crafting_table, Item_repair, Item).
