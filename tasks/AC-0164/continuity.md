# AC-0164 — thread chunk I/O off main thread — continuity

## 00 — 2026-08-31 — IMPLEMENTED, light gates green, ready for coordinator
- **Shipped (FileAccess-on-worker — primary directive, fallback NOT needed):** the entire AC-0155 column I/O now runs on the WorkerThreadPool (same singleton as threadgen; AC-0082 per-tid-slot + stale-drop handoff pattern). `world.gd`: new state (`io_pool`, `_io_read_inflight`/`_io_read_keys`, `_io_write_inflight`/`_io_write_keys`, `_io_slots`, counters `_io_enq/_io_dedup/_io_wdedup/_io_drops/_io_fails/_io_write_n/_io_main_read_ms/_io_main_write_ms`); `_io_read_enqueue` (main-thread cost = file_exists + add_task; true = covered — caller must not gen), `_io_read_worker` (FileAccess READ + `ChunkIO.decode_column` on the worker; fails closed → empty result), `io_poll` + `_io_read_handoff` (provenance `chunk_origin`/`disk_reads`/`disk_read_ms` marked when data LANDS; worker-side ms folded into `disk_read_ms`; decode failure on an empty column → threadgen fallback), `_io_write_enqueue` (per-key dedup; `ensure_dir` on main, path from CAPTURED slot) + `_io_write_worker` (encode + FileAccess WRITE). Sites: `_gen_unit` disk-first async for every column EXCEPT (0,0) (spawn contract stays sync — `_try_disk_load` kept for it); recenter 5×5 burst: a read enqueue (file exists or already in flight) sets the `had_data` snapshot true so the burst worker skips it — the ~500 ms sync 24-col load and the drain's 1-inflate/frame are both gone; `_queue_chunk_save` captures **slot** at enqueue (mid-flight slot change on continue/new-game safe); `_drain_save_queue` keeps 1–2/frame pacing, enqueues only; `_materialize_chunk_data` never sync-loads/gens over an in-flight read; drain pick skips `_io_read_keys` (like `_tg_inflight_keys`); `_process` early-return keeps the frame active while I/O is in flight + `io_poll()`; `_exit_tree` drains read+write tasks before script unload. `main.gd`: chunkio result gains an `io` counter block (gate fields untouched). `chunk_io.gd` + `save.gd` UNTOUCHED (AWCC v1 bytes + SAVE_VERSION 3 unchanged).
- **Main-thread cost measured (same chunkio probe, this box):** per save 32.6 ms → **0.049 / 0.106 ms**; per disk-read 30.5 ms → **0.032 / 0.059 ms** (≈300–950×; after = file_exists + add_task only). Encode ~26 ms / decode ~20 ms now cost worker time.
- **Light gates (all PASS):** G0 ×2 exit 0 / 0 SCRIPT ERROR. SMOKE battery ok:true — player [8.5,137,8.5]/135.43/136.78, interact place_ok true, light 15/10/14/9, fluids 2756/2756 + 914/914 stable + 25/9, genhash 25/25 byte-identical to AC-0091 baseline. chunkio ×2 (fresh HOME): both ok:true, 41/41 files, 41/41 byte-identical, r4 revisit origin 41 disk/0 gen, r50 disk 41/gen 0, wall 40.7 s / 35.4 s (≤60 s), 0 script errors, io drops 0 / fails 0. save ok:true (6/6 blocks, iso, v1_softfail). continue edits_ok/pos_ok/saved_chunk_built_at_spawn true, stable y_trace, core_ms 4904 (AC-0155: 4355/4436). RENDER ×1: `tasks/AC-0164/render_R1.png` 1280×720 m4:ok, clean (grass/trees/water/stone + HUD).
- **Deviations / notes for coordinator:** (1) **boundary r4 walk p95 expected to DROP below the 73–89 band** — evict-time writes (1–2 × ~33 ms/frame at recents, worst at the crossing-16 burst) left the main thread; crossing-16 burst wall may also shrink. (2) `Invalid Task ID` at recenter `is_task_completed` burst-group prune — pre-existing known class (AC-0155/AC-0152 logs; backtrace verified same site). (3) 1× AC-0137-class `threadgen_handoff` Nil→PackedByteArray transient in SMOKE — documented known noise (AC-0155 smokes had 1 each). (4) chunkio run-to-run spread normal (wall 34–44 s; saved columns 108–117 by drain timing; the gated 41-diamond checks exact every run). (5) Infra: `/tmp` is EPHEMERAL per bash call on this box — `AWECRAFT_SNAPSHOT` must be an absolute path, copied into `tasks/AC-0164/` within the same invocation (relative paths fail `save_png`).
- **Coordinator to-do (heavy, after this exit):** boundary r4 ×1 + perf r4 (record p95 vs 73–89 — expect drop), genhash re-run (world/* touched), flake ×1, Windows build + 8080 byte-match, commit (code + results + continuity + render) + push. Scratch logs: `.scratch/AC-0164-gates/`. Results: `tasks/AC-0164/results.html`. Fences held: no TASKS.yaml/CONTINUITY.md/HARNESS.md edits, no git, no Windows build, no policy/codec/SAVE_VERSION changes.
