# AC-0133 continuity — stars-move-with-sky regression (user-reported)

## RUN 1 (research/planner, 2026-08-28)
- Plan delivered: `tasks/AC-0133/plan.html` (7 sections; all Godot file:line anchors + web constants read verbatim this run). **ZERO godot runs** — code-only audit; the transform chain is pinned entirely from source (no runtime probe needed; web search was unavailable in this session).
- **Diagnosis (evidence, not the spec's parent theory):** parent theory = FALSE — "Stars" (MeshInstance3D, main.gd:282-287) is `add_child`-ed to `Main` (main.tscn:5 root Node3D) at main.gd:287, sibling of World/Drops/Entities. Root cause = the per-frame write at **main.gd:1576** (`_update_sky` :1562-1581): `_star_node.global_transform = scam.global_transform` — AC-0035 Run-2 shipped a FULL camera transform copy (position + rotation) instead of the AC-0035 plan's position-only copy, and `star.gdshader:13-15` uses a LOCAL-space quad offset (header :1-6 documents "follows the camera transform every frame, so local space == view space") that billboards only because the node frame tracks the camera. Star centers are baked at r=320 in the node's LOCAL frame (main.gd:326) ⇒ field's world orientation rotates in lockstep with the camera ⇒ every star locked to its screen pixel = "dead pixels".
- **Why AC-0035 missed it:** G6 render = single static frame (screen-fixed vs world-fixed look identical in one frame; no gate yaws); viewlight probe reads only `u_opacity` + presence (main.gd:4529-4536), never the transform. G1 here = the missing transform-behavior gate.
- **Web parity (index.html, read this run):** `__stars` = THREE.Points at SCENE ROOT (:729), absolute r=320 world coords (mulberry32(42), :720-726), `opacity = 1-day` (:1347), the ONLY per-frame write = `position.copy(camera.position)` (:3248, after :3246 sets cam to eye) — position only, rotation NEVER written (grep: exactly 5 `__stars` refs :727/:728/:729/:1347/:3248). Web semantics: field fixed in world orientation, recentered on eye — yaw rotates the field in the view; walking follows the eye, no parallax. EYE 1.62 both sides (index.html:116 / player.gd:3 / player.tscn:17 camera local (0,1.62,0) ⇒ cam global pos = eye).
- **Fix design (plan §5):** (1) main.gd:1576 → `_star_node.global_position = scam.global_position` (position copy only; rotation never set anywhere ⇒ stays identity); (2) star.gdshader vertex :13-15 → world-space camera-basis billboard: `w = (MODEL_MATRIX*vec4(VERTEX,1)).xyz; off = (row0(mat3(VIEW_MATRIX))*(UV.x-0.5) + row1*(UV.y-0.5))*0.70 (via transpose); VERTEX = w + off - vec3(MODEL_MATRIX[3])` — field orientation stays identity, quads face the camera; MODEL_MATRIX vertex-stage usage PROVEN in-tree (AC-0035 player-light v_world_pos varying, 4.7.1 llvmpipe GL-compiled); fallback row extraction via component access if `transpose()` unsupported. Mesh builder (:316-344) + mulberry helpers (:290-313) + opacity push (:1581) + render_mode (star.gdshader:8) byte-identical (R3/R4). NOTE: AC-0035 PLAN §5.3 anchor positions are the proven-wrong artifacts (AC-0035 continuity lines 29-30) — shipped loop is authoritative.
- **Probe design (plan §5.4):** new `AWECRAFT_LOGIC=stars` branch in `_run_game` after the viewlight branch (main.gd:932-935) → `_stars_test(spawn)` (sync, no await, NO world settle — star transform is chunk-independent, few seconds): null-guard → `Game.time_of_day = 0.0` + `_update_sky()` → `player.look(0.0, 0.0)` + `_update_sky()` → sample A {path `/root/Main/Stars`, parent name, star global_position/global_rotation(degrees), eye = cam.global_position, basis-angle-from-identity (Quaternion(global_basis).angle_to(IDENTITY).deg()), u_opacity, count = ARRAY_INDEX size/6 = 500} → `player.look(PI/2.0, 0.0)` + `_update_sky()` → sample B. `player.look` (player.gd:1180-1183) = the established direct camera-set (AWECRAFT_AIM path main.gd:1285/:1309); physics inert (Game.mode ≠ "play", player.gd:197-199). ok = parent ≠ Camera3D AND basis angle < 1° at BOTH samples AND |star_pos − eye| < 0.01 at BOTH AND opacity > 0.95 AND count == 500. RESULT JSON keyed per spec {ok, parent_name, eye_a, eye_b, star_pos_a, star_pos_b, star_rot_a, star_rot_b, yaw_delta_deg, opacity_a, count} + mode/seed/path/elapsed_ms.
- **PRE/POST expectations:** PRE (current tree) = ok:false on rotation only — parent "Main" passes, star_pos==eye passes (position copy is inside the transform copy), star_rot_a [0,0,0] (spawn yaw 0, player.gd:30-31), star_rot_b [0,90,0] (basis 90° > 1°) — the honest A/B the spec asked for ("fails the rotation and/or parent checks"). POST = ok:true — star_rot_a == star_rot_b == [0,0,0], star_pos == eye at both, opacity 1.0, count 500.
- **Tier (plan §6):** main.gd + star.gdshader = SMOKE-only (no world/*|lighting.gd): G0 + G1-PRE→POST + G2 viewlight EXACT (star {present,count 500,op 0.0/1.0} + AC-0035 values) + G3 SMOKE `player;interact` EXACT + G4 one night R=1 render (AWECRAFT_TIME=0.0 AWECRAFT_CAM=eyeup, PNG → tasks/AC-0133/). NO genhash/boundary/perf/flake/r50. Coordinator heavy job: g0 + stars fresh + viewlight fresh + smoke2 + build + curls → `.scratch/AC-0133-gates/`.
- **Run-2 file list (plan §2):** main.gd (1-line fix :1576 + probe branch ~:936 + `_stars_test`/`_star_basis_deg` beside the viewlight block :4407-4572), star.gdshader (vertex :13-15 + header :1-6). Read-only: _build_star_mesh :316-344, viewlight probe, player.gd, world/*, lighting.gd, data.gd, index.html, TASKS.yaml, docs/halo-loot-brainstorm.html.
- **Open items for Run-2 (plan §7 tail):** (1) probe-first: G1-PRE before any fix, record PRE JSON; (2) GL-verify the new vertex shader via the G4 render (star DOTS not streaks + 0 shader errors; throwaway xvfb `--quit` compile-check allowed for GLSL iteration, ≤1-2, not a G4 render); (3) gate order + results.html + RUN 2 entry + EXIT.
- State: plan.html + this log written; NO code changes; NO godot runs. Godot was FREE at start (AC-0132 gate marker exists; still ps-check per protocol). Ready for Run-2 (subagent, medium).

## RUN 2 (builder, 2026-08-28)
- Probe-first per plan §7: landed ONLY the `AWECRAFT_LOGIC=stars` branch (main.gd, after the viewlight branch ~:936) + `_stars_test(spawn)` + `_star_basis_deg(n)` (appended beside the viewlight probe block, ~:4575). Probe body verbatim per plan §5.4 (night t=0.0; sample A at `player.look(0,0)`; sample B at `player.look(PI/2,0)` + explicit `_update_sky()` between; null-guarded; sync, no await, no world settle). Deviations from the plan's pseudocode (engine 4.7.1 API, all hit via parse/parse-only, zero semantic change): `Quaternion.angle_to` unavailable → basis angle via trace `acos((tr(R)−1)/2)` (Euler-free, same measure); `Basis.xx/.yy/.zz` → columns `b.x.x + b.y.y + b.z.z`; `deg()` global unavailable → `* 180.0 / PI`; `ArrayMesh.get_arrays_for_surface` → `surface_get_arrays(0)` (count = index size / 6).
- G0 PRE: rc 0, 0 SCRIPT ERROR (`_star_basis_deg` parse iterations were all parse errors, none shipped).
- G1-PRE RESULT (verbatim, `.scratch/AC-0133/stars_pre.log`): `{"count":500,"elapsed_ms":0,"eye_a":[8.5,36.6199989318848,8.5],"eye_b":[8.5,36.6199989318848,8.5],"mode":"stars","ok":false,"opacity_a":1.0,"parent_name":"Main","path":"/root/Main/Stars","seed":44,"star_pos_a":[8.5,36.6199989318848,8.5],"star_pos_b":[8.5,36.6199989318848,8.5],"star_rot_a":[0.0,0.0,0.0],"star_rot_b":[0.0,90.0,0.0],"yaw_delta_deg":90.0}` — ok:false on the ROTATION check only (basis_deg_b = 90° > 1°); parent "Main" passes, star_pos == eye both samples passes, opacity 1.0, count 500 — the honest A/B the spec asked for. RC 0, 0 SCRIPT ERROR.
- State: PRE recorded; fix NOT yet applied (main.gd:1576 + star.gdshader vertex pending).
- POST fix landed (plan §5): (a) main.gd `_update_sky()` :1576 `global_transform` → `global_position` (web :3248 position-copy parity, comment added, null-guards kept); (b) star.gdshader vertex → world-space camera-basis billboard per §5.2 (header rewritten; render_mode/uniforms/fragment byte-identical; mesh builder untouched). One engine-shading deviation from §5.2 verbatim: `vec3(MODEL_MATRIX[3])` (vec4→vec3 constructor) is rejected by this 4.7.1 shading language (`SHADER ERROR: Invalid arguments for the built-in function: "vec3(vec4)"`, render throw 1, stars not rendered) → `MODEL_MATRIX[3].xyz` swizzle, same math. Throwaway compile-check render (scratch path) = 0 shader/error lines before the final G4 shot.
- G1-POST RESULT (verbatim, `.scratch/AC-0133/stars_post.log`): `{"count":500,"elapsed_ms":0,"eye_a":[8.5,36.6199989318848,8.5],"eye_b":[8.5,36.6199989318848,8.5],"mode":"stars","ok":true,"opacity_a":1.0,"parent_name":"Main","path":"/root/Main/Stars","seed":44,"star_pos_a":[8.5,36.6199989318848,8.5],"star_pos_b":[8.5,36.6199989318848,8.5],"star_rot_a":[0.0,0.0,0.0],"star_rot_b":[0.0,0.0,0.0],"yaw_delta_deg":90.0}` — ok:true: field basis 0.0° at BOTH samples (does not rotate under 90° yaw), star_pos == eye at both, opacity 1.0, count 500. RC 0, 0 SCRIPT ERROR.
- G2 viewlight re-pass (`.scratch/AC-0133/viewlight_post.log`): EXACT vs AC-0035 — star {present true, count 500, opacity_noon 0.0, opacity_midnight 1.0}; cave0 mat_L 0.41333 (vm_no_pl 0.12); outdoor 1.0; torch 0.94133; d0 0.41333 @ level 5; surface [2,32,2]; pocket [0,25,-4]; ok:true; rc 0, 0 SCRIPT ERROR.
- G3 SMOKE `player;interact` (`.scratch/AC-0133/smoke_post.log`): EXACT — player horizontal_moved 2.82 / jump_peak_y 36.43 / is_on_floor true; interact breakable_id 2 / drop_spawned true / place_ok true / after_place_cell 2; battery ok:true; rc 0, 0 SCRIPT ERROR. No genhash (no world/*|data.gd), no boundary/perf (SMOKE tier).
- G4 RENDER: throw 1 (shipped-verbatim shader) = SHADER ERROR `vec3(vec4)` → stars not rendered (known-broken, 146 KB, also carried the known transient threadgen_handoff world.gd flake); fix = `MODEL_MATRIX[3].xyz`; throwaway compile-check render (scratch, 0 shader/error); FINAL render → `tasks/AC-0133/stars_night.png` (AWECRAFT_TIME=0.0 AWECRAFT_RADIUS=1 AWECRAFT_CAM=eyeup, 239 KB) — rc 0, 0 shader/error lines, star DOTS (screen-aligned, not streaks) across the sky — visually verified.
- FENCE verified: `git status --short` = only `godot/core/star.gdshader` + `godot/scenes/main.gd` modified (+ untracked foreign docs/halo-loot-brainstorm.html, tasks/AC-0109/ pre-existing, tasks/AC-0133/ this task). No git ops, no build_windows.sh (coordinator heavy job: g0 + stars fresh + viewlight fresh + smoke + build + curls → `.scratch/AC-0133-gates/`).
- State: COMPLETE — all gates pass (G0×2, G1-PRE/POST honest A/B, G2 EXACT, G3 EXACT, G4 clean). Deliverables: `AC-0133-results.html` (PRE+POST verbatim, root cause, web :3248 parity rationale, render embedded) + `stars_night.png` + this log. EXIT.

## RUN 2.5 (coordinator heavy gates + FINAL, 2026-08-28 10:00)
- Gate job bash-97 (09:59-10:00): g0 rc=0; stars probe REPRODUCED ok=true (parent Main, rot [0,0,0] both samples under 90° yaw, opacity 1.0, count 500); viewlight EXACT; smoke2 EXACT; build RC=0 exe 109,625,224 B; daemons 200.
- DECISION: PASS — status → done; commit (explicit paths) + push; NEXT = AC-0134 random-lit-chunks-at-night (user regression; spec written by coordinator; Run-1 launching).
