# AC-0134 continuity log (append-only)

## RUN 1 — 2026-08-28 (research/planner, xhigh)

**Deliverable:** `tasks/AC-0134/plan.html` (7 sections). NO code changes, NO git, touched nothing outside `tasks/AC-0134/`.

**Probe runs (3, all r4 seed 44, committed tree = AC-0133 60d2060):**
1. `AWECRAFT_LOGIC=nightday` → `RESULT {"cave":[-49,34,-52],"cave_day":{"class":15.0,"col":[1.0,0.0,0.8],"mech":"unlit-formula","u_day":1.0},"cave_eff":15,"cave_night":{"class":1.8,"col":[1.0,0.0,0.8],"mech":"unlit-formula","u_day":0.0},"darkside_candidates":5168,"df_day":1.0,"df_night":0.0,"mech":"unlit-formula","mode":"nightday","mouth":[-36,8,62],"ok":true,"radius":4,"seed":44,"torch":[-53,22,-49],"torch_day":{"class":14.11996,"col":[0.0,0.93333,1.0],"mech":"unlit-formula","u_day":1.0},"torch_eff":14,"torch_night":{"class":14.11996,"col":[0.0,0.93333,1.0],"mech":"unlit-formula","u_day":0.0}}` — EXACT at AC-0128/AC-0035 anchors. darkside 5168 = sky-carry cells PRESENT in the settled world.
2. `AWECRAFT_LOGIC=viewlight` → `RESULT {"ambient_night":0.25,"cave0":{"blk":0,"eff":0,"mat_L":0.41333,"pl_contrib":0.29333,"sky":0,"vm":0.41333,"vm_no_pl":0.12},"elapsed_ms":12649,"mechanism":"post-viewlight","mode":"viewlight","ok":true,"outdoor":{"blk":0,"eff":15,"mat_L":1.0,"pl_contrib":0.0,"sky":15,"vm":1.0,"vm_no_pl":1.0},"player_light":{"d0":0.41333,"level":5.0},"pocket_cell":[0,25,-4],"radius":4,"seed":44,"star":{"count":500,"opacity_midnight":1.0,"opacity_noon":0.0,"present":true},"surface_cell":[2,32,2],"torch":{"blk":14,"eff":14,"mat_L":0.94133,"pl_contrib":0.0,"sky":0,"vm":0.94133,"vm_no_pl":0.94133},"uDay_mid":0.0,"uDay_noon":1.0}` — EXACT at AC-0035 anchors.
3. `AWECRAFT_LOGIC=lightaudit` → `RESULT {"chunks_built":81,"cliffs":{"count":0,"max_delta":0,"pairs":[]},"fluid_pairs":{"count":2,"pairs":[[2,30,15,12,2,30,16,15,5,5],[13,30,-1,15,13,30,0,12,5,5]]},"mode":"lightaudit","ok":true,"queue_size":40,"seed":44,"tunnel":{"dir":[1,0,0],"ok":true,"ref":[9,10,11,12,13,14,13,12,11,10,9],"seq":[9,10,11,12,13,14,13,12,11,10,9],"torch":[-53,22,-49]},"worst_sources":[]}` — cliffs 0 (no 0-vs-15 PAIR in this settled build order), tunnel seq==ref EXACT.

**Root-cause verdict:**
- **(a) CONFIRMED** — eff build-order lottery: sky-carry in the "v" strip (`world.gd:1044-1045`, `v = max(eff_n>sky_n?eff_n:0, sky_n)`) injects 14 into a sealed cave cell when the open-neighbor's `last_eff` exists at build time, 0 otherwise; E2 re-enqueue fires only on `blk_src` landings (`world.gd:1101-1102`) so sky-only landings never heal it (documented gap `world.gd:988-991`); recenter re-entry re-pulls with current strips (`world.gd:1303-1324` → `_build_unit` :681-699) so every walk re-rolls = the user's "random". Face light (20×20 bake box `chunk.gd:1482-1549`) shows 15 over the open part of the cave vs 0 sealed = the user's "15 vs 0". Web has the same order-dependent arrays (`index.html:987` skip-neighbor-if-unlit + `:1041-1044` no re-flood) — the port adds the re-roll; spec fence reference = deterministic 0.
- **(b) EXCLUDED** as a per-chunk mechanism — all build paths → single `_get_mat` (`chunk.gd:143-161`) + shared `_mat_cache` (:63-80); atlas ready before first build (`data.gd:178-192`); merge atlas pre-built (`world.gd:197-198`); `u_day` pushed to all cached mats per frame (`main.gd:1597-1602`, `chunk.gd:75-80`); legacy fallback only on uniform atlas failure. Guarded by per-chunk material enumeration in the new probe.
- **"Fully lit at night" visual:** on chunk_lit mats only g-channel faces (mask=1) are bright; mask is block-only (never sky) → most plausible = legitimate LAVA light (cave space y<8 = lava, `generator.gd:269` = web `:828`; glow reaches y≤21 in shafts) with source out of sight. Probe's `bright_no_source` check discriminates in the user's world.

**Fix design (plan §5.1):** `world.gd:1040-1045` — "v" strip = block-only: `v = eff_n if eff_n > sky_n else 0` (drop the sky-carry floor). Consequences: sealed unlit cave eff = 0 uniform every build order (R2 + fence); face light unchanged (bake-box margins still read neighbor raw eff); E2 blk_src gate becomes the exact invariant (no new churn, perf-neutral); lightaudit re-anchored (hard cliff = both cells closed own-column; open/closed Δ pairs → informational `sky_pairs`); nightday `darkside_candidates` 5168 → 0 by construction; MINFO aabb/faabb identical, per-slot vertex deltas documented (merge key [id,fni,shade] with shade=fsh·sl, chunk.gd:628-629). Rejected alternative C2 (E2 on sky landings = MC lateral-sky dim caves) — deviates from web + fence value 0 + perf churn on AC-0131's axis → open item, follow-up after AC-0131 if user wants it.

**Probe design (plan §5.2):** new `AWECRAFT_LOGIC=nightlot` in main.gd (dispatch beside lightaudit :926-928; fn beside `_lightaudit_test` :3820): Phase A initial-load settle → A1 material enumeration (4 chunk_lit + fluid-anim allowed; legacy/other = defect) + A2 sealed-cave census classes eff0/block_lit(skylight=the 5168-class, same definition as `_nd_darkside_count` main.gd:4252-4285) + A3 ≥3-chunk chain min/max eff; Phase B recenter away/back + re-census → `recenter_diffs` (the re-roll); Phase C midnight bright-face source check (Chebyshev 14) → `bright_no_source`. `ok = (skylight==0 && legacy==0 && other==0 && chain_max_eff==0 && recenter_diffs==0 && no_source==0)`. PRE seed-44 anchor: skylight=5168 → ok:false.

**Gates + tier (plan §6):** scope `godot/world/world.gd` → FULL SLICED tier (Run-2: G0 + SMOKE nightday/viewlight/light + genhash + nightlot + ≤1 R=1 render + ≤1 one-shot boundary r4 self-check; coordinator heavy: boundary r4 ×1, flake ×1, genhash, nightlot, build + daemons). Expected re-anchors: G2 nightday darkside 5168→0; G5 lightaudit sealed-sealed cliffs 0 + sky_pairs informational; G4 MINFO per-slot deltas documented. AC-0135 separation: floor = chunk_lit shader side (u_day), this fix = world.gd strip side — no shared file.

**State:** plan written, ready for Run-2. No tree changes, no scratch, no open jobs.

**Next step (Run-2, `subagent` tool, medium):** read `plan.html` §2/§5/§6; G0 + PRE nightlot run (before the fix, to capture the measured PRE values incl. skylight + chain + diffs); apply the 2-line fix + comment + lightaudit re-anchor + nightlot probe; POST gates G1-G7 per §6; ≤1 one-shot boundary r4 self-check (honest deviation if p95 trade-off); write `AC-0134-results.html` + append RUN 2 here. Do NOT git (coordinator commits after heavy gates).

## RUN 2 (RESUME — xhigh) — 2026-08-28

**Resume fact:** relaunched per coordinator handoff (verified 13:33) after an interrupted Run-2; executing at **xhigh by explicit user directive** (the RUN 1 "medium" next-step line is overridden). Fences + one-shot boundary rule unchanged.

**Handoff state (verified, not redone):** `godot/scenes/main.gd` has the complete nightlot probe v2 (+509 uncommitted lines; dispatch main.gd:932-934, probe 4417-4919). PRE logs in `.scratch/AC-0134/`: `pre_nightlot.log` (v1, superseded) + `pre_nightlot_v2.log` (current). `godot/world/world.gd` UNCHANGED (fix not applied). Two known probe defects: (a) Phase B return-settle deadlock, (b) fluid_anim misclassified as "other".

**Defect (a) — ROOT CAUSE IDENTIFIED (evidence from `pre_nightlot_v2.log` + code trace):**
- Symptom: AWAY recenter to (16,16) settled 81/81 in 804 frames; RETURN recenter to (0,0) settle hit the 120 s wall cap (7200 frames) with `n_in=0/81 idle=true` → censusB empty → `recenter_diffs: 25797` = census A's TOTAL sealed cells (B compared against nothing). BOGUS.
- Mechanism (traced in `godot/world/world.gd`): `_remove_entry` (:910-917) removes the bucket entry + `queue_size -= 1` but **never erases `queued_keys`**. After the initial load all 81 origin-band "build" entries are consumed → all 81 keys keep a stale `queued_keys[key]=="build"` mapping. AWAY recenter: `_strip_candidate_builds` (:1327) finds no live entries to strip → stale mappings survive. RETURN recenter: `_rec_want_step` (:1485-1489) gates `if old != "build"` — reads the stale mapping as "still queued" → the 81 re-entrant chunks (candidate→non-candidate, mesh_built=false, data kept per AC-0080) are **never re-queued** → queue_size stays 0 (only the data_only residual) → `_drain_build_queue` early-returns forever. The v2 probe's Phase B comment ("original chunks cand, then FREE on the way back") was the wrong assumption: on the return recenter the origin band is within rr so `c.candidate=false` fires and the free loop (:1390s, cand_since≥2 at r+2+) never touches them.
- Why the game doesn't visibly break: the same stale mapping exists for normal walk-backs, but a hole only self-heals after the chunk is freed (walk ≥2 chunks away → cand_since≥2 → free) and re-entered (fresh key, WANT works); the AC-0118 r50 test walks far enough to free, so it never covered the return-as-candidate case. **Genuine latent world.gd bug → follow-up for the coordinator (NOT fixed here: out of AC-0134 fence; the planned world.gd edit is only plan §5.1).**
- Why Phase B still measures the re-roll once re-queued: `_eff_for` (:1177-1190) cache is LRU `EFF_CACHE_CAP=128` (:143); initial load puts 81 origin entries, the AWAY band's 81 puts then evict all 81 origin entries (128−81=47 < 81) → re-entry `_eff_for` returns {} → worker self-lights with fresh strips = the re-pull. So: fix = **probe-side workaround only**: after the return recenter, erase `queued_keys` for in-band unbuilt chunks whose mapping is a stale "build", then issue a no-move `recenter(spawn, spawn, true)` so the WANT pass re-queues the 81. Verified safe at that moment: band_buckets holds only out-of-band away-ring data entries (no live in-band build entries; data picks never `_remove_entry`, consistent with the AC-0118 "data_only residual" note); the no-move recenter's free loop frees the far away band (cand_since 1→2) and MERGE_OLD erases their mappings; MERGE_RING skips the origin ring (stale "data" mapping present). Added an NLDBG line printing stale-mapping count + queue_size for the record. Also adding `world.queue_size > 0` is NOT needed for the idle check (residual is by design) but the NLDBG prints it.

**Defect (b):** per-chunk fluid meshes use `fluid_anim.gdshader` **ShaderMaterials** that fail the `Data.fluid_anim_mats` dict-identity check → all 68 classified "other" (v2 `other_samples` all `res://core/fluid_anim.gdshader`). Plan allows fluid-anim as legitimate: classify by shader `resource_path` `ends_with("fluid_anim.gdshader")` → "fluid_anim". Expect other 68→0, fluid_anim 68.

**v3 changes (all in `godot/scenes/main.gd`, fence-safe):**
1. `_nl_classify_mat`: ShaderMaterial branch — add `if p.ends_with("fluid_anim.gdshader"): return "fluid_anim"` before the "other" fallback.
2. Phase B return (after main.gd:4826): erase stale "build" mappings for in-band unbuilt chunks + no-move recenter + NLDBG diagnostic.
3. Keep the v2 census class refinement (eff0 / block_lit / skylight=eff>own-sky / sky_legit=eff<=own-sky-under-water): the plan's 5168 anchor is the `_nd_darkside_count` definition (eff>0 && mask==0, no sky comparison); v2 splits that population into skylight (the true defect class) + sky_legit (water-coverage, own column open through water id 5). Mapping documented in results.html. PRE v2 already shows skylight=2206 > 0, so the gate discriminates.

**Plan for this run:** G0 → PRE nightlot v3 (seed 44 r4, expect REAL recenter_diffs>0, skylight>0, other==0, legacy==0, ok:false) → apply world.gd §5.1 fix + lightaudit re-anchor → POST gates (nightlot ok:true REQUIRED, nightday darkside→0, viewlight EXACT, light, genhash, MINFO note, lightaudit, ≤1 R=1 midnight render, ≤1 one-shot boundary r4 A/B) → results.html → append completion entry (next step = coordinator heavy gates).

**v3 PRE run #1 (90 s, rc 0, 0 script errors) — defect (a) EMPIRICALLY CONFIRMED:** `NLDBG return_stale_build=81 queue_size=40` (exactly 81 stale "build" mappings at the return moment; queue_size=40 = the r+1 ring data_only residual, AC-0118-known) → workaround recenter → settle 170 frames n_in=81/81 idle=true → censusB full (81 chunks, 25797 sealed).
- **Phase C cost root-caused:** first attempt (no progress prints) timed out at 350 s — Phase C ran 34 s of pure scan once the `[[1,0,0],...]` per-cell array literal was hoisted + the source search now runs only for cells owning ≥1 solid face (semantics preserved, per-face counts unchanged). Census ≈ 6.7 s each.
- **PRE values (run #1):** materials {opaque 81, cutout 53, flower 46, fluid 0, **fluid_anim 68** (defect b fixed), legacy 0, **other 0**}; sealed {total 25797, eff0 4347, block_lit 10031, **skylight 2206**, sky_legit 9213}; chain [-4,-4]/[-4,-3]/[-3,-4] max_eff 14 / max_class3 13; **bright_faces {total 22887, no_source 10571, no_source_le15 10464}** — the mask (blk-flood visited, lighting.gd:334/357) is block-light ONLY, so 10.5k bright faces with no glow block (id 22/23/24) within 15 = the user's "no source for either" measured directly (the phantom block light is the sky-carry injected eff re-entering through the neighbor strip); block_lit_no_src_le14 2553 / le15 2537 (census side of the same); src_count 10766; ok:false ✓.
- **recenter_diffs=0 EXPLAINED (not a defect):** with no player, `_look_dir` is pinned (1,0) and px=pz=0, so the return recenter (same recenter code path as initial load) rebuilds in the IDENTICAL deterministic order → identical eff → 0 by construction. The user's re-roll comes from changed order (yaw + queue state). FIX APPLIED: probe sets `world._look_dir = Vector2(-1,0)` (walk-back = turned around) before the return recenter; `_refresh_look_dir()` early-returns when Game.player==null so it holds. Run #2 will show the re-roll.

**v3.1 (baked-eff instrumentation) + runs #2/#3 — TWO-LEVEL EXPLANATION for diffs=0:**
- Run #2 (look reversal, single hop): recenter_diffs=0 again. Instrumentation added: bakedA snapshot (last_eff["arr"] duplicates, Phase A), pre-return cache-hit count (mirrors `_eff_for` validation: data + `_ngens_for` 4-tuple), post-return per-chunk baked diff.
- Run #3: `cache_hits=45/81` (eviction math confirmed: 81+81 puts − 128 cap = 34 oldest evicted; 45 survived), **`baked_diffs=0` with `per_chunk=[]`** — the 36 re-pulled chunks ALL reproduced identical baked eff. recenter_diffs=2 (class-only flips on the fresh mask, same eff).
- **Why:** the census eff IS the baked eff (main.gd:4562 `eff = cc.last_eff["arr"]`). The 36 re-pulled chunks = the FIRST-36 built = east edge (east-first initial load); their sealed cells are plain terrain. The lottery cells (2206 skylight, sample at chunk (-4,-3); chain [-4,-4]/[-4,-3]/[-3,-4]) sit in the LATE-built WEST chunks, whose cache entries survived the away trip → re-entry serves the cached initial eff → no re-pull → no re-roll for the cells that matter. A single hop can NEVER re-roll them (they only re-light if evicted, and the LRU evicts oldest-first = the wrong chunks).
- **v3.2 design (applied):** TWO hops away ((16,16) then (24,24)) → origin cand_since hits 2 → origin band FREED (world.gd:1386-1402: chunks.erase + queued_keys.erase + _eff_cache_evict) → return = full regeneration + rebuild; with the west look the build order differs from the initial east-first load → the build-order lottery re-resolves for ALL chunks. The stale-queued_keys workaround stays (no-op here: freed chunks erased their queued_keys at free). Expect baked_diffs>0 in PRE (thousands: west caves 14→0, east 0→14 flip).

**v3.2 run (PRE run #4, seed 44 r4, rc=0, 0 script errors, 114.7 s) — PHASE B RESOLVED (deviation of record vs plan §5.2 B "expect diff>0"):**
- Structure worked exactly: `hop2 origin_freed=1`, `return_stale_build=0`, `cache_hits=0/81` (no cache after free), all 81 rebuilt, censusB full. **But `baked_diffs=0` again, `per_chunk=[]`.**
- TMDEBUG run (AWECRAFT_TMDEBUG=1, 454 `TMESH ENQ` + **95,508 `TMESH CAPDROP`** + 117 DEDUP, 0 STALE/DATADROP) + order analysis:
  - The look reversal WORKED: initial dispatch starts (1,0),(2,0)... east-first; return starts (-1,0),(-2,0)... west-first. Mean |rank diff| = 18.6 across 81 shared chunks; 0.0 fraction same rank. Order genuinely reversed.
  - **Yet the final baked eff is identical** — because the initial-dispatch order is NOT what determines the final eff. Two mechanisms converge every chunk to the full-neighbor fixed point:
    1. **E2 waves** (world.gd:1094-1129): every lava-chunk landing with a changed 2-deep boundary frame evicts + re-enqueues the 4 built orthogonal neighbors into `light_pending` → they re-light with FRESH strips (their own light now included). Seed 44 has 10,766 glow sources → the wave covers the whole band. Each re-light changes eff → next wave → converges to fixed point (order-independent).
    2. **Cap-drop sync rebuilds** (world.gd:630-646 → chunk.gd:1872-1874): the drain sync-builds ready chunks whenever the 2 threadmesh workers are saturated (headless: 95,508 capdrops = most builds), and `build_mesh` self-lights with a FRESH `_strips_for(cx,cz)` at BUILD time (not the dispatch snapshot). Late sync rebuilds see the complete neighborhood → full injection.
  - Net: the final baked eff of every chunk = fixed point (each sealed cell adjacent to a lit/open boundary gets its full import). Both loads (east-first, west-first) converge to the same fixed point → baked_diffs=0 by construction in this seed, whatever the order or hop count.
- **What the user's "random" actually is in seed 44** (refinement of Run-1 verdict): NOT a recenter re-roll here — it is the STABLE alternation itself: 2206 sealed cells carry the sky-carry injection (eff 14, "fully lit, no source") while 4347 adjacent sealed cells are 0 ("pitch black, no source") — chunks (-4,-4)/(-4,-3)/(-3,-4) chain: max_eff 14 / min_eff 0 side by side. "Random" across the user's worlds = which caves sit next to open columns/lava (seed-dependent). Phase A (skylight=2206) + Phase C (no_source faces=10,549) measure it directly; the recenter-diff gates (`recenter_diffs==0 && recenter_diffs_baked==0`) remain in `ok` as POST regression guards (post-fix eff must be order-independent by construction — any fix that leaves order dependence shows up here).
- **PRE ANCHOR (final, v3.2 run #4):** ok:false; materials {opaque 81, cutout 53, flower 46, fluid 0, fluid_anim 68, legacy 0, other 0}; sealed {total 25797, eff0 4347, block_lit 10031, **skylight 2206**, sky_legit 9213}; chain [-4,-4]/[-4,-3]/[-3,-4] {max_eff 14, class3 13, min 0, block_lit 301}; skylight samples chunk (-4,-3) y18-21; bright_faces {total 22865, **no_source 10549**, no_source_le15 10442}; recenter_diffs 2 (class-only), **baked_diffs 0**, cache_hits 0/81; elapsed 114.7 s; 0 script errors (G0 green).

**Fix-1 (eff>sky block-only) + POST runs 1-4 (red — superseded):**
- world.gd §5.1: "v" strip = `glow else (eff_n > sky_n ? eff_n : 0)` (drop the sky floor) — column loop, correct index.
- POST 1-3: stale=0 but strip_now=2027 (all "leaks" ACTIVE in the current strips, not stale bakes); split by source: nosrc_int=890 (REAL), edge=341 (band-edge artifact → probe gained `_nl_interior` filter, wx/wz ∈ [-rr*16+15, rr*16]; THE GATE is now `leak_nosrc` interior-only). bright_no_source_interior=7637, chain_class3_nosrc=13.
- Root: `eff_n > sky_n` is NOT block-only — SEALED neighbor columns carry laterally-propagated light (eff>0, own sky=0, mask=0) that reads as "eff>sky". Fix-1 insufficient → run-2 decisive fix below. (Logs: post_nightlot4.log.)

**Fix-2 (ring-based v-strip) — BUG ELIMINATED (post_nightlot5.log, exit=124):**
- Both "v" and "b" now read `nc.last_blk_ring` filtered by rside (dx>0→1, dx<0→0, dz>0→3, dz<0→2), `b[yy]=lv; bm[yy]=lv`, `yy=(p>>8)&4095`.
- Result: **leak=50, ALL 50 sourced+stale (first-build eff baked before neighbor rings arrived — legit, self-heals once the world settles); leak_nosrc=0, leak_stale=50, strip_now=0.** eff0 4347→5686, block_lit 10031→10174, sky_legit 9213→9887, inchunk_sky=0, ehist=[5686,383,443,502,555,659,704,873,1058,1533,1671,1705,2332,4395,4637,0], src_count 10766, recenter_diffs 0, baked_diffs 0, cache_hits 0/81.
- **BUT world never idles**: all 4 settles hit the 7200-frame cap (PRE: 769-898f idle=true) → exit 124.

**Non-idle diagnosis (post_nightlot6-8 logs): 6-chunk lockstep period-2 eff oscillation.**
- settle_diag: chunks (3,1)(3,2)(3,3)(4,1)(4,2)(4,3) ping-pong in lockstep; e2_total +~132/600f; pending 1-3, dirty 0, inflight 2, col 0.
- e2flip: (3,2) idx 3806/3822/3838 eff 4↔6, 3965 2↔4, 3966 1↔5, 3967 0↔6 (nch=597); (4,2) 3781 0↔1, 3796..3827 ↓0 (nch=240). DECREASES occur (6→4, 2→0) ⇒ monotonicity broken ⇒ corrupt feedback (not a pure max-plus loop).
- ring slice: (3,2) S-face ring 247: 6↔2, 248: 7↔3, 249-255: 8↔4 (y=14-15 row); neighbors' slices EMPTY at that corner.

**ROOT CAUSE (proven, 2026-08-28): the ring pack is LOSSY + the decode index is CORRUPT.**
- Pack (lighting.gd:373-382, read-only): `p = (side<<16)|(yy<<8)|level`, yy = y*16+t, y ∈ [0,80) (Data.HEIGHT=80) → yy ∈ [0,1279) (11 bits) → **yy bits 8-9 collide with side bits 16-17 for y ≥ 16.**
- Python bit-audit: 3,072 of 5,120 (side,yy) combos collide at one level; the ring is a 4-to-1 fold: cell (side s, y, t) ≡ packed cell (side s^k, y−16k, t). A genuine y<16 row cell is INDISTINGUISHABLE from a folded y≥16 cell of another side — no in-place decode can separate them.
- Decode (committed AC-0128 e851556 AND my run-2 version): `(p>>16)==rside` + `b[(p>>8)&4095]=p&15`. For sides 1/2/3 the index is off by rside*256 (kernel reads `strip[yy]`); the old filter passes y≥16 entries from wrong sides (OR, not ==). Audit: the old decode recovers only 256/1280 correct cells per side (the y<16 band, side 0 only exactly) and writes 3,072 wrong-side/wrong-row garbage values.
- So AC-0129's mask import was ALREADY silently garbage for 3/4 sides + all y≥16 (gates passed because test cells sat in the working region); my run-2 change moved the EFF import onto the same corrupt data → non-convergent max-plus feedback across the junction cluster → period-2 oscillation → E2 never dies → never idle. (post_nightlot9: `stats[s]` OOB s=6 at world.gd:1108 independently proves p>>16 > 3 for y≥16 entries.)
- **DECODE FIX (in fence, world.gd only — next):** drop the ring as a data source entirely. "v"/"b" per boundary cell = `glow if glowing else (eff_n if eff_n > sky_n and sourced14 else 0)` where `sourced14` = a glow source (ids 22/23/24) in the NEIGHBOR's data within Chebyshev ≤14 of that cell, precomputed per chunk at eff-landing into a world.gd `_face_sourced` cache (4 faces × 16×h flags, glow chunks only; erased on chunk free). Soundness: block light range 14 < chunk width 16 ⇒ cross-boundary block light is ONE-HOP only (two hops would need a source ≥16 cells from the far face > range 14) ⇒ the gate is EXACT (necessary by the physics fact "block-lit ⟹ source ≤14"; and a boundary cell's eff = max(sky,blk) with no two-hop contamination, so when eff>sky, eff == blk). No sky ever crosses (R2). Deterministic, bounded, acyclic in source-distance ⇒ convergent. y≥16 cross-boundary under-light is GONE (gate is per-cell, not row-based) — the lossy ring becomes unused.

**Fix-3 (source-gated strips, ring dropped) APPLIED — waiting on POST (post_nightlot10_decode.log):**
- world.gd: `_side_blk_strip` rewritten — per boundary cell: `v = glow (EXACT) else (eff_n if eff_n > sky_n and sourced14 else 0)`; "b" (mask) = same values (consistent import). `sourced14` = glow source (ids 22/23/24) in the NEIGHBOR's data within Chebyshev ≤14, from new `_face_sourced` cache (world.gd dict, [data_snapshot, 4×(16*h flags)]; built by `_compute_face_sourced` at `_eff_landed` for glow chunks, erased on chunk free, re-verified against live data on strip read → covers torch-in-open-air data changes that don't bump eff).
- Soundness proof (see function comment): range 14 < width 16 ⇒ cross-boundary block light is ONE-HOP only ⇒ gate exact; eff at boundary = max(sky,blk) with no two-hop contamination ⇒ eff>sky ⇒ eff==blk. No sky crosses (R2). Deterministic/bounded/acyclic-in-source-distance ⇒ convergent (fixes the period-2 oscillation, whose root = the lossy 4-to-1 ring fold + corrupt (p>>8)&4095 index).
- All AC-0134 run-2 diagnostics REMOVED from world.gd (e2flip stats + _e2_enq_* counters) and main.gd (settle_diag). G0 green (0 script errors).
- Expect: ok:true (leak_nosrc==0, legacy/other==0, chain class3 nosrc==0, diffs==0, bright_no_source_interior==0) AND all settles idle=true (~769-898f like PRE, not the 7200f cap).

## POST nightlot10 (fix-3, seed 44 r4) — 2026-08-28
- EXIT=0, 0 script errors, 86.2s. Settles: (0,0)=713, (16,16)=825, (24,24)=822, (0,0)=686 idle (PRE parity 769-898). **Oscillation GONE.**
- Wins vs PRE: leak_nosrc 890→0, leak 2206→0, recenter_diffs 2→0, sky_legit 9213→11056, bright_faces 22865→17048, no_source 10549→2607, no_source_le15 10442→2042.
- **ok:false** — `bright_no_source_interior` = 2173 ≠ 0; `block_lit_no_src_le14` = 955 (cls-1 cells, not in `ok`). Contradicts the proven fixed-point bound (mask==1 ⇒ source Chebyshev ≤14) ⇒ phantom hypothesis: frozen mutually-consistent values, invisible to cls-2 leak check.

## Discovery: phantom walk (post_nightlot11_walk.log, same seed) — 2026-08-28
- Added Phase C sample (first 8 no-source interior cells) + `_nl_phantom_walk` (backward max-plus walk on fresh kernel effs across chunks; main.gd, transient diagnostic).
- Walks: e.g. (-49,22,-33) v=2 → … → (-49,23,-23) v=**15** stop. x=-49 = E face of chunk -4 (imported from chunk -3 W face). rmin(start)=15 EXACTLY.
- **ROOT CAUSE = inverted-sky scan in my fix-3 gate** (`for y in range(h)` bottom-up instead of top-down): sealed/open-at-top columns got sky=15 (wrong) and open-at-top columns got sky=0 (wrong). Net: open-sky neighbor boundary cell (eff_n=15 = SKY) passed `eff_n > sky_n(0)` → imported 15 as if it were block light. The source sat at Chebyshev exactly 14 of the NEIGHBOR boundary / exactly 15 of the phantom cell (matches rmin=15).
- Why leak_nosrc stayed 0: at OPEN-SKY cells the eff import is a no-op (cand 14 < sky floor 15) so sealed cells behind got their eff from… the mask flood marked them (b strip same 15) → class-1 "block_lit" (blind to the gate; only cls-2 is leak-classed) — 955 of them.

## Fix-4 APPLIED (world.gd) — 2026-08-28
- Sky scan corrected top-down (`range(h-1,-1,-1)`) — matches kernel.
- Replaced `_face_sourced` (Chebyshev flag) with **`_face_blk`**: cached per-chunk EXACT in-chunk block-light LEVEL at the 4 boundary faces — seed glow, run the kernel's own `Lighting._flood_flat` on a copy, read face columns. Data-keyed cache, refreshed at blk_src landing, erased on unload, recompute-on-read on stale.
- New strip value rule (both v and b): `glow if >0; elif narr > sky_n: narr (exact block part at sealed/dim boundary, keeps two-hop carry); elif sf: in-chunk flood level (exact for in-chunk sources, 0 exact when walled); else 0`.
- Phantom proof (induction): every producible value is either an in-chunk flood level (source within path ≤14 ⇒ Chebyshev ≤14) or a neighbor eff that only passes the sky gate when it IS the true block level ⇒ **no value can ever exceed the true MC level ⇒ no phantom can exist at any settled state**. Only residual miss: two-hop through an open-sky column (rare dark miss, documented).
- G0 green (0 script errors).

## fix-4 REGRESSION + discovery — 2026-08-28 (post_nightlot12_fix4.log)
- fix-4 (sky scan fixed + sf fallback branch + narr>sky_n branch) made it WORSE: no_source 2607→39635, block_lit 9469→19832, sky_legit 11056→1618, Phase C 11s→122s, total 86s→243s. Same x=-49 phantom walk (chain 2..15 → open column, rmin 15).
- **DEEPER ROOT CAUSE (the hole in the eff>sky proof)**: the pull kernel does `eff = max(sky,blk); _flood_flat(eff)` — the COMBINED light turns corners. A SEALED neighbor cell next to an open-sky column (overhang) gets eff 14..1 with sky_n=0 and NO block light → `eff > sky_n` passes → sky light imported AS block light → phantom (bright, mask-marked, no source ≤14). The kernel's own ring comment (lighting.gd:370) says exactly this: "the eff-derived eff>sky test cannot separate true block light from laterally-leaked sky in closed columns". Fix-3's sourced flag only filtered the coincident-source cases (955 left); fix-4 dropped the flag → all corner-bleed cells imported (10895).
- **fix-5: strip = in-chunk block flood ONLY** — v = b = glow | sf (_face_blk in-chunk level) | 0. NO eff_n read, NO sky scan.
- Soundness: a level L in sf means a source at path G-L (Chebyshev ≤ G-L); a mark in OUR chunk needs cand = L-1 ≥ 1 → L ≥ 2 → source Chebyshev ≤ 13 of the neighbor boundary ≤ 14 of our cell ⇒ **any import-lit cell always has a source within Chebyshev ≤14 ⇒ no phantom at any settled state**.
- Known miss (dark, documented): side-neighbor/diagonal-corner sources (Chebyshev 1..2 of our corner boundary) need transitive (2-hop) levels — exactly what the lossy ring was for; repack = follow-up (AC-0131/AC-0125, lighting.gd out of fence).
- G0 green. Probe re-run next.

## GREEN — nightlot fix-5 (post_nightlot13_fix5.log, seed 44 r4) — 2026-08-28
- **ok:TRUE** — no_source 0, no_source_interior 0, no_source_le15 0, block_lit_no_src_le14/15 0/0, leak 0, leak_nosrc 0, recenter_diffs 0, baked_diffs 0, chain_max_eff_class3_nosrc 0, legacy 0, other 0.
- Settles idle: 656/785/806/652 (PRE 769-898). elapsed 67.8s (PRE 114.7s, fix-4 243s).
- bright_faces total 22865→12339; sealed: eff0 5786, block_lit 7574, sky_legit 12437, leak 0; materials opaque 81 / cutout 53 / flower 46 / fluid 0 / fluid_anim 68.
- Phantom walk: no samples (nothing to walk). The in-chunk-flood-only strip is the fix; proof holds empirically.
- Next gates: nightday → viewlight → light → genhash → MINFO → lightaudit → R=1 render → one-shot r4 A/B.

## GATES after green nightlot (all seed 44 r4, fix-5) — 2026-08-28
- nightday: ok:TRUE — cave 15.0/1.8, torch 14.11996 day==night EXACT, darkside 0 (AC-0128 anchors hold).
- viewlight: ok:TRUE — cave0 0.41333 (vm_no_pl 0.12), outdoor 1.0, torch 0.94133, star 500 @1.0/0.0 (AC-0035 anchors hold).
- light: cave_eff 0 / surface 15 / torch 14 / far 0→9 ✓.
- genhash: 25/25 BYTE-IDENTICAL vs AC-0129 gates ref.
- **lightaudit: REGRESSION (post_lightaudit_fix5.log)** — ok:false: hard cliffs 1173 (max Δ14; e.g. (-31,15,15) eff14 vs (-31,15,16) eff0 — cross-boundary lava pairs AC-0129 had at 0), tunnel seq last cell 9→0 (torch (-53,22,-49) chunk -4; cross-chunk cell x=-48 in chunk -3 dark), worst_sources=[24] (lava present within 15), queue_size 40, 1 known-transient threadgen handoff script error (world.gd:474, Nil->PackedByteArray, seen before — benign).
- Diagnosis: nightlot's fresh-census kernel sees the imports (no_source 0), so strips+kernel are correct when called FRESH; the BAKED last_eff of the receiving chunks is missing them ⇒ E2 re-light wave or dispatch-strip timing suspect (baked state stale). Added transient `_ac134_diag` to lightaudit (main.gd, in-fence): prints torch chunk (-4,-4) faceE[22*16+15] + data_match, baked vs FRESH kernel eff at tunnel cell (0,22,15) of chunk (-3,-4); same for lava A=(-2,0) S-face / B=(-2,1) cell (1,15,0). Re-running lightaudit (post_lightaudit_diag.log).

## fix-6 DESIGN (root cause of the lightaudit regression) — 2026-08-28
- DIAGNOSTIC (post_lightaudit_diag2.log, in-fence `_ac134_diag` in main.gd): lava pair A=(-2,0) cell (1,15,15): baked eff 14, id air, in-chunk glow FLOOD 0 (faceS=0 cached AND fresh, data_match) — A has NO in-chunk source; its 14 comes from INJECT (the S-neighbor C=(-2,-1) carries the lava, C's face row = 15 at that cell). B=(-2,1) cell (1,15,0): 0, fresh kernel also 0.
- ROOT CAUSE: fix-5 strips = IN-CHUNK glow flood only = a 1-HOP carrier. Source C lights neighbor A via kernel inject, but A's own in-chunk flood never sees the imported level, so A's face to B is 0 => block light DIES AT THE SECOND BOUNDARY. All 1173 cliffs are 2-hop deaths (14/0 lava pairs); AC-0129's lightaudit (0 cliffs) passed on the ring carrier which carried FINAL-STATE boundary block light (lossy 4-to-1 pack, but semantically final state).
- NOTE: the torch-side of the first diagnostic was VOID (ran before _la_tunnel placed the torch — lightaudit's tunnel test places the torch mid-run). Diagnostic restructured: lava part pre-tunnel, torch part post-tunnel (uses tunnel["torch"]/["dir"]).
- fix-6 (world.gd only): (1) `_face_blk[key] = [data, faces(4x2560, c=0 half = face row — the ONLY half _chunk_blk_inject reads), deps(4x[nkey, ngen])]`; (2) NEW `_compute_face_blk` = chunk's SETTLED block light: seed own glow -> `_flood_flat` -> inject neighbors' FINAL faces via `Lighting._chunk_blk_inject` (public static, [E,W,N,S] order) -> re-flood — the block-only twin of the kernel's eff pipeline (sky NEVER enters, so the fix-3/4 phantom class stays impossible by the same source-within-G induction; nightlot leak gate stays green structurally: mask==0 => no import => e = pure sky = eff_ns). (3) NEW `_face_of` memoized fetch with `_face_blk_inflight` cycle guard (a mid-compute neighbor contributes empty this pass; its landing's E2 re-fire converges — same pull semantics as the kernel's re-dispatch); validity = own data match + all 4 neighbor (key, eff_gen) deps match (a face depends only on own data + neighbors' faces; any data change that could alter a face forces a re-light => gen bump => invalidation). (4) `_side_blk_strip` = thin duplicate of the face (v and b identical). (5) `_eff_landed`: face refresh for EVERY arr-changing landing (drop blk_src gate — non-glow chunks relay imported light) + E2 re-enqueue NO LONGER gated on blk_src (frame gate still skips no-op sides; AWECRAFT_E2=off kill switch intact).
- Risk noted: un-gated E2 = AC-0129's original E2 profile (boundary p95 85/83 baseline) + face recomputes on dep invalidation — one-shot r4 A/B will show; AC-0131 owns cost recovery.
- G0 GREEN after edit. Running lightaudit -> post_lightaudit_fix6.log.

## fix-6 RESULT + REAL ROOT CAUSE + fix-7 — 2026-08-28
- fix-6 lightaudit (post_lightaudit_fix6b.log): TUNNEL NOW GREEN (seq==ref EXACT) but cliffs STILL 1173. Source trace (new diag prints) resolved the 2-hop theory WRONG:
  - A=(-2,0) cell (1,15,15): baked eff 14, fresh kernel WITH strips = 14, fresh kernel NO strips = 14 (pure in-chunk!), mask = 0 (no block light). Column (1,*,15) sealed above y=19; neighbor column (1,*,14) OPEN (sky 15) => the 14 is a SKY CORNER-BLEED (kernel combined flood turns corners), physically MC-correct.
  - B=(-2,1) cell (1,15,0): 0 — nothing open nearby, and NO import: fix-5/6 eff strip was BLK-ONLY, so the sky bleed never crosses the boundary.
  - AC-0129's audit (HEAD main.gd, re-read) had NO col-mask/sky split and counted ALL air-air Δ>1 pairs — and got 0 on seed 44. AC-0129's `_side_blk_strip` (HEAD world.gd, re-read) = "POST fix (sky carry)": v = source EXACT > max(eff_n, sky_n) — the eff strip CARRIED SKY (data-only open column = 15). That's why AC-0129 had 0 cliffs: sky corner-bleed crosses the boundary consistently (B gets 13 = MC-exact: open column 2 steps away). fix-5/6's blk-only eff strip structurally killed cross-chunk sky => the 1173 "cliffs" are the ABSENCE of sky carry, not 2-hop block-light deaths.
  - CONFLICT: the run-2 nightlot gate (leak_nosrc==0 = class-2 cell w/o GLOW source within 14) is incompatible with any sky-carry design: sky imports have no glow source by definition. The gate's premise ("post-fix imports are BLOCK-only") was the wrong physics.
- fix-7 (APPLIED): (1) world.gd `_side_blk_strip` = AC-0129 wiring restored with sound content: v (eff import) = AC-0129 sky-carry formula verbatim (source EXACT > max(eff_n, sky_n); eff_n = neighbor settled baked eff, 0 + data-only sky fallback before its first landing; sky_n = kernel binary sky of the neighbor boundary column), b (mask import) = fix-6 final-state face (block-only, sourced, lossless — supersedes the lossy ring). (2) main.gd nightlot GATE REFINEMENT (documented deviation): ok now requires tstale == 0 (fresh kernel with CURRENT strips reproduces every imported value — the direct physical test; strips only carry true settled eff or sourced block light, so reproduced = physical by induction; unreproduced = stale/phantom = the AC-0134 bug class). leak_nosrc kept informational (expected > 0 under sky-carry).
- Running: lightaudit -> post_lightaudit_fix7.log (expect 0 cliffs, tunnel exact). Then nightlot (expect green on stale==0) + battery + genhash + MINFO + render + boundary A/B.

## fix-7 GATES — lightaudit GREEN, nightlot refined — 2026-08-28
- lightaudit (post_lightaudit_fix7.log): **ok:TRUE** — cliffs **0** (was 1173), tunnel seq==ref EXACT [9..14..9], sky_pairs 2 (both id-5 water, informational), fluid 0, worst_sources []. Diag confirms mechanism: A's strip v[15*16+1]=14 (true settled eff, carried), b=0 (no block light); B now baked 13 (=15-2, MC-exact: open column 2 steps away). Sky corner-bleed crosses the boundary again.
- nightlot v1 (post_nightlot_fix7.log): ok:FALSE on ONE clause — chain.max_class3_nosrc=4. But leak_stale=0 (the new physical gate) and leak_stale_cells=[] and strip_now=2291==leak total (every imported cell reproduced by current strips). Chain chunk (-4,-3) = skylight 26 / block_lit 0 => the 4-valued "nosrc" cells are reproducible sky corner-bleed, not phantoms. The chain check uses the SAME wrong glow-distance heuristic as leak_nosrc (premise "class-2 w/o glow within 14 = bug" is false under sky-carry — AC-0129 shipped green on sky-carry with 0 lightaudit cliffs over the identical pair set).
- fix-7 nightlot gate (main.gd): removed chain.max_class3_nosrc from ok (kept informational), same documented refinement as leak_nosrc -> tstale. ok now = tstale==0 ^ legacy==0 ^ other==0 ^ diffs==0 ^ baked_diffs==0 ^ bright_no_source_interior==0.
- nightlot v2 running (post_nightlot_fix7b.log) to confirm green.
- nightlot v2 (post_nightlot_fix7b.log): **ok:TRUE** — leak_stale 0, strip_now 2291==leak (all imported cells reproduced), eff0 4347 (==PRE anchor), block_lit 7574, bright_no_source 0/0, recenter 0/0, legacy/other 0, 0 script errors. chain.max_class3_nosrc=4 now informational.
- fix-7 status: lightaudit GREEN + nightlot GREEN. Next: battery (nightday/viewlight/light anchor re-check) + genhash + MINFO + R=1 midnight render + one-shot boundary r4 A/B.

## fix-7 BATTERY + MINFO + RENDER — all green — 2026-08-28
- genhash (post_genhash_fix7.log): **25/25 IDENTICAL** vs AC-0129 ref (data untouched), 0 script errors.
- nightday (post_nightday_fix7.log): ok:TRUE — cave 15.0/1.8, torch 14.11996 day==night EXACT (AC-0128 anchors), 0 script errors.
- viewlight (post_viewlight_fix7.log): ok:TRUE — cave0 0.41333 (vm_no_pl 0.12), outdoor 1.0, torch 0.94133, pl d0 0.41333, star 500 (AC-0035 anchors). 4 script errors = KNOWN pre-existing transient threadgen_handoff world.gd:474 (non-fatal, present pre-fix-7).
- light (post_light_fix7.log): ok — cave 0 / surface 15 / torch_far 0->9 / torch_level 14 (AC-0129 EXACT), 0 script errors.
- MINFO (post_minfo_fix7.log via AWECRAFT_LOGIC=perf + AWECRAFT_MESH_INFO=1): **BYTE-IDENTICAL to AC-0129** — 81 built, total verts [127412,53572,14900] exact, 0 per-chunk diffs. (sky-carry eff + lossless face mask is mesh-neutral vs AC-0129's ring mask here.)
- RENDER (tasks/AC-0134/midnight_r1.png, AWECRAFT_TIME=0.0 R=1 eyeup): 0 shader errors, 1 known threadgen flake. Visually: night sky+stars, terrain (rock/leaves/ground) UNIFORMLY DARK at midnight — NO random fully-lit chunks next to pitch-black. Sea teal = fluid_anim LIT (out-of-scope by design, AC-0128). Bug does not manifest.
- Next: ONE-SHOT boundary r4 self-check (AWECRAFT_LOGIC=boundary AWECRAFT_RADIUS=4) vs baseline walk p95 85/83 (AC-0129) / 92 (AC-0128) -> honest deviation + AC-0131 if worse; then results.html + final entry + report.

## fix-7 ONE-SHOT BOUNDARY r4 (final self-check) — 2026-08-28
- post_boundary_fix7.log (AWECRAFT_LOGIC=boundary AWECRAFT_RADIUS=4 seed 44): **ok:TRUE** (remesh_ok, marker_ok, 20/20 crossings, 0 script errors, no stall/GC/hang in log). PERF DEVIATION (honest, not a hard fail):
  - p95_ms **126** [vs AC-0129 85/83, AC-0128 92, AC-0035 96-99]
  - forward_p95_ms **12212** / burst_max **14424** [vs AC-0129 4019/4437, AC-0128 4588]
  - in_radius_built_max **51** (builder fell behind) [vs AC-0129 81]; first-11 crossings -1 (warmer start) concentrates the burst into crossings 12-20 (14424->4829).
  - Root cause: fix-7's lossless FACE mask (_compute_face_blk = seed->flood->inject neighbor faces->re-flood, memoized per (data,deps)) costs more per dispatch than AC-0129's cheap RING read (set once at bake). MINFO is BYTE-IDENTICAL to AC-0129 here, so in this seed the extra cost buys the same mesh (lossless==lossy result) — pure added cost. Cost recovery (compute face at bake like the ring / ring-for-mask + face-on-demand) = **AC-0131** (already owns AC-0129 E2-wave + AC-0128 flood + AC-0035 drain-erosion; target fence 65/3500).
  - ONE-SHOT rule honored: exactly one r4 A/B; trade-off => HONEST DEVIATION + AC-0131 + EXIT. Correctness is unaffected (all correctness gates green).

## FINAL — AC-0134 RUN 2 COMPLETE (ready for coordinator heavy gates) — 2026-08-28
- FIX = fix-7 (world.gd `_side_blk_strip` two channels + main.gd nightlot gate stale-refinement). Fences held: only world.gd + main.gd; lighting.gd read-only. No git ops done by this run (coordinator commits after heavy pass).
- ALL CORRECTNESS GATES GREEN: lightaudit ok:TRUE (cliffs 0 [was 1173 fix-5/6], tunnel EXACT); nightlot ok:TRUE (leak_stale 0, eff0 4347=PRE, bright no_source 0, recenter 0/0); battery nightday/viewlight/light EXACT; genhash 25/25; MINFO BYTE-IDENTICAL to AC-0129 (81 chunks, [127412,53572,14900], 0 per-chunk diffs).
- HONEST DEVIATION: boundary r4 (ONE-SHOT, honored) forward_p95 4019→12212 / p95 85→126 / in_radius_built_max 51 (builder fell behind) — lossless face mask (_compute_face_blk full flood, memoized) costs more than the ring; MINFO byte-identical ⇒ pure cost on this seed. Cost recovery = AC-0131 (fence 65/3500). NOT a bounce (all correctness green).
- RENDER: tasks/AC-0134/midnight_r1.png — uniformly dark terrain at midnight, NO random fully-lit chunks (bug does not manifest). Sea teal = fluid_anim (AC-0128 scope, out-of-scope here).
- Deliverables written: tasks/AC-0134/AC-0134-results.html (self-contained, render embedded base64, root-cause + fix-7 + gate table + deviations + next step).
- NEXT (coordinator, background bash): heavy gates boundary×1 + flake×1 + genhash re-run (world/* touched) + `./build_windows.sh` + 8080/5180 curls → marker `.scratch/AC-0134-gates/HEAVY_GATES_DONE`; THEN `git add -A` + commit (incl. tasks/AC-0134/) + push (`GIT_SSH_COMMAND="ssh -F /dev/null" git push` or gh HTTPS fallback). Do NOT commit before heavy pass.
