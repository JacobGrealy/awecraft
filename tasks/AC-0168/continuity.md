# AC-0168 continuity

## RUN — 2026-09-02 — AC-0168 (subagent xhigh, plan+implement+verify one turn)

- 02:5x — READ: CONTINUITY 00u, ARCHITECTURE (contract: no comments, snake_case), HARNESS §1/§3/§4, spec.html (scaffold), TASKS.yaml notes verbatim, AC-0187 + AC-0182 results. Godot free (no ps hit, no .godot_busy).
- CODE MAP VERIFIED (line drift vs task brief — grep found truth): `_enter_candidate` = world.gd:2985 (sets mesh_built=false + chunk._enter_candidate_slabs() = the kill); free hysteresis = recenter world.gd:3062-3082 (`two_out = not in_circle_ring and taxi > b1_eff+2`; cand_since≥2 → free); r+1 predicate = world.gd:2080/2199 (drain stale check) + in_stream_set/in_circle_ring:971-985; visible writer = world.gd:681-690 (frustum cull only — no candidacy hide anywhere; chunk.gd has zero `visible`); fog = daynight.gd:11-12 + main.gd:1760-1761 (VERIFIED CORRECT, untouched).
- KEY FINDING: stream set = circle ∪ diamond(b1+1) ∪ circle-ring; the ring (fog-hidden, 1-out) is IN the stream set (WANT early-returns nb==3) so a meshed chunk crossing the circle edge keeps its mesh while in the ring; the KILL happens one recenter later when it leaves the ring → `_enter_candidate` (mesh_built=false + slabs mesh-nulled). At R≥13 meshed candidates therefore start at the two_out band. Step-back within ~2 chunks of the edge → re-entry with mesh_built=false → WANT re-queues "build" → worker re-mesh gap = the flicker at the fog line. Fix = hide not kill: retain the mesh in `_enter_candidate`; free timing (2 recenter events at two_out) unchanged; the stale-queued_keys AC-0134 re-entry hole also closes for r+1 (meshed re-entrants are never WANT-queued).
- PLAN written: tasks/AC-0168/plan.html (Goal / files / gates / risks).
- NEXT: implement world.gd + chunk.gd + main.gd edgeretain probe → G0 → probe → SMOKE+genhash → lodswap → (opt render) → results.html.
- IMPLEMENTED: world.gd `_enter_candidate` no longer kills (mesh_built kept, slabs whole, instances never hidden); removed chunk.gd `_enter_candidate_slabs()` (now dead); added `_stage_check(c, key)` on candidate re-entry (re-stages a band-0 collision body if it was still pending at candidacy — body-retain edge case); 3 stale comments updated (cull doc, candidate doc, recenter hysteresis doc). main.gd: `edgeretain` dispatch + probe (R=8, 3 tracked edge chunks: T1 axial band-1 full, T2/T3 diagonal band-2 coarse; phases boot→out1→out2→hold×3→out3→back1→back2; per-step ring counts, mesh identity, per-frame back-hole/hidden sampling, fog coverage).
- G0 green (2x). 1st edgeretain run: hit 3600-frame build cap (R=8=201 chunks slower than cap; headless ~60fps) → raised caps to 20000 + ERBOOT/EREDGE progress + fail diagnostics (queue/built/queued). Re-running.
- BUG in probe v2 (not the fix): `_er_chunk_mesh_id` used `-1`/"<0" as the no-mesh sentinel, but `get_instance_id()` is a signed 64-bit ObjectID that can be legitimately negative → every mesh with a negative ID read as "no mesh" → false "tracked edge chunk never built" while diag showed `built:true` for all three. Fixed: separate `_er_chunk_has_mesh(c)` predicate; wait/kill checks use it; mesh_id only compared for identity. G0 green again; edgeretain v3 running.
- edgeretain v3: feature evidence all green (T1 5×one_out held, vis_min 10; T2 freed exactly at 2 events; T3 back1 re-entry no remesh) but back_hole_frames=28 — T2 legitimately RE-CREATED as band-3 data-only (ring) at back2; band-3 never meshes by design → excluded band≥3 from hole/kill/vis bookkeeping.
- v4 first run: SIGSEGV (134) in `_tm_worker_run` (world.gd:1289 `_tm_slots.get`) + "caller thread can't call propagate_notification on /root" — PRE-EXISTING flaky engine race (same signal-11 class in .scratch/AC-0143/g0.log + AC-0108-gates; AC-0152 comment documents the _tm_slots racy window). Re-run of identical v4 code: PASS (ok:true, 80.7 s, 0 SCRIPT ERROR). Not an AC-0168 regression; note to coordinator.
- ENV NOTE: each bash call runs in a fresh bwrap sandbox — `ps` does NOT see godot processes started by other calls; the `.scratch/.godot_busy` marker is the real one-at-a-time lock (it worked; ps check was blind).
- GATES ALL GREEN: SMOKE battery ok:true 258s — player 135.43/136.78/+2.82, interact place_ok 2/0 inv_grew, light 15/10/14 far1→9, fluids sea 2756/2756 stable backed 914 wol 25 lava 9 (all = AC-0187 baseline); genhash 25/25 byte-identical to .scratch/AC-0187-gates/genhash_A.txt AND genhash_B.txt; lodswap first run FLAKED (cross1 flip past 2400-frame wait cap; R=15 drain ~35% slow that window; no_hole held) → re-run GREEN ok:true 28.1s at baseline values (cross1 atomic flip 780, flicker 0/0, retain_window true). T never a candidate in lodswap → change is a no-op there; flake = probe fixed-frame cap under build-drain slowdown (documented as D6).
- DELIVERED: plan.html, continuity.md, AC-0168-results.html (full gate table + 6 deviations D1-D6), spec.html FILL sections (Goal/requirements/gates/Scope/Fences), code (world.gd + chunk.gd + main.gd probe). No git, no TASKS.yaml, no daynight/fog touch.
