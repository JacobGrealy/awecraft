# AC-0156 — light-cache-per-section — continuity

## RUN — 2026-09-02 10:37 EDT — AC-0156
- **Shipped:** `godot/core/chunk_io.gd` — AWCC `VERSION` 1→2: `encode_column(data, fl, seed, height, light={})` appends a light section (u32 size + present flag + blk_src + eff nibbles + mask nibbles, nibble-packed per 16³ section); `decode_column` accepts v1 (→ no light) and v2 (→ `{"arr","mask","blk_src"}`), MD5 spans the light section, malformed light fails closed to recompute; new `encode_column_legacy` (v1 layout) for the back-compat probe. `godot/world/chunk.gd` — `saved_light` (restored light consumed by the first build) + `light_recomputes` (per-chunk kernel counter: build_mesh self-light + build_accs self-light via `res["light_recomputed"]` into both apply paths). `godot/world/world.gd` — save: `_queue_chunk_save` captures `_save_light_for` (eff cache, data-equal, no pending re-light; else no light) through the write worker into the codec; load: `_try_disk_load`/`_io_read_handoff` store `c.saved_light` via `_saved_light_from_res` (size-validated, v1 → `{}`); `_apply_edits_to_chunk` returns changed (changed edit clears saved_light + marks; idempotent re-apply returns false early and KEEPS it — required for zero-recompute restore); build: `_build_unit` feeds `c.saved_light` when cache empty (`light_saved_restores` counter), cleared at all 5 apply points. `godot/scenes/main.gd` — `AWECRAFT_LOGIC=lightcache` probe arm (R=2; targeted cache-fresh wait, not global settle).
- **Light gates (all PASS):** G0 exit 0 / 0 SCRIPT ERROR. lightcache probe `ok:true` wall 7.9 s — `light_saved_nonzero:true` (mask_sum 1172, torch cell block 14), `restored_no_recompute:true`, `recompute_count:0`, `restored_matches_saved:true` (eff+mask byte-identical incl. on-disk decode), `legacy_v1_recomputes:true`, `clear_wipes_light:true`, `saved_restores:1`. SMOKE battery `ok:true` 260 s / 0 SCRIPT ERROR — player 135.43/136.78, interact place_ok, light 15/10/14 far 1→9 (exact hold), fluids 2756/914 stable 25/9, genhash 25/25 byte-identical to AC-0178 baseline (set compare; baseline file is numerically sorted). Codec self-test green (v2 lit/unlit, v1, wrong-seed reject). RENDER R=1 PNG `tasks/AC-0156/render_r1.png` (1280×720, zero SCRIPT ERROR, lit surface correct).
- **Deviations (for coordinator):** (1) probe at R=2 not R=1 — at R=1 the 3×3 corners are band-3 data-only so `_startup_pending()` never clears and the `light_pending` flush stalls forever (hang); engine unchanged. (2) `_apply_edits_to_chunk` idempotent re-apply now returns false without re-marking (was a wasted re-bake on every disk-load with edits) — no world-state change, required for zero recompute. (3) First v2 decode was broken (fe end compared to post-light MD5 offset → every v2 blob rejected; v1 fine) — caught by a standalone self-test (`lc_selftest.gd`), rewrote decode with fixed offsets; self-test green. (4) Env lesson: `AWECRAFT_RADIUS` is NOT in `HARNESS_ENVS` — launching with only it boots menu-first and idles forever headless (4 wasted runs); `AWECRAFT_LOGIC` is required and also drives the radius path. (5) Honest scope: blob carries eff+mask (the channels the renderer consumes); separate sky/blk are kernel-internal, never persisted — documented in plan/results, not faked. (6) Lit column blob ≈ 27 KB compressed in the probe (48K eff + 24K mask + ~197K blocks raw pre-zlib) — light roughly 2-3× the AC-0155 per-column size; note for AC-0175 region budget if needed.
- **Pre-existing noise (not ours):** `SNAPDRAIN not fully drained after 3000 frames` in the render arm only (same as prior task renders).
- **Coordinator to-do (heavy, after this exit):** re-run light gates; r4 boundary ×2 + perf; `./build_windows.sh`; commit (code + plan + results + render) + push. AC-0175 region storage rides the same codec → light preserved automatically. Scratch gate logs: `.scratch/AC-0156-gates/`. Results: `tasks/AC-0156/AC-0156-results.html`. No git/Windows/TASKS/HARNESS/00o/lighting.gd/data.gd touched (fences held).
