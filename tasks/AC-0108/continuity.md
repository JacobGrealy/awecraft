## RUN 0 (coordinator, 2026-08-26)
- AC-0108 = vertical-subchunks-16x16x16 (user, priority 2): split the 16x80x16 column chunk into 5 vertical 16x16x16 slabs; each slab owns its MeshInstance3D + ConcavePolygonShape3D; data/fl layout STAYS column (edit compat); build_mesh/col per-slab. Benefit: finer frustum/greedy culling + faster per-edit col rebuild + R=50 memory drop. Verify: R=50 mem drop, per-slab _apply_edits_to_chunk, r=4 walk p95, G0/G1.
- Spec v1 filled (gates G1-G6): G2 byte-identity [genhash 25/25 + full battery exact + MINFO per-chunk vert sums UNCHANGED (total 125552) + MATINFO 3/3], G3 per-slab col rebuild probe [slab-border edits touch only affected slab(s); mesh output byte-identical], G4 R=50 mem POST <= PRE (fresh PRE anchor by Run-1; AC-0078 gate anchor 144.6 MB), G5 boundary floor [walk p95 <= 65, fwd <= 3500 + first10 >= 8, irb >= 60, flap 0, collision-cost check at up-to-405 bodies, RECSLICE <= 12.2+noise, perf no-regression, flake], G6 windows build.
- Key design questions for Run-1: slab-boundary faces (per-slab EMIT with column-scope CONTEXT — hidden-face/greedy/AO/light at y=16/32/48/64 borders must be byte-identical; MINFO per-chunk sums are the proof), the AC-0107 worker path (workers build all 5 slabs of a column in one pass vs main-thread slab builds), the 5x collision body count physics cost, queue/recenter scope (column-scoped first pass; per-slab distance culling = candidate follow-up).
- In-tree at research time: AC-0120 material cache (uncommitted, heavy gates running — slabs reference the shared materials, no new allocs; MATINFO 3/3 fence).
- Known anchors (post-AC-0078, 2026-08-26): boundary r4 walk p95 62-63 / fwd 2959-3216 / first10 10 / irb 81 / flap 0; perf r4 total 5731 / first_draw 194 / p95 56 / mem delta r4 30.9 MB / r50 144.6 MB; RECSLICE r50 10.0/8.0; genhash 61de26e9; MINFO r4 125552; battery exact. Run-1 MUST re-measure fresh PRE anchors (tree includes AC-0120 cache).
- Ops: HOME=/tmp/dsh_home + absolute engine path /home/angrygiant/tools/godot/godot (NEVER $HOME-relative — the AC-0078 v1 gate job died on that); one godot at a time; Run-1 = headless probes ONLY and MUST be godot-busy aware (AC-0120 heavy gates run in a background job first: check pgrep -f godot + .scratch/AC-0120/HEAVY_GATES_DONE before EVERY godot call); r50 timeout ~480000 ms; scratch .scratch/AC-0108/; no git/builds/TASKS.yaml; never kill python server pids.
- Run-1 LAUNCHING (subagent_plan, xhigh) - overlaps the AC-0120 heavy gates.

## RUN 1 (planner, 2026-08-27 00:10)
- Static map DONE (chunk.gd 1674L, world.gd 1766L, main.gd 6121L, HARNESS.md, data.gd/generator.gd constants). No .gd edits, no builds. plan.html written.
- Godot was busy at launch (transient process at 00:07 — first probe self-aborted by design); free at 00:10.
- **KEY FINDING: AC-0120's heavy-gate log (.scratch/AC-0120/gates.log, 22:41-22:49, current tree) already contains almost all fresh PRE anchors** — reused instead of re-running (tree unchanged since; all gates rc=0). Saved to .scratch/AC-0108/: pre_minfo_r4.txt (121 lines / 81 built / opaque total **125552 exact** / fluid 110112), pre_genhash.txt (25 lines, 0,0=e59af29b1a1a88403c4bdeae591dba7d), pre_results.txt.
- Fresh anchors (current tree, AC-0120 in-tree): perf r4 total 5829 / first_draw 195 / p95 56 / mem 27.9 MB / col shapes 81, ms 58, n 54, max 2 / build_units 722 / drain 418f / staged 72-0-0; boundary r4 x3 walk p95 62/62/62 (max 119-122), fwd 2992/2959/2975, first10 10/10/10, irb 81, flap 0, mem 30.6 MB, col ~300/226/4, staged 232, unbodied 0, remesh_ok; **boundary r50 mem_delta 140.6 MB (fresh G4 anchor, replaces AC-0078 144.6)**, walk p95 60, RECSLICE 68.0/8.0, built 297/10201, resident 10712; MATINFO distinct_std=3 total_allocs=3; battery5 exact (player 2738ms after_fly 37.78 / interact 1965 place [8,34,8] / light 2931 torch 14 / fluids 3391 sea 1406-2730 / buckets 460).
- Fresh probes run (job bash-54, .scratch/AC-0108/probe.log): G0 rc=0 zero SCRIPT ERROR; **editperf PRE: cell [8,34,8] (y=34 = slab 2), edited_id 1 (grass) -> 0, total_ms 241, single_build_ms 66, max_frame_build_ms 66, flush_frames 5** — spawn chunk = sync build path + immediate col rebuild = PRE full-column edit cost (G3 anchor).
- Design decisions (full detail in plan.html):
  1. Slab model: `class Slab` plain struct in chunk.gd (NOT a Node3D): y0, built, mesh_instance, fluid_instance, flora_instance, collision_body, col_dirty. 5 per chunk; instances stay children of the chunk node -> c.queue_free() / candidate kill / recenter all unchanged (subtree). chunk.mesh_built = all-5-slabs, set atomically at end of build_mesh/apply_accs (one main-thread call -> no partial visibility).
  2. Byte-identity: merge grid stays COLUMN-WIDE (16x80x16 per face, chunk.gd:481-541 unchanged); each emitted quad ROUTES to the slab of its ANCHOR block — merged: c0[4]/16 (fi 0/1/4/5: v0=block y) or c0[5]/16 (fi 2/3: plane=block y); non-merged/fluid/xquad: rec y/16. Same quads, same order, same verts/UVs -> per-chunk MINFO sums unchanged (total 125552) = G2 proof. Merged quads may extend up to 3 rows into the slab above their anchor (documented; slab AABB slightly coarser at borders; union complete).
  3. Worker (AC-0107): ONE build_accs call per column returns {"slabs": [[ao,ac,af_w,af_l,ak,ax] x5], "light", "wms"} (30 acc dicts); single-pass work unchanged + O(1) routing per quad; drain pacing/cap/dedup unchanged; thread-safety unchanged (geometry arrays only; _get_mat stays main-thread — AC-0120 fence intact).
  4. Collision: per-slab StaticBody3D+ConcavePolygonShape3D from that slab's opaque surface (slab mesh surface 0); edit rule: slabs = {y/16} U {(y-1)/16 if y%16==0} U {(y+1)/16 if y%16==15} (block face + neighbor y-1 top / y+1 bottom + greedy 3-row vertical reach); body rebuilt ONLY for dirty slabs (1-2 of 5) on edit; first build/candidate-reentry/save-restore = all 5; staging (AC-0077) stays chunk-keyed (stage when any slab dirty & missing); col_immediate Chebyshev <=1 rule unchanged; set_fluid path UNCHANGED (no col_dirty today — fluids produce no opaque body). r4 bodies 81 -> ~300-405 (typ. 3-4/chunk); r50 ~9-13k.
  5. Queue/recenter: column-scoped first pass (ALL band/drain/recenter/hysteresis code unchanged — slabs die with the chunk); frustum culling = free per-MeshInstance3D (5 small AABBs); per-slab distance culling = documented follow-up (NOT free: per-slab queue entries, hysteresis, _startup_pending/AC-0079 wall semantics).
  6. Memory: G4 = POST <= 140.6 MB (fresh r50 anchor). Honest expectation: flat to slightly UP in pass 1 (per-slab object overhead: up to 5 ArrayMesh + 5 bodies + ~15 instances/chunk vs 3 instances + 1 body; geometry/data+fl/face bytes unchanged) — no structural drop until per-slab culling lands. Report honestly if over; register the culling follow-up as the real lever.
- Harness probes that MUST adapt (Run-2, main.gd harness-only): _matinfo_counts (297-318, walk slabs for MATINFO 3/3), mesh_info (world.gd:1502-1526, sum per surface slot across slab instances — keep JSON shape for per-chunk diff), collision_body refs (world.gd:948/977/984 -> has_slab_body helpers; main.gd:4043 continue_probe, 5249 remesh_ok, 5260 unbodied), AWECRAFT_ONLY (1008-1019) + DBGCHUNK (1020-1036) debug blocks, _count_shapes (4827, works as-is, expect ~300-405 at r4). NEW G3 probe: AWECRAFT_LOGIC=editslab (env-gated) — scripted pillar edits at slab borders y=15/16, y=31/32 + interior y=24, per-slab body-build counters, asserts slabs_touched == expected set + total_ms <= 241 PRE + MINFO sums unchanged.
- G5 collision gates at 5x bodies: walk p95 <= 65 (PRE 62), collision_max_ms <= 10 (PRE 4), collision ms/n <= 2.0 (PRE 1.35); RECSLICE max <= 12.2+noise (PRE 8.0); perf r4 total <= ~6700 (PRE 5829), first_draw <= 250 (PRE 195).
- Deliverables: tasks/AC-0108/plan.html (7 sections) + this RUN 1 entry + .scratch/AC-0108/ anchors. Report sent to coordinator.


## RUN 2 (builder, medium, 2026-08-27 03:00)
- Implemented plan §2 fully: chunk.gd 1674→1803L, world.gd 1766→1788L, main.gd 6121→6210L. Slab plain-struct model (5 per chunk: y0/built/mesh_instance/fluid_instance/flora_instance/collision_body/col_dirty + sidx/fsidx slot maps); merge grid stays COLUMN-WIDE, only emit destination routes per anchor (merged: c0[4]|c0[5] /16; non-merged+fluid: r[1]/16); build_accs returns 5×6 acc dicts {"slabs",...}; build_mesh/apply_accs per-slab _assemble_slab; _build_slab_collision per slab with perf_slab_body_builds/ms counters; _post_build_collision + build_dirty_slab_bodies; air-slab col_dirty auto-clear; world.gd: set_block→mark_edit_slabs, _apply_edits→mark_all_slabs_dirty, _stage_check/_col_drain_step/_enter_candidate/mesh_info per-slab, _make_chunk_node init_slabs; main.gd: _matinfo_counts/AWECRAFT_ONLY/DBGCHUNK/water-test/continue-probe/boundary/atlas/tint/trees adapted + NEW _editslab_test (G3).
- GATES: G0 PASS (0 SCRIPT ERROR). G2 MINFO-diff r4: 121/121 chunks BYTE-IDENTICAL (diff 0 bytes; sums opaque 125552 / fluid 178584 exact); MATINFO distinct_std=3 total_allocs=3. perf r4 RESULT: total_ms 5754 (≤6700), build_ms 2385, first_draw 191 (≤250), p50 14, p95 55 (≤65), frame_max 95, mem 50011453→82147565 (32.1 MB, PRE 27.9 — per-slab object overhead, G4 is coordinator's), collision_shapes 223 (PRE 81, expected rise), col ms 64/n 62/max 2 (1.03 avg), staged 72/0/0, chunks 81/121, build_units 711, drain 411f. genhash 25/25 BYTE-EQUAL (battery arm).
- G3 editslab RESULT: slabs_touched [0,1]/[1]/[1,2] ALL EXACT, cell_after 0/0/0, mesh_built true, col_ms 1.0/0.0/1.0 (vs PRE single-body 66ms), total_ms 252/257/304 vs ≤241 anchor → **HONEST DEVIATION (wall only)**: probe does 6/2/6 set_block calls vs PRE's single edit; A/B control (unmodified _editperf, same single-edit workload) = total_ms 253 (PRE 241, +5% noise), single_build 66=66, flush_frames 5=5 → no mechanism regression. final_minfo edit-chunk verts [3964,192,712] (PRE 3888; +76 from break exposure — recorded for the record).
- SMOKE battery (player;interact;light;fluids;buckets;genhash) ALL EXACT: player 2.82/36.43/floor + after_fly 37.78; interact place [8,34,8] 2/true/true; light 14/15/0/9; fluids 2730/2730+1406/1406 stable/1/[5,7]/[5,8]/25/9; buckets [5,8]/[5,8]; genhash 25/25. zero script errors.
- Render R=1 iso seed 44 → tasks/AC-0108/post_r1_iso.png (82079 B); pixel-diff vs AC-0120 baseline: **11/921600 px, max Δ2** (≤25 px tolerance PASS; fluid-anim phase jitter signature).
- DEVIATIONS (report to coordinator): (1) mark_edit_slabs WIDER than plan literal — v0-reach [y-3,y] closure (plan formula under-marks y∈{17,18,33,34,49,50,65,66}); G3 expectations identical under both. (2) Slab sidx/fsidx extension fields (plan field list had none) — mesh_info maps surfaces→slots without material-identity hacks (keeps _get_mat main-thread-only, AC-0120 fence). (3) _enter_candidate_slabs SETS col_dirty (plan's "clear" typo; re-entry-rebuild requires set). (4) air-slab col_dirty auto-clear (plan silent; prevents stuck flags → staged counters + water-test spin). (5) G3 wall total_ms 252-304 > 241 anchor — workload artifact (6/2/6 set_blocks vs 1), A/B control 253≈241, no regression. (6) ops: plan §6 relative AWECRAFT_SNAPSHOT path rejected by Godot (project-dir resolution) — absolute path used (same as AC-0120).
- MINFO fverts subtlety found+fixed: old fi.mesh == mi.mesh (one shared ArrayMesh) so old fverts enumerated ALL 4 slots incl. opaque; first implementation read fluid slots only from slabs with a fluid instance (dropped ao verts of fluid-free slabs, fverts slot0 110112→57540) — fixed by reading per-slab MAIN mesh (fallback fi.mesh for fluid-only slabs) gated on chunk-has-any-fluid; rerun → 0-byte diff.
- PENDING (coordinator heavy gates, NOT run here per scope): G4 R=50 memory (mem_delta anchor 140.6), G5 boundary r4/r50 + RECSLICE + flake, G6 windows build.
- Deliverables: tasks/AC-0108/AC-0108-results.html + this entry + .scratch/AC-0108/ (post_perf_r4.log, post_minfo_r4.txt, post_minfo_r4_diff.txt EMPTY, post_battery.log, post_genhash.txt, post_r1_render.log). Report sent.
