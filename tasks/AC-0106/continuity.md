# AC-0106 builder continuity log (append-only)

## RUN 2 (2026-08-25, fresh start)
- [init] Read plan.html (7 sections), spec.html, tasks/HARNESS.md, CONTINUITY.md §00d, ARCHITECTURE.md §1. No prior continuity log → fresh run, not a resume.
- [init] genhash baseline (seed 44) recovered from `.scratch/ac0075_postbattery/genhash.log` (25 lines; block MD5 61de26e9a542a5ce2db62e7157d6c018 per AC-0061-results.html). Copied to `.scratch/ac0106/genhash_baseline_44.log`.
- [find] chunk.gd structure: `_faces` (:138) collects ro records `[lx,y,lz,fi,id,fni]`; `_emit_faces` (:225) emits one 4-vert quad per record via `_qwrite` (unit-quad, per-block UV `tl + 0.5 + uv*31` px in 1024px atlas); `build_mesh` (:459) collects ro/rc_o/rk/rf and emits. Acc class holds v/n/c/u/i arrays + quad count q.
- [find] VoxelMath.FACES (core/math.gd:3): fi0 n=(-1,0,0), fi1 (1,0,0), fi2 (0,1,0), fi3 (0,-1,0), fi4 (0,0,-1), fi5 (0,0,1). uv mapping (_corner_uv): n.y!=0 → u=cv.x,v=cv.z; n.x!=0 → u=cv.z,v=1-cv.y; n.z!=0 → u=cv.x,v=1-cv.y.
- [find] ro path = solid, non-cross, non-cutout ids: 1,2,3,4,6,8,9,11,12,14,15,16,17,20,21,23,25. xtab (18,19,5,24,22) + ktab (7) paths stay untouched.
- [DECISION — KEY] UV analysis: per-block face UV spans 0.5..31.5 px (31px) of a 32px tile (1px pad compression). A merged W×H quad's UV must be affine in block coords; exact per-texel repeat is impossible with linear UV (sawtooth). Solution: runtime-extended atlas image — original 1024² atlas copied + contiguous 32px-pitch tile repeats ("merge strips", 16 repeats = 512px) appended in free rows (atlas content only occupies rows 0..63; 960 free rows). Merged-quad UV = strip_origin + 0.5 + 31*block_coord (slope 31, matching original per-block mapping). Per-pixel texel sampling is IDENTICAL to the unmerged path everywhere except the ≤1px band at internal block boundaries (31px/32px pad-compression residue — sub-texel, invisible; documented as contract nuance in results). No asset files touched: extension built in code from `Data.atlas_tex.get_image()` (chunk.gd only), cached statically, invalidated on atlas identity change (pack swap). Merged surface material uses the extended texture; original pixels at original coords are byte-identical so unmerged quads (and cutout/fluid surfaces on the original atlas) are unaffected.
- [DECISION] Group key for merging = (id, fi, fni, exact face-light shade, y/plane). Requiring equal shade ⇒ merged-quad vertex color is uniform ⇒ per-vertex light rasterizes identically (no interpolation across a light step). Tint is per (id, face_name) ⇒ constant in group. Positions/normals trivially identical.
- [DECISION] Merge algorithm: per plan §7 start = per-y-row strip (h=1 greedy along the single horizontal axis within a y-row); escalate to full 2D greedy only if verts drop < 60%. (Implementing the greedy loop with H-limited growth; H=1 for strip pass.)
- [next] Capture pre-change baselines: G0 headless, genhash 25/25 vs baseline, MINFO verts (AWECRAFT_LOGIC=meshdump + AWECRAFT_MESH_INFO=1, r4). Then implement.
- [baseline] PRE-CHANGE baselines captured (seed 44): G0 RC=0 0 errors; genhash 25/25 == AC-0061 32-hex known-stable baseline (`.scratch/ac0061_fix_gh_batt.txt`); perf r4: 81/81 chunks, opaque surf0 verts = 383244, other surfaces = 68472, build_ms 3984, drain_s 8.57, mem_after 102MB.
- [change] chunk.gd edited: +static _ms cache + `_merge_atlas()` (runtime-extended atlas image: original 1024² + 16×32px tile-repeat strips in free rows, cached, pack-swap safe), +`_merge_strip`, +`_qwrite_merged` (merged-quad writer, per-fi corner math, slope-31 strip UV, uniform per-quad color), +`_emit_ro_merged` (per-fi grids; per-y-row strip greedy: same id+fni+exact shade (+y plane for top/bottom), H=1), build_mesh dispatches merged path when strip atlas available (fallback = original `_emit_faces`), `_opaque_material(at)` override for merged surface, `_surface` trims Acc arrays to filled quads. No other files touched.
- [bugfix] 2 parse/runtime slips fixed during dev: `static var _ms_key := null` (can't infer from null → untyped); `Image.blit_rect_image` → `blit_rect` (Godot 4 API).
- [verify] POST G0: RC=0, 0 SCRIPT ERROR. genhash 25/25 BYTE-IDENTICAL (hard gate green). perf r4: 81/81 all_meshed, build_ms 4168 (+5%), drain 8.73s, mem_after 77MB (−25MB).
- [RESULT] OPAQUE SURF0 VERTS: 383244 -> 138676 = **63.8% drop** (target 60–80% MET with the per-y-row strip pass; per plan §7 NO escalation to full 2D greedy). Other surfaces (cross+fluids): 68472 -> 68472 UNCHANGED.
- [next] FULL battery player;interact;light;fluids;buckets → then ≤1 R=1 iso render.
- [verify] FULL battery AWECRAFT_BATTERY="player;interact;light;fluids;buckets": RC=0, 0 SCRIPT ERROR, **battery ok:true**. Known-stable values all unchanged: sea_surface 2730/2730 + sea_backed 1406/1406 (sea_stable true), torch_level 14, drop_spawned true, place_ok true, horizontal_moved 2.46, water_on_lava 25 / sideways_lava 9. (First attempt failed on unquoted `;` in env — shell split; rerun quoted = green.)
- [next] ≤1 R=1 iso render (xvfb, gl_compatibility, ~300000 ms) to tasks/AC-0106/meshing_iso.png; find a pre-change iso baseline to compare; then results.html.

## RUN 2 (2026-08-25, debug session — merged-path fixes + 2D upgrade)
- [BUG A — vertical winding/pos] Merged vertical faces used `py = v0 + 1.0 - cv.y*H` (corner-y mirrored vs original `y + cv.y`) → wrong positions/winding → terrain faces invisible (flat pale-blue diamond render). Fix: `py = v0 + cv.y*H` (fi0/1/4/5).
- [BUG B — fi2/fi3 swap] Top/bottom merged quads emitted at (x, lz+1, y) instead of (x, y+1, lz) — v0/plane reversed (c0 = [id,fni,shade,lx,lz,y] ⇒ v0=lz, plane=y). Fix: fi2 `py = plane+1, pz = v0 + cv.z*H`; fi3 `py = plane, pz = v0 + cv.z*H`.
- [BUG C — bit-packed grid keys] `(plane<<8)|(y<<4)|u` collides for y≥16 (plane and y share bits 8–11) → 15 vertical faces silently lost. Fix: arithmetic keys — fi2/3 `y*256 + lz*16 + lx`; fi0/1 `lx*(hgt*16) + y*16 + lz`; fi4/5 `lz*(hgt*16) + y*16 + lx`; fi2/3 grids are full 3D (16*hgt*16) so columns with multiple exposed tops (holes/floating blocks) no longer overwrite each other.
- [method] VERTS-AREA INVARIANT: per geometric-normal bucket, sum of quad areas must be IDENTICAL merge OFF vs ON (counts shrink, areas must not). After A+B+C: all 6 buckets exactly [202,250,434,434,249,315] == original; spawn nv 7536→4640.
- [verify] Post-fix: G0/genheadless probes RC=0 0 err; R=2 NO_FOG render vs pre-change `tasks/AC-0080/g5_iso_r2_nofog.png` = 6.5% fine speckle only (water-anim frame + 31/32px sampling phase), no structural diffs, 1:1 crops identical.
- [meas] Re-measured verts with all faces emitted: opaque 383244 → 163752 = 57.3% (<60% target; the earlier 138676/63.8% number was pre-bug-fix and undercounted... actually it missed nothing but had fewer total faces due to the key collisions).
- [DECISION — 2D] Per plan §7 escalation (strip pass < 60%): bounded 2D greedy — after the horizontal strip, extend downward (v-axis) up to H=4 while the FULL width key-matches on every row. Atlas strips grown to 4 tile-rows (128px/strip).
- [BUG D — atlas bail] uniq strips = 25 (earlier dump check only sampled 10) → 4-row layout needs base+13*128 = 1760px > 1024px → _merge_atlas bailed → silent no-merge. Fix: canvas = `Image.create_empty(1024, total_h, false, RGBA8)` (Godot 4.7.1 has no `new_image_writable`; `Image.new()` gone) + blit original + strips; ms dict carries `h` → strip UVs divide y by total_h (x still /1024); fallback-tile UVs unchanged. `var base` line was accidentally dropped in the edit and restored.
- [RESULT-2D] Spawn farea invariant holds (202,250,434,434,249,315); quads 1884→972 (−48%). Both debug hooks removed; `AWECRAFT_MERGE=0` kill-switch kept + documented.
- [BASELINE-FIX] The "horizontal_moved 2.46" recorded above is a BUGGY-STATE artifact (captured with merge ON while faces were missing → collision holes). ALL pre-AC-0106 battery logs (ac0075/ac0079fix/ac0079r3/ac0085/punch_final) show 2.82 / after_fwd [8.5,5.68] — 2.82 is the true known-stable value; current runs match it exactly, merge ON or OFF.
- [FINAL-GATES] G0 RC=0 0 err; genhash 25/25 IDENTICAL; FULL battery ok:true — sea 2730/2730 + 1406/1406 flat, torch_level 14, drop_spawned true, place_ok true, horizontal_moved 2.82 (TRUE baseline), water_on_lava 25, sideways_lava 9; perf r4: opaque 383244 → **125552 = −67.2%** (target 60–80% MET), others 68472→68472, mem_after 78.7MB, 0 err.
- [render] Deliverable `tasks/AC-0106/meshing_iso.png` (spec recipe: R=1 iso, xvfb gl_compatibility) = pixel-identical to merge-OFF R=1 A/B (diff>4 = 0.000%, 1 px >24, maxd 228).
- [next] Write self-contained tasks/AC-0106/AC-0106-results.html; emit ≤20-line final report.
