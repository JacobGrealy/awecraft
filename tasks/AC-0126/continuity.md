## RUN 0 (coordinator, 2026-08-27)
- AC-0126 = break-batched-light-mesh (user, priority 1): the block-break light+mesh currently runs SYNC in one frame (world.gd _process :134 light_dirty->pending, :159 flush; sync light-BFS + build_mesh remesh of the edited chunk + up to 5 neighbors; edge-of-lit-region break, ~900 light cells = 50-120ms spike). Fix per the notes: bulk-box light pass (world.gd:922, AC-0077 pattern) over the edit set + neighbor remesh staggered 1/frame via the AC-0107 threaded worker path + collision staged AC-0077 (per-slab via AC-0108). Gate: edge break frame p95 <15ms, interior <10ms, torch 14 unchanged, battery green.
- Spec v1 filled (gates G0-G5): G1 breakspike probe [NEW env-gated; interior frame-window p95 <10ms, edge <15ms, settle bounded, post-settle MINFO+light A/B IDENTICAL to the PRE sync path, torch 14], G2 byte-identity [genhash 25/25 + full battery exact + MINFO 125552/121 lines + MATINFO 3/3], G3 boundary floor [65/3500/10/irb>=60/0/collision_max<=10/ms-per-n<=2.0] + RECSLICE <=12.2+noise + perf no-regression (total<=6700, first_draw<=250, p95<=65) + flake, G4 r50 mem fence (no new resident state; Run-1 measures fresh post-AC-0108 anchor), G5 windows build.
- Design questions for Run-1: the exact edit set (3x3 LIGHT_NEIGHBOR vs remesh mark radius, the x1-5), the bulk-box kernel reuse (world.gd:922 — confirm line; does the ~900-cell case fit one <=15ms pass or need AC-0079 off-main), the stagger queue (reuse drain scoring vs new 1/frame limiter; recenter must not starve), the edited chunk itself (sync-with-budget vs worker — the 66ms single_build PRE is the sync path; feedback-latency trade), coalescing under rapid successive breaks.
- In-tree at research time: AC-0108 slabs (uncommitted, heavy gates RUNNING as background job .scratch/AC-0108-gates/ — run-2 SMOKE green: 121/121 MINFO byte-identical, editslab [0,1]/[1]/[1,2] exact, battery exact, render 11px) + AC-0120 material cache (committed after AC-0108 gates — code ships in the joint commit). Run-1 MUST be godot-busy aware: check pgrep -f godot + .scratch/AC-0108-gates/HEAVY_GATES_DONE before EVERY godot call; static research first.
- Known anchors (post-AC-0108 SMOKE, 2026-08-27): perf r4 5754/191/55/32.1MB/collision_shapes 223; boundary r4 62/2959-2992/10/81/0 (pre-AC-0108 62-63/2959-3216); MINFO 125552/121; genhash 61de26e9; editperf PRE cell [8,34,8] total 241 / single_build 66 / flush 5; r50 mem 140.6 pre-AC-0108 (fresh post-AC-0108 anchor comes from the running gate job). Run-1 re-measures fresh PREs once the gate job finishes.
- Ops: HOME=/tmp/dsh_home + absolute engine /home/angrygiant/tools/godot/godot; one godot at a time; r50 timeout 480000 ms; scratch .scratch/AC-0126/; no git/builds/TASKS.yaml; never kill python server pids.
- Run-1 LAUNCHING (subagent_plan, xhigh) - overlaps the AC-0108 heavy gates.

## RUN 1 (research/planner, 2026-08-27)
- Deliverable: `tasks/AC-0126/plan.html` (7 sections, current line numbers, verbatim constants, pinned cells, exact gate commands + fresh anchors + RESULT shape + A/B method, risks). Scratch: `.scratch/AC-0126/` (sim_world.py, sim_out.json, pre_anchors.{sh,log}, PRE_ANCHORS_DONE, genhash_seed44.log, godot_dump_*.log, dump_gen.gd, bcell_env.txt).
- **KEY FINDINGS (supersede RUN 0's stale line refs)**: the flush light gate is DEAD (BULK_LIGHT_CELLS_MAX=3000 < min box 32000 cells; _build_batch :1438 zero callers). The 50-120 ms spike = sync `build_mesh` fallbacks in `_mesh_dispatch` :600: spawn-chunk contract sync (:609, ~65 ms), missing-diagonal sync (:621), and **cap-drop sync (:634-642) when the 2-slot pool saturates** (flush marks ≤9 chunks, workers ~50 ms/chunk → most cap-drop). `set_block` :1345 needs NO change: chunk-key dedup in light_dirty/light_pending IS the coalescing rule.
- **DESIGN (in plan §2)**: world.gd only (main.gd = probe only; chunk.gd NO change). Flush :273-313: delete dead gate + flush_eff machinery + `_pending_box/_box_ready/_bulk_box_cells/_bulk_light/_build_batch`; 1 edit-remesh/frame (new EDIT_FLUSH_MAX_PER_FRAME=1; FLUSH_MAX_PER_FRAME=2 stays for tex_refresh); `_mesh_dispatch` gains 6th arg `defer_on_cap` — **cap-drop → requeue + perf_edit_defers, never sync** (removes the spike); 4 new pure counters (perf_edit_dispatches/defers/syncs/light_passes, probe-first so PRE/POST both have them). Collision: no change (mark_edit_slabs :1369 + _stage_check/_col_drain_step). Edit set stays the 3×3 (NOT 1 chunk — neighbor meshes need remesh for boundary culling even though contained light is own-data-only).
- **PROBE CELLS (pinned, seed 44, AWECRAFT_RADIUS=6)**: interior (-32,31,32) chunk (-2,2) Sand id4 delta=1; edge (-49,31,41) chunk (-4,2) Sand id4 **delta=296** (sand ceiling over dark cave — the largest contained delta in the r6 region); coalesce = chunk (1,2) 5 Grass-id1 cells in 2 frames (3+2): (16,34,41)(17,34,42)(16,34,40) | (17,34,41)(17,35,43), 14 in-chunk candidates, 516 light cells. Invariant: every edit 3×3 ⊆ |·|≤5 (fully worker-buildable at r6) AND excludes spawn (0,0) (its ~65 ms contract sync would wreck a p95 gate) — r=4 was rejected because edge-chunk 3×3s touch the rim (missing-diagonal sync). Settle bound: 600 physics frames.
- **PORT VALIDATION**: `.scratch/AC-0126/sim_world.py` (full generator + contained-light port) now **25/25 genhash MD5 match** vs godot (`genhash_seed44.log`) and **6/6 raw-chunk byte diff** (godot bytes via `godot -s <abs path> .scratch/AC-0126/dump_gen.gd` — absolute-path `-s` works, adds zero repo files; script dumps GDGENHASH + GDGENDATA hex + GDHASH probes). Bugs found+fixed: (1) ore n2/n3 y-coordinate wrongly carried the x/z offset (+900/+1700) — GDScript `_fbm3col` y-tables (t91/t92/t61/t62) have offset 0, only x0/z0 carry it; (2) flora loop read `heights[(lz,lx)]` swapped → roses/dandelions missing (1-cell diffs in 6 chunks). Default game seed is **44** (main.gd:224 `44 if seed_env == ""`), NOT 1 (game.gd:5 is the autoload initial only) — so no-env genhash = seed 44.
- **FRESH PRE ANCHORS** (this session, `pre_anchors.log`, default-seed-44 world, all RC=0): editperf {cell [8,34,8], flush_frames 5, max_frame_build_ms 65, single_build_ms 65, total_ms 242}; genhash 0,0=e59af29b1a1a88403c4bdeae591dba7d GENMS 805 (25-line list = pre_anchors.log :17-41); battery light;fluids exact (sea 1406/2730, torch 14/9→0→9, ok:true); boundary r4 p95 62/max 122/fwd_p95 2982/burst 2978/irb 81/flap 0/collision 223 (mem 33.1, queue 95); perf r4 total 5814/build 2424/first_draw 195/p50 14/p95 58/frame_max 74/32.1 MB/collision 223/read_sync_gen 0/light_self 81/staged 72-0-0; MINFO r4 121 lines byte-identical to .scratch/AC-0108/pre_minfo_r4.txt (125552/178584), MATINFO 3/3; RECSLICE r50 max 10.0/8.0 + r50 mem 146.9 (committed AC-0108 gate log, identical tree — not re-run). NOTE: RUN 0's "genhash 61de26e9" is the pre-AC-0108 scaffold value (spec generated 2026-08-26); the fresh e59af29b… anchor is the G2 reference.
- **breakspike PRE anchors**: not capturable by Run-1 (probe doesn't exist yet) — Run-2 captures them in its probe-first step (plan §5 G1) and records them in results.html + this log.
- DEVIATIONS: none from spec; probe radius is 6 (not 4) by design (rim/spawn invariant above) — stated in plan §4/§7.
- NEXT: coordinator → Run-2 (subagent, medium) per plan.html, PROBE-FIRST (breakspike + counters on UNMODIFIED logic → G0 → PRE breakspike anchors → implement → rerun → A/B empty diff on eff_md5+minfo) → Run-2 smoke (G0/G1/G2 + ≤1 render) → coordinator heavy gates (G3/G4) + G5 build → commit+push.

