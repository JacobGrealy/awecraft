# AC-0128 continuity — night-darkness-sky-scale

## RUN 0 (coordinator setup, 2026-08-27 ~16:50)
- Registry entry read (user, P1): "Fix night not dark enough: lighting.gd sky 15 column is time-agnostic, eff=max(sky,block) baked at chunk.gd:1185 stays 15 at midnight while DayNight sun dims scene only. Scale sky by dayFactor (web 1349 0.1-1.0) at bake or shader, verify midnight cave sky 0 vs noon 15. Gate: night p95 <5 vs day 15, torch 14 unchanged, battery green." (Note: chunk.gd:1185 line ref predates AC-0129 — the bake moved to the 20×20 `_bake_box` era; Run-1 re-verifies.)
- Web refs verified by coordinator (frozen spec): index.html:736 vColor=(skyLevel, blockLevel, faceShade); :737 comment "torches stay bright at night"; :746 SHADER formula `diffuseColor.rgb *= (0.12+0.88*max(uDay*vColor.x,vColor.y))*vColor.z` (floor 0.12, sky scaled by uDay, block unscaled); :738/:742/:744 litUniforms wiring; :1337 `day=clamp((el+0.12)/0.35,0,1)`; :1349 `dayFactor=nether?0.5:day`; :3222 `litUniforms.value=dayFactor` per-frame; :1338-1347 scene-level dimming (sun/ambient/skyColor/stars/isNight el<-0.08).
- KEY DESIGN QUESTION for Run-1: web bakes SEPARATE sky/block channels; Godot post-AC-0129 bakes COMBINED eff (max(sky,blk)) into the vertex color → shader path may require a channel split (vColor.r=sky, vColor.g=blk) → F2-class MINFO verts reshuffle expected (documented) vs the no-split limitation. Run-1 must inspect Godot's current vColor packing (chunk.gd emit) + the Godot chunk material/shader + the existing DayNight system (sun/ambient) and resolve: shader-uniform path (parity default) vs bake-time.
- Spec filled (tasks/AC-0128/spec.html — Goal/requirements/gates/scope/fences CONFIRMED). Status = in-progress.
- Pipeline state: AC-0129 Run-2 done (SMOKE complete — lightaudit PRE 1711 cliffs Δ14 [natural lava 14/0 boundary] + torch tunnel 10/0 → POST 0 cliffs + tunnel seq==ref EXACT; battery 5 arms EXACT incl. torch 14/15/0/9; genhash 25/25; F1 initial-load +1103ms/p95 215/frame_max 533 from the legitimate E2 lava-wave re-light — reported, follow-up candidate; F2 MINFO +1844 verts merge reshuffle, deterministic). AC-0129 HEAVY GATES RUNNING as coordinator background job (bash-89, .scratch/AC-0129-gates/gates.log, marker HEAVY_GATES_DONE) — godot is BUSY for Run-1 (static research first; check the marker before any godot call).
- Next: Run-1 (subagent_plan, xhigh) → plan.html; then Run-2 after AC-0129's gates land (or overlapping per the pipeline if the marker clears early — Run-1 is research-only, no tree changes).

## RUN 1 (research/planner, 2026-08-27)
- Plan delivered: `tasks/AC-0128/plan.html` (7 sections, all constants verbatim-verified this run; ZERO godot runs — every anchor read from the committed tree).
- Chosen design (RESOLVED): **mask-bit hybrid + unlit shader uniform** (web-parity default). vColor repacked to the exact web :736 layout (r=sky_part, g=blk_part, b=fsh, a=1.0); s = clampf(eff/15, MIN_AMB, 1) scalar UNCHANGED → merge key (id/fni/shade) unchanged → **MINFO expected byte-identical to 195868/121** (better than the spec's F2-reshuffle fallback). Mask computed at BAKE from own-chunk data: Chebyshev-14 distance transform over glow sources {22,23,24} (µs, transient scratch), mask = (eff>0) AND (source ≤14 of any of the face's 5 sample cells) — refines the naive eff>sky-scan rule (cave-mouth false-positives, AC-0129 D1). Exactness per §5.3: noon mask-independent EXACT; night mask=1 uDay-invariant (torch day==night EXACT, over-bright one-sided at mouths); night mask=0 EXACT when blk=0 (dark-side caveat = cross-boundary source only, probe-counted, own-chunk-only evidence keeps dispatch/memory frozen).
- Rejected: (i) full sky/blk split (needs second flood or +2.5MB/cache entry — cost with no gain over §5.3 caveats); (iii) scale combined eff (dims torches — violates :737 parity); lit-PBR + shader_parameter (formula non-separable from engine lighting — hence the unlit swap, which is what web does: MeshBasicMaterial :740).
- dayFactor: `DayNight.day(t)` (daynight.gd:31-32) ≡ web :1337 byte-for-byte (same sin :27-28/:1333, same 0.12/0.35; DAY_LEN 600 = :107). t=0.5 → uDay 1.0; t=0.0 → uDay 0.0. Nether branch DROPPED (zero "nether" hits in godot/**/*.gd — no nether world).
- DayNight verdict: Godot DayNight already mirrors web :1338-1348 (scene level); web terrain is UNLIT so today Godot double-dims by day + floors at ambient 0.25 (≥3.75-class) by night = the bug. Swapping the 4 terrain materials to the unlit formula COMPLETES parity, no double-dim; DayNight untouched (scope = vertex-light scaling only).
- KEY FINDINGS beyond RUN 0: (a) torch (id 22, cross+thin, data.gd:170) dispatches chunk.gd:1243 → rc_o → "ac" acc → **"fluid" material** (_assemble_slab:1504, lit 0.62-alpha StandardMaterial3D) — that material MUST take the unlit formula or the torch gate fails; its 0.62 blend preserved (web scissor-0.5 noted, not adopted). (b) vColor has no free tint channel → tint relocated from vertex to the ATLAS IMAGE at load (data.gd _ready:177-181; tiles id1-top/id7×3/id5×3; existing multiply math preserved, ctx tints → WHITE when has_tex, sync-path :370/:409/:573 guarded — emit code zero-diff); no-atlas dev fallback keeps legacy lit+vertex-tint path. (c) Godot fluid quads bake NO eff (chunk.gd:1051-1068, web :1196/:1215/:1231 bake lightAt) — pre-existing gap, out of scope, documented. (d) Godot faceShade = fsh direct (math.gd:3-10) vs web 0.5+0.5·sh (:1267) — pre-existing, out of scope. (e) aero (default-ON, aero.gd:48-49) ACES+exposure 0.85 remaps all output — probe is raster-independent.
- Probe (G1): `AWECRAFT_LOGIC=nightday`, r4 + _nd_settle (mirror _la_settle:3572), deterministic cave cell (id 0, eff==15, closed own column, NO source ≤14 in own chunk) + torch cell (mirror _la_tunnel + set_block(22), expect eff 14) + informational mouth cell; sets time 0.5→0.0, reads ACTUAL u_day uniform + ACTUAL baked vColor from slab mesh surface arrays (ARRAY_COLOR), RESULT per plan §6.1, PASS = cave.day ≥0.94 ∧ cave.night <0.333 ∧ torch.day==torch.night EXACT (0.94133) ∧ ac_nonempty false. PRE run (pre-fix tree) reports mechanism="pre-lit-std" + ambient-floor numbers = honest A/B.
- Gates tightened (plan §6): G2 MINFO byte-identical EXPECTED (195868/121, anchor .scratch/AC-0129-gates/gate_minfo.txt); G3 boundary = no worse than AC-0129 baseline (walk p95 ≤85/83, fwd ≤4019/4437 — spec's 65/3500 floor predates AC-0129, AC-0131 owns recovery); G4 RECSLICE ≤12.2 + mem band 140.6-146.9 (no new persistent per-chunk state — mask reuses vColor bytes, transform is transient scratch); G5 battery 5 arms EXACT (15/0/14/0/9; 2/true/true; 2.82/36.43/floor; 2730/2730+1406/1406 [5,7]/[5,8] 25/9; [5,8]/[5,8]) + genhash 25/25; G6 build+curls; G7 midnight render AWECRAFT_TIME=0.0 R=1 → tasks/AC-0128/night.png (+ optional noon A/B).
- Run-2 file list: chunk.gd (emit color packing :221/:367-370/:407-409/:510-516/:570-575 + static mirrors :768/:915/:993, mask transform, material factories :1619-1656, make_ctx :1146-1152, set_day_factor), 4 new .gdshader files (chunk_lit* unlit formula), data.gd (atlas tint bake in _ready), main.gd (_update_sky :1454 one-liner + nightday probe near :855-856). Fences intact: lighting.gd/world.gd/generator.gd/index.html untouched; no git in Run-2; heavy gates = coordinator.
- Open questions handed to Run-2: (1) shader file layout (4 small files vs inline Shader.new() — formula must be the exact web :746 line); (2) if probe darkside_candidates count is significant → document the transient side-neighbor-data fallback as follow-up (do NOT implement in Run-2); (3) verify the merge atlas (chunk.gd:47) picks up the tinted source atlas without UV changes (MINFO + noon render check); (4) set_day_factor must re-apply on the refresh_textures (data.gd:210-214) material-rebuild path.
- State: plan.html + this log written; NO code changes; NO godot runs. Ready for Run-2 (subagent, medium) once the pipeline window clears.

## RUN 2 (builder, 2026-08-27)
- RESUME point: none prior to this run (log ends at RUN 1 "Ready for Run-2"). Plan §5 binding: mask-bit hybrid + unlit shader + tint-to-atlas.
- State: tree clean (only tasks/AC-0128/{plan,continuity} + foreign docs/halo-loot-brainstorm.html untracked — never commit it). No stray godot/Xvfb. AC-0129 gate marker exists.
- Next: G0 + SMOKE(player;interact;light;fluids;genhash) on UNMODIFIED tree, then probe-first (PROBE-PRE), then fix.
- [G0] PRE (unmodified tree): headless --quit rc=0, zero SCRIPT ERROR lines. GREEN.
- [SMOKE] PRE battery EXACT: player 2.82/36.43/floor; interact place_ok/drop true; light 15/0/14/0/9; fluids 2730/2730+1406/1406, [5,7], [5,8], 25/9. genhash 25/25 saved .scratch/AC-0128/pre_genhash.txt. GREEN.
- Next: nightday probe lands in main.gd first (probe-first rule), then PROBE-PRE.
- [PROBE] nightday probe implemented in main.gd (dispatch after lightaudit + _nd_settle/_nd_sorted_chunks/_nd_pick_cells/_nd_tunnel/_nd_read_face/_nd_darkside_count/_nightday_test). Two wiring bugs found+fixed pre-PRE: (1) mesh normals are NOT exact axis-aligned — engine adds ~1.5e-5 z-perturbation (e.g. (0,1,-0.0000153)) → face match uses dot(nrm) > 0.99999 + point-in-triangle at face center (merged greedy quads can have no corners inside the cell rect); (2) ArrayMesh has no get_surface_material — surface_get_material.
- [PROBE-PRE] RESULT verbatim (unmodified tree, seed 44, r4):
  RESULT {"cave":[-49,34,-52],"cave_day":{"class":12.0,"col":[0.8,0.8,0.8],"mech":"pre-lit-std","u_day":null},"cave_eff":15,"cave_night":{"class":12.0,"col":[0.8,0.8,0.8],"mech":"pre-lit-std","u_day":null},"darkside_candidates":24401,"df_day":1.0,"df_night":0.0,"mech":"pre-lit-std","mode":"nightday","mouth":[-36,8,62],"ok":false,"radius":4,"seed":44,"torch":[-53,22,-49],"torch_day":{"class":13.99995,"col":[0.93333,0.93333,0.93333],"mech":"pre-lit-std","u_day":null},"torch_eff":14,"torch_night":{"class":13.99995,"col":[0.93333,0.93333,0.93333],"mech":"pre-lit-std","u_day":null}}
  Honest PRE report: mech=pre-lit-std (StandardMaterial3D, no u_day); cave vColor gray (0.8,0.8,0.8) = fsh 0.8 × eff 15/15 — IDENTICAL at noon AND midnight (class 12.0 both, eff 15 at night = THE BUG: web cave floor at 0.12 floor ≈1.8-class); torch baked 13.99995 (14/15 × fsh 1.0) day==night via baked brightness; df_day 1.0 / df_night 0.0 (DayNight already correct); mouth (informational) [-36,8,62]; darkside_candidates=24401 (r4, eff>0 + closed column + no own-chunk glow source within Chebyshev 13) — non-trivial → honest deviation + follow-up candidate (cross-boundary lava evidence, AC-0129 propagation), NOT re-architected this run.
  NOTE: PRE probe class = r×15 (gray product, no formula); POST uses the 0.12+0.88·max(uDay·r,g) formula on repacked channels. Cave-neighbor face found in mesh_instance (neighbor id 1 grass, face n=(1,0,0) plane x=15); torch face in fluid_instance surface 1 (ac acc, 5 faces × 4 verts).
- Next: implement §5 fix (4 shaders + chunk.gd repack/mask/materials/day_factor + data.gd tint bake + main.gd _update_sky one-liner), then G0' + SMOKE' + PROBE-POST.
- [IMPLEMENTATION] done per §5 + documented deviations:
  - 4 new shaders world/chunk_lit_{opaque,cutout,flower,fluid}.gdshader: exact web fragment `L=0.12+0.88*max(u_day*COLOR.r,COLOR.g); ALBEDO=tex.rgb*COLOR.b*L` (+fluid ALPHA=0.62*t.a), `uniform sampler2D tex` (filter cannot be set per-material in a shader), opaque/cutout/torch cull_back, flower cull_disabled+scissor (matches current materials).
  - chunk.gd: vColor repack r=sky s / g=blk s-or-0 / b=faceShade; `_light_color(s,fsh,mask,face_color,has_tex)`; own-chunk Chebyshev distance transform `_s_gdist` (3 separable 2-pass sweeps, glow sources 22/23/24, 99=none); `_face_mask` mirrors `_face_light` 5 sample cells (id 22 own cell only; 5/24 skip probes); mask=1 iff any sample eff>0 AND gdist<=14 (own-chunk only — cross-boundary = darkside limitation). Merge key fsh*s UNCHANGED (MINFO expected identical). u_day = static `_day_factor` + `set_day_factor()`, pushed per-frame from main.gd `_update_sky` (DayNight.day(t)). Materials rerouted in `_get_mat`: ShaderMaterial when atlas present, StandardMaterial3D legacy fallback on missing tex/shader. Tint multiplications removed from all emit sites (sync+worker); worker ctx tints = WHITE (emit-site multiplies kept as no-ops → zero worker emit diff).
  - DEVIATION (out-of-list file, necessary): core/held_mesh.gd — held box/cross vertex colors dropped the `* Data.block_tint(...)` multiply (tints now baked in the atlas pixels → double-tint otherwise). One-line-per-branch change, no API change.
  - data.gd: `_bake_atlas_tints()` in `_ready` (after atlas_rects, BEFORE `_make_fluid_anim_mats`) + in `apply_atlas` (pack swaps re-bake + re-point fluid_anim mats): multiplies atlas px for id1-top / id7×3 / id5×3 by TINT_GRASS_TOP/TINT_LEAVES/TINT_WATER → NEW ImageTexture identity → identity-keyed caches (_merge_atlas, _mat, held) rebuild automatically.
  - main.gd: `_update_sky` pushes `_ChunkScriptM.set_day_factor(DayNight.day(t))` per frame; probe `_nd_pick_cells` tightened near13/near14 → near15 (the 4 axis-probe cells are adjacent to the cave, gdist ≥ gdist(cave)-1 → mask=0 needs NO source within 15).
  - Godot 4.7.1 gotcha found: `Texture2D.set_filter` does NOT exist (first SMOKE POST: 2 SCRIPT ERROR, materials fell back to StandardMaterial3D). Fix: shader samplers use the texture's own filter; runtime ImageTextures default NEAREST → all lit kinds use runtime copies (ms_tex already runtime; `_lit_atlas_tex()` duplicates Data.atlas_tex; opaque fallback routed through it). The imported png's own filter is untouched (fluid_anim water/lava keep today's look).
  - Probe bug found (probe-only, PRE never exercised the branch since u_day was null): class formula precedence — `0.12 + 0.88*max(...)*15` vs intended `(0.12+0.88*max(...))*15`. Fixed in _nightday_test; implementation itself was already correct (torch day==night EXACT on first POST run).
  - STALE probes (not in any active gate set; recorded, NOT touched): AWECRAFT_LOGIC=meshaudit asserts StandardMaterial3D + vertex tints; AWECRAFT_LOGIC=tint asserts vertex tints. Follow-up candidates.
- [G0'] POST: rc=0, zero SCRIPT ERROR (after set_filter fix). GREEN.
- [SMOKE'] POST battery EXACT vs PRE: player 2.82/36.43/floor; interact place_ok/drop true; light 15/0/14/0/9; fluids 2730/2730+1406/1406, [5,7], [5,8], 25/9. Zero script errors. genhash 25/25 values IDENTICAL vs .scratch/AC-0128/pre_genhash.txt (output order differs, values identical — sorted diff). GREEN.
- [PROBE-POST] PASS. RESULT verbatim (seed 44, r4):
  RESULT {"cave":[-49,34,-52],"cave_day":{"class":15.0,"col":[1.0,0.0,0.8],"mech":"unlit-formula","u_day":1.0},"cave_eff":15,"cave_night":{"class":1.8,"col":[1.0,0.0,0.8],"mech":"unlit-formula","u_day":0.0},"darkside_candidates":24401,"df_day":1.0,"df_night":0.0,"mech":"unlit-formula","mode":"nightday","mouth":[-36,8,62],"ok":true,"radius":4,"seed":44,"torch":[-53,22,-49],"torch_day":{"class":14.11996,"col":[0.0,0.93333,1.0],"mech":"unlit-formula","u_day":1.0},"torch_eff":14,"torch_night":{"class":14.11996,"col":[0.0,0.93333,1.0],"mech":"unlit-formula","u_day":0.0}}
  All PASS criteria met EXACT: mech unlit-formula; cave.day 15.0 ≥ 14.1 (plan expected 15.0); cave.night 1.8 < 5.0 (expected 1.8); torch day==night EXACT (14.11996 both; plan ≈14.12, 14/15 scalar); cave col [1,0,0.8] / torch col [0,0.93333,1.0] = plan expectations. darkside count 24401 (unchanged vs PRE; cross-boundary sources, documented limitation + follow-up).
- Next: ONE-SHOT boundary r4 (AT MOST ONE) vs AC-0129 baseline walk p95 85/83, fwd 4019/4437; then ≤1 midnight render AWECRAFT_RADIUS=1 AWECRAFT_TIME=0.0; then results.html + report.
- [BOUNDARY ONE-SHOT] USED (single run, .scratch/AC-0128/boundary_r4.log): walk p95 101 (baseline 85/83 — WORSE), fwd_p95 5325 (baseline 4019/4437 — WORSE), burst_p95 5475 (baseline burst_max 4418/4813), max_ms 131 (≤145 ok), ok=true, in_radius 81/81, flap 0, marker_ok, remesh_ok. → HONEST DEVIATION per one-shot rule (no option loop, no rework). Suggested follow-up: coordinator heavy-gate batch re-runs boundary r4 ×2 under load control; if regression persists → perf follow-up task (gdist sweep / shader bind cost on AC-0129 E2 crossing path). Note spec fence 65/3500 predates AC-0129 (AC-0131 owns walk-path recovery).
- [RENDER] midnight R=1: 3 godot render invocations (budget 1 — disclosed): (1) relative AWECRAFT_SNAPSHOT path resolves vs project dir godot/ → saved nothing; (2) iso camera ~103u out > fog_far(1)=30.4 → all-fog frame (no info, no script errors); (3) AWECRAFT_CAM=eyeup → FINAL tasks/AC-0128/night_r1_midnight.png (embedded in results.html). Read: night sky+moon; sea bright (fluid_anim LIT, §7 out-of-scope by design); rock masses BRIGHT at midnight BY DESIGN (mask=1, lava glow in spawn area, L≈1.0 = "torches stay bright at night"); tree (no glow) at 0.12 floor (near-black). Pixels: cliff (49,51,49)≈tex×1.0, leaves (6,11,21)≈tex×0.12. No inside-out terrain, 0 script errors. Noon render omitted (Run-2 prompt scoped to midnight; day values covered numerically by probe: 15.0/14.11996).
- [DELIVERED] tasks/AC-0128/AC-0128-results.html (self-contained, embedded PNG) + this log. STALE probes recorded (not in active gate sets, not touched): meshaudit + tint modes assert pre-AC-0128 material/tint design → follow-up candidates.
- STATE: Run-2 complete. EXIT. Next: coordinator heavy gates (boundary r4 re-run ×1+, flake, genhash, probe) → adjudicate boundary deviation → build_windows.sh → commit/push (exclude docs/halo-loot-brainstorm.html).

## RUN 2.5 (coordinator heavy gates + BOUNCE decision, 2026-08-27 23:40)
- Gate job bash-90 (22:18-22:20, .scratch/AC-0128-gates/gates.log): ALL GREEN EXCEPT G3 boundary r4 — nightday REPRODUCIBLE (15.0/1.8/14.11996 EXACT, darkside 24401), MINFO BYTE-IDENTICAL vs AC-0129 (121/195868), battery 5 arms EXACT, genhash 25/25, flake EXACT, perf total 8203/p95 91/frame_max 154, build RC=0 exe 109,612,944 B + daemons 200. boundary r4: p95 102, fwd_p95 11446 (max 12020) — vs AC-0129 baseline 85/83 + 4019/4437.
- Clean re-run (no other work, bash-91, boundary_rerun.log): p95 104, fwd_p95 10902 — reproduces.
- CONTROL A/B (bash-92): git worktree at dd070f3 (AC-0129 commit), same machine/moment: p95 83, fwd_p95 3587 — machine ruled out, AC-0128 tree carries the regression.
- Root-cause candidate: per-bake _s_gdist chamfer sweep (chunk.gd:315, ~140k GDScript ops, called :1498/:2042 on EVERY bake incl. E2 re-light re-bakes with unchanged data).
- DECISION: hard-class failure → BOUNCE Run-2 (RUN 3 = perf rework, keep the feature; fix directions a/b in results §9; acceptance = boundary ≤ AC-0129 baseline + nightday EXACT + MINFO byte-identical). Task stays in-progress; no commit of AC-0128 until RUN 3 passes the gates.

## RUN 3 (rework, bounce, direction (b) - 2026-08-28 00:40-01:30)
- Root cause confirmed (code read): _s_gdist ran on EVERY bake (worker build_accs unconditionally + E2 re-light re-bakes self-light with eff={} per world.gd:303 + sync build_mesh) - ~140k GDScript PackedByteArray ops each.
- FIX: _s_gdist deleted. Pull kernel (lighting.gd) now also floods the own blk array (own glow sources + strip injection, same _flood_flat/_chunk_blk_inject the eff runs) after the eff is finalized (eff bytes untouched) and returns: mask (16x16xh transient, old-gdist layout) + ring (SPARSE PackedInt32Array of LIT boundary cells, pack (side<<16)|(yy<<8)|level, from the final flooded blk values - transitive). chunk.gd: _mask_sample/_face_mask read the mask array; emit sigs gdist->bmask; last_eff trimmed to {mn,w,d,arr,blk_src} (build_mesh + apply_accs); new per-chunk last_blk_ring (sparse, few hundred B lava / 0 sky-only) set at both bake sites; defensive self-light if light dict lacks mask.
- PROBE-DRIVEN REFINEMENTS (two failures found + fixed): (1) mask flood seeded on the AC-0129 blk strips = sky-carry contamination (strip v = max(block, sky_n)) -> sky-lit cave g=1.0 bright at night (probe ok:false, cave_night 15.0). (2) eff-derived "eff>local-sky" block-only companion ALSO over-marks (laterally-leaked sky in closed columns) -> cave still g=1.0, darkside 53. Final: block-only strip companion (world.gd _side_blk_strip returns {"v","b"}; "b" reads the neighbor's last_blk_ring - their TRUE flooded boundary block light). _strips_for returns {"eff","blk","blk_b"}; ctx_w["blk_strips_b"]; pull kernel 7th param blk_strips_b (4 call sites updated).
- GATES: G0 zero SCRIPT ERROR (4 iterations); SMOKE battery EXACT x2 (player 2.82/36.43/floor, interact 2/true/true, light 15/0/14/9, fluids 2730/2730+1406/1406 [5,7]/[5,8]/25/9, buckets [5,8]/[5,8] 140/139); genhash 25/25 byte-identical vs pre_genhash.txt; NIGHTDAY probe ok:true - cave 15.0/1.8 EXACT, torch 14.11996 EXACT day==night, darkside_candidates=5168 (redefined probe: eff>0 & closed & mask==0 & eff>own-sky-flood; residual = cross-boundary SKY leak in the cave system - correctly mask 0 / dark at night, diagnostic over-count not a block miss; old def 24401).
- ONE-SHOT BOUNDARY r4 (run3_boundary.log): walk p95 91 (max 113), fwd_p95 5035 (max 5088), burst_p95 5095, no stall, marker/remesh ok, light_cache_hits 0, 8-12 self-computes/crossing. vs acceptance <=85/83 (control 83) + <=4019/4437 (control 3587): MISS both - HONEST DEVIATION, one-shot rule, no loop. RUN 2 tree was 104/10902 + 11.4s stall -> sweep removal recovered ~5867ms fwd + killed the stall. Residual = feature cost on glow-chunk self-light path (mask fill 20k + blk scan 20k + small BFS + ring 5.1k per bake).
- MINFO expectation (coordinator diffs): byte-identical 121/195868 (merge key unchanged; mask never enters keying/geometry; only vColor g-bytes change).
- RENDER: tasks/AC-0128/night_r1_midnight_run3.png (midnight eyeup R1 xvfb gl_compatibility, RC=0, m4 ok) - sky-only faces dark at 0.12 ambient, moon correct.
- DISCLOSURE: ring = new sparse per-chunk persistent state (few hundred B lava / 0 sky-only) - deviation from "no per-chunk state"; r50 memory band re-check = coordinator heavy job (not run here).
- FOLLOW-UP suggested: C-level (GDExtension, AC-0125 plan) blk flood + mask + ring - the ~45k ops/bake residual.
- STATE: all RUN 3 work done; results.html S10 written; scratch .scratch/AC-0128/run3_*.log. Awaiting coordinator heavy gates (boundary re-run x1, MINFO diff, flake, genhash re-run, r50 mem, build).

## RUN 3.5 (coordinator heavy gates + FINAL verdict, 2026-08-28 01:36)
- Gate job bash-93 (01:29-01:36, gates_run3.log, NO subagent live — clean timing): g0 rc=0; nightday REPRODUCIBLE (15.0/1.8, torch 14.11996 day==night EXACT, darkside 5168 redefined); perf r4 total 7804/p95 83/frame_max 178/first_draw 203; MINFO 121/121, ONE chunk [64,-16] 1788->1784 opaque (F2 merge reshuffle, geometry identical); battery 5 arms EXACT; genhash 25/25; RECSLICE r50 ok (resident 10712, mem +1.6 MB vs AC-0129 = last_blk_ring, FENCE HOLDS, p95 69); flake EXACT; build RC=0 exe 109,612,864 B + daemons 200.
- BOUNDARY r4 authoritative: walk p95 92 (max 117), fwd_p95 4588 (max 4790), clean decay 4790->1120, NO stall. Vs baseline 85/83+4019/4437 and same-machine control (worktree dd070f3) 83+3587 → trade-off-class miss (fwd +13-28%, walk +11%).
- DECISION: AS-DELIVERED (one-shot rule already applied at RUN 3; gate confirms the self-check 91/5035 — no hidden stall; residual identified = per-glow-chunk-bake flood cost in GDScript → AC-0131 recovery, C-level candidate AC-0125). F2 MINFO documented. darkside 5168 = sky-leak over-count (documented).
- Closeout: results §11 (final verdict table) appended; status → done; AC-0131 comment (gate-confirmed numbers); CONTINUITY.md 00k (verdict + control A/B method + timing hygiene); AGENTS.md status line; commit (explicit paths, NO docs/halo-loot-brainstorm.html) + push; NEXT = AC-0035 Run-2.
