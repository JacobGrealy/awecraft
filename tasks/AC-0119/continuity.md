# AC-0119 continuity log (append-only)

## RUN 1 (research/planner, xhigh) — 2026-08-26

### State: reading code → pre-change baselines
- Read: spec.html (G1–G6 + scope + fences), CONTINUITY.md 00e, ARCHITECTURE.md, two-phase template (7 sections), index.html world.block :882-887, world.gd (full 1759 lines), lighting.gd (full 420), chunk.gd (read surface), main.gd harness (boot/battery/perf/boundary), AC-0107 + AC-0077 + AC-0118 continuity/results (31 ms diagnostic context).

### Key code facts (fresh re-parse — registry line refs STALE, use these)
- `fluids.gd` does NOT exist; fluids live in `world.gd`.
- Sync-gen sites in world.gd (fresh): `_gen_unit` sync branch **:426** (spawn (0,0) + web path — legitimate drain leg); `_threadgen_worker` **:460** `generate_args` (worker — legitimate); `create_chunk` **:1037-1044** sync gen (+optional mesh); `_chunk_data` **:1049-1069** — c==null branch: threadgen enqueue (inst −1) then `c = chunks.get(key)`; if still null → `create_chunk` (SYNC, no ONDEMANDGEN print!); is_empty branch **:1062-1068** → sync `WorldGen.generate` + `ONDEMANDGEN cx,cz gen_ms=` print (unconditional).
- `get_block` **:1380-1384** → `_chunk_data` → `c.get_local` (chunk.gd:114 `data[...]` — OOB script error on empty PackedByteArray, so get_block must guard empty data).
- `set_block` **:1386-1412** (create=true default → `_chunk_data` → sync gen; web `setBlock` :888 NEVER generates — no chunk → return).
- `set_fluid` **:1575+** (create default FALSE → chunks.get only).
- `_nb_block` **:1618-1621** (fluid tick neighbor read; nc null or empty → `get_block` → sync gen).
- `fluid_level` **:1599-1613** — already air-safe (returns 0 on null/empty chunk), no change needed.
- `tick_fluids` **:1623-1740** — region = ±`fluid_tick_radius` blocks (14) ≈ ±1 CHUNK around player chunk (not ±14 chunks): `_nb_block` on the player chunk's 4 cardinal neighbors can sync-gen at most ~4 chunks/tick transiently; web parity = air-on-miss (web `world.block` :884-885).
- `surface_top`/`spawn_point` **:1498-1509** — get_block loop → spawn-chunk sync gen (legitimate boot; spawn (0,0) always sync by rule).
- `light_at` **:1511-1517** — box player ±8 blocks (≤±1 chunk, in-radius); reads via compute_light_split.
- Lighting.gd neighbor-snapshot sites (fresh): `compute_light_split` **:85-88** (null-probe `world.get_block(wx,0,wz)` :87 + re-fetch) and **:108-110** (per-cell `world.get_block` in c==null branch); `compute_light_flat` **:170-173** (null-probe :172) and **:193-196** (per-cell :196). `compute_light_flat_batch`/`_chunk_light_into`/`compute_light_flat_chunk` (:272-329) are self-contained (own data only) — untouched.
- chunk.gd `_build_snap` **:1239-1297** (sync mesher): per diagonal neighbor — `nc = chunks.get(nkey)`; if null → **probe `get_world_block.call(nxc*16+8, 4, nzc*16+8)` :1273** (the sync-gen trigger → `_chunk_data` → ONDEMANDGEN/create_chunk, up to 4 per build) then re-fetch; **direct read `nc.data[drow+gi]` :1289 with NO empty check → index-OOB script error if nc is a stub (present, empty data)**; else per-cell `get_world_block.call` :1294.
- chunk.gd `_build_snap_data` **:1023-1065** (worker): comment says 8 neighbor copies ALWAYS present in nbs (dispatch rule: missing neighbor → sync path) — worker path never sees missing neighbor data. No change.
- world.gd `_mesh_dispatch` **:583-641** (AC-0107): all 8 diagonals present+nonempty → threadmesh worker w/ fresh `duplicate()` copies; ANY diagonal null/empty → **sync `c.build_mesh(get_block, eff)`** (legacy path, keeps working post-change with air reads; the AC-0107 dispatch fence says don't touch it).
- Drain: `_build_ready` **:377-382** requires 4-orthogonal data (diagonals NOT required — that's why sync fallback exists). `_collect_pool` **:359-372** skips meshed (built chunks never re-queued by drain). `_drain_build_queue` **:766-824**: data pick → `stub_chunk` (no data) + `_gen_unit` (threadgen or sync). WANT/STUB/MERGE phases (AC-0118) **:1126-1218**: WANT re-queues only non-meshed in-band chunks; STUB creates empty stubs for the whole (2r+3)²; MERGE_RING adds data_only entries for the r+1 ring → **every diagonal neighbor of every in-band chunk is either in-band or has a data_only drain entry ⇒ no permanent air holes, BUT nothing marks the ALREADY-BUILT neighbor dirty when the ring data lands** (threadgen_handoff :499-504 sets c.data + init_fl + _apply_edits — no neighbor marking) ⇒ the plan must add a "neighbor-data-landed → mark built 8-neighbors light_dirty" hook (small new helper, called from the gen-landing sites; rides the existing light-flush remesh path — no drain/dispatch internal changes).
- `_eff_cache` (AC-0077) keyed on `c.data` identity (:917-922, :947) — a miss (no entry) is never a fake signature; air-on-miss doesn't corrupt it. Empty-data chunks are skipped by the batch (:915).
- main.gd: `spawn_point()` at boot → surface_top → spawn-chunk sync gen (legitimate). Battery: `_batt_reset_state` :126-150 zeros all data (`fill(0)` — NOT empty) + recenter(true). `light`/`fluids` modes: `recenter(..., false)` → sync `create_chunk` for all 81 in-radius (legitimate harness boot path, "logic-test path not play path" per AC-0071). `perf` :910-916 (recenter true, no player); `boundary` :917-925 (fluid_sim on, recenter true, `_await_boundary_core` waits 9×9 meshed, then 20-chunk fly-walk + settle + re-entry remesh check). RESULT fields incl. built_final/irb/flap/ok.
- Env gates: `AWECRAFT_TGDEBUG=1` → TGEN ENQ/DATADROP/STALE prints (world.gd:157); `AWECRAFT_TIMING=1` → GENCHUNK + per-build prints; `ONDEMANDGEN` prints unconditionally (world.gd:1068); `AWECRAFT_RADIUS` (main.gd:254-260); default seed 44.
- Gen call-site audit (grep `WorldGen.`): world.gd :426 (drain sync — KEEP), :460 (worker — KEEP), :1039 (create_chunk — callers: recenter mesh_now=false harness path :1193 KEEP; _chunk_data read path :1058 REMOVE), :1064 (_chunk_data is_empty — REMOVE, the ONDEMANDGEN). main.gd :159 (spawn-chunk boot sync — KEEP), :633/:5278 (MINFO/diag pre-gens — test-only, KEEP), :5599/:5742-5745/:5766 (genhash harness arms — KEEP), :5526/:5541 (`_putc` internal sim — KEEP). All `WorldGen.terrain_height`/`biome_at`/`_fbm2chunk`/`B_*` hits are noise/biome lookups, not chunk gen.

### Baselines — MEASURED (all RC=0, zero script errors, seed 44, r4)
- **boundary r4** (`AWECRAFT_LOGIC=boundary AWECRAFT_RADIUS=4 AWECRAFT_TGDEBUG=1`, `.scratch/AC-0119/pre_boundary_r4.log`): read-path sync-gen = **0 ONDEMANDGEN + 2× create_chunk** (`TGEN DATADROP 1,0` = post-walk marker-chunk read of freed chunk; `TGEN DATADROP 4,0` = post-teleport read) + 1× spawn-boot (0,0) ≈ 62-104 ms. Anchors: ok:true, built_final 81, irb_final 81, irb_max 81, irb_min 19, irp 81, flap 0, queue_size 93, p50 14 / p95 33 / max 77, walk_s 19.52, crossings 20, loads 220 / unloads 209, resident 133, marker_ok true, remesh_ok true, unbodied 0, staged 232/0, light_cache_hits 226 / batch_calls 15 / batch_chunks 253 / self 24, fluid p95 12.53 / max 14.31, forward_p95 4279, burst_p95 4279, trailing_p95 34, mem 29.6 MB. (TGEN CAPDROP spam = known AC-0082 drain re-pick churn, not errors; ENQ 300 / GENHAND 294 / GENCHUNK 0.)
- **perf r4 desktop** (`AWECRAFT_LOGIC=perf AWECRAFT_RADIUS=4 AWECRAFT_TGDEBUG=1`, pre_perf_r4.log): 0 read-path (spawn boot only, 1 DATADROP). all_meshed true, total_ms 6457, gen_ms 695, build_ms 356, build_units 1364, p95 49, max 86, first_draw 267, frames 396, drain_s 6.46, staged 72/0.
- **perf r4 web-sim** (`AWECRAFT_THREADGEN=0 AWECRAFT_THREADMESH=0`, pre_perf_r4_web.log): 0 read-path (spawn boot gen only). all_meshed true, total_ms 8628, gen_ms 4154 (≈52 ms/chunk sync drain), build_ms 2972 (all sync builds), build_units 199, p95 55, max 88, first_draw 272, frames 524.
- **Baseline finding:** the "31 ms × up to 4 per build" worst case did NOT materialize at r4 (ONDEMANDGEN=0 in all 3 runs) — threadgen keeps up + 4-ortho build gate ⇒ diagonal data present at build time. What materializes: 2-3 read-path create_chunk sync gens per streaming run. The probe site is removed structurally anyway (covers slower-gen conditions).

### Audit correction (supersedes the line-27 entry above)
- Authoritative grep this run: main.gd gen sites = **:159 (inside `_genhash_print` — diagnostic, NOT spawn boot), :633, :5278, :5599, :5742-5745, :5766** — no `_putc`/5526/5541 sites exist. The spawn-chunk sync boot is NOT a direct call — it happens via the READ path (spawn_point → surface_top → get_block → _chunk_data → create_chunk); plan §2.1(g) makes it explicit via new `_ensure_spawn_chunk()` in `spawn_point()` (port rule, idempotent).
- Audit outcome for plan §2.3: **2 removed** (world.gd:1064 is_empty branch incl. ONDEMANDGEN print; world.gd:1058 create_chunk-from-`_chunk_data`), **9 kept** (:426 drain sync, :460 worker, :1039 create_chunk [callers post-change = recenter(false) :1193 + _ensure_spawn_chunk], main.gd :159/:633/:5278/:5599/:5742-5745/:5766), 2 definitions N/A.

### Plan COMPLETE — tasks/AC-0119/plan.html written (7 template sections, in order)
- Six decisions answered in plan: (1) air-on-miss per site with exact return values (§2.1 a-g — `_chunk_data` pure lookup, `get_block` 0, `set_block` no-op, lighting b=0 air column, `_nb_block` 0, `_build_snap` dv=0/fv=0); (2) full audit table §2.3 (2 removed / 9 kept); (3) convergence proof §2.2 — new `_data_landed(cx,cz)` helper marks built 4-ortho-ready 8-neighbors `light_dirty`, called from `threadgen_handoff` :501 + `_gen_unit` :428 only, rides the existing light-flush remesh path (no drain/dispatch internals touched); (4) baselines §5.0 (above); (5) risks a-e §7 (no permanent air holes by drain-entry coverage + landing-order induction; fluids battery unaffected — recenter(false) fully generates; eff cache no fake signatures; worker untouched; player physics web-faithful); (6) gates §5.1 G1-G6 exact commands + expected values.
- **2 scope additions flagged in plan (coordinator must approve before Run-2):** (1) chunk.gd `_build_snap` :1271-1295 — probe delete + empty-data guard (OOB fix, MANDATORY: post-change missing/empty diagonal is the normal frontier case and :1289 `nc.data[...]` has no empty check); (2) `_data_landed` hook (new helper + 2 call lines, world.gd only).
- Probe design §2.4: `perf_read_sync_gen` (+ms) — incremented only in the is_empty branch (deleted post-change ⇒ structurally 0) + `perf_create_sync_gen` in `create_chunk`; both + ms in perf/boundary RESULT. G4 post: read_sync_gen==0 (hard), create_sync_gen==1 (spawn boot only).
- Harness change §2.5: boundary `marker_ok` read :5013 moved to AFTER the re-entry remesh loop :5052 (pre-change it only passed via read-path sync gen of a freed chunk). Default-logic-run `ocean_water_at_sea`/`stone_at_depth` flip reported, not gated.
- Constants re-verified this run for the verbatim gate: CHUNK 16 / HEIGHT 80 / SEA 30 (data.gd:3-5), SPAWN_X 8 / SPAWN_Z 8 / SPAWN_H 34 (generator.gd:24-26), CFG HEIGHT 80 / SEA 30 (index.html:102-103), biome_at thresholds (generator.gd:45-54), scheduling constants world.gd :6-11/:19-23/:25/:26/:139 all as cited in plan §3.

### State: DONE — plan.html delivered, awaiting coordinator gate → Run-2 (medium)
- Next (Run-2): implement plan §2 exactly (world.gd a-g, lighting.gd 2 fns, chunk.gd `_build_snap`, main.gd probe fields + marker move), verify G1-G6 per plan §5.1, write AC-0119-results.html. Logs under .scratch/AC-0119/ (pre_* = baselines; post_* = Run-2's).
