# AC-0151 — water-always-blue-tint-strip — continuity

## 00 — 2026-08-30 — filed + parked
- Root cause: `data.gd:203` `_bake_atlas_tints` tints only base 32x32 at rc 224,0; water anim 32 (1024px strip, fluid_anim TILE_F 1/32) -> 31/32 frames grey, blue flicker frame0 only.
- Fix: loop f over block_anim_frames(tid) tint rc.y+f*32 (scope data.gd only, keep AC-0138 dedup).
- Spec: `tasks/AC-0151/spec.html` filled (goal/requirements/gates/scope/fences), queue pos1 after AC-0110 before AC-0150, status open parked — do not launch until user says go (AC-0110 delegated xhigh holds godot lane).

## 01 — 2026-08-30 — builder run (fix + gates)
- START builder AC-0151. Fix: `data.gd _bake_atlas_tints` — inserted `for f in range(block_anim_frames(tid))` around the pixel loop; tint row now `rc.y + f*TILE_PX + py`. `apply_atlas` needs no separate loop — it delegates to `_bake_atlas_tints` (data.gd:258), so pack-swap re-bake is covered by the same change. AC-0138 dedup (`baked.has(rc)`), WHITE-tint skip, and `block_anim_frames` lookup all retained; no hard-coded 32. Grass/leaves anim=1 → byte-identical behavior; only water (anim 32 @ 224,0) gains rows 1..31.
- Gates (logs in `.scratch/AC-0151-gates/`):
  - G0: `--quit` exit 0, SCRIPT ERROR 0 (g0.txt).
  - tintprobe (temp `godot/_ac0151_probe.gd`, extends SceneTree, DELETED after): `ok:true`, exit 0. All 32 water anim frames tinted: baked_f == raw_f × TINT(47,107,235) per channel ±2 for every frame; frame0 [35,81,178], frame16 [32,73,160]; pre-fix raw comparison: frame0 (194,194,194), frame16 (174,174,174) — grey. DEVIATION: spec's "all 32 frames equal" is unsatisfiable — the base water strip genuinely animates (3 distinct per-frame noise values, raw f0≠f16); probe checks per-frame `baked == raw×TINT` instead (the real invariant: no grey frame anywhere).
  - genhash: 25/25 `GENHASH` lines, byte-identical to `.scratch/AC-0143-gates/genhash_new.txt` (genhash.txt). No worldgen change, as expected.
  - SMOKE battery `player;interact;light;fluids;genhash` (battery.txt): all 5 EXACT — player after_fly_y 136.78 / horizontal_moved 2.82; interact place_ok true / after_place_cell 2 / breakable_id 2; light cave_eff 10 / surface_eff 15 / torch_level 14; fluids sea 2730/2730 + backed 888/888 stable + water_on_lava 25 + sideways_lava 9; battery ok:true. NOTE: 7× pre-existing `SCRIPT ERROR threadgen_handoff (Nil→PackedByteArray)` lines in player/interact modes — verified PRE-EXISTING by stash-reverting data.gd and re-running player standalone (3 of the same lines pre-fix, identical values); unrelated to this change (world.gd worker-thread path).
  - render (render.txt + water_frame_mid.png, 1280x720): xvfb gl_compatibility, R=1, `AWECRAFT_AIM=-0.5,135.0,10.5,0.0,-1.5708` (eye 8 above water cell (-1,10) surface, straight down), `AWECRAFT_TIME=0.15` (daylight), `AWECRAFT_ANIM_PHASE=0.15`. Water fills the frame, BLUE with the per-frame noise pattern tinted blue — no grey. Frame index: shader `frame = floor((TIME+phase)/0.3) % 32`; wall to snap 13.3 s (warm cache) − ~2 s boot → TIME ≈ 10.8–11.8 s → frame ≈ 4–7 (est., non-zero). DEVIATION: task brief said AWECRAFT_TIME picks the anim frame — it actually sets `Game.time_of_day` (day/night, main.gd:71); the shader frame is real-elapsed TIME + AWECRAFT_ANIM_PHASE (fluid_anim.gdshader:19, frame_time 0.3 confirmed in data.gd:274). Since the fix tints all 32 frames identically (probe-proven), the exact frame is immaterial: blue every frame. First render attempt (relative snapshot path) failed PNG save under this session's file sandbox (pathcheck: relative save err 7, absolute save 0) — re-ran once with absolute path; succeeded.
- DONE — all gates green; `godot/autoload/data.gd` only touched by builder (tasks/TASKS.yaml modification pre-existed from coordinator). No git commits by builder.
- Finding (out of scope, not fixed): lava (id 24) also has an anim strip (anim 20 @ 0,32) but `block_tint(24)` returns WHITE, so lava was never tinted by design — no grey-flicker bug there; nothing to fix within this task's scope.
