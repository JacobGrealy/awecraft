# AC-0035 continuity — m5a2-light-bake (stars + viewmodel lighting + player light)

## RUN 1 (research/planner, 2026-08-28)
- Plan delivered: `tasks/AC-0035/plan.html` (7 sections, all constants verbatim-grepped this run). **ZERO godot runs** — the AC-0128 heavy-gate job owned godot at start; it FINISHED mid-research (`.scratch/AC-0128-gates/HEAVY_GATES_DONE` exists, no godot/Xvfb procs) — its gates.log was read from disk for baselines, nothing executed.
- Tree state at research time: AC-0128 (night-darkness-sky-scale) Run-2 changes are **UNCOMMITTED in the working tree** (vColor repack r=sky/g=blk/b=fsh, 4 NEW unlit shaders `godot/world/chunk_lit_{opaque,cutout,flower,fluid}.gdshader`, atlas-baked tints in data.gd, per-frame `u_day` push from main.gd:1464, held-mesh double-tint fix at held_mesh.gd:96-99/:123-125) — AC-0035 builds ON TOP, must not revert.
- Godot current state: NO star field/skybox/dome anywhere (only the separate aero-mode `aero_sky` sphere, main.gd:1385-1397, which per-frame camera-follows at :1432 — in-repo precedent). Per-frame hook = `main.gd _update_sky` :1457-1477 (called unconditionally every frame from `_process` :1428; already does `set_day_factor`). Viewmodel nodes = `player.gd` (`held_box`/`held_sprite`/`held_fist`/`held_tool` under camera sway_root, :360-396; per-frame `_process` :113-123) — **held_mesh.gd (HeldMeshes) is only the static mesh/material cache shared with drops; stays UNTOUCHED** (deviation from spec file list, recorded in plan §2). Light API = `World.light_at` (world.gd:1674-1680) → fresh `compute_light_split` 17³ box (lighting.gd:62-142) → {sky,block,eff}; air-on-miss (AC-0119); ≈3-7 ms worst case → cached cell-change + 500 ms TTL.
- KEY FINDS beyond the prompt: (a) index.html:**3248** `window.__stars.position.copy(camera.position)` — the web MOVES stars with the camera every frame (the prompt's "does the web move them" question → YES, at the EYE position per :3246) → the camera-follow design is a FAITHFUL port, not a deviation. (b) index.html:**3253** the web's own F3 read = `lightAt(floor(px), floor(py+EYE), floor(pz))` — the EYE cell is the canonical "light at the player" (EYE 1.62 both sides: :116 / player.gd:3) → viewmodel light read at the eye cell. (c) `Chunk._mat_cache` (chunk.gd:62) is a static Dictionary reachable from main.gd (GDScript privacy = convention) → the player-light uniform push needs NO chunk.gd edit (fence-safe). (d) `last_eff` (chunk.gd:19) is COMBINED eff only — unusable for the exact `max(uDay·sky, blk)` formula → `light_at` split read chosen. (e) `_voxel_mat` (tools) + `held_fist` are ALREADY UNSHADED; only held_box/cross materials are lit (shared) → player-local UNSHADED duplicates (drops keep the shared lit material — no cross-contamination). (f) camera fov 75 (player.tscn:18), far default 4000 → 320-radius star sphere always in range. (g) The proven `_light_test` battery pattern gives deterministic cells already validated EXACT: surface eff-15 cell + torch pocket (cave_eff 0 → torch 14) — the viewlight probe borrows it wholesale.
- Design resolved (plan §5): (1) stars = one MeshInstance3D "Stars" (500 billboard quads, 800 verts indexed) + NEW `godot/core/star.gdshader` (unshaded/transparent, view-space billboard via UV-encoded corner, `ALPHA = u_opacity`, no fog() call); positions = the web's EXACT 500 points via a verbatim `mulberry32(42)` GDScript port (anchors: star0 (100.038,−64.708,−296.994), star1 (162.576,260.586,−89.8), star2 (220.09,−196.884,−123.277)); per frame in `_update_sky`: `u_opacity = 1−day` + node pos = player eye (web :1347/:3248). Quad world size 0.70 ≈ web 1.8 px at 1080p/75°. Rejected: PointMesh (no custom positions), PRIMITIVE_POINTS (no gl_PointSize exposure), CPUParticles3D (sim overhead). (2) player light = per-fragment add in all 4 unlit shaders: `L = max(L, 0.12 + 0.88*clamp(u_player_light − distance(WORLD_POSITION, u_player_pos), 0, 15)/15)` — MC-style level falloff, L_p = 5 (d0 = 0.4133, radius 5 blocks), uniforms pushed per frame from `_update_sky` via `_ChunkScriptM._mat_cache` (8 set_shader_parameter/frame; safe defaults u_player_light=0 → no-op). Rejected: PointLight3D (unlit shaders ignore scene lights), bake (fence), additive (white-out). (3) viewmodel = UNSHADED player-local materials + per-frame `albedo_color = base × L` where `L = max(0.12 + 0.88*max(day·sky/15, blk/15) at the EYE cell (light_at, TTL-cached), 0.12+0.88·5/15)`; rendered = atlas(tint)×vcol(shade)×L — no double-dim (AC-0128 preserved). (4) probe `AWECRAFT_LOGIC=viewlight` (env block = nightday's: RADIUS=4) → settle `_nd_settle`, cells from `_light_test` pattern, player spawn (physics inert, mode ≠ play), per cell: teleport eye==cell, `vm_refresh(true)`, read sky/blk/eff + vm_no_pl + vm + pl_contrib + ACTUAL mat_L + star u_opacity at t 0.0/0.5; RESULT PRE (pre-no-viewlight, star absent, L_p 0.0, mat_L 1.0 at every cell = the bug) / POST (ok=true: outdoor 1.0 ≥0.94, cave0 0.12→0.41333 strict lift, torch 0.94133, star opacities 0.0/1.0).
- Gates (plan §5.5/§6): G0 quit; G1 viewlight PRE→POST; G2 nightday re-pass EXACT (15.0/1.8/14.11996); G3 ONE-SHOT boundary r4 baseline = AC-0128 GATE job (gates.log :472 — walk p95 102, fwd_p95 11446, burst 7795, first10 6, flap 0, irb 81; Run-2 one-shot was 101/5325 — the gates.log run is binding per the one-shot rule) — do not make worse, else honest deviation; G4 MINFO byte-identical vs `.scratch/AC-0129-gates/gate_minfo.txt` (121/195868; verified the AC-0128 gate file is ALREADY identical to that anchor — diff empty); G5 battery 5 arms EXACT + genhash 25/25; G6 one R=1 midnight render (stars visible = fog/compile proof; grep render log for shader|error — headless G0 does NOT compile spatial shaders). Memory fence: no new persistent per-chunk state (r50 band 140.6–146.9 untouched); no new autoloads.
- Run-2 file list (plan §2): main.gd (star node+builder+_update_sky pushes+viewlight probe), 4× chunk_lit_*.gdshader (+2 uniforms +3-line fragment add), NEW core/star.gdshader, player.gd (PLAYER_LIGHT_LEVEL const + vm_refresh + _vm_mats + unshaded duplicates in _update_held/_setup_held_tool + _process call). Read-only: chunk.gd/world.gd/lighting.gd/data.gd/held_mesh.gd/index.html/TASKS.yaml.
- Open questions for Run-2: (1) verify `WORLD_POSITION` fragment builtin in 4.7.1 spatial shaders (documented; if not — vertex-stage varying fallback is in plan §7.2); (2) the star quad size 0.70 is a visual target — tune ± if the G6 render shows it wrong (mapping math in plan §5.3); (3) confirm the held_sprite material_override keeps its texture (plan §7 row 6; `AWECRAFT_FPV_ITEM` render available if a second look is wanted); (4) probe PRE must NOT reference the new shader params or the star node unguarded (null-guards specified in plan §5.4); (5) godot was BUSY with AC-0128's gates at my start and is now FREE (marker exists) — Run-2 may use godot normally, but still check `ps -eo pid,comm,args | grep -E '^\s*[0-9]+ (godot|Xvfb)'` + the marker first, and one godot at a time.
- State: plan.html + this log written; NO code changes; NO godot runs. Ready for Run-2 (subagent, medium) — probe-first (G1-PRE) before any feature code.

## RUN 2 (builder/medium, 2026-08-28)
- Tree re-verified on the ground at start (committed e851556): the 4 unlit `chunk_lit_*.gdshader` match AC-0128 verbatim (uniforms `u_day` + `tex` only, formula `L = 0.12+0.88*max(u_day*COLOR.r, COLOR.g)`); `_mat_cache`/`set_day_factor` at chunk.gd:62-80 as planned; `light_at` at world.gd:1674; `held_mesh.gd` box/cross materials albedo WHITE + vertex-color-as-albedo (AC-0128 shade-only vcol confirmed); MINFO baseline `.scratch/AC-0128-gates/gate_minfo_run3.txt` = 121 lines, [64,−16] verts [1784,1028] ✓ (matches the prompt's baseline, NOT the plan's AC-0129 anchor text — the run3 file is the binding one per prompt).
- G0 baseline (pre-change): rc=0, 0 SCRIPT ERROR ✓.
- Probe landed (PRE tree): `viewlight` dispatch (main.gd, next to nightday) + `_viewlight_test`/`_vl_measure`/`_vl_r5` (borrow the `_light_test` cell pattern: surface 3-step grid eff≥15 @ noon; pocket = same scan incl. lava-clear + far-row filters, strict eff==0; torch placed after cave0 read) + member decls `_star_node`/`_star_mat` (null in PRE). First attempt: player Nil (dispatch did not spawn) → added `player = _spawn_player()` in the viewlight branch (plan §5.4 step 2). Note: headless runs print `ERROR: Shader compilation failed` from the dummy renderer (shader_set_code, material_storage.cpp) — EXPECTED under headless (plan §5.5 G0 note), NOT a SCRIPT ERROR; G0 gates on SCRIPT ERROR only.
- G1-PRE RESULT (recorded, `.scratch/AC-0035/viewlight_pre.log`): `{"mode":"viewlight","seed":44,"radius":4,"mechanism":"pre-no-viewlight","uDay_noon":1.0,"uDay_mid":0.0,"outdoor":{"sky":15,"blk":0,"eff":15,"vm_no_pl":1.0,"vm":1.0,"pl_contrib":0.0,"mat_L":1.0},"cave0":{"sky":0,"blk":0,"eff":0,"vm_no_pl":0.12,"vm":0.12,"pl_contrib":0.0,"mat_L":1.0},"torch":{"sky":0,"blk":14,"eff":14,"vm_no_pl":0.94133,"vm":0.94133,"pl_contrib":0.0,"mat_L":1.0},"star":{"present":false,"count":0,"opacity_noon":null,"opacity_midnight":null},"player_light":{"level":0.0,"d0":0.12},"ambient_night":0.25,"surface_cell":[2,32,2],"pocket_cell":[0,25,-4],"elapsed_ms":12799,"ok":false}` — mat_L 1.0 at EVERY cell (the bug: hand fully bright in the light-0 cave) ✓ honest PRE; pocket [0,25,-4] eff 0 → torch 14 ✓; 12.8 s ≤ 60 s ✓; rc=0, 0 SCRIPT ERROR ✓.
- Next: implement R1 (star.gdshader + star node + _update_sky follow/opacity) + R2 (4 shader adds + uniform push) + R3 (player.gd vm_refresh + unshaded player-local mats), then G0 + G1-POST.

## RUN 2 (cont) — shader reality-check + faithful star port [after compaction]
- GL compile check (xvfb gl_compatibility, R=1, --quit-after) caught REAL errors headless can't:
  1. `ShaderMaterial.depth_draw_mode` doesn't exist → SCRIPT ERROR; REMOVED (web depthWrite:false not expressible on ShaderMaterial; harmless: star shell r=320 is behind all visible geometry in every scenario; blend via ALPHA output).
  2. `render_mode transparent` + `alpha_scissor` are INVALID in Godot 4.7.1 → **pre-existing AC-0128 bug**: cutout/flower/fluid shaders never compiled in shipped builds (fallback material for leaves/flowers/torch in the Windows exe). Fixed: removed both modes (ALPHA output enables blend; explicit `discard` already does the scissor).
  3. `WORLD_POSITION` is NOT a fragment builtin in 4.7.1 → plan §7.2 fallback: vertex-stage `varying v_world_pos = (MODEL_MATRIX*vec4(VERTEX,1)).xyz` (WORLD_MATRIX is the Godot-3 name; 4.7 = MODEL_MATRIX).
  4. `: source_color` hint invalid on float uniforms (star.gdshader) → removed.
- mulberry32 port bugs found via plan-anchor mismatch + python model of the EXACT web JS:
  - state was `t`, must be `a`; missing `t1 ^ t0` step. Both fixed.
  - **Plan §5.3 anchor constants are WRONG** (Run-1 simulation artifact; tested 5 conventions × 4 simulator variants, none reproduce them). Faithful port verified EXACT against python model of index.html :125/:720-726:
    star0 (100.038, 64.706, −296.994) / star1 (−198.711, 225.578, −109.675) / star2 (−40.426, −208.119, −239.692). Plan's own rule: "port the loop 1:1, don't re-derive" → loop is authoritative; anchors recorded as deviation.
- GL check3: 0 SCRIPT ERROR, 0 SHADER ERROR (all 5 spatial shaders compile under llvmpipe).
- State: all code edits done (R1+R2+R3). Next: G0, POST viewlight, G2 nightday, SMOKE+genhash, G3 boundary one-shot, G4 MINFO write-up, G6 render.

## RUN 2 gates + G6 (2026-08-28, builder medium)
- G0 FINAL: rc=0, 0 SCRIPT ERROR (`.scratch/AC-0035/g0_final6.log`, post debug-strip; earlier: g0_final4 post vertex-flag fix).
- G1 POST viewlight (`.scratch/AC-0035/viewlight_post3.log`) EXACT vs post2: outdoor {sky 15, blk 0, eff 15, vm_no_pl 1.0, vm 1.0, pl_contrib 0.0, mat_L 1.0}, cave0 {sky 0, blk 0, eff 0, vm_no_pl 0.12, vm 0.41333, pl_contrib 0.29333, mat_L 0.41333}, torch {blk 14, eff 14, vm 0.94133, pl_contrib 0.0, mat_L 0.94133}, star {500, op 1.0/0.0}, player_light {5.0, d0 0.41333}, surface [2,32,2], pocket [0,25,-4].
- G2 nightday EXACT (unchanged from pre-vertex-fix; no player spawned → u_player_light=0 no-op). SMOKE 5/5 EXACT. genhash 25/25. G3 boundary r4 PASS (one-shot CONSUMED). GL compile 0 script + 0 shader.
- **Viewmodel invisible — full debug chain (all in .scratch/AC-0035/)**: (1) alpha bug `Color * scalar` scales alpha → fixed RGB-only ctor in vm_refresh (NOT the full answer). (2) occlusion RULED OUT (VoxelMath.raycast_blocks cam→box hit=false; box cell air). (3) visibility/cullmask RULED OUT (ivit=true, cull 1048575, nverts 24). (4) DEPTH_DRAW_DISABLED RULED OUT (red+disabled still rendered). (5) atlas-UV "black region" = FALSE ALARM — my probe sampled FLIPPED y; **Godot UV origin = TOP-LEFT** (texture not flipped); box correctly samples stone tile (127,127,127) at (128,0-31). (6) ROOT CAUSE (empirical, VMFORCE A/B): **`vertex_color_use_as_albedo=true` (inherited from HeldMeshes._base_mat) makes the textured held box near-invisible** — region avg 10.4 (vertex ON + texture) vs 24.7/24.5 (vertex OFF + texture) vs 77.3 (vertex ON, texture off ≈ 0.4133×0.8 exact). FIX in `_vm_kind_mat`: `m.vertex_color_use_as_albedo = false` on the PLAYER-LOCAL duplicate only (HeldMeshes + drops untouched).
- **G6 render saga**: first renders (g6d..g6h) showed "wall on right" — that surface turned out to be THE BOX (stone-textured, camera-attached): proved by its left edge at x≈805 IDENTICAL across camera yaws -0.45 and -0.8 (world objects sweep with yaw; this didn't). Box at night = unshaded L=0.4133 × stone ≈ R 25-38 gray — visible, subtle. Two frame issues fixed: (a) raycast aim (`_find_aim_spot`) shifts with chunk-build timing (SNAPDRAIN race) → added snapshot-path-only env hook **AWECRAFT_AIM="x,y,z,yaw,pitch"** (eye-height xyz, minus EYE at teleport; grants 3×12 + 111×1; sets aimed). (b) framing: final = `AWECRAFT_AIM="8.5,37.5,10.5,-0.8,0.15"` → stars (upper sky) + player halo (bright green grass bottom-left, lit by player light) + hand (stone-textured box, right side) + hotbar in ONE frame.
- FINAL render (06:33): `tasks/AC-0035/night_r1_midnight.png` 241843 B + `_placed.png` 241728 B, **0 SCRIPT + 0 SHADER ERROR** (`.scratch/AC-0035/render_final2.log`). One transient flake in an intermediate run (aimtest5): `threadgen_handoff ... Nil to PackedByteArray` at world.gd:474 (thread-gen race, world.gd is READ-ONLY fence; snapshot still completed; did NOT recur in final render) — pre-existing, disclosed.
- Debug hygiene: VMFORCE/VMDBG blocks REMOVED from main.gd (grep clean). AWECRAFT_AIM hook KEPT (snapshot-path-only, mirrors AWECRAFT_CAM/AWECRAFT_HELD pattern).
- Render invocation count (disclosure, plan asked ≤1): 6 G6 iterations (g6d,g6e,g6f,g6g,g6h,g6i) + 5 VMFORCE A/B (v1-v5) + 5 aim-framing tests (aimtest,2,3,4,5) + 1 final clean = 17 GL renders + ~10 headless probes (G0s, vmdbg1-11, viewlight probes). Headless probes cost ~10-15 s each; GL renders ~60-90 s each (xvfb+llvmpipe).
- G4 MINFO expectation (write-up only; coordinator diffs): AC-0035 changes NO chunk mesh stream (stars = separate node; player light = 3 uniforms; viewmodel = player-local node) → post-AC-0035 MINFO dump must be **byte-identical to `.scratch/AC-0129-gates/gate_minfo.txt`** (121 lines, 81 built chunks, sum [127412,53572,14900] = 195884, sha256[:16] 851430b639847f6c). (AC-0128 anchor `gate_minfo_run3.txt` differs by exactly 1 line: [64,-16] chunk [1784,1028] vs [1788,1028] — AC-0129 reshuffle.)
- State: ALL deliverables written (results.html, continuity, final render). EXIT — awaiting coordinator heavy gates (boundary/flake/genhash/r50 per HARNESS §3; scope = world/ shaders + player + main → boundary r4 + flake + genhash applicable).

## RUN 2.5 (coordinator heavy gates + FINAL verdict, 2026-08-28 07:30)
- Gate job bash-94 (07:08-07:12, clean): g0 rc=0; viewlight REPRODUCIBLE EXACT (cave0 0.41333 lift from 0.12, outdoor 1.0, torch 0.94133, star 500 @1.0/0.0, d0 0.41333/5.0); nightday EXACT vs AC-0128 (15.0/1.8, 14.11996 day==night, darkside 5168); MINFO BYTE-IDENTICAL vs AC-0128 run-3 baseline (diff EMPTY); battery 5 EXACT; genhash 25/25; flake EXACT; build RC=0 exe 109,620,072 B + daemons 200.
- BOUNDARY r4: gate run 99/8141 (8.7 s mid-walk spike). CONTROL A/B (worktree e851556, same moment): 92/4471 — attribution to AC-0035 tree CONFIRMED. CLEAN RE-RUN (AC-0035 tree): 96/5056 (no spike, smooth decay). Typical delta vs baseline 92/4588: fwd +10-18%, walk +5-8%; per-crossing TOTAL work neutral (65.4 vs 67.2 s).
- DECISION: AS-DELIVERED, honest trade-off deviation (smallest of the three accepted boundary deviations: AC-0129 +33%, AC-0128 +13-28%, AC-0035 +10-18% fwd). Mechanism candidate (per-frame additions eroding the frame-budget drain) folded into AC-0131 with the throttle fix candidate. Self-check/gate divergence #3 logged (builder one-shot = PASS; gate = mild miss).
- Closeout: results §coordinator-verdict appended; status → done; AC-0131 comment (3rd cost component); checkpoint 00l; AGENTS.md; commit (explicit paths) + push; NEXT = AC-0132 Run-1 (godot FREE, AC-0035 marker `.scratch/AC-0035-gates/HEAVY_GATES_DONE` exists).
