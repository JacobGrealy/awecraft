# AC-0035 continuity — m5a2-light-bake (stars + viewmodel lighting + player light)

## RUN 1 (research/planner, 2026-08-28)
- Plan delivered: `tasks/AC-0035/plan.html` (7 sections, all constants verbatim-grepped this run). **ZERO godot runs** — the AC-0128 heavy-gate job owned godot at start; it FINISHED mid-research (`.scratch/AC-0128-gates/HEAVY_GATES_DONE` exists, no godot/Xvfb procs) — its gates.log was read from disk for baselines, nothing executed.
- Tree state at research time: AC-0128 (night-darkness-sky-scale) Run-2 changes are **UNCOMMITTED in the working tree** (vColor repack r=sky/g=blk/b=fsh, 4 NEW unlit shaders `godot/world/chunk_lit_{opaque,cutout,flower,fluid}.gdshader`, atlas-baked tints in data.gd, per-frame `u_day` push from main.gd:1464, held-mesh double-tint fix at held_mesh.gd:96-99/:123-125) — AC-0035 builds ON TOP, must not revert.
- Godot current state: NO star field/skybox/dome anywhere (only the separate aero-mode `aero_sky` sphere, main.gd:1385-1397, which per-frame camera-follows at :1432 — in-repo precedent). Per-frame hook = `main.gd _update_sky` :1457-1477 (called unconditionally every frame from `_process` :1428; already does `set_day_factor`). Viewmodel nodes = `player.gd` (`held_box`/`held_sprite`/`held_fist`/`held_tool` under camera sway_root, :360-396; per-frame `_process` :113-123) — **held_mesh.gd (HeldMeshes) is only the static mesh/material cache shared with drops; stays UNTOUCHED** (deviation from spec file list, recorded in plan §2). Light API = `World.light_at` (world.gd:1674-1680) → fresh `compute_light_split` 17³ box (lighting.gd:62-142) → {sky,block,eff}; air-on-miss (AC-0119); ≈3-7 ms worst case → cached cell-change + 500 ms TTL.
- KEY FINDS beyond the prompt: (a) index.html:**3248** `window.__stars.position.copy(camera.position)` — the web MOVES stars with the camera every frame (the prompt's "does the web move them" question → YES, at the EYE position per :3246) → the camera-follow design is a FAITHFUL port, not a deviation. (b) index.html:**3253** the web's own F3 read = `lightAt(floor(px), floor(py+EYE), floor(pz))` — the EYE cell is the canonical "light at the player" (EYE 1.62 both sides: :116 / player.gd:3) → viewmodel light read at the eye cell. (c) `Chunk._mat_cache` (chunk.gd:62) is a static Dictionary reachable from main.gd (GDScript privacy = convention) → the player-light uniform push needs NO chunk.gd edit (fence-safe). (d) `last_eff` (chunk.gd:19) is COMBINED eff only — unusable for the exact `max(uDay·sky, blk)` formula → `light_at` split read chosen. (e) `_voxel_mat` (tools) + `held_fist` are ALREADY UNSHADED; only held_box/cross materials are lit (shared) → player-local UNSHADED duplicates (drops keep the shared lit material — no cross-contamination). (f) camera fov 75 (player.tscn:18), far default 4000 → 320-radius star sphere always in range. (g) The proven `_light_test` battery pattern gives deterministic cells already validated EXACT: surface eff-15 cell + torch pocket (cave_eff 0 → torch 14) — the viewlight probe borrows it wholesale.
- Design resolved (plan §5): (1) stars = one MeshInstance3D "Stars" (500 billboard quads, 800 verts indexed) + NEW `godot/core/star.gdshader` (unshaded/transparent, view-space billboard via UV-encoded corner, `ALPHA = u_opacity`, no fog() call); positions = the web's EXACT 500 points via a verbatim `mulberry32(42)` GDScript port (anchors: star0 (100.038,−64.708,−296.994), star1 (162.576,260.586,−89.8), star2 (220.09,−196.884,−123.277)); per frame in `_update_sky`: `u_opacity = 1−day` + node pos = player eye (web :1347/:3248). Quad world size 0.70 ≈ web 1.8 px at 1080p/75°. Rejected: PointMesh (no custom positions), PRIMITIVE_POINTS (no gl_PointSize exposure), CPUParticles3D (sim overhead). (2) player light = per-fragment add in all 4 unlit shaders: `L = max(L, 0.12 + 0.88*clamp(u_player_light − distance(WORLD_POSITION, u_player_pos), 0, 15)/15)` — MC-style level falloff, L_p = 5 (d0 = 0.4133, radius 5 blocks), uniforms pushed per frame from `_update_sky` via `_ChunkScriptM._mat_cache` (8 set_shader_parameter/frame; safe defaults u_player_light=0 → no-op). Rejected: PointLight3D (unlit shaders ignore scene lights), bake (fence), additive (white-out). (3) viewmodel = UNSHADED player-local materials + per-frame `albedo_color = base × L` where `L = max(0.12 + 0.88*max(day·sky/15, blk/15) at the EYE cell (light_at, TTL-cached), 0.12+0.88·5/15)`; rendered = atlas(tint)×vcol(shade)×L — no double-dim (AC-0128 preserved). (4) probe `AWECRAFT_LOGIC=viewlight` (env block = nightday's: RADIUS=4) → settle `_nd_settle`, cells from `_light_test` pattern, player spawn (physics inert, mode ≠ play), per cell: teleport eye==cell, `vm_refresh(true)`, read sky/blk/eff + vm_no_pl + vm + pl_contrib + ACTUAL mat_L + star u_opacity at t 0.0/0.5; RESULT PRE (pre-no-viewlight, star absent, L_p 0.0, mat_L 1.0 at every cell = the bug) / POST (ok=true: outdoor 1.0 ≥0.94, cave0 0.12→0.41333 strict lift, torch 0.94133, star opacities 0.0/1.0).
- Gates (plan §5.5/§6): G0 quit; G1 viewlight PRE→POST; G2 nightday re-pass EXACT (15.0/1.8/14.11996); G3 ONE-SHOT boundary r4 baseline = AC-0128 GATE job (gates.log :472 — walk p95 102, fwd_p95 11446, burst 7795, first10 6, flap 0, irb 81; Run-2 one-shot was 101/5325 — the gates.log run is binding per the one-shot rule) — do not make worse, else honest deviation; G4 MINFO byte-identical vs `.scratch/AC-0129-gates/gate_minfo.txt` (121/195868; verified the AC-0128 gate file is ALREADY identical to that anchor — diff empty); G5 battery 5 arms EXACT + genhash 25/25; G6 one R=1 midnight render (stars visible = fog/compile proof; grep render log for shader|error — headless G0 does NOT compile spatial shaders). Memory fence: no new persistent per-chunk state (r50 band 140.6–146.9 untouched); no new autoloads.
- Run-2 file list (plan §2): main.gd (star node+builder+_update_sky pushes+viewlight probe), 4× chunk_lit_*.gdshader (+2 uniforms +3-line fragment add), NEW core/star.gdshader, player.gd (PLAYER_LIGHT_LEVEL const + vm_refresh + _vm_mats + unshaded duplicates in _update_held/_setup_held_tool + _process call). Read-only: chunk.gd/world.gd/lighting.gd/data.gd/held_mesh.gd/index.html/TASKS.yaml.
- Open questions for Run-2: (1) verify `WORLD_POSITION` fragment builtin in 4.7.1 spatial shaders (documented; if not — vertex-stage varying fallback is in plan §7.2); (2) the star quad size 0.70 is a visual target — tune ± if the G6 render shows it wrong (mapping math in plan §5.3); (3) confirm the held_sprite material_override keeps its texture (plan §7 row 6; `AWECRAFT_FPV_ITEM` render available if a second look is wanted); (4) probe PRE must NOT reference the new shader params or the star node unguarded (null-guards specified in plan §5.4); (5) godot was BUSY with AC-0128's gates at my start and is now FREE (marker exists) — Run-2 may use godot normally, but still check `ps -eo pid,comm,args | grep -E '^\s*[0-9]+ (godot|Xvfb)'` + the marker first, and one godot at a time.
- State: plan.html + this log written; NO code changes; NO godot runs. Ready for Run-2 (subagent, medium) — probe-first (G1-PRE) before any feature code.
