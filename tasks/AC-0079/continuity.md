# AC-0079 continuity log — chunk-stream-look-priority-scoring (AC-0034 child, P1.1)

## RUN 0 — coordinator context (2026-08-26, spec v3)
- **v1** (2026-08-23): look-priority + hysteresis, stalled before code; hysteresis split off → AC-0080 (landed).
- **v2** (2026-08-24): IMPLEMENTED — the scored pick pool is IN THE TREE (`world.gd` `_look_yaw`/`_look_dir` ~:140, `_refresh_look_dir` ~:666, `_entry_score` ~:683, scored drain pool; `main.gd` boundary fields `forward_wall_ms_per_crossing`/`forward_p95_ms`/`trailing_wall_ms_per_crossing`/`trailing_p95_ms` ~:5081+). G1/G2/G4/G5 PASS, **G3 PARTIAL FAIL (honest)**: r2 fwd 847–854 > tr 156–162 (fwd≤tr FAIL); r4 fwd 4962–7992 > 3500 FAIL, irb_max 55 < 60 FAIL; r50 recenter 100.3 ✓. Conclusion: *scoring alone does not improve drain rate* — the per-crossing 9-chunk wall is drain-THROUGHPUT-limited. G6 was deferred. Full record: `AC-0079-results.html` (408 lines, rounds 1–3) + renders `ac0079fix_iso_r2.png`/`ac0079r3_iso_r2.png`.
- **Since v2, the tree moved** (all DONE): AC-0099, AC-0107 (threaded mesh — build_ms −96.5%), AC-0118 (recenter = 5-phase sliced resume; `_build_queue_for_center` DELETED), AC-0077 (batch light + collision staging + gen-pick in-flight skip; per-crossing main-thread contained light 17–34 ms/chunk = 150–300 ms/crossing — the quantified walk-frame cost; follow-up "batch light off-main" unregistered), AC-0119 (air-on-miss read paths; read_sync_gen hard 0), AC-0124 (web export dropped — no web arms; Windows-only; build gate = `./build_windows.sh` only; 8443 gone).
- **Current-tree anchors (v2 scoring active; re-measure in Run-1):** boundary r4: built 81, irb 81/81/19, flap 0, queue 93, walk p95 33 / max ~77-78, **fwd_p95 4284–4840** (the gap vs the ≤3500 gate), r50 RECSLICE p95 ~11/7-8, perf r4 ~6450/43, battery exact, genhash 25/25.
- **v3 goal:** keep the scoring; DIAGNOSE why the forward wall still takes ~4.3–4.8 s/crossing (per-stage per-crossing budget: gen/mesh/light/collision/recenter-slice/idle + actual pick-order measurement) and FIX the bottleneck so G3 passes (fwd ≤ tr at r2/r4, fwd ≤ 3500 at r4, no walk/recenter regression). If the evidence demands the AC-0077 follow-up (contained light off-main), the plan flags it as a scope addition (coordinator approves before Run-2). Spec v3 = `spec.html` (gates G1–G6 re-anchored: exact-value battery, genhash 25/25, fresh Run-1 anchors, windows-only build).
- **State:** spec v3 filled, status in-progress (user re-queued AC-0079 to front, 2026-08-26). Run-1 (subagent_plan, xhigh) launches next.

## RUN 1 — research/planner (2026-08-26, completed)
- **Deliverable:** `tasks/AC-0079/plan.html` (7 sections in order; v3-specific content nested inside them: diagnosis §1.1, per-crossing budget table §1.2 first-class, option adjudication + exact file:line change list §2.1, gate-reachability verdict §5.1). No .gd edited; tree clean at 55a3cd4.
- **DIAGNOSIS (measured, probes in `.scratch/AC-0079/`):** steady-state fwd wall (r4 0.85–1.0 s) = **build-dispatch pacing, NOT gen**: lead column (k+r) DATA lands in the PREVIOUS window (speed-4 probe: leadPre 8/9 threadgen + 1 cz=0 sync gen 40 ms main, world.gd:412 rule), but its 9-chunk BUILD dispatch starts ~190 ms post-crossing and spans ~650 ms (9 × ~72 ms) — 72 ms/dispatch = AC-0077 main-thread batch light (17–34 ms/chunk) inflating frames so budget collapses to 1 build unit/frame (last_build_us ≥ BUILD_FAST_US 15 ms). Worker mesh (~44 ms/chunk) off-main, not critical. Cold start (crossings 1–6) = 3×3-core fill backlog: crossing 1 = 36-chunk fill = 36×~220 ms/3 gen (TG3) + main-light build → 4908 ms baseline. Trailing walls crossings 1–4 = **−1 never built even at TG6**: scored forward-first data pick leaves backward columns LAST (col −3 data ready ~2.2 s TG6) past cheby>r+1 release (t=2.0 s @ speed 16) → tr_p95 pinned at check floor (42–43 ms, n=16).
- **Dead hypotheses (A/B r4, baseline fwd_p95 4293–4319):** DRAIN_MS=100 → 4251 (neutral, walk max 73→144); THREADMESH_N=6 → 5127 (worse, CPU oversubscription); COLSTAGE=0 → 4871 (worse; collision 313 ms total); recenter slice 0–1 ms/crossing (new_n=20/1 frame). Only THREADGEN_N=6 helps: 3321 (−23%) but cuts COLD wall only.
- **Fresh anchors (current tree, TG3/TM3):** r2 fwd_p95 593 / tr 6 / walk p95 23; r4 fwd_p95 4293–4319 / tr 41–43 / walk p95 33 / irb 81/81/19 / queue 93; TG6 r4 fwd 3321 (cold [3445,3315,2424,…], steady ~770–840) tr [−1×4,62,1,…] tr_p95 42 walk p95 50; perf r4 total 6468 / p95 48 / first_draw 262 / gen_ms(main) 700 / build 329/1369u / light 81 / fog_ok / read_sync_gen 0; genhash 25/25; full 5-mode battery EXACT ok:true (sea 2730/2730+1406/1406, torch 14, horiz 2.82, lava 25/9, place/drop true in-battery); r50 RECSLICE max 10.0/7.0 ms AND 10.0/8.0 ms (2 independent runs; 2 slices each/27-28 frames/new_n 2060/queue 10398), fwd/tr fields present, ok:true, p95 44 — stable, 24% under the 12.2 cap.
- **Observation (flagged, not a gate):** standalone `AWECRAFT_LOGIC=interact` → drop_spawned:false, but the G2 gate command (full 5-mode battery) → true. Pre-existing at HEAD; order-dependent; interact/entity path outside drain scope.
- **Chosen design = Option C (combo), all world.gd:** C1 lead-column pre-build (~15 lines: `_collect_pool` :697 gains `include_fb` param admitting the forward band cx==last_pcx+r, |cz−last_pcz|≤r when in-radius READY pool empty; drain second pass :748–766); C2 light off-main (SCOPE ADDITION — AC-0077 follow-up, flagged for coordinator approval: delete `_bl_batch_step()` calls :728–729/:740–741 + `_build_unit` single-item branch :645–655; worker self-lights via existing chunk.gd:1135 path, byte-identical kernel `_chunk_light_into` lighting.gd:228; eff cache kept (fed by worker handoff eff_trust); 10 threads/6 cores = 1.7×, measured TG6 walk p95 33→50); C3 threadgen default :148 `mini(cores,6)`.
- **Expected post (derived):** r2 fwd≈tr≈floor → fwd≤tr expected PASS (thin, §7d); r4 fwd_p95 ≈ 0.2–0.6 s ≤ 3500 PASS (6–17×); **r4 fwd≤tr NOT REACHABLE in scope** (structural 4–10× gap: fwd_p95 ≥ ~0.2 s from 3×3-core cold outliers vs tr_p95 ≤ ~60 ms from 4×−1 pinned; escapes = TG≥12 / ring-order data pick / hysteresis change / harness core change — all fenced); r50 RECSLICE ≤12.2 PASS (slice untouched); perf total ≈5200–6000, p95 ≈30–45 (improvement direction).
- **State:** plan.html written; coordinator gate next → Run-2 (subagent, medium) with continuity.md as handoff. C2 needs explicit coordinator sign-off (scope addition) before Run-2.
