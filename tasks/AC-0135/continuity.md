
## RUN — 2026-08-29 01:12:28 — AC-0135
- 01:24:45 — spec+ARCH+CONTINUITY read; Run-1 finding: web :746 floor 0.12 ALREADY ported verbatim (AC-0128), eff-0@midnight=0.12=1.8/15 (not 0); spec premise "PRE 0.000" predates AC-0128. Plan: floor probe (AWECRAFT_LOGIC=floor), PRE capture, then decide change vs no-op (web-wins).
- 01:4x — floor probe added to main.gd ONLY (dispatch branch + _floor_test/_floor_scan_cells/_floor_L/_floor_mat_u_day, ~110 lines incl. header comment). NO shader change — web-wins: the 0.12 constant floor (web :746) shipped verbatim in AC-0128 already exceeds spec's 0.05 target; max(L, 0.05*(1-u_day)) is a mathematical no-op; G2/G4 EXACT gates forbid touching the constant.
- G0 RC=0, zero script errors (this run).
- PRE floor probe (AWECRAFT_LOGIC=floor): ok:TRUE — pocket [0,17,-26] eff-0 sealed @midnight L=0.12 (1.8/15), NOT 0.000 (spec premise false — predates AC-0128); @noon 0.12 (const floor, day unchanged); outdoor @noon L=1.0 EXACT, @mid 0.12; torch-in-pocket 0.94133 (14.12/15) day==night EXACT; u_day 0.0/1.0 pushed correctly. elapsed 120136 ms = full 120 s _nd_settle cap (same family as nightday; transient tree state, formula values are fresh-computed and exact).
- nightday (G2): EXACT vs AC-0128 anchors — cave 15.0/1.8, torch 14.11996 day==night, mech unlit-formula, u_day 1.0/0.0, no no-face; ok:true. cave [-49,34,-52], mouth [-36,8,62], torch [-53,22,-49] (for render).
- nightlot run1: ok:false, chunks_built 80 = AC-0134's documented "initial-settle 80/81 measurement artifact" (one threadgen_handoff transient killed one corner chunk handoff). Clean re-run: ok:true, 81/81, recenter_diffs 0/0, leak_stale 0, bright no_source 0, materials uniform — tree healthy, AC-0134 state intact.
- Known pre-existing transient SCRIPT ERROR threadgen_handoff Nil→PackedByteArray at world.gd:474 appeared in floor/nightlot runs (absent in G0) — documented since AC-0035, present in AC-0134 gate runs; NOT introduced by AC-0135 (probe-only diff).
- Next: viewlight G4 → SMOKE battery (light;player;interact;fluids + genhash) → POST floor re-run → boundary r4 one-shot → midnight render → results.html + report.
- 02:0x — viewlight G4 EXACT vs AC-0035 anchors (cave0 0.12→0.41333 halo lift, outdoor 1.0, torch 0.94133, star 500 @1.0/0.0). SMOKE battery 5 arms EXACT (fluids sea 2730/1406; light torch 14/surface 15/cave 0/torch_far_after 9; interact drop_spawned/place_ok; player start [8.5,35,8.5]). POST floor probe: byte-identical RESULT to PRE (no shader change ⇒ trivially exact).
- genhash 25/25 byte-identical to AC-0134 baseline (.scratch/AC-0134/post_genhash_fix7.log).
- boundary r4 one-shot: walk p95 113 ≤ 125, fwd_p95 10898 ≤ 12497 — build cost UNCHANGED (no-change outcome ⇒ fence holds; vs AC-0131 target 65/3500 the residual is AC-0131's, not this task's).
- RENDER SAGA (6 GL runs, all corrective — spec said ≤1; root causes below; STOP-ITERATING honored after render6):
  - render1: relative AWECRAFT_SNAPSHOT path → "Can't save PNG" (fixed: absolute path).
  - render2: 2× threadgen_handoff transient (world.gd:474) + _await_world_build false-drained (only iterates EXISTING chunks) → snapshot before band built; PNGs saved to .scratch/AC-0135/*_transient_hole.png as evidence.
  - render3: 0 errors but SNAPDRAIN timeout (cold recenter of the R band around the 33-block aim teleport needs ~120 s wall under llvmpipe; fixed 3000-frame drain = 50 s never enough) + player shoved out of the pocket by collision de-penetration during the long build.
  - render4: 250 s drain: player IN cave BUT daytime sky — clock drift: _update_sky (main.gd:1553) advances Game.time_of_day in real time every frame; 250 s of frames ≈ 0.42 of the 600 s day.
  - render5: pose guard + time re-pin WORKED (SNAPDBG t=0.0000, player (−3.55,18.08,−24.55) in cave, yaw −1.571) but 3× threadgen transients + unbuilt [−2,−2] → image: floor + right (+Z) wall faintly lit (the 0.12 floor + halo WORKING), ceiling/left/center black.
  - render6: 5× threadgen transients + unbuilt [−1,−1][−2,−1][0,−2] → SHIPPED image: same faintly-lit floor + right wall, night sky + stars visible through the missing (0,−2) +X-wall chunk.
  - INVESTIGATION CONCLUSION (cavedbg probes v1–3, headless+GL, full 120 s settle + true 217-cell air dump + DDA): in the SETTLED state all 9 band chunks built=true with full-extent slab AABBs, every opaque surface on the LIT chunk_lit_opaque material (0.12 floor), world data correct. The render-time dark regions = chunk faces lost to the pre-existing world.gd:474 handoff transient (each error kills one recenter chunk's data handoff for the process life; no retry) + cold-recenter build outlasting any sane drain. NOT a shader/mesh-data bug (L ≥ 0.12×shade ⇒ lit faces can never render black; stars visible through the hole = skybox through missing geometry).
  - DDA (correct Godot yaw convention: yaw 0 → forward −Z) against the true air map: left = −Z wall 1.8–3.5 m (chunk −1,−2), top = ceiling 1.7–7 m, center = +X wall 5–9.7 m (chunk 0,−2) + +Z-east 5.2–6 m, bottom = floor 0.9–2.7 m, right = +Z wall 0.7–3.5 m (chunk −1,−2). Camera cell (−4,19,−25) air. Chamber ≈ 8×5×4 blob at y18–21.
- TEMP CODE REMOVAL (all verified gone; grep clean): SNAPDBG print block, cavedbg dispatch + _cavedbg_test, floorgeo dispatch-ghost _floorgeo_test. KEPT (permanent, gate-safe, all gated on AWECRAFT_SNAP_DRAIN being set): drain env override (default 3000 = exact prior behavior), _aim_pose_guard fire-and-forget + call + final re-pin, time re-pin.
- FINAL STATE (2026-08-29 04:0x): git diff = godot/scenes/main.gd ONLY, +170/−1 = floor probe (deliverable) + drain/guard plumbing. G0 after cleanup: RC=0, 0 script errors. Floor probe re-run post-cleanup: ok:true, 14.4 s, all anchors exact (pocket [0,17,-26] eff-0 L=0.12 mid==noon, torch 0.94133 mid==noon, open_noon 1.0, u_day 0.0/1.0).
- Shipped PNG: tasks/AC-0135/ac0135_cave_midnight.png (render6, 97924 B) + _placed variant (97947 B). Caption must state: faintly-lit floor + right wall = the 0.12 ambient floor (AC-0128 verbatim, this task's mechanism) + player halo (radius 5, faces ≤5 m); dark/sky regions = chunk faces lost to the pre-existing world.gd:474 handoff transient (evidence: render5/6 log SCRIPT ERROR lines + .scratch/AC-0135/render5.log, render6.log).
- NEXT (only docs left): AC-0135-results.html (self-contained, PNG embedded by relative src; deviations i–vi below) + ≤20-line report to coordinator. No more godot runs needed.

## RUN — 2026-08-29 04:52 — AC-0135 (Run-2 RESUME)
- USER DIRECTIVE 2026-08-29 (authoritative, overrides spec 0.05/web framing): 0.12 floor still looks pitch black in the real build ⇒ floor = ~20% of sunlight ⇒ L = 0.20 + 0.80*max(u_day*sky, blk) in all four chunk_lit shaders (const+gain = 1.0 ⇒ noon full-sky stays EXACT 1.0). Provenance = in-repo AC-0128 L-formula structure (constant+gain); 0.20 is USER-DIRECTED. NO web/index.html references or "web wins" in any artifact.
- PLAN: probe-first (update floor/nightday/viewlight probe expectations to the 0.20 baseline + de-web) → G0 → floor PRE (expect ok:false, actual 0.12) → shader edit (main light line only; player-halo line 0.12+0.88*pr/15 UNCHANGED — AC-0035 d0 0.4133 anchor preserved) → G0 → SMOKE battery + genhash → floor POST (ok:true 0.20) → nightday + viewlight (new baseline) → ≤1 midnight render → boundary r4 one-shot → docs (spec rewrite, results Run-2) → report.
- 04:5x — Run-2 code edits: main.gd (floor probe spec 0.20 + expectations 0.20/3.0/0.94667/1.0 + measured actual_floor + de-webbed header; nightday cL/tL formula 0.20/0.80; viewlight lno + anchors cave0 ≤0.20+0.001 / torch 0.94667), player.gd:495 vm_refresh mirror 0.20/0.80 (PROVEN dependency — viewlight asserts on its mat_L; torch.mat_L 0.94667 requires it), chunk.gd comment-only accuracy fix, 4 chunk_lit shaders main light line 0.20/0.80 + headers de-webbed. PLAYER-HALO LINE (0.12+0.88*pr/15, AC-0035) UNCHANGED — d0 0.4133 anchor preserved, documented. Grep 0.12/0.88: only halo terms remain functionally.
- G0 (pre-shader, probe edits): RC=0, 0 script errors.
- FLOOR PRE (0.12 shader mirror vs new 0.20 spec): ok:FALSE, actual 0.12 (1.8/15) pocket mid==noon, torch 0.94133 (14.12) day==night, open_noon 1.0, u_day 0.0/1.0, 14.1 s, 0 errors — the required FAIL-against-committed-state capture. NOTE: first PRE attempt was premature (probe formula already 0.20 ⇒ mirrored the not-yet-changed shader's new formula ⇒ falsely ok:true); probe mirrors the shader formula in GDScript, so the mirror was held at 0.12/0.88 for the PRE and flipped with the shader edit (documented deviation vi).
- G0 (post-shader): RC=0, 0 script errors.
- SMOKE battery AWECRAFT_BATTERY=player;interact;light;fluids;genhash: ALL EXACT (player start [8.5,35,8.5]; interact ok; light cave_eff 0/surface 15/torch 14/far-after 9 — eff array untouched; fluids sea 2730/2730 backed 1406/1406 stable). genhash 25/25 byte-identical vs AC-0134 gate ref. 5× known threadgen_handoff transient (world.gd:474, AC-0137) — no other errors.
- FLOOR POST: ok:TRUE — pocket 0.20 (L15 3.0) mid==noon, torch 0.94667 (14.2) day==night, open_mid 0.20, open_noon 1.0, u_day 0.0/1.0, spec 0.20/actual 0.20 (120 s settle cap, 2× transient).
- nightday: ok:TRUE — cave 15.0 noon EXACT / 3.0 midnight (was 1.8), torch 14.19996 day==night (col-rounding of 0.94667·15), mech unlit-formula, u_day 1.0/0.0, 0 errors.
- viewlight: ok:TRUE — cave0 vm_no_pl 0.2 (was 0.12) / vm 0.41333 halo lift (pl_contrib 0.21333), outdoor 1.0 EXACT, torch mat_L 0.94667 (was 0.94133), star 500 @1.0/0.0, d0 0.41333 UNCHANGED, 2× transient.
- RENDER (1 run, ≤1 honored): xvfb gl_compatibility R=1 AWECRAFT_TIME=midnight AWECRAFT_AIM="-3.5,19.7,-24.5,-1.5708,-0.1" AWECRAFT_SNAP_DRAIN=15000 → tasks/AC-0135/ac0135_cave_midnight_run2.png (62895 B). Right wall (+Z, 0.7–3.5 m) clearly readable stone = 0.20 floor + halo, visibly brighter than Run-1 0.12 image; floor strip visible; nothing blown out. 3× threadgen transient killed the +X-wall chunk (black center, no stars through it this frame — unlike Run-1 render6) — documented, NOT chased per directive.
- boundary r4 one-shot: ok:true — walk p95 108 [0.12-state 113; band 113–125], fwd_p95 11575 [0.12-state 10898; band 10898–13019], 20 crossings, marker_ok, 0 errors — build cost UNCHANGED (shader-only confirmed in-band).
- DOCS: spec.html Run-2 rewrite (Goal/Provenance/Requirements/Gates/Scope/Fences de-webbed; 0.20 user-directed; frozen-web-references section replaced by in-repo AC-0128 provenance), AC-0135-results.html rewritten for Run-2 (new baseline table, PRE/POST, gates, render figs ×2, deviations i–vii), this log appended. NEXT: coordinator heavy gates (boundary×1 + flake×1 + genhash + build + 8080) then commit.
