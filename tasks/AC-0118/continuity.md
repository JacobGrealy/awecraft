# AC-0118 continuity log (append-only handoff record)

## RUN 1 (research/planner, xhigh) — 2026-08-25

### Entry 1 — context read, design decided
- Read: spec.html (G1-G6 + fences), CONTINUITY.md 00e, ARCHITECTURE.md, tasks/templates/two-phase.md (7-section schema), world.gd (full: recenter :956-1046, _build_queue_for_center :838-903, drain :668-786, _collect_pool :641, _entry_score :627, dispatches threadgen_poll :430 / threadmesh_poll :486 / _mesh_dispatch :546, _process :196-274), index.html updateWorldStreaming :1295-1326 (gen ring ≤3/frame :1299-1304, mesh ring 8ms cap + 3/frame :1305-1317, unload :1319-1325), main.gd boundary/perf harness (perf :910-915 → _perf_test :4640; boundary :917-925 → _await_boundary_core :4734 + _boundary_test :4753-5076), player.gd _recenter :1079-1086, AC-0076 + AC-0079 results.
- KEY DATA (AC-0079 G3, current code, r=50 RECPROBE): initial recenter total_ms=98.8-100.3 (free 0.0, rebuild 100.3, new_n=10608); walk crossings total_ms=32.6-35.8 (free 0.1, rebuild 25.5-27.7, new_n=204, chunks=10712). → hysteresis walk ≈ total−rebuild−free ≈ 7-8 ms @r50 (must stay <15 ms → keep hysteresis synchronous, see plan §5).
- MECHANISM DECIDED: frame-budgeted SLICED rebuild (web-style count+8ms caps, 5 sequential phases WANT→STUB→MERGE_OLD→MERGE_WANT→MERGE_RING, exact per-cell semantics of today's _build_queue_for_center/stub loops, dx-major order preserved so final band arrays + dict insertion order are byte-identical to today's single-shot rebuild → drain pick order identical). Rejected lazy incremental: a 1-chunk move changes every entry's Manhattan distance → re-bucketing is O(R²) anyway, or it would alter band/cursor semantics (AC-0079 gate risk).
- CRITICAL CORRECTNESS DETAIL: WANT snapshot spans frames; a want chunk can be meshed by the drain before MERGE_WANT → MERGE_WANT must re-check mesh_built and skip (else a meshed chunk gets a "build" entry the drain never consumes → queue_size never 0 → G5 fail). Cannot happen in today's single-shot (no frame elapses) — the re-check is a no-op against today's semantics.
- Slice state vars + constants + _process hook (early-return :199 gains `and not _rec_pending`; `_recenter_slice()` called between threadmesh_poll :272 and _drain_build_queue :273) — full spec in plan.html §5.
- G4 measurement decided: RECPROBE total_ms per recenter call (walk crossings = 20 lines in boundary r50 run) → p95 across crossings; + new RECSLICE line (slice total/max/frame count/queue/new_n) at SWAP. r50 pre-change: initial ≈97-100 ms, walk ≈33-36 ms. r4 baseline: p95 65, irb_max 67, flap 0 (AC-0079/HARNESS :139) — re-measuring now.

### Entry 2 — pre-change baseline probes launched (background job bash-9)
- Script: `.scratch/ac0118_probe_chain.sh` (ONE sequential chain — godot runs one at a time; HOME=/tmp/dsh_home per ops rule; all calls `--path godot` from repo root):
  - P0 G0 headless `--quit` → `.scratch/AC-0118/p0_g0.log`
  - P1 genhash → `.scratch/AC-0118/p1_genhash.log`, lines → `.scratch/AC-0118/genhash_baseline.txt` (25 expected)
  - P2 boundary r4 → `.scratch/AC-0118/p2_boundary_r4.log` (RESULT: p95_ms, in_radius_built_max, flap, ok)
  - P3 perf r50 + RECPROBE → `.scratch/AC-0118/p3_perf_r50.log` (recenter_ms, first_draw_ms, p95_ms, total_ms)
  - P4 boundary r50 + RECPROBE → `.scratch/AC-0118/p4_boundary_r50.log` (21 RECPROBE lines + RESULT)
- Expected wall ~45 min (r50 runs ≈14 + ≈23 min per HARNESS.md).
- NEXT: collect job_output(bash-9) → fill plan.html §7 A/B baseline table + verify pre-change numbers → finalize plan.html → report. Run-2 relaunch reads THIS file first.
- Constraints honored: NO .gd edits (research only), no git, no builds, no renders (perf/scheduling task — G6 render skipped per brief; zero script errors is the hard gate), no python server kills.

### Entry 3 — pre-change baselines COMPLETE (job bash-9, 22:17-22:36 EDT, all rc=0, zero script errors)
Logs: `.scratch/AC-0118/{p0_g0,p1_genhash,p2_boundary_r4,p3_perf_r50,p4_boundary_r50}.log`; genhash baseline (25 lines) → `.scratch/AC-0118/genhash_baseline.txt`.
- **G0/G1**: rc=0, 0 SCRIPT ERROR lines.
- **G2 genhash**: 25 lines → baseline file (post-change must diff empty).
- **G3 battery**: NOT run by the probe chain (Run-1 is research-only; G3 is a Run-2 post-change gate — the battery values are stable knowns from the spec; Run-2 runs it per plan §G3).
- **Fresh r4 (boundary, threading ON default)**: `p95_ms=21` (p50 14, max 68), `in_radius_built_max=68`, built_final 81, flap 0, remesh_ok true, marker_ok true, ok true, forward_p95 5200, trailing_p95 206, burst_p95 5199, walk_s 19.52. **⚠️ GATE RE-ANCHORING:** the spec/registry "r4 p95 65, irb_max 67" are PRE-AC-0107 numbers (AC-0107 threaded mesh+light cut frame p95 62→21 — see CONTINUITY AC-0107 entry). A/B "unchanged" = against 21/68, i.e. post ≤26 / 67-69 / flap 0.
- **Fresh r50 cold start (perf, RECPROBE)**: RECPROBE initial `total_ms=102.6` (rebuild 102.6, free 0.0, new_n 10608, queue 10609); RESULT `recenter_ms=102`, `first_draw_ms=366`, frame `p95_ms=29`/max 78, total_ms 848955 (51005 frames, all_meshed false — normal r50 cap).
- **Fresh r50 walk (boundary, RECPROBE)**: 22 lines = 1 cold start (105.1) + 21 crossings (20 walk + 1 settle re-center, new_n=721). Crossings: **p95 ≈41 ms** (40.8 ceil-index / 41.3 linear; min 34.0, max 43.5, mean 38.0); rebuild 26.7-34.7, free 0.0-0.1, **residual (hysteresis+setup) 7.2-13.3 ms mean 8.9** → post-change call p95 ≈10-11 < 15 ✓ (margin ~4 ms; contingency = slice the hysteresis walk, plan §5.10). RESULT: frame `p95_ms=41`/max 123, irb_max 105 (mid-walk, not a gate), built_final 228, flap 0, remesh_ok/marker_ok/ok true, walk_s 19.6.
- **plan.html FINAL**: all 7 sections complete, spec gate numbering G0-G6, A/B table filled with fresh pre-change numbers, exact commands per gate, re-anchoring note in §G4. Run-2 (subagent tool, medium effort) can implement from §5 verbatim.
- Deviations from the brief: (a) r4 gate re-anchored 65/67 → 21/68 (pre-AC-0107 values stale; documented in plan); (b) genhash baseline file lives in .scratch (ephemeral) — plan §G2 instructs Run-2 to paste both column sets into results.html as durable evidence; (c) G6 has no render gate in the spec (spec G6 = tree clean + results.html) — plan notes no-render decision explicitly.
- Plan.html: `tasks/AC-0118/plan.html`. Baseline table: §7 G4. Design: §5 (5-phase sliced rebuild, MERGE_WANT meshed re-check, _process :199 early-return fix + slice hook between threadmesh_poll :272 and _drain_build_queue :273, _build_queue_for_center :838-903 deleted).

## RUN 2 (builder, medium) — 2026-08-25

### Entry 1 — context read + implementation complete
- Read continuity.md (RUN 1), plan.html (7 sections, gated), spec.html, world.gd (recenter :956-1046 pre-edit, _build_queue_for_center :838-903, drain :668-786, _process :196-274, consts :19-21, vars :46-53), main.gd boundary harness :4753-5076 + perf :4640-4714 + logic dispatch :895-975, player.gd _recenter :1079-1086. All plan line refs verified against the live tree.
- **Files changed:**
  - `godot/world/world.gd`: +consts `REC_SLICE_BUDGET_MS := 8` / `REC_UNITS_PER_FRAME := 2048` (after DRAIN_MS_DEFAULT); +13 `_rec_*` state vars after drain_budget_ms; `_process` early-return gains `and not _rec_pending`; `_recenter_slice()` hook between threadmesh_poll and _drain_build_queue; `recenter()` stub loops + `_build_queue_for_center` call replaced by pending-state setup (tr1/_rp_walk_ms timing kept so RECPROBE rebuild_ms ≈ setup-only); `_build_queue_for_center` DELETED; NEW `_recenter_slice()` + `_rec_want_step`/`_rec_stub_step`/`_rec_merge_old_step`/`_rec_merge_want_step`/`_rec_merge_ring_step` after `recenter`; SWAP at phase-4 exhaustion (verbatim :894-902 + `_rp_stub_n = _rec_new_n` moved to post-loop in `_recenter_slice` same frame so RECSLICE prints the real stub count); RECSLICE probe at completion.
  - `godot/scenes/main.gd`: +1 line `"queue_size": int(world.queue_size)` in `_boundary_test` RESULT dict.
- **Implementation notes / minor deviations (additive only, no behavior change):**
  - Plan pseudocode `[ [] x (2*rr+3) ]` is not GDScript — implemented as `for i in range(2*rr+3): _rec_new_buckets.append([])` (matches old :841-843 style). GDScript has no `div`/`mod` operators — cell math uses `/`/`%` on non-negative ints (identical semantics).
  - Two extra private state vars beyond plan §5.2 list: `_rec_i` (index within current MERGE_OLD band — the (b,i) 2D domain needs two coordinates) and `_rec_want_keys` (WANT insertion-order key list; GDScript dicts have no indexed key access, so the cursor domain is the parallel list, appended at WANT time). Both private, no observable effect.
  - MERGE_OLD iterates the LIVE old buckets (per plan §5.3) with (band, index) cursors; drain removals before the cursor in an already-passed band can shift indices — analysis: drain is score-driven (consumes lowest-band entries first) and every band holds ≤4·(2r+1) « 2048 units, so a band is always consumed in ≤1 slice frame (slice runs before drain in the same frame) → no in-band interleaving possible at any harness radius; noted in results.html.
  - `_enter_candidate` comment: stale reference to deleted `_build_queue_for_center` updated to "the recenter WANT pass" (1 word, keeps the comment accurate).
  - RECSLICE prints after per-frame stat accumulation (includes the SWAP frame), capturing `_rp_stub_n` before the `_rp_stub_n = _rec_new_n` reassignment (reassignment deferred to the same `_recenter_slice` post-loop branch — identical-frame semantics).
- **Zero script errors hard gate:** first G0 hit a parse error (`div`/`mod` — not GDScript operators); fixed → G0 RC=0, 0 script errors.

### Entry 2 — gates G0-G3 PASS (post-change)
- **G0/G1**: `godot --headless --path godot --quit` RC=0, 0 script errors. `AWECRAFT_LOGIC=player` RC=0, 0 script errors, `horizontal_moved 2.82` (exact).
- **G2 genhash**: 25/25, `diff` vs `.scratch/AC-0118/genhash_baseline.txt` EMPTY (byte-identical).
- **G3 battery** `AWECRAFT_BATTERY="player;interact;light;fluids;buckets"`: RC=0, 0 script errors, `ok:true`; sea 2730/2730 + 1406/1406, torch_level 14, horizontal_moved 2.82, place_ok true, drop_spawned true, water_on_lava_result 25, sideways_lava_result 9 — ALL exact.
- **G4 in flight**: r50 boundary walk (RECPROBE) launched as background job bash-10 (~23 min). Next: collect → r50 perf (background ~14 min) → r4 boundary → G5 flake loop (boundary x2 + player x4) → results.html.
- Ops: godot ONE at a time (sequential background jobs); HOME=/tmp/dsh_home per call; no git, no builds, no renders (plan §G6 no-render decision), no python-server kills.

### Entry 3 — gates G4/G5/G6 PASS; task complete (deviations below)
- **G4 r50 walk** (q4_boundary_r50.log): 21 crossing recenter calls min 8.4 / p95 **12.2** (12.3 ceil) / max 12.3 / mean 10.2 → <15 gate PASS (margin 2.7 ms; plan §5.10 predicted 10-11 — flagged for AC-0119). RECSLICE: cold start 130.0/9.0/28 frames/new_n 10608/stubs 10608/queue 10609 (== pre-change set); end-of-walk 65.0/6.0/27/2060/838/10486. All max_ms < 15.
- **Honest finding (r50 only)**: under continuous-walk load the ~52k-unit slice takes longer than the ~59-frame crossing window (8 ms wall budget → ~700-1000 units/frame), so the 19 mid-walk rebuilds restart per crossing (plan §5.7 design) and only cold-start + end-of-walk complete. Queue stale ≤1-2 crossings mid-walk; A/B world state identical (built_final 228 = pre, ok true, flap 0, remesh/marker true, irb_max 108 vs 105, r50 walk RESULT p95 46/max 99 vs pre 41/123 — p95 +5 ms, max improved). Flagged for AC-0119.
- **G4 r50 perf** (q4_perf_r50.log): recenter_ms **0** (pre 102) PASS; first_draw 366→454 (+88 ms, inside R4 band, not gated); frame p95/max 31/83 vs pre 29/78 (+2/+5, drain-dominated noise, not in formal Gate: line, reported honestly); total 849050/51005 frames (pre 848955).
- **G4c r4 boundary** (q4c_boundary_r4.log): p95 **21** (≤26), max 46, irb_max **69** (67-69), irb_final 81/81, flap 0, ok/remesh/marker true → PASS.
- **G5**: boundary x2 + player x4 all RC=0/0 errors/no hang; irb_final 81/81 + built_final 81 + flap 0 in all boundary runs; queue_size **95 in all three** post boundary runs.
- **G5 gate correction (deviation, measured)**: plan's `queue_size == 0` is unreachable by design — `data_only` entries are NEVER removed from band_buckets (drain only `_remove_entry`'s build entries; data pick advances the sticky cursor past them, pre-change behavior, untouched). A/B: restored original world.gd (git HEAD) with the new RESULT field → pre run also **queue_size 95** at the identical moment (p95 21/max 48/irb_max 70/built_final 81). Gate re-expressed: residual identical pre/post + all build entries consumed. world.gd restored + diff-verified.
- **G6**: `git status --short` → only `M godot/world/world.gd`, `M godot/scenes/main.gd` (+1), `M tasks/AC-0118/continuity.md`, `?? tasks/AC-0118/AC-0118-results.html`. No render gate (plan decision).
- **Deliverables**: `tasks/AC-0118/AC-0118-results.html` (self-contained: A/B tables, RECPROBE/RECSLICE excerpts, genhash 25/25 columns pasted from the ephemeral baseline, drain A/B evidence, flake table, gate verdicts, deviations). Status: **DONE** — ready for coordinator verification + commit.
- Logs (ephemeral, .scratch/AC-0118/): q4_boundary_r50.log, q4_perf_r50.log, q4c_boundary_r4.log, g5_boundary_1/2.log, g5_player_1-4.log, q5ab_boundary_r4_pre.log, g0_headless.log, g1_player.log, g2_genhash.log, g3_battery.log, genhash_baseline.txt, g2_genhash_lines.txt, genhash_table_rows.html, world_gd_orig.gd, world_gd_mine.gd.
