## RUN 0 (coordinator, 2026-08-26)
- AC-0120 = shared-material-cache-no-per-build-alloc (P1 follow-up of the meshing line): chunk.gd:860 allocates 3-5 StandardMaterial3D per chunk build (~18/crossing) vs the web 3 shared materials (index.html:1290). Fix: shared material cache keyed by (kind, tint), reused across all chunks; ArrayMesh stays per-chunk; material alloc per crossing = 0; visuals byte-identical (greedy UVs intact, MINFO r4 verts 125552).
- Spec v1 filled (gates G1-G6): G2 visual/data identity [genhash 25/25 + full battery exact + MINFO r4 verts 125552], G3 alloc probe [Run-1 defines: distinct StandardMaterial3D instances at steady state = cache constant, :860 site unreachable], G4 perf no-regression + memory, G5 boundary floor = POST-AC-0078/AC-0079 anchors [walk p95 <= 65, fwd_p95 <= 3500 + first10 >= 8, irb >= 60, flap 0, RECSLICE <= 12.2, perf total ~5715], G6 windows build.
- Worker-compat is the key design question (AC-0107 workers build on fresh duplicate() copies - the worker path must reference the SHARED materials; thread-safety of the cache read from N workers - Run-1 must settle it).
- Fresh-tree anchors (post-AC-0079, 2026-08-26): boundary r4 walk p95 62-65 / fwd_p95 2932-3238 / first10 10 / irb 81 / flap 0; r2 fwd 463-593; r50 RECSLICE 10.0/8.0; perf r4 total 5715 / first_draw 192 / read_sync_gen 0; MINFO r4 verts 125552 (AC-0117 anchor); genhash 25/25 (61de26e9); battery exact.
- Ops: HOME=/tmp/dsh_home pattern; one godot at a time; Run-1 = headless probes ONLY and MUST be godot-busy aware (AC-0078 heavy gates run in a background job first: check pgrep + .scratch/AC-0078/HEAVY_GATES_DONE before every godot call); r50 timeout 1200000; scratch .scratch/AC-0120/; no git/builds/TASKS.yaml; never kill python server pids.
- Run-1 LAUNCHING (subagent_plan, xhigh) - overlaps the AC-0078 heavy gates.

## RUN 1 (research/planner, xhigh, 2026-08-26)
- DELIVERABLE: `tasks/AC-0120/plan.html` (7 sections, constants verbatim). Full detail there; this is the handoff log.
- **GODOT STATUS AT RUN-1 START:** AC-0078 heavy-gate job had ALREADY FINISHED at 20:30 (marker present) — but its gates.log shows ALL 14 gates FAILED environmentally: `env: '/tmp/dsh_home/tools/godot/godot': No such file or directory` (rc=127 each) + windows build rc=1 (gate script used `$HOME/tools/godot/godot`; /tmp is per-call tmpfs so the binary never existed there). Stale exe (20:25, 109564736B) still served on 8080. **Coordinator must re-run AC-0078 gates with the ABSOLUTE binary path `/home/angrygiant/tools/godot/godot`** (Run-1's + plan's commands all do). No godot/Xvfb procs were running during the Run-1 probe window (pgrep self-match artifact: PID 1 bwrap carries the pattern string; verified via ps aux + marker mtime + gates.log tail before each call).
- **MATERIAL TOPOLOGY (final, with file:line):** 4 per-build `StandardMaterial3D.new()` factories in chunk.gd: `_opaque_material` :1300 (albedo = merged atlas ms.tex else Data.atlas_tex, NEAREST, vertex colors, rough 0.95), `_cutout_material` :1315 (ALPHA_SCISSOR thr 0.5), `_flower_material` :1322 (+CULL_DISABLED), `_fluid_material` :1328 (ALPHA 0.62, rough 0.15, torches-only kind). Call sites: sync build_mesh :1499/:1508/:1523/:1526; worker-handoff apply_accs :1569/:1578/:1593/:1596. Water(5)/lava(24) = 2 SHARED `ShaderMaterial` in `Data.fluid_anim_mats` (data.gd:32, 218-231, created ONCE at Data._ready :188) — already shared, untouched. All other tree material sites (main.gd:1338/1354 sky, held_mesh.gd:71, player.gd:345/368/386/647) are one-shot globals, not per-chunk. **Steady-state PRE (r4, 81 built) = 81 opaque + n_leaf cutout + n_flower flower + 0 torch ≈ 130-170 distinct StandardMaterial3D + 2 shared ShaderMaterials.** MESH-STRUCTURE FACT: `mesh_instance` and `fluid_instance` are TWO MeshInstance3Ds sharing ONE ArrayMesh per chunk (fluid surfaces added to the mesh object after parenting, chunk.gd:1500-1508) — all 68 fluid chunks in fresh MINFO have verts==fverts; probe must dedupe by instance id; flora mesh is the 2nd ArrayMesh.
- **WEB 3 MATERIALS (isomorphism target):** index.html:750-752 `matOpaque=litMaterial()` / `matCutout=litMaterial({alphaTest:0.5})` / `matTrans=litMaterial({transparent:true,depthWrite:false})` (base :740 MeshBasicMaterial map:atlasTex,vertexColors,DoubleSide); partition :1141-1146 (water/lava→matTrans, cutout/cross→matCutout, else matOpaque); attach :1287-1293. **Godot minimal = 4 StandardMaterial3D** (renderer: transparency mode + cull mode are per-material state → scissor/blend/cull-disabled are 3 kinds; web animates water/lava via shared-texture frame flips, Godot via per-material shader = the 2 pre-existing shared ShaderMaterials). Final shared set = 4+2 = 6 distinct across all chunks. Tint collapses out of the key (baked per-vertex, chunk.gd:327-329/366-367) → key = kind (+ texture identity for opaque).
- **CACHE DESIGN (decided):** owner = STATICS in chunk.gd next to `_ms_*` (chunk.gd:42-44), NOT world.gd/autoload — rationale: chunk.gd is the only consumer (world.gd has zero material refs), matches the existing atlas-cache idiom, zero wiring (world.gd UNCHANGED — fence shrinks). `static func _get_mat(kind, ms_tex)` with lazy identity-key invalidation `(_mat_atlas != Data.atlas_tex or _mat_ms != ms_tex) → clear` (texture-swap/merge-off safe, 4 allocs per texture generation only). Lifetime: outlives worlds by design, survives New/Continue harmlessly (materials ref only global cached textures); lazy rebuild on texture swap. ≤4 live entries, kind-only keys (fence).
- **WORKER-COMPAT VERDICT:** safe by construction — NO material crosses a thread boundary. Worker = only `ChunkScript.build_accs` (pure-static, Packed*Arrays + ctx/ms snapshots; ms has rects+h only, no texture — world.gd:643-647, chunk.gd:1135-1225 zero material refs). Assignment is main-thread-only: sync build_mesh (world.gd:610/623/638) + apply_accs via threadmesh_handoff (world.gd:589 from threadmesh_poll :536/:321). Cache dict touched by exactly one thread.
- **G3 PROBE:** env-gated by EXISTING `AWECRAFT_MESH_INFO` (in HARNESS_ENVS main.gd:279 — no new env), prints one `MATINFO built_chunks=N distinct_std=N distinct_all=N total_allocs=N` line. (a) steady-state scene walk in main.gd `_matinfo_counts()` helper covering mesh+fluid+flora instances (mesh_info() world.gd:1502 misses flora — the probe covers all three, dedupe by instance id); (b) build-path counter `_mat_alloc_count` static in chunk.gd at the create path of _get_mat. Print sites: `_perf_test` after the MINFO loop (main.gd:4836-4838) + `_boundary_test` before final Debug.result (main.gd:5262). PRE/POST: PRE distinct_std ≈ 130-170 (scales w/ chunks), total_allocs ≫ 81 (804 build units in the perf run) — exact PRE recorded by Run-2 probe-first run (Run-1 is read-only, cannot add the walk); POST distinct_std=3, total_allocs=3, constant through 20 crossings (G3b boundary+MESH_INFO run). One steady-state measurement proves 0/crossing because POST total_allocs = kinds created at first contact and never grows, PRE scales with builds.
- **FRESH PRE-CHANGE ANCHORS (measured 2026-08-26, .scratch/AC-0120/):** G0 RC=0 0err; **perf r4: total 5870 / build_ms 2276 / first_draw 191 / p95 57 / max 70 / fog_ok / read_sync_gen 0 / chunks 81 (121 total) / all_meshed / mem 49.86→79.58 MB (Δ29.72) / build_units 804** (perf_r4_minfo.log); **MINFO r4: 125552 EXACT** surface-0 sum over 81 built, 68 fluid chunks (perf_r4_minfo.log); **genhash 25/25, MD5-of-25-lines 61de26e9a542a5ce2db62e7157d6c018** (genhash.log); **battery spot light;fluids ok:true EXACT** (torch 14, sea 2730/2730+1406/1406, 25/9, [5,7],[5,8], delta 1) (battery_spot.log); **boundary r4 ok:true: irb 81, walk p95 61, fwd_p95 3233, first10 10, flap 0, max 119, queue 95, 20 crossings** (boundary_r4.log). r50/RECSLICE skipped (no recenter interaction) — coordinator job.
- **GATES (plan §5, exact):** G1 --quit RC=0 0err; G2 = genhash 25/25 (61de26e9) + FULL battery exact + MINFO r4 `AWECRAFT_LOGIC=perf AWECRAFT_MESH_INFO=1 AWECRAFT_RADIUS=4` verts 125552 EXACT (perf path, NOT the unknown-logic fallback main.gd:971 which prints pre-settle — AC-0117 G4 note); G3 = probe pre→post (3/3 constants) + post-crossings boundary run; G4 perf within noise + first_draw ≤300 + mem Δ<0.5MB; G5 boundary floor (p95≤~65, fwd≤3500, first10≥8, irb≥60, flap 0) + coordinator RECSLICE r50 ≤12.2 + flake; G6 ./build_windows.sh RC=0 + fresh exe + 8080/5180 200.
- **CURRENT STATE / NEXT STEP (for Run-2):** plan.html complete; Run-2 implements in this ORDER: (1) main.gd probe (helper + 2 gated prints + preload const) → run G3a PRE (probe-first, unmodified chunk.gd) → (2) chunk.gd cache (statics + _get_mat + 8 call-site swaps; factories unchanged) → (3) G0 + G3a POST + G2c + SMOKE battery + ≤1 R=1 render (tasks/AC-0120/post_r1_iso.png) → results.html; coordinator runs heavy gates (absolute godot path!) + AC-0078 re-gate. No git/builds in Run-1/Run-2 beyond G6.

## RUN 2 (builder, medium, 2026-08-26)
- Step A done: probe harness in main.gd (`const _ChunkScriptM = preload("res://world/chunk.gd")` after `_harness_env_set`, `_matinfo_counts()` helper, MATINFO print in `_perf_test` MINFO block + `_boundary_test` before `Debug.result`, env-gated on existing AWECRAFT_MESH_INFO) + `_mat_alloc_count` static in chunk.gd with +1 in each of the 4 factories (PRE counter; removed in Step C — POST counts via `_get_mat` only).
- G0 PRE: rc=0, 0 script errors (`.scratch/AC-0120/g0_pre.log`).
- Step B PRE run (`AWECRAFT_LOGIC=perf AWECRAFT_MESH_INFO=1 AWECRAFT_RADIUS=4`): **MATINFO built_chunks=81 distinct_std=180 distinct_all=182 total_allocs=325** (within the plan's ~130-170 band for distinct_std; total_allocs ≫ 81 as expected — factory counter counts the call chain, e.g. cutout→opaque+cutout). MINFO surface-0 sum = **125552 EXACT** (81 built). perf: total 5737 / build 2348 / first_draw 192 / fog_ok / read_sync_gen 0 / all_meshed. 0 script errors (`.scratch/AC-0120/pre_r4_minfo.log`).
- DEVIATION (found by trace before flip): plan §2.4 `_get_mat` code verbatim re-clears the cache on EVERY build — call order is opaque(ms.tex) → fluid/cutout/flower(ms_tex=null default): after opaque sets `_mat_ms`=merged-tex, the next kind's `null` mismatches → clear → next chunk's opaque mismatches again. POST would be ~4 allocs/build, breaking gate total_allocs=3. Fix: guard the ms-tex half of the invalidation with `kind == "opaque"` (1 line; matches §2.4 key text "opaque ADDITIONALLY identity-keyed on (Data.atlas_tex, ms_tex)"). Call-site swaps unchanged per plan.
- Step C done (cache flip): chunk.gd statics block → 4 statics + `static func _get_mat(kind, ms_tex)` (plan §2.4 + the 1-line opaque-guard deviation above); 8 call-site swaps (sync :1525/:1534/:1549/:1552, handoff :1595/:1604/:1619/:1622); anim sites :1537/:1540/:1607/:1610 + `_fluid_anim_material` + `_merge_atlas` untouched.
- DEVIATION 2 (fix, compile gate): plan §2.4 assumed `_get_mat` (static) can call the 4 factories — they were instance funcs → 5 parse errors on first G0 POST. Fix: all 4 factories `static func` (bodies verbatim, pure: only StandardMaterial3D + Data autoload, zero instance state; no external callers — grep-verified). G0 POST then rc=0, 0 script errors.
- G0 POST: rc=0, 0 script errors (`.scratch/AC-0120/g0_post.log`).
- Step D POST run: **MATINFO built_chunks=81 distinct_std=3 distinct_all=5 total_allocs=3** (exact gate: 3 kinds — opaque/cutout/flower, fluid-torch kind absent in natural world; constant). MINFO surface-0 sum = **125552 EXACT** (81 built). perf: total 5844 / build 2341 / first_draw 189 / p95 56 / max 94 / fog_ok / read_sync_gen 0 / all_meshed / mem 49.89→79.18 MB (Δ29.29 vs PRE-anchor Δ29.72). 0 script errors (`.scratch/AC-0120/post_r4_minfo.log`).
- Step E SMOKE battery (`AWECRAFT_BATTERY="player;interact;light;fluids;buckets;genhash"`): rc=0, 0 script errors — ALL EXACT: player is_on_floor true / jump_peak 36.43 / horizontal_moved 2.82; interact breakable 2 / highlight true / drop true / after_place 2 / place_ok true; light 14/15/0/9; fluids sea 2730/2730 + 1406/1406 stable true / water_delta 1 / shore [5,7] / source [5,8] / 25/9; buckets scoop_before [5,8] / place_after [5,8]; genhash 25/25, MD5-of-25-full-GENHASH-lines = 61de26e9a542a5ce2db62e7157d6c018 (anchor match; the 25 lines are byte-identical to Run-1's pre-change genhash.log). (`.scratch/AC-0120/smoke_battery.log`)
- Step F render: R=1 iso → `tasks/AC-0120/post_r1_iso.png` (1280x720, 82077 B; rc=0, 0 script errors). NOTE: plan §6 recipe's relative AWECRAFT_SNAPSHOT path fails (Godot resolves it against the project dir — "Can't save PNG at path"); past renders (AC-0078/AC-0106) used the ABSOLUTE path — used that. Pixel-diff vs `tasks/AC-0078/ac0078_iso_r1.png`: 25/921600 px differ, max channel-sum Δ2 (fluid-anim 1-frame phase jitter on water edges; ocean translucency / terrain / leaves visually identical).
- STATE: SMOKE complete — awaiting coordinator heavy gates (G3b boundary+MESH_INFO, G4 full perf, G5 boundary r4 floor + RECSLICE r50 + flake, G6 windows build — absolute godot path). No git (coordinator commits). Deliverable `tasks/AC-0120/AC-0120-results.html` written.
