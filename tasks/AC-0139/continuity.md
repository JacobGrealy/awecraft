# AC-0139 continuity — split-main-menu-into-own-scene

## RUN — 2026-08-29 — AC-0139 spec written, parked after AC-0138
- Task added AC-0139, queued at position 2 (right after AC-0138), status open.
- Spec filled: goal two-scene split, requirements new menu.tscn + extract menu code from main.gd + remove dead preloads, gates G0+menu render+play transition+dead-import check, scope menu.tscn/ui/menu.gd/main.gd/main.tscn.
- Parked per user — not launched yet.

## RUN — 2026-08-29 13:28 EDT — AC-0139 (xhigh single-phase builder, user p1)
- Started. Read spec.html + ARCHITECTURE.md + CONTINUITY 00l. Investigating main.gd menu state machine next.
- PLAN (minimum-change): keep main.tscn as project main scene; gameplay root instantiates NEW scenes/menu.tscn (root Control + ui/menu.gd) as child in menu state; Start/New World frees it and enters play — exactly what harness arms see today. Menu UI tree goes in the .tscn (visually editable), logic/state in ui/menu.gd. No harness-arm reorg (AC-0140's job).
- DESIGN (settled, minimum-change): main.tscn STAYS project main scene (all harness arms boot through it — zero env changes). New godot/scenes/menu.tscn: root "Menu" Control (ui/menu.gd attached, class_name Menu) + child CanvasLayer "Layer" (layer=20, preserves exact current draw order above HUD/world) holding the full menu node tree (MainBox/PauseBox/OptionsBox full-rect Controls + ColorRect bgs + CenterContainer/VBox + all Labels/Buttons/LineEdit/sliders/checkboxes/OptionButton + FileDialog). ALL signal connections declared in the .tscn (incl. binds for slot rows + options source). ui/menu.gd: extends Control, keeps state machine + handlers + refresh/sync logic; _ready collects node refs + dynamic init (Build.ID version label, seed text, slot arrays, FileDialog config); deletes all _build_*/_mk_* constructors + unused color consts (OVERLAY_BG/MENU_BG/TITLE_C). main.gd: MenuScript preload → MenuRes = preload("res://scenes/menu.tscn"), var menu_ui: Menu, _make_menu() instantiates the scene; NO harness-arm code touched (quitmenu/mainmenuexit/menu_boot/menu_shot/_process transitions all unchanged → same env vars, same RESULT shapes).
- INVESTIGATION FINDINGS: menu state already lived in ui/menu.gd (CanvasLayer, code-built UI); main.gd only wires it (_make_menu + 5 callbacks + mode-transition show_pause/hide_pause). Menu entry/exit flows preserved: (1) boot → want_menu → _boot_menu; (2) AWECRAFT_MENU_BOOT=1 + AWECRAFT_SNAPSHOT → _boot_menu + menu_ui.play_clicked() → gameplay + snap (this is my play-transition gate); (3) AWECRAFT_MENU_SHOT → menu-only snap branch; (4) AWECRAFT_LOGIC=quitmenu → _make_menu + play→pause→quit→menu; (5) AWECRAFT_LOGIC=mainmenuexit → _boot_menu + exit button emit; (6) in-game P/Esc → Game.pause → _process → menu_ui.show_pause; (7) pause "Quit to Menu" → quit_to_menu (autosave + free game nodes + show_main); (8) pause Options/Esc/P via _unhandled_input. FileDialog config (file_mode/access/filter) + file_selected connect kept in _ready (enum values, not layout).
- MILESTONES: menu.tscn written (root Menu Control + CanvasLayer "Layer" layer 20 + full node tree + 22 tscn connections); ui/menu.gd rewritten (class_name Menu, extends Control, 576→338 lines, builders deleted); main.gd 3 edits (MenuRes preload, menu_ui: Menu, _make_menu instantiate). First G0 red: stale global class cache (no Menu) → `--import` pass rebuilt cache → G0 GREEN rc=0 0 SCRIPT ERROR.
- GATES ALL GREEN: G0 rc=0 0 SCRIPT ERROR [after 1 --import pass to refresh stale class cache — first G0 red was stale-cache only]. SMOKE: player after_fly_y 37.78 EXACT [+1 known AC-0137 threadgen transient], interact all-true EXACT, quitmenu ok:true [proper invocation = AWECRAFT_STDERR_FILE redirect; 0 stderr errors], mainmenuexit ready_to_quit:true exit_found:true RC=0. Render 1 [AWECRAFT_MENU_SHOT, xvfb gl_compatibility R=1]: menu on screen — title/subtitle/version, Play, seed 44 + New World, 3 slot rows, Options/Exit/Pack, help strip; no world behind [RESULT {"menu":true,"mode":"menu"}]. Render 2 [AWECRAFT_MENU_BOOT=1 + AWECRAFT_SNAPSHOT]: real Play path (menu_ui.play_clicked → _menu_play → new world seed 44 after quitmenu cleared slots 0-2) → gameplay visible: world + hotbar + hearts + held item, menu gone [RESULT {"m4":"ok","w":1280,"h":720}]; snapshot flow's standard _placed byproduct wrote play_from_menu_placed.png [same render run]; 4× known threadgen transient + SNAPDRAIN R=1 timing artifact documented. Dead-import audit CLEAN: only menu reference in main.gd = MenuRes preload (intended instantiation path); every other preload/load in main.gd + ui/menu.gd is in-use (_ChunkScriptM = pre-existing AC-0120 harness preload, in-use, AC-0140's job). Deliverables: tasks/AC-0139/AC-0139-results.html [self-contained, 2 embedded renders] + menu_boot.png + play_from_menu.png. Fences held [no main.tscn/project.godot change, no TASKS.yaml, no git, no builds, no docs]. BUILDER DONE — ready for coordinator heavy-gate pass + commit.
