# AC-0129 continuity (cave-lighting-propagation-fix)

## RUN 0 (coordinator setup, 2026-08-27)
- Registry (user, priority 1): "Fix cave lit next to pitch black with no reason: BFS torch 14 -1/step lighting.gd:146 should fade smooth 14->0. Audit per-chunk contained batch (AC-0077) vs air-on-miss frontier (AC-0119) cache/data-signature and recenter slice 0118 neighbor readiness; probe light_at in r4 cave shows 14 next to 0 with no torch. Fix propagation/caching so cave eff is consistent. Gate: cave tunnel smooth gradient, no hard 14/0 cliff, MINFO/light_at green."
- Spec v1 filled (all 5 sections) — task-specific requirements + gates + scope + fences.
- Anchors in force (post-AC-0126, commit d37f5f5): perf r4 5851/194/57/32.1MB; boundary r4 62/3035/10/81/0 (coll 223 shapes max 3ms); MINFO 125552 (121 lines, .scratch/AC-0108/pre_minfo_r4.txt); MATINFO 3/3; genhash 25/25 (seed 44, .scratch/AC-0126/genhash_seed44.log); battery exact (torch 14/15/0/9, sea 2730/1406 stable, [5,8]); r50 mem band 140.6-146.9; RECSLICE max 12.2; exe 109,576,544B.
- Context for Run-1: AC-0077 = per-chunk-contained light batch (compute_light_flat_batch; eff cached; workers skip own light when eff_trust); AC-0119 = air-on-miss on all read paths (world.block returns air for missing chunks, no sync gen); AC-0118 = budgeted 5-phase recenter slice; AC-0107 = threaded worker (fresh duplicate(), self-lights contained kernel when eff empty); AC-0120 = shared material cache (3 allocs); AC-0108 = slabs (5x 16x16x16 per column); AC-0126 = edit stagger 1/frame + cap-drop requeue (commit d37f5f5).
- Run-1 (subagent_plan, xhigh) LAUNCHING: reproduce+localize the cliff (lightaudit probe), prove the mechanism from the three suspects with file:line, design the fix + fresh PRE anchors + plan.html 7 sections + RUN 1.

## RUN 1 (research/planner, xhigh, 2026-08-27 11:25–11:50 EDT)
- **MECHANISM PROVEN (static, high confidence) — suspect (a) fires, (b)/(c) do NOT (for block light):** every mesh-eff path is chunk-contained (STANDALONE_MARGIN=0 chunk.gd:4; `_chunk_light_into` lighting.gd:228–265 does ZERO cross-chunk reads) → block light can NEVER cross a boundary in baked eff (torch at local x=0: own 14,13,…; neighbor local 15 = 0). Persists after settle: `_eff_for` (world.gd:918–924) validates by OWN data only. The literal AC-0077 batch (`compute_light_flat_batch` lighting.gd:289) is DEAD (0 callers; light_batch_calls=0 in AC-0126 boundary RESULT) — the live contained path is AC-0107 worker self-light (build_accs chunk.gd:1184–1185). **SECOND MECHANISM found:** bake sampler `_s_effl` (chunk.gd:650–659; sync `_effl` :166–175) returns 15 (full bright) for out-of-bounds x/z → with margin-0 light box, every boundary face in a dark cave bakes at 15 (web `lightAt` :963–970 reads the REAL neighbor light; [15,0] only when missing). Both artifacts share one fix.
- **Fix (plan.html §2):** (1) lighting.gd new `compute_light_flat_chunk_pull(data,cx,cz,h,strips)`: scan → combine eff=max(sky,blk) → ONE `_flood_flat` (local closure) → **queue-gated injection** (web parity: only boundary cells with eff>0 import `strip_col0 − _att[own]`) → re-flood only if raised → returns core 16×16 + `blk_src`. Sync path `compute_light_flat` gets the same (internal neighbor last_eff lookup, exact-chunk box only). `compute_light_split` (light_at/battery) UNTOUCHED. (2) chunk.gd `build_accs`: 8 strips (4 side 2560B = 2 cols ×16×h, 4 corner 320B = 2×2×h; layout fixed in plan) arrive in per-entry `ctx` → eff-empty → pull kernel; **BAKE BOX** = 20×20×h assembled (core at +2,+2 + margin from strips) passed to emit — `_s_effl`/`_face_light` zero changes (index-generic); res carries `blk_src`; `apply_accs` unchanged (last_eff stays CORE 16×16 → no memory growth). New var `eff_gen`. (3) world.gd: `_strips_for`/`_ngens_for` helpers; `_mesh_dispatch` entry ctx = {**_tm_ctx, "strips"} + "ngen" (dispatch-captured); eff cache entries store ngen (4 eff_gen ints) — `_eff_for` validates own data + all 4 neighbor gens; **E2 hook** `_eff_landed` (handoff before/after apply_accs + sync builds): changed ⇒ eff_gen+=1; changed && own blk_src ⇒ evict+enqueue 4 built orthogonal neighbors via light_pending (web lightOnNewChunk analog, one-shot, depth≤2, ZERO churn for surface landings since blk_src=false). Edit 3×3 + recenter + AC-0126 invariants unchanged.
- **Web parity proofs (plan §1.3/§3):** combined pull ≡ per-kind pull (max-BFS max-distributivity, c≥1: max(a−c,b−c)=max(a,b)−c); web queue gate ⇒ injection gate eff[B]>0 (ungated would leak sky 15 into enclosed cave boundaries — a new artifact); documented deviations: Godot E2 goes BEYOND the web's new-chunk reverse gap (web remesh ≠ re-light, index.html:1295–1326 — web bug, fixed); combined vs sky/blk channels pre-existing (AC-0128).
- **Battery interaction: NONE old→new or new→old** — all 4 values read `world.light_at` (world.gd:1437 margin-8 box via untouched `compute_light_split`); MINFO 125552/121 (geometry not color — AC-0126 proved same property); genhash 25/25 (data-only).
- **lightaudit probe designed (plan §2.D), NEW main.gd mode, Run-2 builds probe-FIRST:** steady settle (breakspike pattern :2868–2883) → cliff scan over baked last_eff (all adjacent passable pairs |Δ|>1, 81 chunks, world coords, top 8) → deterministic boundary-crossing torch tunnel (fixed scan order; 5+5 air run crossing a boundary; light_at darkness pre-check; set_block 22; settle; seq 11 mesh effs vs 11 light_at refs, |i|≤8 compare) → worst_sources (31×31×15 _glow attribution) → RESULT JSON {cliffs{count,max_delta,pairs}, tunnel{torch,dir,seq,ref,ok}, worst_sources, ok}. PRE gate: cliffs>0 + Δ≥13; POST: cliffs==0 + tunnel.ok + ok. No existing probe can read baked eff values (only breakspike MD5, main.gd:2948) → reproduction = Run-2's PRE run.
- **FRESH PRE ANCHORS (this session, job bash-76, 11:30–11:33 EDT, .scratch/AC-0129/summary.txt, tree d37f5f5):** G0 RC=0/0err · battery 5-arm EXACT (player 2747ms moved 2.82 jump 36.43; interact place/drop true [8,34,8]; light 2916ms 14/15/0/9; fluids 1406/2730 flat [5,7][5,8] 25/9; genhash 898ms; ok true total 116633ms) · genhash 25/25 diff 0 vs .scratch/AC-0126/genhash_seed44.log · MINFO r4 121 lines diff 0 vs .scratch/AC-0108/pre_minfo_r4.txt (125552) · MATINFO 81/3/5/3 · perf r4 total 5875 build 2374 first_draw 196 p95 57 frame_max 70 mem 50057034→82193782 collision 223 read_sync_gen 0 light_self 81 staged 72/0/0 · boundary r4 p95 64 max 87 fwd_p95 3268 fwd_first10 10 burst_p95 3268 irb 81/81/18 flap 0 queue 95 mem Δ31.7 collision 222 walk 20/19.51s marker/remesh ok. r50 mem 140.6–146.9 + RECSLICE ≤12.2 = AC-0126 (10:24–10:32 today, same tree) NOT re-run (25-min runs exceed probe-job bounds; coordinator heavy pipeline re-runs).
- **Deliverables:** `tasks/AC-0129/plan.html` (7 sections: goal+mechanism, files-to-touch function-level, frozen-spec refs table, Data ids+probe cells, gates+fresh anchors+lightaudit RESULT shape+PRE/POST A/B, snapshots=none (sub-surface effect, no torches in fresh world), risks a–g) + this entry + `.scratch/AC-0129/` anchors.
- **NEXT: Run-2 (subagent, medium) PROBE-FIRST:** (1) add lightaudit probe to UNMODIFIED tree → G0 → PRE run (cliffs>0) → (2) implement plan §2 (lighting.gd pull kernel + chunk.gd bake box + world.gd strips/ngen/E2) → POST run (cliffs 0, tunnel.ok) → SMOKE battery + genhash + ≤1 optional render → results.html + this log. Then coordinator heavy gates (G3/G4) + G5 build + commit+push. Fences: no git/builds by Run-2, scratch → .scratch/AC-0129/, godot one at a time.

## RUN 1.1 (research/planner, xhigh, 2026-08-27 — coordinator gate-check revision, STATIC ONLY)
- **Factual correction (plan §1.3):** the web shows the SAME block-light cliff — re-derived from index.html: `floodLight` seeds its queue ONLY from own-array >0 cells (:974; for blk = LOCAL sources only, set :1019); the cross-chunk pull (:982–996) fires only while a frontier (queued) cell is on the boundary, and a cell only enters the queue if >0 (seed :974 or import :994) → a dark chunk (no local source) has an EMPTY queue → its flood never runs → it never imports the neighbor's light; `recomputeLightAround` (:1032–1039) recomputes the dark neighbor from B's OWN sources → boundary stays 0. The web's pull is PARTIAL (queue-gated, own-flood-reaches-boundary, nv>v+1) — it trades light between two ALREADY-LIT boundary cells but never lights a dark chunk: the user's (14,0) pair exists in the web too. RUN 1's "web shows no cliffs / web parity" framing is corrected; the fix target is Minecraft semantics (AGENTS.md canonical; user: "should fade smooth 14→0") = a user-directed correction BEYOND the web, now documented deviation (iii).
- **Design fix (plan §2.A.1 d/e + §2.C.1):** the RUN 1 gate `eff[B] > 0` fired only where our boundary was already locally lit — it EXCLUDED exactly the user's dark boundary cell (13 next to 0 would have survived → G1 tunnel gate would have FAILED in POST). Revision: the dispatch carries TWO strip sets — the COMBINED eff strips (20×20 bake box, unchanged) + NEW blk-only strips (4 sides), derived main-thread in `_strips_for` from the neighbor's `data` + `last_eff` per boundary cell: source (22/23/24 → 14/12/15, data.gd) = exact; else eff_n > sky_n (sky_n = column scan, web rule :1013–1021 / Godot twin `_chunk_light_into`) → blk_n = eff_n (exact: eff=max(sky,blk), blk>sky ⇒ blk=eff); else 0 CONSERVATIVE (sky_n=15 open column: real blk<15 invisible in the neighbor's own combined eff max(15,blk)=15; closed column falls in the exact branch — the user's case is always exact). Injection: `cand = blk_strip[c0] − _att[ids[B]]; if cand > eff[B]: eff[B] = cand; changed = true` — NO gate (fires into dark cells); sky-leak prevention moves from the gate to the strip content (open-column cells carry 0 → sky-15 never enters an enclosed cave). Max-distributivity no longer load-bearing (single-kind blk injection).
- **Invalidation re-verified, churn-free by construction:** cache validity = own data + 4 neighbor eff_gen (unchanged); E2 = eff byte-change && own blk_src (unchanged — a lava-chunk landing now correctly lights neighbors, their strips read the lava source 15); plain surface landings have an all-0 blk strip = the contained result → the injection is a no-op → the contained eff is already final → no re-request is ever generated (independent of the gate). Residual corners documented (§7d/7i): a closed-column cell whose eff is laterally-flooded sky (cave MOUTH within the neighbor chunk) over-carries eff as blk — smooth continuation into connected passable cells (Δ≤1 pairs, MC-consistent for connected air, bounded ≤14, no full-bright leak); conservative open-column 0 = web-faithful, no gate-relevant change.
- **Gates (plan §5):** unchanged EXCEPT explicit: AC-0126 breakspike eff-MD5 anchors are STALE BY DESIGN (new kernel changes boundary eff in the edit region — intended); binding gates = lightaudit PRE/POST + MINFO 125552/121 (geometry) + G3 floors + genhash 25/25 + battery EXACT (re-verified: battery reads world.light_at = compute_light_split, untouched → 14/15/0/9 stay EXACT). G4 fence stated explicitly (§7j): NO persistent per-chunk blk array (+20480 B/chunk ≈ +137 MB at r50 → fence fail); blk strips transient, last_eff stays CORE 16×16. New risks: (h) main-thread derivation cost (≈30k ops/dispatch bounded, detector-covered, all-0 pre-check fallback), (i) conservative corner, (j) G4 anti-optimization note.
- **Deliverables:** revised `tasks/AC-0129/plan.html` (in place, self-contained) + this entry. No new godot runs (static revision; bash-76 anchors stand).
- **Open question for Run-2 (gate reading — gate text UNCHANGED):** the lightaudit cliff scan counts every adjacent passable pair |Δ|>1 — intrinsic attenuation pairs (water adj. air: Δ2–3, present in the web's own flood) and sky-light cave-mouth pairs (15 vs 0 across a passable opening) may appear in POST even with the block-light cliff fully fixed. The RESULT records top-8 pairs with values + worst_sources, so the coordinator can judge from the POST RESULT; if only such pairs remain, read the gate as "no block-light cliff" — flag in results.html, do NOT chase count==0 by modifying the probe.
- **NEXT: unchanged from RUN 1** — Run-2 (subagent, medium) PROBE-FIRST: lightaudit on unmodified tree → G0 → PRE (cliffs>0, incl. the 13/0-class tunnel boundary pair) → implement revised §2 (dual strips + un-gated injection) → POST (block-light cliffs 0, tunnel.ok: seq smooth into the dark side, ref==seq |i|≤8) → SMOKE + genhash → results.html + this log → coordinator heavy gates + G5 + commit+push. Fences: no git/builds by Run-2, scratch → .scratch/AC-0129/, godot one at a time.

## RUN 1.2 (coordinator gate-check of the RUN 1.1 revision)
- Line refs re-verified against the tree: STANDALONE_MARGIN chunk.gd:4; _s_effl chunk.gd:650 (the 15-OOB sampler); _chunk_light_into lighting.gd:228; _chunk_light_into callers lighting.gd:277/309 (the dead batch :309); _eff_cache_put world.gd:904; _eff_for world.gd:918; threadmesh_handoff world.gd:546 (cache put :578); web seed index.html:974, sky/blk fill :1010-1021, source fill :1019 — all as cited.
- Coordinator amendment (applied to plan.html §2.D.2b/e + §5): the lightaudit HARD-CLIFF count is restricted to NON-FLUID pairs (0 air / 28 portal) — the spec's cliff is an air-pair property, and fluid interfaces legitimately carry |Δ| up to the attenuation (web flood subtracts LIGHT_ATT per step, index.html:997-1001: water-column bottom vs air below = Δ up to 3 per interface; the r4 probe world has a sea → counting fluid pairs would make POST count==0 unreachable without touching the bug). Fluid pairs reported separately as fluid_pairs {count, top3}, informational, never gated.
- Plan GATE-CHECKED (RUN 1.1 + this amendment). Run-2 = probe-first (lightaudit into the unmodified tree → PRE), implement §2, POST, SMOKE battery + genhash + MINFO record, exit (heavy gates = coordinator's background job).
