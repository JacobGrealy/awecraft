# AC-0187 continuity

## RUN — 2026-09-02 00:56 EDT — AC-0187
(priority-remesh-on-block-edit, subagent xhigh: PLAN+IMPLEMENT+VERIFY)

- Baseline editperf (pre-change): `{"cell_after":0,"flush_done":true,"flush_frames":9,"max_frame_build_ms":22,"single_build_ms":22,"total_ms":452}`.
- Design evolved 3 pieces → 5: (1) `edit_front` priority queue + set_block sync dispatch; (2) dedicated `EditPool` lane (Thread+Mutex+Semaphore, `mark_done` flag, no WorkerThreadPool cap contention); (3) slab-scoped fast remesh (`build_accs si0/si1/d_off`, scoped face loop + ymask boundary pre-pass, `apply_edit_accs`, `mesh_gen` bump, stale pre-edit light via `_eff_stored_eq`, `eff_trust=false`); (4) scoped row snapshots (per-row `append_array(slice)`, ~35× smaller, kills GC stall) + scoped drop check; (5) perf pieces: scoped strip memoization (neighbor data_gen/eff_gen key, cap 32, zero b-channels), b-channel zeroing (skips ~190ms `_face_of` cold miss), **edit-yield** (`edit_inflight_count`: while a scoped edit is in flight the main thread defers far-queue drain, light flush, tex refresh, and non-edit handoffs so the edit handoff lands on a short frame), **priority pass** in `threadmesh_poll` (+ `_physics_process` poll) so a done edit is handed off before any other work.
- Probe timeline (R50, queue ~8244): 932/147ms (handoff stalled behind 300ms+ drain bursts) → yield + priority pass → 85/108ms (worker emit 56-117ms) → emit breakdown found `_s_emit_ro_merged` (merge-atlas) = 5-15× slower per quad than plain path → scoped builds now use plain per-face emit (merge is geometry-identical, wave's full build restores merged mesh in 1-3 frames) + `_s_face_light` early-out at 15 → **break 31.8ms / place 39.5ms, ok:true, drops=0**.
- G0 clean (SE=0) after every edit batch.
- Gates: G0 ✓; **editfront ✓ ok:true** (break 32.5ms / place 37.3ms, drops=0, queue 8244, scoped=2, full=0, prime=true); **editperf ✓** (flush_frames 9/9, max_frame 22/22, single_build 22/22 = baseline; total_ms 494 vs 452 = yield trade-off, see results); **lodswap ✓ ok:true** (28.4s, no_hole, flicker ok, retain ok). SMOKE battery running.
- **FINAL: ALL GATES GREEN.** SMOKE battery ok:true (player 135.43/136.78+2.82, interact place_ok, light 15/10/14, fluids 2756/914+25/9). Genhash 25/25 byte-identical vs AC-0164 gate log (diff of the 25 GENHASH lines). `trees` probe genhash_gate:false@(2,2) = stale Aug-18 reference (pre-existing; AC-0091 voided old baselines; generator.gd last touched Aug 30) — documented as D7, not a regression. Render (optional) skipped — coordinator heavy stage. Results in tasks/AC-0187/AC-0187-results.html (8 deviations D1-D8). Ready for coordinator commit + heavy boundary r4 + build_windows.sh.
- editperf diagnosis (worth keeping): the counter measures the "current flush burst"; it was inflated (94-100 frames, 303-715ms) because (a) a pre-existing stale-true `flush_active` (burst drained at section start → clear skipped) and (b) MY `_edit_front_add` set `flush_active=true` at set_block, so the post-edit burst never reset the counters and merged with the pre-edit E2 churn. Both fixed: clear runs whenever `light_pending` is empty (outside the section), and the redundant set was removed (the idle check already covers edit_front). Post-fix counters match baseline exactly. The residual 42ms total_ms = the yield window (the 3×3 wave's dispatch starts after the scoped edit lands) — the hole itself lands ~100ms earlier than the baseline's wave.
