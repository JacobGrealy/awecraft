## RUN — 2026-08-29 08:42 EDT — AC-0109
- Started: read spec.html + ARCHITECTURE.md + CONTINUITY.md before any edit/godot.
- Plan: Run-1 = pin mesh structure + audit existing culling; design cull + spin probe; implement; G0; G1 spin PRE (feature off/absent) + POST; G2 MINFO; G3 boundary r4 one-shot; G4 battery+genhash; G5 render optional; results.html.

### Run-1 findings (mesh structure PINNED + culling audit + engine API)
- Mesh structure: chunk = 5 vertical slabs 16×16×16 (y0=0/16/32/48/64), `slabs: Array` on chunk (chunk.gd:32), `Slab` class chunk.gd:46 with `mesh_instance/fluid_instance/flora_instance: MeshInstance3D`, `built`, `col_dirty`. Per-slab AABBs = the cull structure. Chunk node pos = (cx*16, 0, cz*16); slab world AABB = (cx*16, y0, cz*16)+(16,16,16). Column AABB center (cx*16+8, 40, cz*16+8) r=(8,40,8) [Data.HEIGHT=80]; slab center (cx*16+8, y0+8, cz*16+8) r=(8,8,8).
- Culling audit: NO per-frame frustum/visibility culling in-repo. Existing visibility writers: (1) AWECRAFT_ONLY probe-only static filter (main.gd:1110-1122, sets mesh+fluid .visible=false, NOT flora — probe-only, never in battery); (2) distance-candidate path r+1 (AC-0080) kills meshes — different mechanism, untouched; (3) AC-0133 star field = world-root parent, not chunk children → untouched by design.
- Engine 4.7.1 (verified empirically, no web refs): MeshInstance3D has NO `extra_culling_margin` property (assignment = script error; `"extra_culling_margin" in mi` false) → engine cull-margin knob unavailable → GDScript per-frame test is the only path. Camera3D: NO get_camera_frustum(); has get_frustum/is_position_in_frustum (no margin param). Plane: 2-arg ctor Plane(normal, point) OK, Plane.new() parse error, method is distance_to(point) (NOT distance_to_point). cam defaults near=0.05 far=4000 fov=75 (player.tscn: fov 75, no near/far override).
- Chosen mechanism: manual 6-plane frustum (8 corners from cam.global_transform+fov+aspect+near/far; per-plane normal from 3 world corners, flipped to face frustum centroid), margin M=32.0 (2× slab width — a slab becomes invisible only once fully past the margin-expanded plane ⇒ transitions happen off-screen). Test = supporting-corner: slab cull ⇔ ∃ plane d+span < −M; definitely-visible ⇔ ∀ planes d−span ≥ −M; else per-slab exact. d = plane.distance_to(center); span = r·|n| (precomputed per plane: col_span, slab_span). Column AABB first (early-out both directions); partial → per-slab, slab distance reused from column via dy*n.y.
- No new per-chunk state: world-level only — 6 Plane scratch, 3×PackedFloat32Array(6) spans, PackedFloat32Array(6) dc, PackedFloat32Array(6) ny, 1 scratch Vector3, last-cam Transform3D, enabled flag. Set `visible` only on change (read instance.visible). Skip: instance == null (candidates r+1 have mesh=null; rebuilt instances default visible=true until next camera-change pass — transient, documented).
- Cull pass call site: TOP of world.gd `_process` (line ~251), BEFORE the all-queues-empty early return (line ~254). Early-out on unchanged cam global_transform. Kill switch AWECRAFT_FRUSTUM_CULL=0 (default on); also disabled when AWECRAFT_ONLY set (keeps that probe filter authoritative — honest guard).
- Spin probe (main.gd, new AWECRAFT_LOGIC=spin): r4, seed 44 (default), spawn player, recenter+settle, force window 1280×720 (deterministic aspect), drive player.camera global_transform only (position = captured eye, yaw step 0→TAU in 120 steps ×3°, pitch 0; step 30 = sidelook, step 120 = back to start), 121 samples per process_frame: verts_visible from probe-local vert cache (instance_id→verts via surface_get_arrays, built ONCE post-settle — no per-frame surface reads), chunk-visible = ALL its non-null instances visible, transitions = V→I edge with prior invisibility within [t−8, t−2] (flicker window), queue_samples per step, then bottom look (pitch −90, 3 frames, vis_bottom) then return to start (vis_end). RESULT keys: ok, transitions, vis_start, vis_bottom, vis_end, verts_visible_start, verts_visible_sidelook, total_built, queue_samples (+ mode/seed/radius/steps/built_chunks/elapsed_ms house fields). ok = transitions==0 AND verts_sidelook < total_built AND vis_end==vis_start AND queue flat. PRE (AWECRAFT_FRUSTUM_CULL=0): ok:false + verts identical across looks (all visible). Player camera: _apply_rotation only on input (player.gd:1158) → probe owns the transform; player stationary → no recenter → queue flat.
- SPINPERF render-mode audit line (DisplayServer != headless): RENDER_TOTAL_OBJECTS_IN_FRAME + light counts, PRE vs POST under xvfb (no snapshot) to see whether the 4.7.1 renderer counts submitted or all objects.
- Run order: G0 → spin PRE → spin POST → G3 boundary r4 ONE-SHOT (AC-0035-lesson judge; in-band walk p95 108–126 / fwd_p95 10898–13019, AC-0136 = 123/11675) → G2 MINFO diff vs .scratch/AC-0129-gates/gate_minfo.txt → G4 battery player;interact;fluids;genhash (known-stable: fluids 2730/1406, interact true/true, player after_fly 37.78) → G5: 1 render spin-run (audit) + 1 R=1 snapshot to tasks/AC-0109/ (visual sanity).

## MILESTONES — Run-1 gates (2026-08-29 ~10:30 EDT)

- **G0 PASS**: `--quit` menu load RC=0, 0 script errors (final code, after all edits).
- **Spin settle fix (pre-existing stall, NOT this task)**: `_nd_settle(60000)` hit its 120 s cap with a player present (spin PRE: 74/81 built; known-good viewlight probe reproduced the identical 120.2 s stall → pre-existing). perf probe (no player) builds 81/81 in 9.85 s → player presence + AC-0137-class threadgen_handoff transients (5–21 per spin run, 0 in perf) leave 2–7 r4 chunks unbuilt with a flat 40-item queue residual. Fix: spin probe uses a bounded settle (2400 physics frames, proceed with what is built, report `built_chunks`) — wall time 41.3–41.5 s ≤ 60 s budget. Documented; AC-0137 owns the root transient.
- **Probe bugs found+fixed (2 runs of debugging)**:
  1. **Stale cull cache**: the pass latches `cam.global_transform` on the first camera frame — before any chunks exist (world starts before the player) — so the spin's step 0 and all of settle skip re-culling → step-0 sample showed everything visible. Fix: `world.invalidate_cull_cache()` (world.gd, probe hook, marker Transform3D that can never match) called by the probe before the spin loop.
  2. **Frame off-by-one**: the first transform set lands on a `physics_frame` emission, so that frame's `world._process` already ran → every spin sample lagged one pose (step 0 showed no cull; the "step-1 storm of 26" was actually the T0 cull). Fix: one extra `process_frame` await at step 0 (phase sync; steps ≥1 set on process_frame emission and are exact).
  3. **Flicker-counter sentinel**: `last_invis.get(ci, -1)` made EVERY first-ever V→I edge at step ≤ 8 a false flicker (−1 ≥ step−8) → the 29 "transitions" were all this. Fix: sentinel −9999.
  (Also verified via SPINXFER readback: same Camera3D current every frame, origin stable — the set_ok=false flake is float roundtrip noise, harmless. AWECRAFT_SPINDBG gated debug kept in probe, prints nothing unless set.)
- **G1 SPIN PRE (AWECRAFT_FRUSTUM_CULL=0) PASS**: `{"ok":false,"transitions":0,"vis_start":78,"vis_bottom":78,"vis_end":78,"verts_visible_start":365408,"verts_visible_sidelook":365408,"total_built":365408,"queue_flat":true}` — verts identical across all looks, all built chunks visible, 41.4 s.
- **G1 SPIN POST PASS**: `{"ok":true,"transitions":0,"vis_start":52,"vis_bottom":58,"vis_end":52,"verts_visible_start":260528,"verts_visible_sidelook":267908,"total_built":373400,"queue_flat":true}` — 28% vert drop at sidelook, state restored (vis_end==vis_start), zero flicker over 360°, queue flat 40. Debug pattern confirms the design: boundary sweeps one chunk per 1–3° step, single contiguous invisible arc per revolution, crisp symmetric test (culled ⇔ ∃ plane d+span<−M, margin 32 m) → no hysteresis, deterministic per pose.
- **G2 MINFO HONEST DEVIATION**: 120/121 lines byte-identical to `.scratch/AC-0129-gates/gate_minfo.txt`; 1 chunk-local delta [16,48] verts 2012 vs 2020 (−8). Reproduces across 3 runs AND with `AWECRAFT_FRUSTUM_CULL=0` (this task's code fully inert) + a second delta [32,−16] −12 verts in the kill-switch run → pre-existing threaded-merge reshuffle drift vs the 2026-08-27 baseline (AC-0136 same class, "−48 verts merge-reshuffle flake"). Culling only writes instance.visible; MINFO dumps mesh arrays — provably unaffected. A/B'd, documented, not retried-as-fix.
- **G3 BOUNDARY r4 ONE-SHOT PASS**: walk `p95_ms` 120 (band 108–126; AC-0136 = 123), `forward_p95_ms` 12429 (band 10898–13019; AC-0136 = 11675) — per-frame cull pass costs nothing measurable at r4.
- **G4 BATTERY PASS**: `ok:true` total 159.9 s — player after_fly_y 37.78 EXACT, fluids sea 2730/1406 EXACT + sea_stable, interact place_ok true, genhash 25 hashes emitted; RC=0, only 17× known threadgen_handoff transient.
- **G5 PASS**: 1× R=1 render → `tasks/AC-0109/cull_shot.png` + `cull_shot_placed.png` (1280×720, visually clean: full terrain, no black-out/hole artifacts; note first attempt's relative path resolved under `godot/` due to --path → reran with absolute path; the 2 PNGs are the single render).
- **All gates done. Deviations: G2 MINFO 1–2 line pre-existing drift (A/B'd kill-switch), spin settle bounded-wait (pre-existing AC-0137-class stall). Results → tasks/AC-0109/AC-0109-results.html.**
