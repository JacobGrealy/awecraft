# AC-0155 — full-chunk-save-not-edits — continuity

## 00 — 2026-08-30 — filed + parked after AC-0152
- Save every 16x384x16 column fully (24x16^3 palette+bitpack+zlib per subchunk, Bedrock LevelDB / Java region) not edits diff. After partially generated, always saved entirely. Fixes r=50 10k GENMS wall, revisits don't re-gen. Depends AC-0152, before AC-0154 lazy-caves (may make it unnecessary). Queue pos 1 after 0152.

## 01 — 2026-08-31 — IMPLEMENTED, light gates green, ready for coordinator
- **Shipped:** NEW `godot/core/chunk_io.gd` (per-subchunk palette+bitpack + n==1 fast path, `fl` array, versioned "AWCC" blob + u32 seed/height + 16B MD5, deflate mode 0; decode validates magic/version/seed/height/sizes/MD5 → `{}` on fail; `dir_for`/`path_for`/`ensure_dir`/`clear_dir`/`encode_column`/`decode_column`). `world.gd`: counters (`disk_reads`/`disk_read_ms`/`gen_count`/`gen_ms_total`/`chunk_origin`), `_try_disk_load`+`_materialize_chunk_data` (disk-first) hooked into create_chunk, recenter(0,0), recenter 5×5 burst, and the drain (1 disk-read/frame via `_gen_last_disk`); write-on-evict: recenter `to_free` loop calls `_queue_chunk_save` (data+fl duplicate) before erase/queue_free, `_process` drains 1–2 cols/frame, main-thread, pure stubs (empty data) never written. `save.gd`: `SAVE_VERSION` 2→3 (load still shape-validated, v2 JSON still loads), `clear(slot)` wipes `chunkdir_N`. `main.gd`: `AWECRAFT_LOGIC=chunkio` arm.
- **Light gates (all PASS):** G0 exit 0 / 0 SCRIPT ERROR. chunkio `ok:true` wall 33.7s — 41/41 files, 41/41 byte-identical (MD5 of 98304B col), r4 revisit origin 41 disk/0 gen (GENMS 0 for the 41-set), r50 revisit disk 41/gen 0, phaseA gen 54/disk 0. SMOKE battery exact (player 135.43/136.78, interact place_ok, light 15/10/14/9, fluids 2756/914 stable 25/9, genhash 25/25 byte-identical to AC-0091 baseline — only GENMS timing line differs), final ok:true. save ok:true (6/6, iso, v1_softfail). continue edits_ok/pos_ok/save_settled/saved_chunk_built_at_spawn true. render R=1 PNG `tasks/AC-0155/render_R1.png` (1280×720, m4:ok, clean world).
- **Deviations (for coordinator):** (1) battery-as-one-run 275s on this box = inter-mode `_await_world_build` mesh-drain waits (values still exact, not an AC-0155 regression — battery never evicts → save queue no-op; single arms at normal pace). (2) boundary walk p95 expected UPWARD drift (write-on-evict amortized 1–2 cols/frame ~26ms) — coordinator measures final. (3) revisit recenter does a synchronous 5×5 disk-load (~500ms one-time hitch per recenter-to-saved-area) — known limit, not in gate set. (4) codec ~26ms enc/20ms dec, ~8.3KB/col lossless. (5) Engine facts: compress round-trips only int modes 0–3 (→MODE 0); `CompressionMode` NOT a GDScript global; MD5 via `HashingContext` instance (`start(HASH_MD5)`/`finish()`).
- **Pre-existing noise (not ours):** `Invalid Task ID` (recenter is_task_completed) + AC-0137-class `threadgen_handoff` Nil→PackedByteArray transients (1–2/run), both documented §00o/§00n.
- **Coordinator to-do (heavy, after this exit):** re-run light gates to confirm; run the r4/r50 boundary walk for the final p95; Windows build (`./build_windows.sh`); commit (code + results + plan + build) + push. Scratch gate logs: `.scratch/AC-0155-gates/`. Results: `tasks/AC-0155/AC-0155-results.html`. No git/Windows/384→80-cut/data.gd/TASKS/HARNESS touched by the implementer (fences held).
