## RUN 0 (coordinator, 2026-08-26)
- AC-0078 = AC-0034 child 4/4 (plan AC-0071 P1.5+P1.6): (1) fluid locality — per-chunk fluid prescreen + 32-block sleep signature, wake sources unchanged, sim results BIT-IDENTICAL; (2) first-load fast path — 3x3 sync core for New AND Continue; Continue stops waiting for the full radius, streams around the existing player.
- Spec v1 filled (gates G1-G6): G2 bit-identical fence (full battery exact), G3 fluid-locality measurement (Run-1 defines), G4 New 3x3 core verify + Continue spawn-no-wait (Run-1 defines the probe), G5 no-regression floor = POST-AC-0079 anchors (walk p95 ~62-65, fwd_p95 <= 3500 + probe >= 8, RECSLICE <= 12.2, perf total ~5715), G6 windows build + 8080/5180.
- Fresh-tree anchors (post-AC-0079, 2026-08-26): boundary r4 walk p95 62-65 / max ~111-134 / fwd_p95 2932-3238 / first10 10 / irb 81 / flap 0; r2 fwd 463-593; r50 RECSLICE 10.0/8.0, walk ~44-67; perf r4 total 5715 / first_draw 192 / read_sync_gen 0; genhash 25/25 (61de26e9); battery exact (sea 2730/2730+1406/1406, torch 14, 2.82, 25/9, place/drop true); exe 109559968B @17:42.
- Ops: HOME=/tmp/dsh_home pattern; one godot at a time; headless probes only in Run-1; r50 timeout 1200000 (background); scratch .scratch/AC-0078/; no git/builds/TASKS.yaml; never kill python server pids (8080/5180).
- Run-1 LAUNCHING (subagent_plan, xhigh).

- **RUN 1 (coordinator note 2026-08-26): Run-1 (xhigh) completed its research + wrote plan.html (7 sections, verified) but was CANCELLED at the tail — right before appending its own RUN 1 log section.** Coordinator gate-check passed: §2 change list is file:line precise; §5 gates have fresh PRE anchors (fluid sim: 216 ticks = 75 scanned/141 slept, scanned 805.5 ms, wet 0, writes 0; fluids-battery probe ticks ~110 ms; NEW first_draw 200; CONTINUE dist3 PRE = fell_after_30f TRUE, y_trace 34.0→31.8 falling, no collision body — the real bug: saved player 48 blocks from the spawn-chunk anchor falls through the world on continue; designed post: 3×3 core wait ~1.2–1.6 s, no fall, y flat). Wake-completeness table covers every fl writer (set_fluid/set_block/_apply_edits_to_chunk = the ONLY writers; single self-healing clear site; ±14-block window > 7-block max source reach). **PROBES KEPT IN TREE (deviation, recorded): Run-1 left env-gated `AWECRAFT_FLUIDPROBE` probe code in world.gd (`_fluidprobe` :92, parse :205-206, FLUIDPROBE prints in tick_fluids, `_fp_writes` counters in set_block/set_fluid) + the `AWECRAFT_LOGIC=continue` probe mode in main.gd (`_continue_probe` :3995-4107, envs AWECRAFT_CONTINUE_DIST/AWECRAFT_CONTINUE_WAIT) — additive, OFF by default, matches the tree's existing TICKTIME/MINFO/RECPROBE pattern; Run-2 verifies behavior identity with the envs UNSET (G0 + battery exact). Coordinator DECISION (spec updated): the New path stays AS-IS (Run-1 refuted the "New has a 3×3 core" premise — spawn-chunk-only, first_draw 200, no fall risk by construction; a 3×3 wait would cost +1.2 s for no measured gain). **PROCESS CHANGE (user 2026-08-26): from this Run-2 onward the HEAVY-GATE PIPELINE is in force** (AGENTS.md + two-phase.md updated): Run-2 = G0 + SMOKE battery + ≤1 R=1 render only; the heavy gates (boundary/perf/r50/flake/full battery/build) run as a COORDINATOR BACKGROUND bash job with a HEAVY_GATES_DONE marker; the next task's Run-1 overlaps the gates (godot-busy aware); commit only after gates pass.

## RUN 2 (builder, medium, 2026-08-26)
- **Implemented plan §2 verbatim** (line numbers re-verified on the live tree before each edit; all matched). Files changed: `godot/world/world.gd` +15/−1, `godot/scenes/main.gd` +21/−1 (git diff: 2 files, 34 insertions, 2 deletions). chunk.gd/debug.gd/player.gd untouched.
- **P1.5 (world.gd, fluid-sim section only):** (a) `var fluid_wet := {}` state block :94; (b) 32-block floor-quantized sleep sig in `tick_fluids` :1631 (`floorf(.../2.0)`, player-null "all" branch untouched); (c) prescreen skip INSIDE the scan loop after the null/empty check :1656-1658 + per-chunk `wet_cells` counter :1659; (d) single self-healing clear site :1740-1741 (`if wet_cells == 0: fluid_wet.erase(ck)` after a fully-scanned chunk); (e) bit set in the three lowest fl writers — `set_fluid` tail when `lvl > 0` :1583-1584, `set_block` fluid-id branch :1375 (clear else-branch NO bit touch), `_apply_edits_to_chunk` when any applied cell `f > 0` :1416-1417.
- **P1.6 (main.gd, boot path + probe fields only):** new `_await_core_3x3(where, max_frames)` helper :4244-4261 (9 core chunks present + data non-empty + mesh_built, physics_frame poll, "CORE3X3 not fully built" warn on timeout, no collision-body wait) inserted after `_await_world_build`; one-line swap in `_continue_slot` :396 → `await _await_core_3x3(target, 3000)`; NEW path `start_game` :435 UNCHANGED (coordinator decision).
- Run-1 env-gated probes (`_fluidprobe`/`AWECRAFT_FLUIDPROBE` in world.gd, `AWECRAFT_LOGIC=continue`/`_continue_probe` in main.gd) KEPT in tree, untouched, as instructed.
- **G0: PASS** — `HOME=/tmp/dsh_home ~/tools/godot/godot --headless --path godot --quit` → RC=0, zero SCRIPT ERROR / Parse Error (log `.scratch/AC-0078/run2_g0.log`).
- **SMOKE battery: ALL EXACT** — `HOME=/tmp/dsh_home AWECRAFT_BATTERY="player;interact;light;fluids;genhash" ~/tools/godot/godot --headless --path godot` → RC=0, 0 errors, battery ok:true: player is_on_floor true / jump_peak_y 36.43 / horizontal_moved 2.82; interact drop_spawned true / place_ok true / breakable_id 2; light torch_level 14 / surface_eff 15 / cave_eff 0 / torch_far_after 9; fluids sea 2730/2730 + 1406/1406 / sea_stable true / water_delta 1 / shore_after [5,7] / source_after [5,8] / water_on_lava_result 25 / sideways_lava_result 9; genhash 25/25 byte-identical to `.scratch/AC-0079/genhash.log` (diff clean). Logs: `.scratch/AC-0078/run2_smoke.log`.
- **Render: PASS** — one R=1 iso into `tasks/AC-0078/ac0078_iso_r1.png` (1280x720, RC=0, 0 script errors, RESULT {cam:iso,m4:ok}) — 3×3 core fully built, no holes at spawn; visually matches the pre-change reference `tasks/AC-0079/ac0079v3_iso_r2.png` (same seed-44 snow plateau + trees + edge water patch).
- **Current state: SMOKE complete — awaiting coordinator heavy gates.** NOT run (per HEAVY-GATE PIPELINE): boundary r4/r2, perf, continue probe (G4), RECSLICE r50, flake, full exact battery, `./build_windows.sh`. Coordinator runs them as a background bash job (`.scratch/AC-0078/gates.log` + `HEAVY_GATES_DONE` marker); commit/push after they pass. Deliverable: `tasks/AC-0078/AC-0078-results.html` (self-contained, render embedded).

## RUN 2.5 (coordinator heavy gates, 2026-08-26)
- **Pipeline v1 note:** first heavy-gate job (20:30) failed ENVIRONMENTALLY (coordinator script bug: `$HOME/tools/godot/godot` resolved against `HOME=/tmp/dsh_home` → 14× rc=127 + build rc=1; no godot ever ran, tree untouched, daemons healthy). v2 relaunched with the absolute engine path /home/angrygiant/tools/godot/godot (logic gates keep HOME=/tmp/dsh_home per-process; build keeps real HOME + XDG overrides). All 15 steps rc=0, 0 SCRIPT ERROR lines (log: .scratch/AC-0078/gates.log, marker HEAVY_GATES_DONE 21:45).
- **G2 FULL 6-arm battery: PASS EXACT** (player 2.82/36.43/floor; interact 2/true/true; light 14/15/0/9; fluids 2730/2730+1406/1406 stable, water_delta 1, shore [5,7], source [5,8], 25/9; buckets [5,8]/[5,8]; genhash 25/25). Bit-identity fence held.
- **G3 fluidprobe: PASS** — sum(slept=0 tick_ms) 0.23 ms vs PRE 805.5 (~3500×); fluid_tick_ms_p95 0.02-0.08 (<1.0); walk p95 62-63 (floor <=65). Coordinator spec-fidelity check (index.html:1053-1092 web tickFluids = full-window scan, no wet cache): no gap — web natural sea also stores fl content only for placed water dynamics per the port decision; godot init_fl (chunk.gd:122-127) documents natural ocean = fl 0/stationary, and pre-change sim data (wet_cells 0, writes 0 in the same world) confirms the prescreen skips exactly what the old sim scanned to find nothing.
- **G4 continue: PASS** — core arm (AWECRAFT_CONTINUE_WAIT=core, appended 22:06 after the gate job omitted the env): dist0 + dist3 (48-block fall-through case) BOTH fell_after_30f FALSE, col_body_at_spawn TRUE, saved_chunk_built_at_spawn TRUE, y flat 34.0x31, load_spawn 1284/1307 ~= core_ms 1282/1306 (+1.3 s vs full-radius wait — the designed trade). Save round-trip ok:true. (Pre arm dist3 still falls = expected A/B reference of the old no-wait path.)
- **G5: PASS** — r4 x3: walk p95 63/62/62, fwd_p95 3004/3216/2959 (<=3500), first10 10/10/10, irb 81, flap 0; RECSLICE r50 130.0/10.0 + 69.0/8.0 (<=12.2+noise); player flake x4 clean; perf r4 total 5731 (anchor 5715), first_draw 194, p95 56, fog_ok, read_sync_gen 0 — no regressions anywhere.
- **G6: PASS** — build RC=0, fresh exe 109,564,736B @21:45 (post-AC-0079: 109,559,968B), 8080+5180 200, no 8081 twin.
- **VERDICT: ALL GATES PASS. AC-0078 done + completion comment. Checkpoint + commit + push; AC-0120 Run-2 next (plan already gate-checked 04183c6).**

