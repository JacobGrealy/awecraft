## RUN 0 (coordinator, 2026-08-26)
- AC-0078 = AC-0034 child 4/4 (plan AC-0071 P1.5+P1.6): (1) fluid locality — per-chunk fluid prescreen + 32-block sleep signature, wake sources unchanged, sim results BIT-IDENTICAL; (2) first-load fast path — 3x3 sync core for New AND Continue; Continue stops waiting for the full radius, streams around the existing player.
- Spec v1 filled (gates G1-G6): G2 bit-identical fence (full battery exact), G3 fluid-locality measurement (Run-1 defines), G4 New 3x3 core verify + Continue spawn-no-wait (Run-1 defines the probe), G5 no-regression floor = POST-AC-0079 anchors (walk p95 ~62-65, fwd_p95 <= 3500 + probe >= 8, RECSLICE <= 12.2, perf total ~5715), G6 windows build + 8080/5180.
- Fresh-tree anchors (post-AC-0079, 2026-08-26): boundary r4 walk p95 62-65 / max ~111-134 / fwd_p95 2932-3238 / first10 10 / irb 81 / flap 0; r2 fwd 463-593; r50 RECSLICE 10.0/8.0, walk ~44-67; perf r4 total 5715 / first_draw 192 / read_sync_gen 0; genhash 25/25 (61de26e9); battery exact (sea 2730/2730+1406/1406, torch 14, 2.82, 25/9, place/drop true); exe 109559968B @17:42.
- Ops: HOME=/tmp/dsh_home pattern; one godot at a time; headless probes only in Run-1; r50 timeout 1200000 (background); scratch .scratch/AC-0078/; no git/builds/TASKS.yaml; never kill python server pids (8080/5180).
- Run-1 LAUNCHING (subagent_plan, xhigh).

- **RUN 1 (coordinator note 2026-08-26): Run-1 (xhigh) completed its research + wrote plan.html (7 sections, verified) but was CANCELLED at the tail — right before appending its own RUN 1 log section.** Coordinator gate-check passed: §2 change list is file:line precise; §5 gates have fresh PRE anchors (fluid sim: 216 ticks = 75 scanned/141 slept, scanned 805.5 ms, wet 0, writes 0; fluids-battery probe ticks ~110 ms; NEW first_draw 200; CONTINUE dist3 PRE = fell_after_30f TRUE, y_trace 34.0→31.8 falling, no collision body — the real bug: saved player 48 blocks from the spawn-chunk anchor falls through the world on continue; designed post: 3×3 core wait ~1.2–1.6 s, no fall, y flat). Wake-completeness table covers every fl writer (set_fluid/set_block/_apply_edits_to_chunk = the ONLY writers; single self-healing clear site; ±14-block window > 7-block max source reach). **PROBES KEPT IN TREE (deviation, recorded): Run-1 left env-gated `AWECRAFT_FLUIDPROBE` probe code in world.gd (`_fluidprobe` :92, parse :205-206, FLUIDPROBE prints in tick_fluids, `_fp_writes` counters in set_block/set_fluid) + the `AWECRAFT_LOGIC=continue` probe mode in main.gd (`_continue_probe` :3995-4107, envs AWECRAFT_CONTINUE_DIST/AWECRAFT_CONTINUE_WAIT) — additive, OFF by default, matches the tree's existing TICKTIME/MINFO/RECPROBE pattern; Run-2 verifies behavior identity with the envs UNSET (G0 + battery exact). Coordinator DECISION (spec updated): the New path stays AS-IS (Run-1 refuted the "New has a 3×3 core" premise — spawn-chunk-only, first_draw 200, no fall risk by construction; a 3×3 wait would cost +1.2 s for no measured gain). **PROCESS CHANGE (user 2026-08-26): from this Run-2 onward the HEAVY-GATE PIPELINE is in force** (AGENTS.md + two-phase.md updated): Run-2 = G0 + SMOKE battery + ≤1 R=1 render only; the heavy gates (boundary/perf/r50/flake/full battery/build) run as a COORDINATOR BACKGROUND bash job with a HEAVY_GATES_DONE marker; the next task's Run-1 overlaps the gates (godot-busy aware); commit only after gates pass.
